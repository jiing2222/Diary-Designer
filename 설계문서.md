# System Diary Designer — 설계 문서

버전 0.2 / 2026-08-04 수정

---

## 1. 이 프로그램이 뭔가

시스템 다이어리 사용자가 일러스트레이터나 파워포인트 없이, **원하는 속지를 만들어 바로 인쇄**할 수 있는 웹 프로그램.

문서 편집기가 아니다. 목표는 **5분 안에 만들고 인쇄**다.

### 지켜야 할 원칙

1. **도트 그리드가 이 프로그램의 바탕이다.** 모든 작업은 도트 위에서 일어난다. 도트는 지워지지 않고 항상 맨 아래에 깔려 있다.
2. **실제 크기가 정확해야 한다.** 3mm 도트는 자로 재도 3mm여야 한다. 이게 안 되면 나머지는 전부 무의미하다.
3. **모든 요소는 되돌릴 수 있어야 한다.** 선 하나, 도트 하나가 전부 독립적인 객체다.
4. **치수는 계산해서 낸다.** 브랜드마다 속지 크기가 다르므로(M6만 해도 125~128mm) 고정값을 믿지 않는다.
5. **기능을 늘리는 것보다 빠른 완성이 우선이다.** 애매하면 뺀다.

### 배포 형태

브라우저에서 실행하는 웹앱. 인터넷 없이 로컬로 돌린다.

---

## 2. 핵심 구조 — 가장 중요한 부분

### 화면과 PDF를 분리한다

가장 흔한 실패가 **캔버스 화면을 그대로 이미지로 떠서 PDF에 넣는 것**이다. 도트가 뭉개지고 인쇄 품질이 망가진다. 절대 이렇게 만들지 않는다.

```
              [ 문서 모델 ]
         모든 좌표가 mm 단위 숫자
            (진실의 원천)
              ↙        ↘
      Konva 렌더러      pdf-lib 익스포터
      화면 편집용        벡터로 직접 그림
      (픽셀)             (포인트)
```

화면과 PDF는 **같은 데이터를 각자 따로 그리는 두 개의 독립적인 뷰**다. 서로를 참조하지 않는다.

덤으로 저장 기능이 공짜로 생긴다. 문서 모델을 JSON으로 그대로 쓰면 되니까.

### 단위 규칙

**문서 모델에 들어가는 모든 숫자는 mm다.** 예외 없다.

| 변환 | 계산식 |
|---|---|
| mm → PDF 포인트 | `mm × 2.834645669` (= 72 ÷ 25.4) |
| mm → 화면 픽셀 | `mm × zoom` |

픽셀이나 포인트 값이 모델에 저장되는 일은 없다. 변환은 그리는 순간에만 한다.

### 좌표계

**속지 왼쪽 위 모서리가 원점 (0, 0).** X는 오른쪽, Y는 아래로 증가.

PDF는 Y축이 위로 증가하므로 익스포터가 뒤집는다. 이 뒤집기는 익스포터 안에서만 일어나고 모델은 신경 쓰지 않는다.

---

## 3. 양식과 인쇄를 분리한다

이 프로그램의 작업 흐름은 두 단계로 나뉜다.

```
  [ 1단계: 양식 만들기 ]              [ 2단계: 인쇄하기 ]

  속지 크기 하나를 골라              용지 크기를 고르고
  앞면/뒷면을 디자인               자동 배치된 칸에
                                  만들어둔 양식을 골라 넣음
   ┌─────────┐
   │ 위클리1  │ M6                    A4 한 장
   │ 앞 / 뒤  │                  ┌──────┬──────┐
   └─────────┘                   │위클리1│데일리1│
   ┌─────────┐                   ├──────┼──────┤
   │ 데일리1  │ M6                │위클리1│ 메모1 │
   │ 앞 / 뒤  │                  └──────┴──────┘
   └─────────┘
                                 칸마다 다른 양식 가능
```

**양식은 용지 크기를 전혀 모른다.** 속지 하나만 신경 쓰면 된다. 어떤 용지에 몇 개씩 배치될지는 인쇄할 때 정해진다.

같은 양식을 4칸 전부에 넣을 수도 있고, 칸마다 다른 양식을 넣을 수도 있다.

---

## 4. 데이터 구조

```ts
type Mm = number;   // 모든 치수는 mm

interface Project {
  version: 1;
  name: string;
  templates: Template[];   // 저장해둔 속지 양식들
  printJob: PrintJob;      // 인쇄 설정
}
```

### 양식 (Template)

```ts
interface Template {
  id: string;
  name: string;              // "위클리1", "데일리1"
  insert: InsertSetting;     // 이 양식이 어떤 속지 크기용인지
  front: Page;               // 앞면
  back: Page | null;         // 뒷면 (없으면 단면)
  repeat: RepeatSetting;     // 몇 쪽을 만들지 (7장 참고)
}
```

### 속지 설정

```ts
interface InsertSetting {
  preset: InsertPreset | 'custom';
  width: Mm;
  height: Mm;
  punch: PunchSetting;
}
```

### 타공 설정 — 화면 안내 전용

구멍은 **화면에만 표시되고 인쇄되지 않는다.** 글자가 타공에 잘리는 걸 막는 안내용이다.

```ts
interface PunchSetting {
  show: boolean;             // 화면에 표시할지
  safeZoneWidth: Mm;         // 왼쪽 안전영역 폭. 기본 10mm, 타공기 따라 조정
  holeCount: number;         // 구멍 개수
  centerX: Mm;               // 왼쪽 끝 → 구멍 중심. 기본 (3 + 지름/2)
  markSize: Mm;              // 화면 표시용 크기. 기본 4mm, A6 이상 6mm
  pitch: Mm;                 // 구멍 중심 간격. 기본 19mm
  groupGap: Mm | null;       // 3+3 배치일 때 두 묶음 사이 중심 간격
  offsetY: Mm;               // 중앙 정렬에서 밀어낼 값. 기본 0
}
```

#### 여백은 입력받지 않는다

구멍 위치는 이렇게 계산한다.

```
구멍 전체 길이 = (구멍 중심 간격들의 합)
상하 여백 = (속지 높이 − 구멍 전체 길이) ÷ 2 + offsetY
```

**여백을 따로 입력받으면 숫자가 서로 안 맞는 상황이 생긴다.** 구멍은 물리적으로 세로 정중앙에 있고 간격 19mm는 바인더 규격이라 못 바꾸므로, 여백은 자동으로 결정되는 종속 변수다. 특수한 경우를 위해 `offsetY`로 밀 수 있게만 열어둔다.

**구멍 간격 19mm는 중심에서 중심까지다.** 6공 바인더의 국제 표준(3/4인치 = 19.05mm)이다.

---

## 5. 속지 규격 기본값

| 규격 | 가로 | 세로 | 구멍 | 배치 | 표시 크기 |
|---|---|---|---|---|---|
| A5 | 148 | 210 | 6 | 3 + **70** + 3 | 6 |
| TA6 (트루 A6) | 105 | 148 | 6 | 3 + **38** + 3 | 6 |
| DA6 (다이어리 A6) | 95 | 171 | 6 | 3 + **51** + 3 | 6 |
| M6 | 80 | 125 | 6 | 균등 | 4 |
| M5 | 62 | 105 | 5 | 균등 | 4 |
| M6 3단 | 220 | 125 | 6 | 균등 | 4 |
| M5 3단 | 202 | 105 | 5 | 균등 | 4 |
| DA9 | 60 | 80 | 3 | 균등 | 4 |

- 묶음 안 간격은 전부 19mm (중심 간격)
- 굵은 숫자는 두 묶음 사이 중심 간격
- 균등 = 전부 19mm 간격으로 고르게
- 구멍 중심 X = 왼쪽 끝에서 3mm + 표시크기의 절반
- 안전영역 = 왼쪽 끝에서 10mm

### 자동 계산되는 상하 여백

| 규격 | 구멍 전체 길이 | 여백 (구멍 중심까지) | 여백 (구멍 가장자리까지) |
|---|---|---|---|
| A5 | 146 | 32.0 | 29.0 |
| TA6 | 114 | 17.0 | 14.0 |
| DA6 | 127 | 22.0 | 19.0 |
| M6 | 95 | 15.0 | 13.0 |
| M5 | 76 | 14.5 | 12.5 |
| M6 3단 | 95 | 15.0 | 13.0 |
| M5 3단 | 76 | 14.5 | 12.5 |
| DA9 | 38 | 21.0 | 19.0 |

**이 표는 출발점일 뿐이다.** 실제 속지는 브랜드마다 다르다. M6만 해도 125~128mm로 제각각이라 고정 여백은 의미가 없다. 사용자가 크기를 바꾸면 **여백은 그 자리에서 다시 계산된다.**

```
M6 세로 125mm → 여백 15.0mm
       127mm → 여백 16.0mm      자동
       128mm → 여백 16.5mm
```

### 실측값과 다른 이유

M6·M5의 실측 여백(19mm, 10mm)은 채택하지 않았다. **기하학적으로 불가능하기 때문이다.**

M6는 세로 125mm에 구멍 6개다. 간격 19mm는 바인더 규격이라 줄일 수 없으므로 구멍이 차지하는 길이는 최소 `19 × 5 = 95mm`. 남는 30mm를 위아래로 나누면 **15mm가 여백의 물리적 최댓값**이다. 19mm면 구멍이 안 들어간다.

M5도 같다. `19 × 4 = 76mm`, 남는 29mm의 절반인 14.5mm가 최댓값이다.

어차피 구멍은 인쇄되지 않는 화면 안내선이므로 이 값으로 확정한다.

**DA9**는 실물이 없어 추정값이다. 19mm 간격 3구멍을 중앙 정렬했다.

**3단 접이**는 본체와 같은 높이를 쓴다. 높이가 다르면 중앙 정렬 시 구멍이 어긋나 같은 바인더에 못 들어간다.

---

## 6. 페이지와 객체

### 레이어 — 도트가 항상 맨 아래

```
  [ 3층 ]  글자 · 도형 · 체크박스
  [ 2층 ]  선 · 표
  [ 1층 ]  도트 그리드    ← 항상 깔려 있음. 지워지지 않음
```

이 순서가 고정이라 **선을 끄면 밑에 있던 도트가 저절로 드러난다.** 도트를 지웠다 살리는 로직이 따로 필요 없다.

```ts
interface Page {
  dotGrid: DotGrid | null;
  objects: DiaryObject[];    // 배열 순서 = 그리는 순서 (뒤가 위)
}

interface DotGrid {
  style: 'dot' | 'grid' | 'horizontal' | 'vertical' | 'none';
  spacing: Mm;               // 간격
  offsetX: Mm;
  offsetY: Mm;
  dotSize: Mm;               // style이 dot일 때
  lineWidth: Mm;             // 나머지 style일 때
  color: string;
  avoidSafeZone: boolean;    // 타공 안전영역은 비워둘지
  showOnScreen: boolean;     // 화면에 보이기
  print: boolean;            // PDF에 포함하기
}
```

#### 모양만 바꾸면 속지 종류가 바뀐다

```
   도트           그리드          가로줄          세로줄
  · · · ·      ┼─┼─┼─┼        ────────      │ │ │ │
  · · · ·      ┼─┼─┼─┼                      │ │ │ │
  · · · ·      ┼─┼─┼─┼        ────────      │ │ │ │
  · · · ·      ┼─┼─┼─┼                      │ │ │ │

  기본 도트속지   그리드속지      줄노트         세로줄노트
```

같은 5mm 간격에서 그리는 방식만 다르다. 줄노트가 공짜로 딸려온다.

#### 두 가지를 분리한다

- `showOnScreen` — 작업하면서 볼지
- `print` — 인쇄물에 남길지

도트를 보면서 작업하고 인쇄할 땐 빼거나, 반대로 설정한 5mm 도트를 그대로 인쇄할 수 있다.

#### 스냅은 모양과 무관하다

**표를 그릴 때의 스냅은 언제나 도트 좌표를 쓴다.** 화면에 그리드로 보이든 줄노트로 보이든 상관없다. 보이는 모양과 붙는 자리를 분리해야 어떤 스타일에서도 같은 위치에 그려진다.

```ts
interface BaseObject {
  id: string;
  x: Mm;
  y: Mm;
  locked: boolean;
}

type DiaryObject =
  | LineObject      // 직선/점선. 굵기, 색, 점선 패턴
  | RectObject      // 사각형. 모서리 둥글기, 채우기, 테두리
  | EllipseObject   // 원/타원
  | TextObject      // 글꼴, 크기(pt), 정렬, 굵게, 기울임, 색
  | CheckboxObject  // 네모/동그라미 체크박스
  | TableObject;    // 표
```

### 표

```ts
interface TableObject extends BaseObject {
  type: 'table';
  colWidths: Mm[];
  rowHeights: Mm[];
  cells: Cell[][];        // [행][열]
  borderStyle: LineStyle;
  hiddenBorders: string[]; // 숨긴 구분선들. "h:2,3" = 2행과 3행 사이 가로선
}
```

도트를 드래그해서 훑으면 그 범위로 표가 생긴다. 행·열 추가·삭제·크기조절을 지원한다.

#### 선을 끄면 도트가 다시 보인다

이게 이 프로그램의 기본 동작이다. **선은 도트를 덮을 뿐 지우지 않는다.**

```
   도트만            드래그해서 연결         가운데 선 끄기
  · · · · ·        ┌───┬───┬───┐        ┌───┬───┬───┐
  · · · · ·        ├───┼───┼───┤        │   ·   ·   │  ← 도트 복귀
  · · · · ·        ├───┼───┼───┤        ├───┼───┼───┤
  · · · · ·        └───┴───┴───┘        └───┴───┴───┘
```

같은 자리를 다시 클릭하면 선이 돌아온다. 결과적으로 칸 병합과 같은 효과가 나지만, 격자 데이터는 반듯하게 유지되고 실행취소도 자연스럽다. `colSpan`/`rowSpan` 같은 복잡한 구조가 필요 없다.

칸 안의 글자는 칸 밖으로 흘러넘칠 수 있게 한다. 그래야 선을 끈 자리에 긴 제목을 쓸 수 있다.

---

## 7. 여러 쪽 자동 생성

위클리 속지 하나를 만들면 그게 53쪽이 되어야 한다. 이걸 손으로 53번 복사할 순 없다.

방식은 두 가지다.

```
   [ 만년형 ]                    [ 연도형 ]

   같은 양식을 N번 반복           날짜가 순서대로 채워짐

   ┌─────────┐                  ┌─────────┐
   │  위클리  │ × 54쪽           │ 1/1~1/7 │
   └─────────┘                  ├─────────┤
                                │1/8~1/14 │
   내용이 전부 같음               ├─────────┤
   개수만 입력                    │   ...   │
                                └─────────┘
                                총 53쪽
```

```ts
type RepeatSetting =
  | { mode: 'single' }
  | { mode: 'repeat'; count: number }        // 만년형
  | { mode: 'dataset'; dataset: Dataset };   // 연도형 = 세트형
```

### 낱장형과 세트형

이 구분이 인쇄 방식을 결정한다.

```
  낱장형 (single · repeat)          세트형 (dataset)
  ────────────────────             ────────────────────
  만년형 위클리 · 메모 · 습관         2027년 위클리 (53쪽)

  1장만 뽑아도 말이 됨                통째로 뽑아야 의미가 있음
  → 다른 양식과 섞어서 배치 가능       → 이 양식 하나만 뽑음
```

**사용자가 인쇄 모드를 고르지 않는다.** 양식을 고르면 저절로 정해진다. 연도형 위클리를 메모와 섞어 뽑는 실수도 구조적으로 막힌다.

### 만년형 — 개수만 입력

양식이 **단면이면 쪽수**, **양면이면 장수**를 입력받는다. 사용자가 쓰는 단위 그대로 물어봐야 헷갈리지 않는다.

```
단면 양식 → "몇 쪽 필요하세요?"  54 쪽 → 54쪽 생성
양면 양식 → "몇 장 필요하세요?"  54 장 → 108쪽 생성 (앞뒤)
```

### 연도형 — 데이터를 순서대로 채움

값을 직접 쓰는 대신 **자동 필드**를 놓으면, 데이터 뭉치에서 순서대로 꺼내 채워진다.

```
  [ 데이터 ]              [ 양식의 필드 ]        [ 생성 결과 ]

  2027년 날짜         ┌──────────────┐        1쪽: 1/1 1/2 ... 1/7
  1/1 ~ 12/31         │ ⟨+0⟩  ⟨+1⟩  │  ───→  2쪽: 1/8 1/9 ... 1/14
                      │ ⟨+2⟩  ⟨+3⟩  │        3쪽: 1/15 ...
  한 쪽당 7개 소비     │ ⟨+4⟩  ⟨+5⟩  │
                      │ ⟨+6⟩         │        총 53쪽
                      └──────────────┘
```

```ts
interface Dataset {
  kind: 'date' | 'number' | 'list';
  perPage: number;        // 한 쪽이 소비하는 개수. 위클리 = 7, 데일리 = 1

  // kind === 'date'
  start?: string;         // "2027-01-01"
  end?: string;           // "2027-12-31"
  step?: 'day' | 'week' | 'month';

  // kind === 'number'
  from?: number; to?: number; by?: number;

  // kind === 'list'
  values?: string[];      // ["월","화","수","목","금","토","일"]
}
```

| 종류 | 예시 | 쓰임 |
|---|---|---|
| 날짜 | 2027-01-01 ~ 12-31 | 연도형 다이어리 |
| 숫자 | 1 ~ 100 | 페이지 번호, 목록 번호 |
| 목록 | 월, 화, 수... | 요일, 월 이름 |
| 월간 달력 | 2027년 12개월 | 아래 참고 |

### 월간 달력은 따로 처리한다

달력 그리드는 서식으로 못 푼다. 2월은 28일이고 3월은 31일이라 칸 개수가 다르고, 1일이 시작하는 요일도 매달 바뀐다.

```
    2027년 3월              2027년 4월
  일 월 화 수 목 금 토     일 월 화 수 목 금 토
     1  2  3  4  5  6                  1  2  3
  ...                     ...
  28 29 30 31              25 26 27 28 29 30
        ↑ 빈칸 위치가 매달 다름
```

**'월간 달력' 전용 종류를 만들어 프로그램이 알아서 채운다.** 사용자는 "2027년 월간"만 고르면 되고, 시작 요일(일요일/월요일)과 이전·다음 달 날짜 표시 여부만 정한다. 범용 계산식을 만드는 것보다 훨씬 적은 일로 훨씬 잘 동작한다.

### 자동 필드

텍스트 객체는 **직접 쓴 글자** 또는 **자동 필드** 중 하나다.

```ts
interface TextObject extends BaseObject {
  type: 'text';
  source:
    | { kind: 'static'; text: string }
    | { kind: 'field'; offset: number; format: string };
  style: TextStyle;
}
```

- `offset` — 그 쪽의 시작값에서 몇 번째인지. 위클리면 칸마다 0~6
- `format` — 어떻게 보일지

편집 화면에서는 필드가 `⟨+0⟩` 같은 표시로 보이고, 미리보기와 PDF에서는 실제 값이 들어간다.

### 계산식은 넣지 않는다

엑셀 함수의 핵심은 `=A1+B1`처럼 **다른 칸을 참조**하는 것이다. 그런데 여기엔 참조할 칸이 없다. 각 쪽은 데이터에서 값 하나(위클리면 7개)를 받아 만들어지고, 옆 칸을 참조할 일이 구조적으로 생기지 않는다.

실제로 필요한 건 전부 **"받은 값 하나를 어떻게 보여줄까"**이고, 그건 함수가 아니라 서식이다.

```
   받은 값: 2027-03-15
        ↓
   3/15  ·  15  ·  월  ·  2027년 3월  ·  MAR  ·  15일  ·  11주차
```

세 가지 방법으로 지정한다.

| | 방식 | 예 |
|---|---|---|
| 서식 | 목록에서 고름 | 아래 표 |
| 파생값 | 목록에서 고름 | 요일 · 주차 · 월 · 남은 날짜 |
| 직접 입력 | 서식 문자열 | `M월 D일 (ddd)` |

기본 제공 서식:

| 종류 | 보기 |
|---|---|
| 주차 | `W11` `Week 11` `11주차` |
| 날짜 | `15` `15日` `15일` `3/15` `3월 15일` `2027-03-15` |
| 요일 | `월` `월요일` `月` `月曜日` `Mon` `Monday` |
| 월 | `3` `3월` `三月` `三` `MAR` `March` |
| 기타 | `D-30` |

세 번째가 사실상 계산식이 들어갈 자리다. 나중에 진짜 필요해지면 `format` 문자열을 식으로 확장하면 되므로 구조는 열어둔다.

### 총 쪽수 계산

```
만년형   쪽수 = 입력한 개수 × (양면이면 2)
연도형   쪽수 = ceil(데이터 총 개수 ÷ perPage) × (양면이면 2)
```

---

## 8. 인쇄 설정

### 두 가지 작업

인쇄 작업은 성격이 완전히 다른 두 가지다. 섞으면 말이 안 된다 — 53쪽짜리 위클리와 1쪽짜리 메모를 한 작업에 넣으면 겹치기 배치가 성립하지 않는다.

```
   [ 세트 뽑기 ]                 [ 낱장 조합 ]

   세트형 양식 하나를 통째로       낱장형 양식을 칸마다
   ┌──────┬──────┐              ┌──────┬──────┐
   │위클리 │위클리 │              │위클리1│데일리1│
   ├──────┼──────┤              ├──────┼──────┤
   │위클리 │위클리 │              │ 메모1 │ 습관1 │
   └──────┴──────┘              └──────┴──────┘
   A4 14장 · 겹치기 배치          A4 1장 · 배치 순서 무의미
```

**어느 쪽인지는 고른 양식이 정한다.** 사용자는 모드를 선택하지 않는다.

```ts
type PrintJob = {
  paper: {
    preset: 'A4' | 'A5' | 'custom';
    width: Mm;
    height: Mm;
    orientation: 'portrait' | 'landscape';
    printMargin: Mm;         // 프린터 물리 여백. 기본 5mm
  };
  insertPreset: string;      // 배치할 속지 크기
  gap: Mm;                   // 속지 사이 간격
  allowRotate: boolean;      // 90도 돌려서 더 넣기
  duplex: boolean;           // 양면 인쇄
  cropMark: {
    mode: 'none' | 'line' | 'mark';
    length: Mm;
    thickness: Mm;
  };
} & (
  | { kind: 'set';   templateId: string;         // 세트형 하나를 통째로
      order: 'sequential' | 'cutStack' }          // 재단 순서
  | { kind: 'sheet'; slots: (string | null)[] }  // 낱장형을 칸마다
);
```

### 자동 배치 계산

```
사용 가능 폭 = 용지 폭 − (프린터 여백 × 2)
가로 개수 = floor( (사용가능폭 + 간격) ÷ (속지폭 + 간격) )
세로 개수 = floor( (사용가능높이 + 간격) ÷ (속지높이 + 간격) )
```

`allowRotate`가 켜져 있으면 90도 돌린 경우도 계산해 **더 많이 들어가는 쪽**을 쓴다. 전체 덩어리는 용지 중앙에 놓는다.

### 양면 인쇄

앞면 왼쪽에 구멍이 있으면 뒷면은 **오른쪽**에 있어야 종이를 뒤집었을 때 맞물린다.

```
뒷면 칸의 x = 용지폭 − 앞면 칸의 x − 속지폭
```

**위치만 반전**시킨다. 속지 안의 내용까지 뒤집으면 안 된다. 각 칸에는 그 칸에 배정된 양식의 `back` 페이지가 들어간다.

### 인쇄 매수

```
칸 수 S = 가로 개수 × 세로 개수
용지 매수 = ceil( 총 쪽수 ÷ (단면이면 S, 양면이면 S × 2) )
PDF 쪽수 = 용지 매수 × (양면이면 2)
```

예: 53쪽 · A4에 M6 4칸

| | 용지 매수 | PDF 쪽수 |
|---|---|---|
| 단면 | 14장 | 14쪽 |
| 양면 | 7장 | 14쪽 |

### ⚠️ 재단 순서 — 놓치기 쉬운 문제

여러 쪽을 한 용지에 여러 칸씩 뽑으면 **자른 뒤 순서가 안 맞는다.** 첫 장에 1·2·3·4쪽이 나란히 있으면 자른 뒤 네 무더기를 손으로 끼워 맞춰야 한다. 53쪽이면 못 할 짓이다.

**겹치기 배치(cutStack)를 기본값으로 한다.** 각 칸이 연속 구간을 담당하도록 미리 섞어두면, 자른 뒤 무더기를 순서대로 포개기만 하면 된다.

```
   [순차 sequential]         [겹치기 cutStack]
   자른 후 손으로 정렬        자른 후 그냥 포개면 끝

  1장: 1  2                1장: 1  15
       3  4                     29 43
  2장: 5  6                2장: 2  16
       7  8                     30 44

  → 뒤죽박죽               → 네 무더기를 순서대로
                             포개면 1,2,3,4... 완성
```

```
칸당 담당 쪽수 = ceil(총 쪽수 ÷ 칸 수)
n번째 칸 · m번째 용지에 들어갈 쪽 = n × 칸당담당쪽수 + m
```

쪽수가 적을 땐 순차 배치가 직관적이므로 선택지로 남긴다.

---

## 9. 인쇄 정확도

PDF를 정확히 만들어도 인쇄 대화상자가 "페이지에 맞춤"이면 96~97%로 축소된다.

1. **검증 눈금자** — PDF 여백 구석에 50mm 눈금자를 항상 인쇄. 자로 재서 50mm면 정상.
2. **안내 문구** — 출력 화면에 "인쇄 설정에서 반드시 **실제 크기 / 100%**를 선택하세요"를 크게 표시.

실제 프린터로 실험하면서 맞춰간다.

---

## 10. 기술 스택

| 용도 | 선택 | 이유 |
|---|---|---|
| 기본 | Vite + React + TypeScript | 빠른 개발, mm 계산을 타입으로 보호 |
| 캔버스 | react-konva | 객체 단위 편집, 선택·드래그 기본 제공 |
| 상태 관리 | Zustand + Immer | 가볍고 실행취소 붙이기 쉬움 |
| 실행취소 | zundo | 처음부터 넣는다 |
| PDF | pdf-lib + @pdf-lib/fontkit | 벡터 직접 그리기, 한글 폰트 임베딩 |

한글 폰트 전체를 넣으면 5MB를 넘는다. pdf-lib의 서브셋 기능으로 실제 쓴 글자만 포함시켜 수십 KB로 줄인다.

### 컴퓨터에 설치된 글꼴 쓰기

브라우저에 따라 방식이 다르다.

| 브라우저 | 방식 |
|---|---|
| **크롬 · 엣지** | 설치된 글꼴 목록을 그대로 불러옴 (Local Font Access API). 권한 1회 허용 |
| 사파리 · 파이어폭스 | `.ttf` / `.otf` 파일을 끌어다 놓아 등록. 한 번 등록하면 저장됨 |

**크롬 기준으로 만들고 파일 등록을 예비로 둔다.** 파일 등록 방식은 어디서나 되므로 최소 기능은 항상 보장된다.

일부 상용 글꼴은 라이선스로 PDF 삽입이 막혀 있다(`fsType` 플래그). 그런 글꼴을 고르면 미리 알려준다.

---

## 11. 화면 구성

상단에 탭 두 개. 반복 설정은 **양식의 속성이지만 인쇄 화면에서도 편집 가능**하다. 항목이 두세 개뿐이라 양쪽에 두는 편이 오가는 수고보다 싸다. 같은 데이터를 두 군데서 보여줄 뿐이다.

### 양식 만들기

```
┌─────────────────────────────────────────────────────────┐
│ [양식 만들기] [인쇄하기]        저장 불러오기 PDF  ↶ ↷  │
├──────┬────────────────────────────┬─────────────────────┤
│ 선택 │                            │ ▸ 속지   M6 80×125  │
│ 선   │      ┌──────────────┐      │ ▸ 도트   5mm  화면만│
│ 표   │      │· · · · · · · │      │ ▸ 반복   만년 54쪽  │
│ 글자 │      │· ┌───┬───┐ · │      │                     │
│ 사각 │      │· ├───┼───┤ · │      │ ── 선택한 표 ──     │
│ 원   │      │· └───┴───┘ · │      │ 굵기  0.3mm         │
│ 체크 │      │· · · · · · · │      │ 색상  ■             │
│      │      └──────────────┘      │ 선 지우개 [ ]       │
│      │        2027 위클리 · 앞면   │                     │
├──────┴────────────────────────────┴─────────────────────┤
│ 앞면 | 뒷면        확대 100%       54쪽 → A4 14장        │
└─────────────────────────────────────────────────────────┘
```

속지 한 장만 크게 보여준다. 하단에 인쇄 매수를 항상 띄워서 탭을 옮기지 않아도 감이 잡히게 한다.

### 인쇄하기

```
┌─────────────────────────────────────────────────────────┐
│ [양식 만들기] [인쇄하기]        저장 불러오기 PDF  ↶ ↷  │
├────────────────────────────────────┬────────────────────┤
│                                    │ 뽑을 양식          │
│      ┌─────────────────┐           │ 📚 2027 위클리 ▾   │
│      │ ┌─────┐ ┌─────┐ │           │ 반복  2027년 53쪽✎ │
│      │ │위클리│ │위클리│ │           │ ───────────────    │
│      │ └─────┘ └─────┘ │           │ 용지   A4 세로     │
│      │ ┌─────┐ ┌─────┐ │           │ 간격   0mm         │
│      │ │위클리│ │위클리│ │           │ 절취선 크롭마크    │
│      │ └─────┘ └─────┘ │           │ 양면   [v]         │
│      └─────────────────┘           │ 순서   겹치기      │
│         1 / 14 쪽   ◀ ▶            │ A4 14장 · PDF 14쪽 │
└────────────────────────────────────┴────────────────────┘
```

용지에 실제로 배치된 모습을 쪽을 넘겨가며 확인한다.

양식 목록에서 **세트형(📚)을 고르면 위 화면**이 되고, **낱장형을 고르면** 칸을 클릭해 하나씩 배정하는 방식으로 바뀐다. 모드를 따로 고르는 버튼은 없다.

---

## 12. 개발 순서

각 단계가 끝날 때마다 **실제로 돌아가는 프로그램**이 나온다.

### 1단계 — 정확도 검증

프로젝트 셋업, mm 좌표계, 용지·속지 설정, 자동 배치, 절취선, PDF 내보내기, 50mm 검증 눈금자.

**끝나면 실제로 인쇄해서 자로 잰다.** 여기서 정확도가 확인돼야 나머지를 만들 의미가 있다.

### 2단계 — 도트와 그리드

도트 그리드, 간격 조절, 모양 선택(도트·그리드·가로줄·세로줄), 화면·인쇄 옵션 분리.

이 시점에서 **도트속지 · 그리드속지 · 줄노트를 실제로 만들어 쓸 수 있다.**

### 3단계 — 타공 위치 표시

규격 프리셋, 구멍 위치 자동 계산, 안전영역 표시, 커스텀 조정.

### 4단계 — 그리기 도구

선택·이동·삭제, 선, 텍스트, 글꼴 불러오기, 실행취소·다시실행, 속성 패널.

### 5단계 — 양식 저장과 배치

양식 개념 도입, JSON 저장·불러오기, 낱장형 칸 배정.

### 6단계 — 표

도트 드래그로 표 생성, 행·열 편집, 구분선 끄기.

### 7단계 — 여러 쪽 생성

만년형 반복, 총 쪽수·인쇄 매수 계산, 겹치기 배치, 여러 쪽 PDF.

### 8단계 — 자동 입력 필드

날짜·숫자·목록 데이터, 세트형 양식, 필드 지정, 서식·파생값, 미리보기. 월간 달력은 그다음.

### 9단계 — 나머지

도형, 체크박스, 양면 인쇄, 아이콘.
