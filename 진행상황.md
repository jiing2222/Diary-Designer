# 진행 상황

마지막 갱신 2026-08-06

이 파일은 **"지금 어디까지 했고 다음에 뭘 할지"**만 적는다.
설계 내용은 [설계문서.md](설계문서.md), 실행 방법은 [README.md](README.md)에 있다.

---

## ✅ 7단계 — 여러 쪽 생성

- 테스트 313개 통과 · 타입 검사 · 빌드 통과

`Template.repeat`이 생겼다 — `single`(기본, 다른 양식과 섞어 낱장 조합)과
`repeat`(이 양식 하나로 여러 장). 설계문서 8장 "두 가지 작업"의 원칙대로,
반복 모드로 바꾸면 인쇄하기 탭의 낱장 조합(칸 배정) UI가 사라지고 매수 안내로
바뀐다 — 두 가지 인쇄 방식은 한 인쇄 작업에 섞이지 않는다.

### 왜 "인쇄 매수"로는 안 되나

내용이 다 똑같으니 프린터의 매수 설정으로 되지 않나 싶었는데, 안 된다. 한
용지에 칸이 여러 개 배치되기 때문이다. M6 4칸짜리 A4에 53쪽이 필요할 때
53장을 인쇄하면 53×4=212칸이 나와 159칸이 버려진다. 실제로는 `ceil(53÷4)=14`장
뿐이고, **마지막 장의 남는 칸 2개만 비워야** 한다. 이 계산과 "마지막 장 일부만
비우기"가 7단계의 핵심이다.

### PDF가 여러 페이지를 만든다

`buildPdf`에 `totalSlots?: number`를 더했다. **주지 않으면 지금까지처럼 한 장,
모든 칸을 채운다** — 기존 호출부(5c·6단계)는 전부 그대로 통과했다. 주면
`sheetsNeeded(totalSlots, 칸 수)`장을 만들고, 마지막 장에서 모자라는 칸은
`BLANK_SLOT`(인쇄 안 함·빈 objects)으로 완전히 비운다. 내용을 억지로 채우거나
앞 칸을 복사해 늘리지 않는다.

**칸을 채우는 로직(`drawDotGrid`·`drawObjects`·`drawTexts`)은 슬롯 인덱스로
동작하던 것을 그대로 페이지 루프 안으로 옮겼을 뿐이다.** 재단선·눈금자도 장마다
다시 그린다 — 한 장 한 장이 완결된 인쇄 단위여야 한다.

### 화면 미리보기와 PDF가 같은 함수로 빈 칸을 정한다

`core/template.ts`에 `filledSlots(sheet, totalSlots, slotsPerSheet)`를 뽑아냈다.
pdf/export.ts와 App.tsx(화면 미리보기)가 **이 함수 하나를 같이 쓴다** — 각자
계산하면 미리보기에서 본 빈 칸이 실제 인쇄물과 어긋날 수 있다. 인쇄하기 탭에
`이전/다음` 버튼으로 장을 넘겨보는 미리보기를 넣었다(`RepeatPrint.tsx`).

### 반복 양식은 낱장 조합에 못 낀다

`SlotAssign`(5c)의 같은 규격 목록에서 `repeat` 양식을 뺐다. 배정해둔 양식이
나중에 반복 모드로 바뀌는 경우도 있어서, `resolveSlotTemplates`가 규격뿐 아니라
`repeat.mode === 'single'`인지도 함께 확인하도록 방어선을 하나 더 뒀다(테스트로
고정) — 자기 하나로 여러 장을 채우는 양식이 다른 양식과 한 칸씩 섞이면 안 된다.

### 저장 파일

`SavedTemplate.repeat`을 선택 필드로 뒀다. **7단계 이전 파일에는 없으므로
`single`로 읽는다** — 판 번호 원칙 그대로다.

### 덤 — 표 도구의 커서 표시를 칸 수로

말씀하신 대로, 표 도구로 드래그할 때 우측하단 치수가 `15 × 10mm` 대신
`3 × 2칸`으로 뜬다. `core/grid.ts`에 `tableSize(lattice, a, b)`를 추가했는데,
`tableLines`와 같은 `between` 계산을 쓴다 — 미리보기 숫자와 실제로 그어질 선이
다른 계산에서 나오면 언젠가 어긋난다. 그리기(선) 도구는 그대로 mm다.

---

## ✅ 6단계 — 표. 도구가 하나 더 생겼다

- 테스트 290개 통과 · 타입 검사 · 빌드 통과

`선택(V) · 그리기(D) · 표(G) · 글자(T)`. 표를 **새 객체 타입이 아니라 새 도구**로
만들었다 — 설계문서의 `TableObject`(colWidths·rowHeights·cells·hiddenBorders)를
그대로 만들지 않았다는 뜻이다.

### `TableObject`가 필요 없는 이유

표에 긋는 선은 그냥 `LineObject`다. 이미 있는 `toggleLines`(같은 자리를 다시 그으면
지워지고, 없는 것만 채운다)가 칸 하나하나의 구분선에도 그대로 적용된다. 그래서
"구분선 끄기"·"행·열 늘리기·줄이기"에 **새 코드가 필요 없었다** — 안쪽 선 하나를
다시 그으면 꺼지고(칸 병합), 표를 더 크게 다시 드래그하면 칸이 늘고, 선을 선택해
지우면 줄어든다. 설계문서도 이 점을 명시한다: "colSpan/rowSpan 같은 복잡한 구조가
필요 없다."

### 새로 만든 것은 `tableLines` 하나다

`core/grid.ts`의 `tableLines(lattice, a, b)` — 두 점 사이의 **모든** 격자점을 잇는
가로·세로 선을 낸다. 기존 "면 그리기"(`rectLines`)는 테두리 4개만 내는데, 표는
안쪽 칸 선까지 전부 낸다.

**격자점은 `lattice.xs`·`ys`에서 그대로 가져온다.** 간격으로 다시 계산하지 않는다 —
안전영역을 피해 격자가 밀려 있거나 끝까지 채우기로 가장자리가 덧붙어도, 실제로
존재하는 점 사이에만 선이 생겨야 한다. 한 줄로만 끌면(가로나 세로가 한 칸도
없으면) 선 하나만 나온다 — `rectLines`와 같은 규칙이다.

### 그리기 도구는 하나도 안 바뀌었다

지난 회차에 "드래그가 항상 표가 되면 기존 사각형 그리기가 사라진다"고 걱정했는데,
**도구를 아예 나누는 것으로 해결했다.** 그리기(D)는 지금처럼 테두리만, 표(G)는
안쪽 선까지. 몸짓(점에서 끄는 드래그)은 똑같고, `onUp`에서 지금 도구를 보고
`rectLines`를 쓸지 `tableLines`를 쓸지만 가른다. 기존 동작은 한 줄도 안 바뀌었다.

표 도구에서 **드래그 없이 클릭만 하면 아무 일도 하지 않는다.** 표는 최소 2칸은
돼야 의미가 있어서, 그리기 도구의 "점 찍고 다음 점 기다리기" 같은 대기 상태를
두지 않았다.

속성 막대·붙을 자리 표시·드래그 중 미리보기(안쪽 칸 선까지 보여준다)는 전부
그리기 도구와 같은 코드를 그대로 탄다 — 표의 선도 결국 보통 선이라 굵기·색·모양
설정을 공유하는 게 자연스럽다.

---

## ✅ 5c — 낱장형 칸 배정. 5단계가 끝났다

- 테스트 281개 통과 · 타입 검사 · 빌드 통과

인쇄하기 탭에서 용지 한 장의 칸마다 **다른 양식**을 넣는다. M6 4칸에 위클리·데일리·
메모·습관을 하나씩 배정하는 것이 이 기능의 목적이다.

### 같은 규격끼리만 섞는다

배치 계산(칸 크기·개수)이 한 규격을 기준으로 **한 번만** 일어난다(설계문서 8장,
"두 가지 작업" 중 낱장 조합). 그래서 지금 양식과 같은 규격의 양식만 칸에 배정할
수 있다. 규격이 다른 것을 억지로 넣으면 배치 자체가 성립하지 않는다.

`store.slotAssignment: Record<number, string>` — 키는 슬롯 번호, 값은 그 칸에
넣을 양식 id. **정하지 않은 칸은 지금 고른 양식(activeId)을 쓴다** — 지금까지의
"모든 칸이 같다"는 동작이 곧 이 값이 비어 있는 경우다.

`resolveSlotTemplates(s, count)`가 칸마다 실제로 쓸 양식을 정한다. 배정이 없거나,
배정된 양식이 지워졌거나, **규격이 다르면** 조용히 기본값(활성 양식)으로 돌아간다.
양식을 지우면 그 양식을 가리키던 배정도 함께 걷어낸다(`pruneSlotAssignment`) —
안 그러면 드롭다운이 있지도 않은 항목을 가리키게 된다.

### 화면과 PDF가 같은 배열을 본다

`App.tsx`가 `resolveSlotTemplates`를 **한 번만** 불러 화면 미리보기와 PDF 양쪽에
넘긴다. 둘이 따로 계산하면 언젠가 어긋난다는 게 이 프로젝트의 오래된 교훈이다.

지금 양식과 같은 칸은 넘기지 않는다. 대부분의 칸이 지금 양식을 그대로 쓰므로,
**기본값 + 다른 칸만** 넘기는 쪽이 다루기 쉽다. 이 "기본값 + 예외"라는 모양을
화면(`PaperPreview.slotOverrides`)과 PDF(`pdf/export.SlotContent`) 양쪽에
같은 식으로 만들었다 — 기존 호출부(단일 양식)는 override가 없는 특수한 경우일
뿐이라 코드도 테스트도 그대로 통과했다.

### PDF는 인쇄 여부도 칸마다 다르게 본다

전에는 "격자를 인쇄할지"를 문서 전체에서 한 번만 봤다. 낱장 조합에서는 어떤
양식은 격자를 켜고 어떤 양식은 끄고 쓸 수 있으므로, 그 판단을 칸을 그리는 순간
마다 하도록 옮겼다(`drawDotGrid`). 배정이 없을 때는 모든 칸이 같은 값을 보므로
예전과 결과가 같다.

글꼴은 문서 전체에 **한 번만** 심는다. 기본 칸에는 글자가 없고 배정된 칸에만
있어도 그 글자를 알아채야 하므로, 폰트를 심을지 판단할 때는 기본값과 모든
override의 글자를 합쳐서 본다.

### 칸 배정 UI

인쇄하기 탭에 `칸 배정` 줄이 생긴다. 칸 배치(가로·세로)와 같은 모양으로 드롭다운을
늘어놓아 어느 칸을 고치는지 바로 알아본다. **같은 규격의 양식이 하나뿐이면(자기
자신뿐이면) 아무것도 보여주지 않는다** — 고를 게 없는데 빈 자리만 차지하면 안 된다.

---

## ✅ 5a·5b 다듬기 — 빈 화면에서 시작한다

- 테스트 269개 통과 · 타입 검사 · 빌드 통과

### 양식 없이 시작한다

새로고침하면 **양식 관리 탭**이 먼저 열리고 갤러리가 비어 있다. 예전에는 M6 양식이
하나 미리 들어가 있었는데, 원하지 않는 규격으로 시작하게 되고 지울 수도 없었다.

`templates`가 빌 수 있게 되면서 `activeTemplate`이 `null`을 돌려준다. 양식이 없으면
`양식 만들기`·`인쇄하기` 탭이 잠기고 갤러리만 열린다. 화면이 읽는 값은 참조가
고정된 기본값으로 받쳐뒀다 — **부를 때마다 새 객체를 만들면 zustand가 매번 바뀐
것으로 보고 무한히 다시 그린다.**

### 이름이 규격에서 나온다

`M6-1`, `M6-2`, `M5-1`. 규격마다 따로 센다. `양식 1`·`양식 2`로는 목록에서 M6와 A5를
구분할 수 없다. 중간을 지워도 번호가 겹치지 않게 가장 큰 번호 다음을 쓴다.

### 새 양식 창에서 용지·속지·격자를 다 고른다

만들고 나서 설정을 하나씩 찾아 고치는 대신, 시작할 때 한 번에 정한다.
**지금 값으로 만들면 몇 칸이 되는지 창 안에서 바로 보여준다.**

**칸 수로도 만들 수 있다.** `가로 10칸`처럼 세면 간격이 딱 나누어떨어지게 계산된다
(`spacingForCells`). 다만 **간격은 가로·세로가 하나뿐이라** 한 축을 정하면 다른 축은
따라 나온다 — 둘 다 마음대로 정하면 칸이 정사각형이 아니게 되고, 그러면 도트
격자라고 부를 수 없다. 그래서 어느 축으로 셀지 고르게 했다.

### 여백 없이 끝까지 — 그린 선도 끝까지 간다

`둥근 끝(round cap)`은 선을 0.1mm **늘린다.** 짧아지는 원인이 아니었다.

진짜 원인은 붙을 자리였다. 격자선은 영역 끝까지 그어지는데(`gridShapes`)
격자점은 안쪽에서 끝나서, 테두리를 그으려 해도 양끝이 여백만큼 못 미쳤다.

```
80mm · 6mm 간격
  격자선  0 ────────────────── 80
  격자점     1  7 … 73  79        ← 여기까지만 붙을 수 있었다
```

끝까지 채울 때는 **영역 가장자리도 붙을 자리로 넣는다.** 끝까지 채우겠다는 뜻은
거기에 그릴 수 있다는 뜻이다.

대신 **그 가장자리 띠는 칸으로 세지 않는다.** 간격보다 좁아 무언가를 앉힐 수 없다.
`xs.length - 1`로 세면 틀리므로 `Lattice.cols`·`rows`를 core가 직접 세어 내놓는다.

### 알림을 오른쪽 아래로

위쪽은 탭·저장·PDF 버튼이 몰려 있어 알림이 그것들을 가렸다.

### 가장자리 도트는 화면에만 (뒤이어 고침)

끝까지 채울 때 넣은 가장자리 자리에 **도트까지 찍혀서** 격자가 한 줄 더 있는
것처럼 보였다. 그 자리는 격자의 일부가 아니라 **끝까지 그을 수 있게 하려고 넣은
안내**다. 그래서 `Lattice.padded`로 표시해두고 **인쇄할 때만 도트를 뺀다.**

- 화면 — 보여야 어디에 붙는지 알고 끝까지 그을 수 있다
- 인쇄 — 찍지 않는다. 재단선 위에 반쯤 잘린 도트가 남으면 격자가 어긋나 보인다

**선은 인쇄해도 그대로 긋는다.** 잘린 칸을 종이 끝에서 닫아주는 것이 그 선이라
빼면 칸이 열려버린다. 도트와 선을 다르게 다루는 것이 요점이라 테스트로 고정했다.

### 편집 화면에서도 칸 수로 격자를 정한다

새 양식 창에만 있던 것을 설정 패널에도 넣었다. **지금 몇 칸인지 보여주고, 고치면
간격이 거꾸로 계산된다.** 가로·세로를 나란히 두되 간격은 하나뿐이라 한쪽을
고치면 다른 쪽이 따라 움직인다.

---

## ✅ 5b — 파일로 저장하고 불러온다

- 테스트 251개 통과 · 타입 검사 · 빌드 통과

맨 위에 `저장` · `불러오기`가 생겼다. 탭이 아니라 헤더에 둔 이유는 저장이
**지금 보고 있는 양식 하나가 아니라 작업 전체**에 대한 일이기 때문이다.

이 프로그램은 서버가 없어서 새로고침 한 번에 작업이 사라진다. 손에 쥘 수 있는
파일이 있어야 안심하고 쓴다.

### 담지 않는 것 둘

**실행취소 이력.** 되돌리기는 이번 작업 중의 이야기다. 파일을 열었더니 남의
과거로 되돌아갈 수 있으면 오히려 놀란다. 지금 모습만 담는다.

**글꼴 파일.** 이름만 담는다. 2.7MB짜리를 base64로 넣으면 파일이 3.6MB씩 불어나고,
남의 글꼴을 실어 옮기는 것은 라이선스상으로도 애매하다. 그리고 **실제로 쓰인 것만**
담는다 — 등록만 해놓고 안 쓴 글꼴까지 적으면 불러올 때 필요도 없는 파일을 찾게 된다.

### 같은 이름으로 다시 등록하면 되살아난다

파일에 이름만 있으니, 열고 나면 그 글꼴을 쓰던 글자는 기본 글꼴로 보인다.
드롭다운에는 `내글꼴 (파일 없음)`으로 남아 무엇이 빠졌는지 알려주고, 불러온 직후에도
어떤 글꼴이 필요한지 알림으로 띄운다.

**같은 이름의 파일을 다시 등록하면 그때의 id를 물려받는다.** 새 id를 주면 글자들이
영영 없는 글꼴을 가리키게 된다. `registerFont(file, reuseId)` 한 줄과 `reserveIds`
(불러온 id와 새 id가 부딪히지 않게 번호를 밀어두는 것)로 해결했다.

### 판 번호를 맨 앞에 박았다

`version: 1`. 앞으로 뒷면이나 표가 생겨도 옛 파일을 읽을 수 있어야 한다.
**없는 값은 기본값으로 채운다** — `dash`·`toEdge`·`minMargin`이 없던 시절의 파일도
새 설정의 기본값으로 열린다(테스트로 고정).

반대로 **더 새로운 판은 거른다.** 판 번호를 알려주며 열지 않는다. 모르는 구조를
억지로 읽어서 조용히 망가뜨리는 것보다 낫다.

### 검사는 화면이 죽지 않을 만큼만

좌표가 숫자인지까지 뜯어보면 코드가 배로 늘고, 그렇게까지 망가진 파일은 사용자가
손으로 고친 경우뿐이다. **양식 목록이 있는지, 각 양식에 크기와 객체 배열이 있는지**만
본다. 걸리면 무슨 일인지 알려주고 **지금 작업은 그대로 둔다.**

---

## ✅ 5a — 양식이 여러 개가 됐다

- 테스트 231개 통과 · 타입 검사 · 빌드 통과
- 기능은 그대로다. **담는 그릇이 바뀌었다**

### 나중에 고친 것 둘

**갤러리 썸네일이 검은 바탕으로 나왔다.** `.insert-sheet .sheet-bg { fill: #fff }`가
편집 화면 안에서만 걸리는 규칙이라, 갤러리의 `<svg>`에서는 **SVG 기본 채움색인
검정**이 그대로 나왔다. 속지 바탕은 어디서 그리든 흰색이므로 부모를 따지지 않게 했다.

**맨 위 `+ 새 양식`이 규격을 묻지 않고 바로 만들었다.** 지금 규격을 물려받는
동작인데, 그건 크기 그룹의 `+`에 어울리는 뜻이다("이 크기로 하나 더"). 맨 위
버튼은 가운데 창을 띄워 규격을 고르게 했다 — **지금 규격이 처음부터 골라져 있어서**
그대로 누르면 예전과 같은 결과가 된다.

### 속지 한 장에 대한 것이 전부 양식 안으로 들어갔다

```
전                                후
  store.insert                      store.templates[]
  store.dotGrid           →           ├ insert
  store.objects                       ├ dotGrid
  store.paper · gap · align…          └ objects (실행취소 포함)
                                    store.activeId
                                    store.paper · gap · align…  (그대로)
```

**양식은 용지를 모른다**(설계문서 3장). 그래서 용지·배치 설정만 store 루트에 남았다.

읽는 자리가 38곳이라 `activeTemplate(s)` 하나로 모았다. 화면은 `useInsert()`
`useDotGrid()` `useObjects()` 지름길을 쓴다.

**실행취소가 양식마다 따로다.** 양식을 바꿨다가 ⌘Z를 눌렀는데 다른 양식의 작업이
사라지면 무섭다. 양식을 바꾸면 고른 것도 함께 푼다 — 다른 양식의 id가 선택 목록에
남아 있으면 지우기·스타일이 엉뚱하게 동작한다.

이 두 가지가 이 변경의 핵심이라 store 테스트로 못 박았다.

### 양식 관리 탭

`양식 관리 → 양식 만들기 → 인쇄하기`. **탭 순서가 곧 작업 순서다** — 고르고,
그리고, 인쇄한다. 지금까지는 첫 칸이 비어 있었다.

크기별로 묶어서 보여준다. **양식이 크기를 소유하는 것이 아니라 크기를 속성으로
가진다.** 같은 M6 양식이 열 개여도 되고, 갤러리는 그 속성으로 줄을 세울 뿐이다.

썸네일은 **편집 화면과 같은 `InsertView`를 작게 그린 것이다.** 따로 이미지를 만들지
않는다 — 화면이 이미 SVG로 그리고 있어서 viewBox만 줄이면 그대로 미리보기가 된다.
양식이 수십 개가 되면 SVG를 수십 장 그리게 되는데, 느려지면 그때 손본다.

`+ 새 양식`은 맨 위에, 각 크기 그룹에도 `+`를 따로 뒀다. 그룹의 `+`는 그 크기로
바로 만든다 — 대부분 한 규격으로 계속 작업하므로 매번 고르게 하면 손이 더 간다.

### 규격을 바꿔 복제한다 — 비례 조정은 하지 않는다

80×125로 만든 것을 75×125로도 뽑고 싶을 때가 있다. **원본을 고치는 대신 사본을
만든다.** 실패해도 잃는 것이 없다.

**비례로 줄이지 않는다.** 80 → 75는 93.75%인데 세로는 그대로라 글자가 납작해지고,
세로까지 줄이면 5mm 격자가 4.69mm가 된다. 5mm 격자는 시판 다이어리와 맞추려고 정한
값이고 모든 작업이 그 위에서 일어난다 — 4.69mm 격자는 아무 의미가 없다.

조용히 자동으로 바뀌는 값이 사고를 낸다는 것을 이 프로젝트는 이미 두 번 겪었다
(줄 간격이 도트를 따라가던 것, `OBJECT_LINE_CAP`이 화면과 PDF에서 달랐던 것).

실제로 일어나는 일은 훨씬 단순하다. 격자 간격도 글자도 선도 그대로고, **칸 하나가
줄어들 뿐이다.** 오른쪽 끝에 걸친 것만 밖으로 나간다. 그래서 **몇 개가 나가는지만
미리 세어서 알려준다**(`outsideCount`). 지우지도 옮기지도 않는다 — 화면과 PDF 모두
속지 밖을 이미 잘라내므로 동작은 안전하고, 몰라서 놀라는 것만 막으면 된다.

### 타입 검사를 잘못 돌리고 있었다

`npx tsc --noEmit`은 루트 tsconfig가 `files: []`에 참조만 걸어둔 구성이라 **아무것도
검사하지 않는다.** 진짜 검사는 `npm run build`의 `tsc -b`였다. 다행히 매 커밋마다
빌드를 돌렸으므로 지나간 것은 없다. 앞으로는 `tsc -b`를 쓴다.

---

## ✅ 4c-2c — 글꼴 파일 등록. 4단계가 끝났다

- 테스트 202개 통과 · 타입 검사 · 빌드 통과

`.ttf`/`.otf`/`.woff`를 등록해서 **글자마다** 골라 쓴다. 속성 막대 맨 앞에 글꼴
드롭다운이 생겼고, 목록 맨 아래 `글꼴 파일 추가…`가 파일 창을 연다.

고르기와 등록을 드롭다운 하나에 함께 둔 것은, **글꼴을 바꾸려다 없다는 걸 아는
순간이 곧 등록하고 싶은 순간**이기 때문이다. 등록하면 방금 그 글자에 바로 씌운다 —
등록만 하고 또 고르게 하면 손이 한 번 더 간다.

### 화면과 PDF가 같은 파일을 본다

화면은 `FontFace`로 브라우저에 심고, PDF는 같은 바이트를 pdf-lib에 넘긴다.
등록하는 순간 `face.load()`까지 마치고, 실패하면 목록에 넣지 않는다 — 넣어놓고
나중에 안 그려지는 것보다 낫다.

**바이트는 store에 넣지 않았다.** 수 MB짜리 버퍼가 상태에 있으면 구독하는
컴포넌트마다 그것을 들여다보게 된다. `fonts/registry`가 들고 있고 store에는
목록(id·이름·CSS 이름)만 둔다.

CSS 이름은 파일 이름이 아니라 id에서 만든다(`user-font-f1`). 같은 이름을 두 번
등록하거나 파일 이름에 따옴표가 들어가도 깨지지 않는다.

### 등록 글꼴에는 굵게를 막았다

그 글꼴의 Bold 파일이 없기 때문이다. 브라우저는 가짜 굵게를 만들어주지만
pdf-lib에는 그 기능이 없어서, 허용하면 **화면만 굵고 인쇄물은 그대로** 나온다.
`core/text`의 `canBold`가 이 규칙을 쥐고 있고, 속성 막대의 굵게 버튼이 흐려진다.
굵은 글꼴이 필요하면 Bold 파일을 따로 등록해서 고른다.

### 새로고침하면 사라진다

양식 저장(5단계)이 생기기 전까지는 어쩔 수 없다. 2.7MB짜리 파일을 양식 JSON에
넣을 수는 없다. 그 글꼴을 쓰던 글자는 **기본 글꼴로 되돌아간다** — 화면도 PDF도
같이 그렇게 동작한다. 글자가 안 보이는 것보다 낫다.

### 폭을 재는 글꼴도 같이 바꿨다

`measureTextBox`가 글꼴 이름을 받는다. 상자가 자동으로 늘어날 때 쓰는 값인데,
그리는 것과 다른 글꼴로 재면 상자가 안 맞는다. 굵기를 넣을 때와 같은 이유다.

---

## ✅ 격자 여백과 선 모양을 열었다

- 테스트 199개 통과 · 타입 검사 · 빌드 통과

여백을 줄이거나 없애고 싶다는 요청. 원래는 **일부러 입력받지 않던 값**이라
그 원칙을 지키는 선에서 열었다.

### 여백이 아니라 `최소 여백`을 받는다

여백 자체를 값으로 받으면 간격과 서로 맞지 않는 상황이 생긴다 — 처음에 피한
이유가 그것이다. 그래서 공식은 그대로 두고 **하한만** 설정으로 열었다.

```
칸 개수 = floor((영역 − 최소여백 × 2) ÷ 간격)     ← 최소여백만 설정값이 됐다
여백    = (영역 − 칸 개수 × 간격) ÷ 2             ← 여전히 종속 변수
```

`MIN_MARGIN` 상수가 `DotGrid.minMargin`이 되고, 상수는 `DEFAULT_MIN_MARGIN`(3mm)로
이름이 바뀌어 기본값만 맡는다. 0~20mm.

**0으로 둬도 정확히 0은 아니다.** 나누어떨어지지 않고 남는 만큼은 좌우에 반씩
남는다 — 80mm에 5mm 간격이면 딱 0이지만 6mm 간격이면 1mm가 남는다. 테스트로
못 박아뒀다. 0에 가까우면 바깥쪽이 재단선에 붙어 자를 때 날아갈 수 있는데,
막지 않고 설정 패널에서 경고만 띄운다.

### 여백 없이 끝까지 — 잘린 칸을 허용한다

처음에는 "선만 영역 끝까지 늘이기"로 만들었는데, 써보니 그게 아니었다. 격자점은
여전히 여백 안쪽에서 끝나서 **양끝에 세로선 없는 띠가 남았다.** 원한 것은
"끝 칸이 잘려도 좋으니 격자 자체가 종이 끝까지 가는 것"이었다.

그래서 `linesToEdge`를 `toEdge`로 바꾸고 의미를 넓혔다.

한 번 더 고쳤다. 처음엔 왼쪽 위 모서리에서 시작하게 했는데, 그러면 **오른쪽과
아래만 잘려서 격자가 한쪽으로 쏠려 보인다.** 가운데 정렬을 그대로 두어야 남는
만큼이 양끝에 반씩 붙고 네 모서리가 고르게 잘린다.

```
80mm · 5mm 간격 → 0, 5, …, 80        딱 떨어져 잘린 칸이 없다
80mm · 6mm 간격 → 1, 7, …, 79        양끝 1mm씩이 잘린 칸
```

그래서 이 옵션은 결국 **여백의 하한을 0으로 두는 것**과 같은 계산이다. 특수한
분기가 없다 — `gridLattice`가 `toEdge`면 `minMargin` 대신 0을 넘길 뿐이다.
선을 영역 끝까지 긋는 일만 `gridShapes`가 따로 맡는다.

**잘린 칸이 버그가 아니라 이 옵션의 요점이다.** 그래서 최소 여백은 이때 쓰이지
않고, 설정 패널에서도 숨는다 — 여백을 없애는 것이 목적인데 하한을 받을 이유가 없다.

이번에는 **격자점 좌표가 실제로 달라진다.** 붙는 자리도 함께 옮겨간다. 간격을
바꾸는 것과 같은 종류의 설정이라 문제될 것이 없다 — 이미 그려둔 것의 좌표는
건드리지 않는다. 보이는 선만 늘이고 붙는 자리는 그대로 두는 것이 오히려 나빴다:
선은 있는데 거기 붙지는 못하는 자리가 생긴다.

끝까지 채울 때는 선도 영역 끝까지 긋는다. 잘린 마지막 칸이 종이 끝에서 닫혀야
칸으로 읽힌다. 기준은 속지가 아니라 **격자 영역**이라, 안전영역을 비워뒀다면
구멍 쪽으로 넘어가지 않는다.

### 격자선도 파선·점선

`DotGrid.dash`. 그은 선과 같은 세 가지다.

점선 비율은 `core/line`의 `dashPattern` 하나에서 나온다. 격자선은 객체가 아니고
화면과 인쇄의 굵기도 다르지만(`SCREEN_GRID_LINE_WIDTH` 대 `GRID_LINE_WIDTH`)
비율은 한 군데여야 한다. 그래서 `dashPattern(dash, width)`로 굵기를 받도록 바꾸고,
선 하나에서 꺼내 쓰는 `dashPatternOf(o)`를 그 위에 얹었다.

점선일 때는 끝을 둥글게 처리한다. 길이가 굵기와 같아서 정확히 원이 된다 —
그래야 `점선`이 점으로 읽힌다.

---

## ✅ 4c-2b — 굵게, 그리고 단축키·격자 복귀

- 테스트 181개 통과 · 타입 검사 · 빌드 통과

### 굵게는 진짜 Bold 파일로 그린다

`Pretendard-Bold.ttf`를 함께 담았다. **가짜 굵게(synthetic bold)를 쓰지 않는다** —
브라우저는 Regular의 획을 부풀려 굵은 척을 해주지만 pdf-lib에는 그 기능이 없어서,
그대로 두면 화면만 굵고 인쇄물은 보통 굵기로 나온다. `OBJECT_LINE_CAP`으로 이미
겪은 그 사고다.

받아온 파일이 저장소의 Regular와 **같은 판인지 해시로 대조했다**(v1.3.9의
`static/alternative`, 두 파일 모두 `6d0af525…`). 판이 다르면 글자 폭이 미묘하게
달라져 화면과 PDF가 어긋난다.

- `core/text.ts`에 `boldOf` — 화면·PDF·폭 재기가 모두 이걸 부른다
- `core/style.ts`에 `FONT_WEIGHT = { regular: 400, bold: 700 }` — `@font-face`
  선언과 같은 값이어야 진짜 파일이 쓰인다
- **폭을 재는 글꼴과 그리는 글꼴이 반드시 같아야 한다.** Bold는 획이 두꺼운 만큼
  폭도 넓어서, Regular로 재고 Bold로 그리면 가운데 정렬이 어긋난다
- 파일 하나가 2.7MB라 **굵은 글자가 실제로 있을 때만** 받고 심는다. 없으면
  망가진 바이트를 넘겨도 터지지 않는다(테스트로 고정)

### 단축키가 크롬과 부딪히던 것

`⌘V`를 누르면 붙여넣기가 되면서 **동시에** 도구가 `고르기`로 바뀌었다. `⌘T`는
새 탭 + `글자`, `⌘L`은 주소창 + `그리기`. 크롬이 우리 것을 뺏은 게 아니라 우리가
크롬 것에 끼어든 쪽이었다 — 단축키 처리가 `e.key`만 보고 수정키를 보지 않았다.

`if (e.metaKey || e.ctrlKey || e.altKey) return;` 한 줄로 빠진다. 키를 `⌘⇧T` 같은
조합으로 옮기는 것은 답이 아니다. 그것도 크롬이 쓰고(닫은 탭 열기), 게다가
`⌘T`·`⌘N`·`⌘W`는 웹페이지가 `preventDefault`로 막을 수도 없다. 맨 글자는 브라우저가
쓰지 않으므로 가장 안전한 자리다.

**그리기를 `L`에서 `D`로 옮겼다.** `L`은 오른손 끝이라 멀다. `V`·`D`·`T`는 왼손만으로
다 닿는다.

### 한 번 격자를 벗어나면 영원히 벗어나 있던 것

4c-2a의 ⌘ 이동으로 밀어낸 글자가, ⌘ 없이 다시 옮겨도 어긋난 만큼을 그대로 달고
다녔다. **이동량**을 칸 단위로 반올림했기 때문이다.

**이제 이동량이 아니라 도착지를 맞춘다.** 고른 것 전체를 감싸는 네모의 왼쪽 위
(`anchor`)가 앉을 자리를 격자에 맞추고, 그 차이만큼 전부 함께 옮긴다. anchor가 이미
격자 위면 돌아갈 어긋남이 0이라 예전과 결과가 정확히 같다(테스트로 고정).

격자점 **목록**이 아니라 **위상**(첫 점 + n칸)에 맞춘다. 목록은 여백 앞에서 끝나서,
그것에 맞추면 지금 가능한 "여백 쪽으로 밀어내기"가 막힌다.

### 그래서 생긴 문제 — 클릭만 해도 튀었다

도착지를 맞추자, 격자를 벗어나 있던 것은 **손이 1픽셀만 떨려도** 최대 반 칸이
밀렸다. 고르려고 눌렀을 뿐인데 옮겨진다.

`DRAG_START = 1mm`를 넘기기 전까지는 **누른 것**으로 본다. 클릭·드래그 판정을
이동량이 0인지가 아니라 손이 움직였는지로 바꿨다.

붙는 자리를 정하는 스냅에는 여전히 문턱값이 없다. 이건 **몸짓을 가르는** 문턱이지
좌표를 정하는 문턱이 아니다.

### 개발 서버가 `127.0.0.1`로 안 열리던 것

`vite.config.ts`에 `server` 설정이 없어 기본값을 쓰는데, Node 17부터 DNS 순서가
바뀌어서 IPv6(`[::1]`)에만 붙었다. `localhost`는 열리는데 `127.0.0.1`은 연결이
거부됐다. `host: true`로 둘 다 연다 — 같은 네트워크의 다른 기기에도 보이므로
집에서만 쓴다는 전제다.

---

## ✅ 4c-2a — 격자를 풀고 옮긴다

- 테스트 173개 통과 · 타입 검사 · 빌드 통과

**⌘(윈도우는 Ctrl)을 누른 채 끌면 격자를 벗어난 자리에 놓인다.** 칸 한가운데에
제목을 얹거나, 선과 선 사이에 글자를 맞춰 넣을 때 쓴다. 누르지 않으면 지금까지처럼
칸 단위로 움직인다.

키를 ⌥가 아니라 ⌘로 정했다. Figma·스케치가 ⌘를 "스냅 무시"에 쓰고, ⌥는 대부분의
도구에서 **복제**라 나중에 복제를 넣을 때 부딪힌다.

### 계산은 core/snap의 moveDelta가 한다

원래 EditorTab 안에서 `Math.round(...)`를 직접 하고 있었다. 격자를 푸는 조건이
붙으면서 규칙이 생겼으므로 core로 옮겼다. 테스트 7개가 붙었다.

**여전히 객체를 다시 스냅하지 않고 이동량만 맞춘다.** 그래서 선의 길이와 각도가
변하지 않고, 이미 격자를 벗어나 있던 것도 그 상대적인 자리를 지키며 함께 움직인다.
격자를 한 번 푼 글자를 나중에 격자 모드로 옮겨도 어긋난 만큼은 그대로 유지된다 —
칸에 다시 붙이고 싶으면 지우고 새로 놓는 것이 아니라 그냥 ⌘ 없이 끌면 된다.

**격자를 풀 때는 0.01mm로 정리한다.** 부동소수점 꼬리(`37.294857...`)가 그대로
저장 파일에 들어가면 사람이 읽을 수 없고, 0.01mm는 어떤 프린터도 구별하지 못한다.

### 상태줄에 띄운다

숨은 단축키는 있는지조차 알 수 없다. 옮기는 동안 맨 아래에 `옮김 3.2 , 1.7 mm ·
⌘로 격자 해제`가 뜨고, 실제로 풀린 동안에는 초록색으로 `· 격자 해제`가 된다.

---

## ✅ 코드 분리 점검 — 고칠 때 헤집는 범위를 줄였다

- 테스트 166개 통과 · 타입 검사 · 빌드 통과
- 기능은 하나도 바뀌지 않았다. 같은 동작을 다른 자리에 놓은 것뿐이다

4c-2에 들어가기 전에 **"고치고 더하기 쉬운 구조인가"**를 점검했다. 두 가지가 걸렸다.

### 선(線)은 core로 절반만 뽑혀 있었다

글자는 화면과 PDF가 `sizeOf`·`alignOf`·`valignOf`·`lineHeightOf`라는 **같은 함수**를
부른다. 그런데 선은 두 뷰가 각자 계산하고 있었다.

| | 화면 | PDF |
|---|---|---|
| 기본 굵기 | `o.width ?? OBJECT_LINE_WIDTH` | 같은 식을 다시 |
| 점선 비율 | `width*6, width*4` | `width*6, width*4` |

양쪽 주석에 *"화면과 같은 비율을 쓴다"*고 적혀 있었다. **알고 있었지만 주석은
강제가 아니다.** 이 프로젝트는 이미 정확히 이 사고를 겪었다 — `OBJECT_LINE_CAP`을
화면만 `round`, PDF는 `butt`로 둬서 **인쇄물에서만** 굵은 선의 꼭짓점이 끊겼던 건.
그때는 값을 core/style로 모아 고쳤는데, `?? 기본값`과 점선 비율 `6·4·1·3`은
그대로 두 군데에 남아 있었다.

[core/line.ts](app/src/core/line.ts)를 만들어 `widthOf`·`colorOf`·`dashOf`·`dashPattern`
넷을 두었다. core/text와 대칭이다. `dashPattern`은 **mm로** 돌려주고, 화면은 그대로
이어 붙이고 PDF는 pt로 바꿔 쓴다 — 비율이 한 군데에만 있다. 테스트 7개로 고정했다.

부수 효과로 `OBJECT_LINE_WIDTH`·`OBJECT_LINE_COLOR`를 import하던 뷰가 사라졌다.
이제 기본값을 아는 건 core뿐이다.

### EditorTab.tsx 1258줄을 쪼갰다

파일이 큰 것 자체보다, **한 번 고칠 때 훑어야 하는 범위**가 문제였다. 지난 회차에
버그 하나 고치면서 `finishEditing` → `setEditingStyle` → `TextInput` → `StyleBar` →
`TextControls` 다섯 군데를 오갔는데 전부 한 파일이라 1200줄을 위아래로 스크롤했다.

자를 자리는 이미 주석으로 그어져 있었고(`/* ── 속성 막대 ── */`, `/* ── 몸짓 ── */`),
실제로 붙어 있지도 않았다 — 속성 막대 구간이 EditorTab 본문과 공유하는 건
`Editing` 타입 **하나뿐**이었다.

```
ui/StyleBar.tsx   388줄  속성 막대. store와 props로만 이야기하고 캔버스를 모른다
ui/gestures.ts    117줄  확정되기 전의 것들(Drag·Editing)과 그 계산. React가 없다
ui/EditorTab.tsx  803줄  (1258에서)
```

**덤으로 Fast Refresh가 살아났다.** EditorTab이 컴포넌트 말고 `sizeLabelOf`도
내보내고 있어서 vite가 매번 `Could not Fast Refresh`를 띄우고 전체를 다시 붙였다.
이제 EditorTab이 내보내는 건 컴포넌트 하나뿐이라 고치는 즉시 화면만 갈린다.

**아직 남은 것:** `EditorTab` **함수 하나가 여전히 648줄**이다. 파일에서 뺀 것은
함수 바깥에 있던 것들이라 컴포넌트 본문은 그대로다. 다음에 자른다면 `<svg>` 안의
"조작 중인 것 미리보기"(527~648줄, 선택 표시·손잡이·유령선·마키·치수)를 통째로
떼는 자리다. 다만 4c-2에서 손댈 곳은 속성 막대와 몸짓 쪽이라 급하지 않다.

---

## ✅ 4c-1 다듬기 4차 — 속성 막대를 손봤다

- 테스트 159개 통과 · 타입 검사 통과

### 크기를 고르려는 순간 편집이 끝나버렸다

글자를 더블클릭해 고치는 중에 속성 막대의 드롭다운을 누르면, 방금 쓴 글이 아니라
**다음에 쓸 글**에 크기가 걸렸다.

입력칸에 `onBlur={onDone}`이 걸려 있던 탓이다. 드롭다운을 누르는 것도 입력칸에게는
포커스를 잃는 일이라 그 순간 편집이 확정·종료되고, 막대는 이미 `쓰는 중`에서
`쓸 글자`로 바뀌어 있었다.

**포커스가 막대 바깥으로 나갈 때만 확정한다.** `relatedTarget`이 `.editor-bar` 안이면
여전히 이 글자를 고치는 중으로 본다. 그리고 드롭다운을 고르고 나면 **입력칸으로
포커스를 돌려준다** — 안 그러면 무엇을 고쳤는지는 보이는데 이어서 칠 수도, Enter로
확정할 수도 없다.

직접 치는 칸(크기·줄 간격)에서는 돌려주지 않는다. 한 글자 칠 때마다 포커스를
빼앗기면 값을 끝까지 칠 수 없다. 대신 그 칸에서 Enter를 치면 돌아간다.

### `기본`이라는 선택지를 없앴다

`기본 0.2mm`와 `0.2mm`가 목록에 나란히 있으면 무엇이 다른지 되묻게 된다.
이제 **목록에서 기본값에 해당하는 항목이 그냥 처음부터 골라져 있다** — 그 항목의
value가 빈 문자열이다. 굵기·선 색·글자 색 셋 다 같은 방식이고, 정렬처럼 이미
그렇게 돼 있던 것들과도 규칙이 맞았다.

안에서는 **여전히 값을 비워둔다.** 나중에 기본값을 바꾸면 손대지 않은 것들이 같이
따라온다는 성질은 그대로다. 예전에 명시적으로 0.2mm를 골라둔 선도 같은 항목에
표시되도록 읽을 때 한 번 정규화한다.

### 글자 크기와 줄 간격은 직접 친다

고를 수 있는 몇 개(6·7·8·9…24pt)로 묶어두면 6.5pt를 쓸 수 없다. 손바닥만 한
속지에서는 0.5pt 차이가 한 줄을 더 넣느냐 마느냐를 가른다. 설정 패널에서 도트
간격을 직접 치는 것과 같은 방식으로 바꿨다.

치는 도중에는 `6.`이나 빈 칸처럼 아직 숫자가 아닌 상태를 반드시 거친다. 그 값을
그대로 올리면 크기가 0이 되거나 NaN이 샌다. 그래서 화면에 보이는 글자는 `NumField`가
따로 들고 있고 **읽을 수 있는 값이 됐을 때만** 위로 올린다.

**줄 간격은 이번에 새로 열었다.** 만들 때 도트 간격이 새겨지는 것은 그대로고,
그 값을 시작점으로 고칠 수 있게 됐다. 한 번 고쳐두면 크기·색과 마찬가지로 다음에
쓰는 글자로 이어진다 — `newTextStyle`이 직접 정해둔 값을 도트 간격으로 덮어쓰지
않는다(`core/text.ts`, 테스트 5개).

### 도트 크기에 글자 크기를 묶지 않기로 했다

2mm 칸에 글자 상자를 만들면 상자가 칸보다 훨씬 크게 나온다는 지적이 있었고,
도트가 작아지면 글자도 따라 작아지게 하자는 제안이 있었다. **하지 않기로 했다.**

바로 위 회차에서 고친 버그와 같은 종류다. 줄 간격이 현재 도트 간격을 읽는 바람에
20mm로 쓴 글이 5mm로 바꾸는 순간 무너졌다. 글자 크기를 도트에 묶으면 도트를 바꾸는
순간 이미 만든 양식의 글자가 전부 변한다. 게다가 글자 크기는 인쇄 가독성 문제이지
격자 문제가 아니다 — 2mm에 비례시키면 5~6pt가 되는데 뽑아보면 읽기 어렵다.
촘촘한 격자는 작은 글자가 아니라 **정밀한 배치**를 위해 쓰는 경우가 많다.

원하던 것은 이미 된다. 마지막에 쓴 크기가 다음 글자로 이어지므로, 촘촘한 격자에서
한 번 6pt로 바꿔두면 그 뒤로 계속 6pt다. 크기가 자유 입력이 되면서 훨씬 편해졌다.

---

## ✅ 4c-1 다듬기 3차 — 글자 편집 버그 두 가지

- 테스트 154개 통과 · 빌드 성공
- **GitHub 저장소 개설** — https://github.com/jiing2222/Diary-Designer

### 인쇄 안내문을 맨 아래 상태줄로

용지 오른쪽 아래 구석에 작게 얹었는데도 미리보기를 가렸다. `용지 210 × 297mm`가
적힌 맨 아래 줄로 옮겼다. 인쇄하기 탭에서만 보인다 — 양식 만드는 중에는 상관없는
이야기다.

### 더블클릭 수정이 깨진 원인

지난 회차에 "고치는 중인 글자를 화면에서 감추기"를 만들면서 그 글자를 목록에서
**빼버렸다**. 그런데 클릭 판정(`hitAt`)은 DOM의 `<text>` 요소를 훑어서 찾는다 —
**편집을 여는 순간 그 글자는 클릭으로 찾을 수 없게 됐다.**

글자 도구에서 더블클릭하면: 첫 클릭이 편집을 열고 → 글자가 DOM에서 사라지고 →
둘째 클릭이 그걸 못 찾아 "빈 곳"으로 판단해 편집을 강제 종료하고 새 빈 상자를
만들었다.

**제거 대신 `visibility: hidden`으로 감춘다.** SVG에서 visibility는 화면에서만
없애고 좌표(`getBBox`)는 그대로 남기므로, 감추는 목적을 지키면서 판정이 산다.
`InsertView`에 `hiddenId` prop을 추가했다.

### 줄 간격을 만들 때 정해서 새겨둔다

전에는 그리는 순간마다 **현재** 도트 간격을 읽었다. 그래서 20mm 도트에서 쓴 글이
5mm로 바꾸는 순간 줄이 소급해서 좁아졌다 — 만들어둔 양식이 설정 하나에 무너지는
셈이었다.

`TextObject.lineHeight`를 추가해 **만들 때의 도트 간격을 객체에 새겨둔다.**
크기·색·정렬과 똑같은 규칙이다(값이 있으면 그것, 없으면 기본값). `core/text.ts`의
`lineHeightOf(t)`가 그 값을 읽고, 화면·PDF·자동 늘어남·타이핑 중 입력칸이 전부
같은 값을 쓴다.

브라우저에서 실측 확인: 20mm 도트에서 3줄을 쓰면 줄 간격 20mm, 도트를 5mm로
바꿔도 `[43.3255, 63.3255, 83.3255]` 그대로다.

---

## ✅ 4c-1 다듬기 2차 — 써보고 나온 것들

- 테스트 151개 통과 · 빌드 성공

### 고치는 중인 글자는 화면에서 감춘다

입력칸이 같은 자리에 겹쳐 있는데 밑에 원래 글자가 그대로 남아 있어서, 지금 고치는
중인 게 무엇인지 헷갈렸다. `EditorTab`이 `InsertView`에 넘기는 `objects`에서
`editing.id`를 걸러낸다 — 입력칸이 뜨는 동안은 그 객체 하나만 화면에서 빠진다.
목록(store)에서 지우는 게 아니라 **그 순간의 렌더링에서만** 뺀다.

### 줄 간격이 도트 간격을 따른다

⇧Enter로 여러 줄을 쓰면 이제 **줄마다 정확히 다음 도트 줄에 앉는다.** 전에는
글자 크기의 1.4배(대략 4.4mm)를 줄 간격으로 썼는데, 5mm 도트보다 조금 좁아서
줄이 늘수록 칸 한가운데에서 위쪽으로 밀렸다.

`core/text.ts`에 `effectiveLineHeight(size, spacing)`을 추가했다 — **도트 간격을
그대로 줄 간격으로 쓰되**, 글자가 도트 간격보다 커서 겹칠 것 같을 때만(예: 2mm
도트에 24pt 글자) 글꼴이 실제로 차지하는 높이로 물러난다. `blockHeight`·
`lineBaselines`가 이 값을 받도록 시그니처가 바뀌어서 PDF(`pdf/export.ts`)·화면
(`ui/InsertView.tsx`)·자동 늘어남 계산(`ui/measureText.ts`)·타이핑 중인 입력칸의
CSS `line-height`(`ui/EditorTab.tsx`)까지 전부 같은 값을 쓰게 고쳤다.

### 편집 화면에서는 넘친 것도 그냥 보인다

두 가지가 겹친 문제였다.

1. **글자가 상자를 넘어 속지 밖으로 나가면 클리핑 때문에 안 보였다.** 하지만
   그건 인쇄할 때나 문제고, 작업 중에는 얼마나 넘쳤는지 보여야 다음에 뭘 할지
   판단할 수 있다. `InsertView`의 클리핑을 `mode === 'print'`일 때만 걸도록 바꿨다
   — 편집 화면(mode='edit')은 이제 자르지 않는다.
2. **선 길이 표시(`.size-label`)가 속지 오른쪽 끝 가까이에서 커서를 따라가다
   보면 사라졌다.** 이건 클리핑이 아니라 **SVG 자체의 기본 동작**이었다 —
   `viewBox` 밖으로 나간 내용은 `overflow: hidden`이 기본값이라 뷰포트에서
   잘린다. `.insert-sheet`에 `overflow: visible`을 줘서 편집 화면 전체가
   뷰박스 밖으로 나간 것도 보여주게 했다.

**둘 다 편집 화면(EditorTab)에만 적용된다.** 인쇄하기 탭의 미리보기(PaperPreview,
mode='print' 기본)와 실제 PDF는 여전히 자른다 — 옆 칸을 침범하는 건 진짜 문제이기
때문이다. PDF를 직접 렌더링해서 옆 칸을 안 넘는 것과, 편집 화면에서 넘친 글자가
캔버스 밖 회색 배경까지 보이는 것 둘 다 확인했다.

### 왼쪽 아래에 칸 개수를 항상 띄운다

도트든 그리드든 가로줄이든 세로줄이든, 지금 격자가 몇 칸인지 `editor-foot`에
계속 보인다(`칸 14 × 23개`). 모양과 무관하게 같은 격자(`gridLattice`)에서 나오는
값이라 스타일을 바꿔도 같은 수가 나온다.

### 인쇄하기 화면을 다시 배치했다

- **미리보기 토글이 양식 만들기의 도구줄과 같은 자리(상단)로 올라갔다.**
  `.print-bar`를 새로 만들어 `.editor-bar`와 같은 자리·같은 생김새로 맞췄다.
  **기본값도 켜짐으로 바꿨다** — 어차피 실제 인쇄 모습을 보려고 켜는 화면이니
  매번 켜는 수고를 없앴다.
- **"100% 인쇄" 안내문을 용지 오른쪽 아래 구석에 작게 옮겼다.** 전에는 위쪽에
  큼직하게 있어서 실제 인쇄 모습을 가렸다.
- **칸 경계선(회색 사각형)을 인쇄 미리보기에서 뺐다.** 작업할 땐 어디가 한 장인지
  보여주는 안내선이지만 실제로 인쇄되는 선이 아닌데, 미리보기에도 계속 남아 있어서
  진짜 인쇄물처럼 안 보였다.
- **"프린터가 못 찍는 가장자리" 표시도 인쇄 미리보기에서 뺐다.** 타공 안내와
  같은 이유 — 화면 전용 경고라 실제 인쇄 모습에는 없다.
- **50mm 검증 눈금자는 기본을 꺼둔 채로 바꿨다.** 1단계에서 이미 50mm 정확도를
  실측으로 검증했으니 매번 다시 잴 필요가 없다. 다만 이건 실제로 PDF에도
  찍히는 내용이라(자로 재는 용도), 켜뒀다면 인쇄 미리보기에서도 그대로 보인다
  — 화면 전용 안내들과 달리 이건 빼지 않는다.

이 작업 중 CSS 버그를 하나 잡았다. 안내문을 용지 위에 얹으려고 `<PaperPreview>`를
`position: relative` 래퍼로 감쌌더니 용지가 갑자기 0×0으로 쪼그라들었다 — SVG는
내재 크기가 없는 요소라, 부모가 플렉스 자식이면서 크기가 애매한 일반 블록이면
브라우저가 크기를 못 정하고 0으로 접어버린다. 래퍼에 `aspect-ratio`(용지 가로:세로,
App.tsx가 인라인으로 전달)와 `height`를 명시적으로 못 박고 SVG는 그 안을 100%로
채우게 해서 고쳤다. **크기를 애매하게 두지 않는 것**이 핵심이었다.

---

## ✅ 4c-1 다듬기 — 여러 줄, 자동 늘어남, 인쇄 미리보기

- 테스트 149개 통과 · 빌드 성공

### 굵은 선의 꼭짓점이 끊기던 것을 고쳤다

**화면과 PDF가 다른 선 끝 처리를 쓰고 있었다.** 화면은 `strokeLinecap="round"`인데
PDF는 지정을 안 해 기본값 `butt`였다 — 그래서 인쇄물에서만 굵은 선의 꼭짓점에
네모난 구멍이 남았다. `OBJECT_LINE_CAP`을 core/style.ts로 올려 화면·PDF가 같은
값을 쓰게 했다. **좌표는 건드리지 않는다** — round는 그리는 순간에만 굵기의
절반을 연장하므로 50mm 선은 여전히 정확히 50mm다.

### 텍스트 상자가 스스로 커진다

칸 하나만 누르고 길게 써도 매번 손으로 늘릴 필요가 없다. 입력하는 동안 실제로
필요한 크기를 캔버스로 재서(`ui/measureText.ts`), 모자라면 격자 칸 단위로
키운다(`core/objects.ts`의 `growBox`). **왼쪽 위는 움직이지 않고, 줄지도 않는다** —
글자를 지운다고 상자가 따라 줄면 입력 중 화면이 계속 흔들린다. 속지 밖으로도
자라지 않는다.

### ⇧Enter로 여러 줄을 쓴다

Enter는 확정, ⇧Enter는 줄바꿈이다. 메모·할일목록처럼 여러 줄이 흔해서 이렇게
배정했다(문서 프로그램과는 반대). `core/text.ts`의 `lineBaselines`가 화면·PDF
모두에 줄 위치를 낸다 — 한 줄일 때는 예전 `baselineY`와 정확히 같은 값이 나오도록
`lineBaselines(...,1)`에 위임했다(회귀 없음).

### T모드 하나로 쓰기·고치기·꾸미기를 다 한다

- **칸 클릭** — 그 칸에 새로 쓴다. **글자를 클릭하면** — 그 자리에서 바로 고친다
  (더블클릭 없이 한 번으로). 다른 도구에서도 **더블클릭하면 T모드로 바뀌며 열린다**
- **속성 막대가 셋으로 나뉜다** — 입력 중이면 그 상자 자체(즉시 반영),
  아니면 고른 것, 그것도 없으면 **앞으로 쓸 글자의 기본값**(`textDraftStyle`).
  이 순서는 store.drawStyle이 선에 이미 하던 방식과 같다
- **스타일이 새는 버그를 고쳤다** — 예전엔 상자를 고칠 때 굵기·정렬·색이 전부
  기본값으로 리셋됐다(`Editing`에 `size`만 있고 `align/valign/color`가 없었다).
  이제 `Editing.style: TextStyle`이 전부 들고 다닌다

### 도구마다 커서 색이 다르다

그리기는 초록(`SNAP_COLOR`), 글자는 파랑(`TEXT_SNAP_COLOR`). T모드도 그리기처럼
마우스가 올라간 칸을 미리 보여준다(`.text-hover`) — 이전엔 그리기만 이 피드백이 있었다.

### 화면 도트가 간격에 따라 작아지고 옅어진다

촘촘한 격자가 위에 그린 것을 가리던 문제. 간격 5mm를 기준으로, 좁을수록 도트가
작고 연해진다(`InsertView.tsx`의 `screenDotSize`/`screenDotOpacity`, 화면 전용 계산이라
"core에는 계산이 없다"는 style.ts 원칙을 지키려 core가 아닌 ui에 뒀다). **인쇄되는
값(DOT_SIZE)은 그대로다** — 화면 표시만 바뀐다.

### 글자만 속지 영역으로 자른다

디자인 화면을 넘어간 글자가 인쇄물에서도 옆 칸까지 찍히던 문제. 화면은 SVG
`clipPath`로, PDF는 pdf-lib의 저수준 연산자(`pushGraphicsState`·`clip`·`endPath`)로
칸마다 속지 경계에서 자른다. **도트·선은 자르지 않는다** — 애초에 격자 안에서만
그려지므로 넘칠 일이 없다. 실제 PDF를 렌더링해서 옆 칸을 침범하지 않는 것을 확인했다.

### 인쇄 미리보기 — 설정을 안 건드리고 실제 모습만 본다

인쇄하기 탭에 토글을 하나 뒀다. 켜면 도트·타공 화면 설정과 무관하게 **실제로
인쇄될 모습만** 보여준다 — 화면용 큰 도트 대신 진짜 0.2mm 도트를, 타공 안내는
아예 없이. `InsertView`에 `mode: 'edit' | 'print'`를 추가해 처리했다. store에는
넣지 않았다 — 탭을 나가면 잊혀도 되는, 보는 방식만 바꾸는 값이다.

### 타입 검사 관련

`npm run build`(`tsc -b`)로 검사한다. `npx tsc --noEmit`은 tsconfig가 solution
스타일이라 아무것도 검사하지 않는다(지난 회차에 확인). 이번 회차에서 fs/zlib을
테스트에 쓸 수 있는지도 확인했는데 — **node 내장 모듈은 import 자체가 안 된다**
(`tsconfig.app.json`의 `types`가 `["vite/client"]`뿐이라 @types/node가 전혀 안 실린다).
PDF 내용을 직접 파싱하는 테스트는 `DecompressionStream`(Web 표준, Node 18+ 전역)으로
우회할 수 있다는 것도 확인해뒀다 — 이번엔 실제 폰트 바이트를 구할 방법이 없어
자동화 테스트 대신 `/tmp` + vite-node + 스크린샷으로 검증했다.

---

## ✅ 4c-1 완료 — 글자를 쓰고 인쇄한다

- 테스트 137개 통과 · 빌드 성공

### 글자 상자는 도트가 만드는 칸이다

```
칸 하나 클릭      그 칸 크기의 상자
여러 칸 드래그    걸친 칸 전체가 상자
빈 채로 나가면    아무것도 안 남는다
```

**빈 상자가 남지 않기 때문에 "글자를 클릭해서 고른다"는 규칙이 성립한다.**
보이지 않는 빈 상자를 잘못 집을 일이 없다. 두 번 누르면 다시 고칠 수 있다.

글자는 상자를 넘칠 수 있다(설계문서 6장). 줄바꿈하지 않으므로 선을 끈 자리에
긴 제목을 쓸 수 있고, 만들기도 훨씬 간단하다.

### 클릭 판정만 화면이 잰다

글자가 실제로 차지하는 크기는 **글꼴이 정하므로 core가 알 수 없다.** 화면에는 이미
그려져 있으니 브라우저에 물어본다 — viewBox가 mm라 `getBBox()`가 곧 mm를 돌려준다.
감싸기 선택은 상자로 판정하고, 클릭만 이 값을 쓴다.

### 글꼴 — 서브셋을 쓰지 않는다

**설계문서 10장의 계획이 한글에서 안 통했다.**

| | 크기 | 결과 |
|---|---|---|
| `subset: true` | 5KB | **글자 누락** — `2027년 3월 셋째 주` → `3월` |
| `subset: false` | 1.2MB | 정상 |

pdf-lib의 서브셋이 한글 글꼴에서 글리프를 빠뜨린다. 전체 임베딩으로 간다 —
글꼴은 문서에 한 번만 들어가므로 쪽수가 늘어도 1.2MB 그대로다.
글자가 하나도 없으면 아예 심지 않는다.

Pretendard(SIL OFL)를 담아뒀다. **OTF는 서브셋 시도 자체가 실패**해서(`Not a CFF Font`)
TTF 판을 쓴다. 화면도 같은 파일을 @font-face로 쓴다 — 다른 글꼴로 보여주면 글자 폭이
달라져서 화면에서 칸에 맞춘 것이 인쇄물에서 넘친다.

### 놓는 자리는 core/text가 한 번만 정한다

화면은 SVG의 `text-anchor`에 맡기고 PDF는 글자 폭을 재서 좌표를 직접 옮긴다.
**방법이 전혀 다르므로 기준점을 한 군데서 낸다.**

세로는 글꼴의 윗선·아랫선 비율(`TEXT_ASCENT` / `TEXT_DESCENT`)로 잡는다. 실제 글꼴
지표를 쓰지 않는 이유는 화면 쪽에서 그 값을 얻기 어렵기 때문이다. 같은 비율을 두 뷰가
함께 쓰면 적어도 서로 어긋나지 않는다.

### 화면의 커닝을 껐다

두 뷰의 가로 정렬을 실제로 재보니 라틴 문자만 어긋났다.

```
              PDF        화면(커닝 켬)   화면(커닝 끔)
Weekly 11주차  30.2518    30.3354        30.2548
2027년 3월…    27.2798    27.2844        27.2844
```

브라우저는 `We` 같은 짝을 자동으로 좁히지만 pdf-lib은 글자 폭을 그냥 더한다.
`font-kerning: none`으로 맞추니 **셋 다 0.005mm 이내**가 됐다.

### 글자 크기만 pt로 받는다

문서 프로그램에서 쓰던 단위라 10pt가 어느 정도인지 감이 있기 때문이다.
**받는 즉시 mm로 바꿔 저장하므로 모델에는 여전히 mm만 있다.** 기본값은 9pt —
손바닥만 한 속지라 문서 프로그램의 기본(11pt)보다 작다.

### 만들면서 알게 된 것

**`npx tsc --noEmit`이 아무것도 검사하고 있지 않았다.** tsconfig가 solution 스타일
(`files: []` + `references`)이라 아무 파일도 포함하지 않는다. 그동안의 타입 검사가
전부 헛돌았고, `DiaryObject`를 선·글자 두 종류로 나누자 20여 개의 오류가 한꺼번에
드러났다. **진짜 타입 검사는 `npm run build`(`tsc -b`)다.**

---

## ✅ 4b 마무리 — 치수가 보인다

- 테스트 125개 통과 (`npm test`) · 빌드 성공 · 타입체크 통과

### 치수 표시

```
끄는 중      커서 오른쪽에      35 × 45mm
고른 뒤      상단 막대에        4개  50 × 75mm
```

**선 하나면 길이, 여러 개면 감싸는 네모의 가로 × 세로다.**
면은 선 네 개라 "면의 크기"라는 값이 따로 없고, 감싸는 네모가 곧 그 크기다.
가로선·세로선은 길이만으로 충분하지만 대각선은 몇 칸짜리인지도 알아야 해서
가로 × 세로로 보여준다.

커서 옆 글자는 확대를 해도 화면에서 같은 크기다 (`11 / scale`). 격자 위에서도
읽히도록 흰 테두리를 두른다.

> SVG 안에서 CSS의 `mm` 단위는 viewBox 좌표가 아니라 물리 단위로 해석된다.
> 테두리 굵기를 CSS에 두면 확대할 때 어긋나므로 글자 크기에 맞춰 컴포넌트에서 정한다.

### 그리기 모드에도 모양 막대

아무것도 안 골랐으면 **앞으로 그을 선**의 모양을 보여준다. 그리기 전에 무엇이
그어질지 보이는 편이 낫다. 정하지 않은 값은 `기본 0.2mm`처럼 실제 값을 함께 적는다.

`store.drawStyle`이 그 값이고, 정하지 않은 키는 그은 선에도 들어가지 않는다 —
나중에 기본값을 바꾸면 그때 그은 선들도 같이 따라온다.

### 도구를 바꾸면 긋다 만 선을 버린다

첫 점만 찍고 고르기로 넘어가면 "다음 점을 누르세요"가 남아 무엇을 기다리는지
알 수 없었다.

### 인쇄 정렬 기본값을 가운데로

좌측 상단이 자르는 횟수는 적지만, 실제로 인쇄해보니 가운데가 낫다.
프린터가 가장자리를 못 찍어서 한쪽으로 몰면 재단 표시가 잘려나간다.

---

## ✅ 4b-2 완료 — 선마다 굵기·색·모양을 정한다

**4단계가 글자만 남았다.**

- 테스트 119개 통과 (`npm test`) · 빌드 성공 · 타입체크 통과

### 끝점 손잡이

**딱 하나만 골랐을 때만 나온다.** 여러 개 골라놓고 손잡이가 잔뜩 뜨면 어느 것을
잡는지 알 수 없다. 손잡이는 선 몸통보다 먼저 집힌다(2.2mm 대 1.6mm) —
겹쳐 있을 때 손잡이를 못 잡으면 늘이려다 매번 통째로 옮겨진다.

끝점도 격자에 붙는다. 두 끝이 한 점으로 모이면 선이 아니므로 그런 상태는 만들지 않는다.

끌다가 이미 있는 선과 같은 자리에 겹치면 **먼저 있던 것만 남긴다**(`dedupe`).
그대로 두면 "겹쳐 그어도 쌓이지 않는다"는 규칙이 깨지고, 나중에 그 자리를 다시
그었을 때 둘이 한꺼번에 사라져 무슨 일인지 알 수 없게 된다.

### 속성 막대

고른 것이 있으면 안내문 자리에 들어간다. 굵기 · 색 · 모양(실선/파선/점선).

**`기본`을 고르면 값을 지운다.** undefined로 두는 것이 아니라 키 자체를 없애서,
저장 파일에도 남지 않고 나중에 core/style의 기본값을 바꾸면 그 선도 같이 따라온다.

여러 개를 골랐는데 값이 서로 다르면 `—`로 두고, 고르면 전부에 적용한다.

### 만들면서 고친 것

**여러 개 골라둔 상태에서 그중 하나를 클릭해도 선택이 줄지 않았다.**
누를 때 선택을 줄이면 여러 개를 함께 끌 수가 없어서 그대로 두는데,
**끌지 않고 그냥 눌렀다 뗐을 때** 그 하나만 남기는 처리가 빠져 있었다.
판단이 손 뗄 때로 와야 한다.

### 화면 기본 확대를 100%로

200%는 너무 가깝다. 100%가 실제 크기다.

---

## ✅ 4b-1 완료 — 표를 그리고 고칠 수 있다

- 테스트 115개 통과 (`npm test`) · 빌드 성공 · 타입체크 통과

### 조작이 곧 종류다

도구를 오갈 필요가 없다. **무엇을 그릴지는 마우스 조작이 정한다.**

```
그리기
  점 클릭 → 점 클릭      선            (대각선도 된다)
  점에서 드래그          면 = 선 네 개

고르기
  클릭 · Shift+클릭 · 감싸기 → 선택
  고른 것 끌기 → 이동 · Delete → 삭제
```

도구는 `고르기`(V) · `그리기`(L) 둘뿐이다. 선/면 버튼은 필요 없다.

### 다시 그으면 지워진다

**하나라도 없으면 없는 것만 채우고, 전부 이미 있으면 전부 지운다.**
면을 반쯤 겹쳐 그렸을 때 일부는 생기고 일부는 사라지는 혼란을 막는다.
겹쳐 그어도 같은 자리에 선이 중복으로 쌓이지 않는다.

방향은 상관없다 — 왼쪽에서 오른쪽으로 긋든 반대로 긋든 같은 선이다.
지우개 도구 없이 마우스만으로 그리고 지운다.

### 면은 사각형이 아니라 선 네 개다

이래야 아랫변만 골라 지우면 도트가 다시 드러난다 — 설계문서 6장이 표에서 쓰는 방식 그대로다.
새 객체 종류도 PDF 코드도 늘지 않는다. 한 줄로 끌면 변이 겹치므로 선 하나만 만든다.

### 옮길 때는 이동량이 스냅된다

선 자체를 다시 스냅하는 것이 아니라 **"몇 칸 움직였나"**를 스냅한다.
7.3mm를 끌면 5mm 격자에서 정확히 5mm 움직이고, **길이와 각도는 한 치도 변하지 않는다.**

### 선을 집는 굵기는 보이는 굵기와 다르다

0.2mm 선은 손으로 정확히 찍을 수 없다. 1.6mm 안에 들어오면 집힌 것으로 치고,
여럿이 걸리면 가장 가까운 하나만 고른다. 스냅에서 "보이는 모양과 붙는 자리를
분리"한 것과 같은 발상이다.

**감싸기는 완전히 들어온 선만** 고른다. 걸치기만 해도 잡히게 하면 격자 위에서
의도치 않게 딸려온다.

### 선택은 실행취소에 들어가지 않는다

문서가 바뀐 것이 아니다. ⌘Z는 그린 것만 되돌리고 선택 상태는 건드리지 않는다.
다만 되돌려서 사라진 객체의 id는 선택 목록에서 걷어낸다.

### 굵기·색·모양은 선택적 필드다

`LineObject`의 `width` · `color` · `dash`는 **값이 없으면 core/style의 기본값을 따른다.**
손대지 않은 선은 값을 갖지 않으므로 나중에 기본값을 바꾸면 전부 같이 따라온다.
따로 정한 선만 자기 값을 지닌다. 화면과 PDF가 같은 규칙을 쓴다.

`export.test.ts`가 이 짝을 고정한다 — 값이 다르면 PDF가 달라지고, 기본값과 같게 주면
아무것도 안 준 것과 똑같은 PDF가 나온다.

---

## ✅ 4a 완료 — 도트에 붙는 선을 그을 수 있다

편집 화면이 생겼고, 도트에서 도트로 끌면 선이 그어진다. 실행취소가 되고 PDF에 그대로 나온다.

- 테스트 93개 통과 (`npm test`) · 빌드 성공 · 타입체크 통과

### 화면이 둘로 나뉘었다

설계문서 11장대로 탭 두 개가 생겼다. `양식 만들기`가 새 편집 화면이고,
1~2단계에서 만든 것이 `인쇄하기`가 됐다.

**속지 한 장의 내용을 그리는 코드를 [ui/InsertView.tsx](app/src/ui/InsertView.tsx)로 빼냈다.**
편집 화면과 인쇄 미리보기가 둘 다 이것을 쓴다. 각자 그리면 언젠가 어긋나는데,
그 어긋남은 인쇄해봐야 알게 된다.

```
              InsertView          ← 도트 → 선 → 글자
               ↙          ↘
      편집 화면              인쇄 미리보기
      크게 한 장             칸마다 place로 옮겨서
```

### 화면 도트는 일부러 크고 진하다

인쇄되는 도트는 0.2mm다. 그대로 화면에 그리면 1픽셀도 되지 않아 어디에 붙는지 보이지 않는다.
화면에서는 안내선 역할이 우선이므로 **0.6mm에 진한 회색**으로 그린다.

**위치는 조금도 다르지 않다.** 격자점 좌표는 화면과 PDF가 같은 core/grid에서 받는다.
다른 것은 굵기와 색뿐이다. "화면과 PDF는 같은 데이터를 각자 그리는 독립적인 뷰"라는 원칙은
데이터가 같다는 뜻이지 똑같이 보여야 한다는 뜻이 아니다.
인쇄물이 어떻게 보일지는 화면이 아니라 실제로 뽑아서 판단한다.

### 스냅에 문턱값이 없다

언제나 가장 가까운 격자점으로 간다. 도트를 벗어난 자리에는 아예 못 긋는다.
규칙이 하나뿐이라 헷갈릴 일이 없고, 설계문서 원칙("모든 작업은 도트 위에서 일어난다")과도 맞는다.

`21.3, 19.4`를 찍으면 `20, 20`으로 붙는 것을 브라우저에서 확인했다.
마우스가 올라간 자리를 초록 점으로 표시해서, 도트를 꺼둬도 어디에 붙을지 보인다.

### 실행취소는 직접 만들었다

설계문서는 `zundo`를 적어뒀지만 되돌릴 대상이 객체 목록 하나뿐이라
[core/history.ts](app/src/core/history.ts) 30줄로 끝났다. 직접 두니
**무엇이 취소되는지 정확히 정할 수 있다** — 용지 크기를 바꾼 것까지 같이 취소되면 곤란하다.
설정은 되돌리는 게 아니라 다시 고르면 되는 것이다.

### Konva를 쓰지 않기로 했다

설계문서 10장은 react-konva를 골랐지만 **코드가 한 줄도 없을 때 세운 계획**이었다.
2단계까지 순수 SVG로 만들어보니 viewBox를 mm로 잡는 방식이 잘 돌아갔고,
`getScreenCTM()` 덕분에 **브라우저가 마우스 위치를 mm로 직접 돌려준다.** 픽셀 통역이 없다.

스냅이 있어서 Konva가 필요한 이유(픽셀 단위 정밀도)가 애초에 없다.
모서리를 잡아당겨 크기를 바꾸는 기능이 필요해지면(6단계 표, 9단계 도형) 그때 그 부분만 만든다.

### 인쇄로 검증한 것

15,20 → 65,20 같은 상자를 그려 PDF 안의 좌표를 직접 쟀다.

```
첫 칸    15,20 → 65,20      (그린 좌표 그대로)
마지막 칸 95,145 → 145,145   (정확히 80,125만큼 밀림)
각 변의 길이  50mm · 40mm    (오차 없음)
```

### 설정이 아이콘 막대로 바뀌었다

항목이 계속 늘어나므로 한 줄로 다 펼쳐두지 않는다. 왼쪽에 아이콘 다섯 개만 세우고,
누른 것의 설정만 옆에 말풍선으로 띄운다. 아이콘 오른쪽 위의 점은 **그 안내가 지금 화면에
보이는 중**이라는 표시다.

```
용지 · 속지 · 도트 격자 · 타공 안내 · 배치·절취선
```

`인쇄 불가 영역`은 용지 안으로 넣었다. 성격이 다르다고 따로 뒀었는데, 용지를 볼 때
같이 보는 것이 자연스럽다.

닫히는 방법은 셋이다 — 같은 아이콘 다시 누르기, 바깥 누르기, Esc.
설정 폭 268px이 52px로 줄어 미리보기가 그만큼 넓어졌다.

### 편집 화면에도 타공이 보인다

[ui/PunchGuide.tsx](app/src/ui/PunchGuide.tsx)로 빼내 편집 화면과 인쇄 미리보기가 같이 쓴다.
글자가 구멍에 잘리는 걸 막는 안내라 그리는 동안 보여야 한다.

### 만들면서 고친 것

**누르고 떼는 것이 한 번에 일어나면 선이 안 그어졌다.** React의 상태 변경은 다음 렌더에나
반영되는데, 뗄 때 시작점이 아직 null로 보였다. 판단은 ref로 하고 화면 미리보기에만 state를 쓴다.

**`pointerup`을 놓치면 드래그가 멈춘 채로 남았다.** 붙잡기(pointer capture)가 안 풀려서
다른 곳을 눌러도 반응하지 않았다. `pointercancel`을 처리하고 뗄 때 명시적으로 풀어준다.

**편집 화면에서 안전영역이 새까맣게 칠해졌다.** 타공 안내의 CSS가 `.preview` 아래로만
걸려 있어서 편집 화면에서는 스타일이 안 붙고 기본 검정이 됐다. 두 화면이 같이 쓰는
조각이므로 부모에 매이지 않게 고쳤다.

---

## ✅ 2단계 완료 — 도트속지를 뽑을 수 있다

간격과 모양을 고르면 도트속지·그리드속지·줄노트가 나오고, **PDF로 인쇄된다.**

- 테스트 79개 통과 (`npm test`) · 빌드 성공 · 타입체크 통과

### 격자 계산 — [core/grid.ts](app/src/core/grid.ts)

함수를 둘로 나눈 것이 요점이다.

```
gridLattice(area, spacing)  →  xs[], ys[], marginX, marginY   ← 스냅이 쓸 좌표
gridShapes(lattice, style)  →  dots[], lines[]                ← 그릴 것
```

**스냅은 gridLattice 쪽을 쓴다.** 화면에 도트로 보이든 줄노트로 보이든 붙는 자리가
같아야 하므로, 이 규칙을 주석이 아니라 함수 경계로 박아뒀다. 6단계에서 표를 그릴 때
`gridLattice`를 그냥 부르면 된다.

네 가지 모양(도트·그리드·가로줄·세로줄)이 전부 같은 격자에서 나온다. 간격을 그대로 두고
모양만 바꾸면 속지 종류가 바뀐다.

### 여백은 입력받지 않는다

타공 여백과 같은 이유다. 간격을 정하면 여백은 저절로 결정되는 종속 변수다.

```
칸 개수 = floor((영역 − 3mm × 2) ÷ 간격)
여백    = (영역 − 칸 개수 × 간격) ÷ 2
```

속지 끝까지 격자를 채우면 바깥쪽 도트가 재단선 위에 앉아 반쯤 잘려나가므로 최소 3mm를 남긴다.

**여백을 항상 3~5mm에 넣는 것은 기하학적으로 불가능하다.** 중앙 정렬이면 여백은
`나머지 ÷ 2`로 못 박히는데, 125mm에 6mm 간격이면 칸 19개(여백 5.5mm)나 20개(여백 2.5mm)뿐이고
그 사이가 없다. 실제 보장은 `3mm ≤ 여백 < 3mm + 간격/2`이고, **간격 4mm 이하면 항상 5mm 밑이다.**
계산된 여백은 패널에 표시된다.

| 간격 | 격자 (M6) | 여백 |
|---|---|---|
| 2.5mm | 30 × 48 | 3.75mm |
| 3mm | 25 × 40 | 4mm |
| 5mm | 15 × 24 | 5mm |
| 7mm | 11 × 18 | 좌우 5 · 상하 3mm |

### 좌표 변환 — [core/place.ts](app/src/core/place.ts)

1단계까지는 필요 없었다. 재단선도 눈금자도 전부 용지 좌표였고, 타공은 화면 전용이라
PDF에 없었다. **도트가 처음으로 "칸 안에 그려지면서 인쇄까지 되는" 내용이다.**

화면은 SVG transform으로, PDF는 좌표를 직접 계산해서 그린다. 둘이 어긋나면 인쇄물만
틀어지고 화면으로는 알 수가 없다. 그래서 변환을 **행렬 여섯 숫자로 한 번만 정하고**
`map()`과 `svg`가 같은 숫자에서 나오게 했다. place.test.ts가 이 일치를 고정한다.

속지 크기는 따로 넘기지 않고 칸에서 되돌린다(`insertSizeOf`). 둘이 안 맞는 상황을
아예 만들지 않기 위해서다.

### 인쇄로 검증한 것

PDF 안의 도트 좌표를 직접 파싱해서 쟀다.

| | 도트 수 | 간격 | 범위 |
|---|---|---|---|
| M6 4장 | 1440 = 15 × 24 × 4 | x `5 · 10` / y `5 · 10` | x 5~155 · y 5~245 |
| 3단접이 (눕힘) | 1032 = 43 × 24 | 전부 `5` | x 5~120 · y 5~215 |

`10mm`는 칸 경계다 — 오른쪽 여백 5 + 다음 칸 왼쪽 여백 5. 눕힌 속지는 220mm 축이
용지 세로로 가서 5~215mm에 놓였고 모든 간격이 오차 없이 5mm다.
**화면(1440개)과 PDF(1440개)가 독립적으로 계산해 정확히 일치한다.**

### showOnScreen과 print를 분리했다

도트를 보면서 작업하고 인쇄할 땐 빼거나, 반대로 화면을 비워두고 설정한 도트만 인쇄할 수 있다.
둘이 어긋나 있으면 패널이 그 사실을 알려준다 — 안 보이는데 인쇄되는 상황을 눈치채지
못하면 곤란하다.

### 만들면서 고친 것

**간격이 속지보다 넓으면 점 한 줄만 찍히고 있었다.** M6에 90mm를 넣으면 여백이 40mm로
벌어지는데 경고도 안 떴다. 칸이 최소 1개는 나와야 격자로 치도록 바꿨다.

### 타공 안전영역 비우기

구멍에 걸리는 도트를 지우는 것이 아니라 **격자가 놓일 영역 자체를 좁힌다.**
좁아진 영역 안에서 중앙 정렬과 여백 3mm 보장이 그대로 다시 일어난다.

```
끔  도트 15 × 24개 · 여백 5mm                    x 5 ~ 75
켬  도트 13 × 24개 · 여백 좌 15 · 우 5 · 상하 5mm   x 15 ~ 75
```

**기본값은 끔이다.** 시판 도트 속지는 대개 구멍 쪽까지 도트가 깔려 있다.

`타공 위치 보기` 체크와는 무관하게 동작한다. 안내선을 껐다고 구멍이 없어지는 것은 아니다.
화면과 PDF 양쪽에서 도트 수(1248개)와 x 범위가 정확히 일치하는 것을 확인했다.

### 격자 오프셋은 넣지 않았다

설계문서 6장의 `offsetX`/`offsetY`를 뺐다. **맞출 대상이 없다** — 표도 선도 글자도
4단계·6단계에나 생긴다. 지금 넣으면 격자를 옆으로 미는 숫자일 뿐인데, 밀면 한쪽 여백이
3mm 밑으로 내려가 바깥 도트가 재단선에 걸린다. 그럼 "오프셋은 여백보다 크면 안 됨"
같은 규칙을 또 붙여야 하고, 이는 아직 존재하지 않는 문제를 위해 규칙을 만드는 셈이다.

4단계에서 정렬할 것이 생기면 그때 필요한 모양이 드러난다.

### 설계문서와 다르게 간 곳

`DotGrid`에서 세 가지를 뺐다.

- **`color`** — "색과 굵기는 core/style.ts 한 군데에만"이라는 규칙과 부딪힌다.
  인쇄해보고 값을 고칠 때 한 파일만 만지도록 규칙 쪽을 택했다
- **`style: 'none'`** — `showOnScreen`·`print` 두 불리언과 상태가 겹친다.
  안 보이게 하는 방법이 두 가지면 나중에 헷갈린다
- **`offsetX`/`offsetY`** — 위 참고

---

## ✅ 인쇄 테스트 통과 — 2단계로 갈 수 있다

M6 4장을 실제로 인쇄해 자로 쟀고 **50mm가 정확히 나왔다.**

이게 이 프로그램의 전제였다. 치수가 맞으니 나머지를 만들 의미가 생겼다.

### 인쇄해보고 정한 값

| | 값 | 비고 |
|---|---|---|
| 절취선 | 연파랑 `#8fb8de` · 0.1mm | 굵으면 어디를 잘라야 할지 애매해진다 |
| 십자 마크 | 한 변 6mm | 팔 하나 3mm |
| 눈금자 | 검정 0.1mm | 굵으면 자로 읽기 어렵다 |
| 도트 지름 | 0.2mm | **확정.** 0.15~0.40 비교 시트를 인쇄해서 고름 |
| 도트·격자 색 | 연회색 `#a6a6a6` | **확정.** 0.4→0.2mm로 줄이니 연해 보여서 색으로 되돌림 |
| 그은 선 | `#9e9e9e` · 0.2mm | **확정.** 선마다 바꿀 수 있고 이 값이 '기본'이다 |
| 배치 정렬 | 가운데 | **확정.** 한쪽으로 몰면 재단 표시가 잘려나간다 |
| 격자·줄 굵기 | 0.12mm | **잠정.** 아직 인쇄해보지 않았다 |
| 화면 도트 (인쇄 안 됨) | `#8c8c8c` · 0.6mm | 눈에 보이는 게 우선. 위치는 인쇄와 똑같다 |

전부 [core/style.ts](app/src/core/style.ts) 한 파일에 있다. 화면과 PDF가 같은 값을 쓴다.

---

## ✅ 1단계 완료 — 인쇄 정확도 검증

용지·속지를 고르면 자동 배치되고, 절취선과 50mm 검증 눈금자가 들어간 PDF가 나온다.
아직 속지 안에 그림은 못 그린다.

- 테스트 47개 통과 (`npm test`)
- 빌드 성공 (`npm run build`)
- 브라우저 동작 확인

### 검증된 것

A5 계산 결과가 설계문서 5장의 표와 정확히 일치한다.

```
상하 여백 29mm · 자동 계산됨
구멍 중심  32 · 51 · 70 · 140 · 159 · 178
```

속지 세로를 바꾸면 여백이 따라 움직인다 (브랜드마다 M6가 125~128mm로 다르므로).

```
125mm → 여백 13mm      127mm → 여백 14mm
126mm → 여백 13.5mm    128mm → 여백 14.5mm
```

PDF 페이지가 `210 × 297mm`로 소수점 9자리까지 정확하고, **이미지가 아니라 벡터로만** 그려진다.
이 두 가지는 `src/pdf/export.test.ts`가 고정하고 있다. 누가 캔버스를 이미지로 떠서 넣으면 테스트가 깨진다.

### 배치와 재단

용지를 남김없이 쓰는 것이 기본이다. 여백 0, 속지 사이 간격 0.

배치 덩어리는 **좌측 상단에 붙인다.** 가운데 두면 사방을 네 번 잘라야 하지만
좌측 상단이면 오른쪽과 아래 두 번으로 끝난다. 가운데 정렬도 선택지로 남겨뒀다.

십자 마크는 재단선이 만나는 자리에 놓되 두 경우를 뺀다.

- **배치 안쪽에 갇힌 교차점** — 재단선 하나는 양 끝 표시 두 개면 자를 수 있다.
  가운데 표시는 속지 위에 자국만 남긴다
- **용지 끝에 딱 붙은 자리** — 거기는 이미 종이 끝이라 자를 것이 없다

A4에 M6 4장이면 십자가 9개에서 7개로 준다. 좌표 계산은 [core/crop.ts](app/src/core/crop.ts)가
한 번만 하고 화면과 PDF가 같이 쓴다.

**속지가 작으면 바깥 표시만으로 부족하다.** 바깥 십자는 대부분 용지 가장자리에
놓이는데 프린터가 거기를 못 찍는다. A4에 M5 8장을 배치하면 십자 10개 중
**실제로 인쇄되는 것은 1개뿐이다.** 그래서 `십자 · 안쪽까지` 옵션을 뒀다.
같은 배치에서 13개 중 4개가 인쇄되고, 재단선마다 최소 하나씩은 보인다.

| 절취선 옵션 | 쓰임 |
|---|---|
| 십자 · 바깥만 | 기본. 속지가 커서 바깥 표시가 인쇄될 때 |
| 십자 · 안쪽까지 | 속지가 작아 바깥 표시가 잘려나갈 때 |
| 재단선 전체 | 하나도 빼지 않고 다 긋는다 |
| 없음 | |

### 화면 안내 — 인쇄되지 않는 것들

타공 위치와 같은 성격으로 **인쇄 불가 영역**(기본 3mm)을 추가했다. 프린터가 물리적으로
못 찍는 가장자리다. 화면에만 보이고 **배치 계산에 관여하지 않는다.** 배치를 실제로
밀어내는 것은 `용지 > 여백` 쪽이다. 둘을 따로 둔 이유는 성격이 다르기 때문이다 —
하나는 경고이고 하나는 배치 규칙이다.

### 만들면서 고친 것

`묶음 간격(groupGap)`에 조작 UI가 없었다. A5에서 크기만 M6로 바꾸면 70mm 간격이 남아
구멍이 속지를 벗어나는데 고칠 방법이 없었다. 컨트롤을 추가하고, 묶음으로 전환할 때
그 속지에 실제로 들어가는 간격을 제안하도록 했다 (`suggestGroupGap`).

`눈금자` 체크를 꺼도 화면에서 눈금자가 사라지지 않았다. `showRuler`가 미리보기까지
전달되지 않아 PDF에서만 빠지고 있었다.

여백이나 방향만 바꿔도 용지가 `사용자 지정`으로 풀렸다. 크기를 건드리지 않았는데
무엇을 고르고 있었는지 잊는 셈이다. 이제 **가로·세로가 실제로 달라질 때만** 풀린다.
두 버그 다 [store.test.ts](app/src/store.test.ts)가 고정한다.

---

## 남은 단계

전체 순서는 설계문서 12장에 있다. 아래는 지금까지 만든 것을 반영해 다시 센 것이다.

### 3단계 — 타공 위치 표시 · **이미 다 됐다**

규격 프리셋, 구멍 위치 자동 계산, 안전영역 표시, 커스텀 조정 — 네 항목 전부
[core/punch.ts](app/src/core/punch.ts)와 설정 패널에 들어가 있다. 1단계에서 배치를
만들려면 구멍 위치가 필요했고, 2단계에서 안전영역을 피하려면 안전영역이 필요했다.
**따로 할 일이 없다.**

### 4단계 — 그리기 도구 · **완료**

- **4a 완료** — 편집 화면, 도트에 붙는 선 긋기, 실행취소, 확대
- **4b-1 완료** — 도구 팔레트, 면 그리기, 다시 그어 지우기, 선택·이동·삭제
- **4b-2 완료** — 끝점 잡아당기기, 속성 막대(선마다 굵기·색·모양)
- **4b 마무리 완료** — 치수 표시, 그릴 선 모양 미리 보기
- **4c-1 완료** — 글자 상자, 내장 글꼴로 PDF까지
- **4c-1 다듬기 완료** — 여러 줄, 자동 늘어남, T모드 통합 편집, 인쇄 미리보기, 꼭짓점 수정
- **4c-2 완료** — 격자 해제 이동 · 굵게 · 글꼴 파일 등록.
  기울임은 **하지 않기로 했다**

### 5단계 — 양식 저장과 배치 · **완료**

- **5a 완료** — 양식 개념, 양식 관리 탭(갤러리)
- **5b 완료** — JSON 저장·불러오기
- **5c 완료** — 낱장형 칸 배정. 같은 규격의 양식끼리 칸마다 다르게 넣는다

### 6단계 — 표 · **완료**

도트 드래그로 표 생성, 행·열 편집, 구분선 끄기.

**2단계에서 깔아둔 `gridLattice`를 그대로 쓴다.** 스냅 좌표가 이미 모양과 분리돼 있어서
줄노트 위에서 표를 그려도 도트 자리에 붙는다.

### 7단계 — 여러 쪽 생성 · **완료**

만년형 반복, 총 쪽수·인쇄 매수 계산, 여러 쪽 PDF.

**cutStack(겹치기 배치)은 뺐다.** 만년형은 모든 칸 내용이 완전히 똑같아서, 자른
뒤 순서를 어떻게 섞어도 결과가 같다 — 섞을 이유 자체가 없다. 쪽마다 내용이
실제로 달라지는 연도형(8단계)에서 다시 볼 문제다.

**낱장 조합 + 반복 인쇄 연결(9b 뒤에 마무리)** — "다른 양식을 한 페이지에
넣으면 반복모드가 안 된다"는 문제였다. 양식의 `repeat`(그 양식 하나가 여러
장을 채우는 것)와는 다른, **인쇄 작업 설정**으로 풀었다. store에 `comboSheets`
(기본 1)를 추가해 "지금 짜둔 칸 배정을 통째로 몇 장 찍을지"를 따로 받는다 —
반복 인쇄처럼 총 칸 수를 나눠 채우다 마지막 장이 모자라는 일이 없다, 매번
완전히 채워진 장만 나온다(사용자와 합의한 방식). `pdf/export.ts`의
`ExportInput.sheets`가 이걸 받고, `totalSlots`(반복 인쇄)가 있으면 그쪽이
우선한다 — 둘은 여전히 섞이지 않는다. 인쇄하기 탭의 `SlotAssign`에 매수
입력을 추가했다. 이 입력은 조합할 다른 양식이 없어도(칸 배정 드롭다운이
안 보여도) 항상 뜬다 — "이 배치를 몇 장 찍을지"는 조합 여부와 무관하다.

### 8단계 — 자동 입력 필드

날짜·숫자·목록 데이터, 세트형 양식, 필드 지정, 서식·파생값, 미리보기. 월간 달력은 그다음.

**가장 큰 단계다.** 설계문서 7장 전체가 여기다. 그래서 4단계로 쪼갠다.

- **8a 완료** — 날짜 데이터셋 계산(core만, 화면 없음). `core/dataset.ts`에
  `Dataset`(지금은 `kind: 'date'`만), `CalendarDate`(연·월·일 정수 — `Date`로
  날짜 산술을 하면 타임존·서머타임에 하루가 밀리는 사고가 나서 피했다),
  `dateAt`(데이터셋의 절대 인덱스 → 날짜, 범위 밖이면 null), `dateAtOffset`
  (쪽·오프셋 → 날짜), `datasetPages`(`ceil(총 개수 ÷ perPage)`)를 만들었다.
  일·주·달 세 단위를 지원한다. 위클리(하루 단위 · 한 쪽 7개) 예시로
  "2027년 365일 → 53쪽"이 설계문서 숫자와 맞는 것을 테스트로 확인했다.
  아직 `Template`/`RepeatSetting`에는 연결하지 않았다 — 8c에서 세트형 설정
  화면과 함께 붙인다.
- **8b 완료** — 자동 필드 편집. `TextObject`에 `field?: {offset, format}`을
  더했다(`text`는 그대로 둬서 옛 저장 파일이 계속 열리고, 필드를 꺼도 원래
  글자로 돌아온다). **이미 만든 글자를 선택했을 때만** 속성 막대에 "자동 필드"
  체크박스가 뜬다 — 입력 중이거나 앞으로 쓸 글자의 기본값으로는 두지 않는다,
  매번 새 글자가 필드로 시작하면 놀란다. 켜면 오프셋·서식 칸이 나타난다.
  `core/text.ts`의 `displayText(t)`가 "이 글자가 실제로 보일 문자열"을 정하는
  유일한 자리다 — 필드면 `⟨+0⟩` 같은 자리표시, 아니면 `text` 그대로. 화면
  (`InsertView`)과 PDF(`pdf/export.ts`)가 똑같이 이 함수를 부른다 — **지금은
  둘 다 자리표시를 보여준다.** 데이터셋에서 진짜 값을 뽑아 채우는 일은 8c의
  몫이고, 그때까지는 화면과 인쇄물이 다른 값을 보이는 것보다 같은 자리표시를
  보이는 편이 덜 놀란다.
- **8c 완료** — 서식 계산 + 진짜 인쇄. `RepeatSetting`에 `{mode:'dataset',
  dataset}`(세트형)을 더했다. `core/format.ts`의 `formatDate`(8b가 정한
  4가지 서식 — `D`·`M/D`·`M월 D일`·`YYYY-MM-DD`, 지금은 한국어만)와
  `resolveObjectsForPage(objects, dataset, page)`가 자동 필드를 실제 값으로
  바꾼다. **결과 객체에서 `field`를 뗀다** — `displayText`는 `field`가 있으면
  자리표시를 우선하므로, 진짜 값을 보여주려면 `field` 없이 `text`만 남겨야
  한다. 이 덕분에 화면(`InsertView`)·PDF(`pdf/export.ts`)의 렌더링 쪽은
  손대지 않았다 — 그린 것 배열을 페이지별로 미리 바꿔치기하는 쪽(App.tsx)만
  새로 생겼다.

  **세트형은 양면이어도 한 장의 용량이 늘지 않는다.** 반복 인쇄(만년형)는
  칸 하나하나가 독립된 자리라 양면이면 앞뒤에 각각 다른 내용을 채워 넣을 수
  있지만, 세트형은 칸 하나가 **한 쪽의 앞뒤**다 — 3주차 카드 뒷면이 47주차를
  가리키면 안 된다. 그래서 `pdf/export.ts`에 `datasetOverrides`(전체 쪽
  번호 기준 — 칸 위치 기준인 `slotOverrides`와 인덱싱이 다르다)라는 별도
  경로를 뒀고, 장수 계산도 `frontBackFilled`가 아니라 `filledSlots` 하나만
  쓴다. 미리보기는 **지금 보는 장만** 계산하고(느려지지 않게), PDF로 내보낼
  때만 전체 쪽을 계산한다.

  이 시점부터 실제로 "2027년 위클리 53쪽"을 뽑아볼 수 있다.
- **8d 대부분 완료 — 겹치기 배치(cutStack) + 파생값·영어 서식.** D-day만
  아직 나중이다(따로 "기준일" 설정이 필요한 더 큰 기능이라 미뤘다).

  세트형 인쇄 설정에 "순서"(순차/겹치기) 토글을 더했다. 순차는 원래 동작 —
  한 장의 칸마다 이어지는 쪽 번호가 들어간다(칸수 4장이면 1·2·3·4). 재단기로
  잘라서 칸별로 쌓으면 그 무더기 안에서 쪽 순서가 뒤죽박죽이라 다시 정렬해야
  한다.

  겹치기는 설계문서가 이미 예로 든 방식이다 — 53쪽·4칸이면 칸0에
  1·15·29·43쪽을, 칸1에 2·16·30·44쪽을 담아서, 자른 뒤 칸별 무더기를 순서대로
  쌓기만 하면 그대로 책이 된다. `core/template.ts`의 `cutStackPage(sheet,
  slotIndex, totalPages, slotsPerSheet, groupSheets?)`가 이 계산을 맡는다.

  **무더기 크기(`groupSheets`)를 추가로 받아 장 단위로 묶는다** — 사용자가
  "재단기가 한 번에 10장씩만 잘린다"고 알려준 요구다. 0(또는 생략)이면 전체를
  한 무더기로 보고(설계문서 원래 방식), N을 주면 N장마다 묶음을 새로 시작해서
  각 묶음이 독립적인 순차 구간을 담당한다 — 묶음끼리는 이어 붙이면 전체 순서가
  맞는다. 인쇄하기 탭의 "무더기" 칸(겹치기를 켰을 때만 보임)에서 정한다. 미리
  보기도 겹치기를 반영해서 장을 넘기며 확인할 수 있다.

  **PDF 전체 바이트 길이로는 이 기능을 검증할 수 없다** — 재배치일 뿐 쪽
  내용의 총량이 그대로라서, 순차든 겹치기든 문서 크기가 똑같다. 그래서
  `pdf/export.test.ts`엔 장수·구성이 어긋나지 않는지 보는 스모크 테스트만
  뒀고, 실제 재배치 로직 검증은 `core/template.test.ts`의 `cutStackPage` 단위
  테스트(설계문서 예시 재현 포함)가 맡는다.

  **파생값·영어 서식도 더했다.** 설계문서 7장의 서식 표 중 남아 있던
  요일·월·주차를 채웠다 — `core/dataset.ts`에 `weekOfYear(d)`를 새로
  만들었다("1월 1일부터 7일씩 끊어 센 몇 번째 묶음인가"라는 가장 단순한
  계산이다. ISO 8601처럼 요일에 맞춰 주 경계를 정렬하지 않는다 — 굳이
  그럴 이유가 없다). `core/format.ts`의 `formatDate`에 `ddd`(월)·
  `dddd`(월요일)·`ddd-en`(Mon)·`dddd-en`(Monday)·`M`(3)·`M월`(3월)·
  `MMM`(MAR)·`MMMM`(March)·`주차`(11주차)·`W`(W11)·`Week`(Week 11)
  11개를 더했고, `core/text.ts`의 `FIELD_FORMATS`에도 같은 id로 올려서
  속성 막대 드롭다운에 바로 나온다 — 새 UI 코드는 필요 없었다. **한자는
  이번에 넣지 않았다** — 달력 오브젝트(10단계)엔 요일 헤더처럼 짧고 반복되는
  자리라 넣었지만, 일반 글자 서식은 지금도 이미 15개라 굳이 더 늘리지
  않았다. 필요해지면 그때 추가한다.

  **D-day는 뺐다.** "기준일로부터 며칠"은 날짜 하나만으로 계산할 수 없고
  — 비교할 기준일이 하나 더 필요하다. 그건 지금의 `{offset, format}` 필드
  모델을 벗어나는 더 큰 변경(데이터셋에 기준일 설정을 추가하고, 서식 계산이
  그 값을 받아야 함)이라 별도로 설계하고 시작한다.

  **할 일 — 서식 목록 UI 정리(9단계와 같이).** 지금 `FIELD_FORMATS` 15개가
  속성 막대에 평평한 드롭다운 하나로 뜨는데, 사용자가 "선택 화면이 너무
  복잡하다"고 지적했다. 다음처럼 두 단계로 나눌 것:
  1. **숫자 / 한글 / 영어 / 한자로 먼저 나눈다.** 숫자는 언어가 없다(15,
     3, 11처럼 그냥 숫자로 보여주면 끝 — `D`·`M`·`주차`의 숫자 버전에
     해당).
  2. **그 안에서 요일 → 일 → 주 → 월 순서로 놓는다.**
  3. **한자를 새로 만든다** — 요일(月 등, `core/calendar.ts`의
     `WEEKDAY_NAMES.hanja`를 재사용할 수 있다)·일(`15日`처럼 날짜+日)·
     월(三月·三처럼 한자 숫자 표기, 1~12를 一·二·三·...·十二로 바꾸는
     작은 변환표가 필요하다) 세 가지.
  UI만 다시 짜는 게 아니라 서식 id 체계도 "언어 먼저, 종류 나중"으로
  다시 정리해야 해서, 아이콘·도형·체크박스(9단계)를 만들 때 속성 막대를
  더 손보게 될 테니 그때 함께 한다.
- **8e — 자동 필드 도구 (편집 화면 도구줄에 다섯 번째로 추가).** 실제로
  써보니 "글자 상자 → 선택 → 속성 막대에서 켜기"(8b) 흐름이 위클리 7칸처럼
  여러 개를 찍을 때 느렸다. 도구줄에 F(자동 필드) 도구를 더해서, 칸을
  클릭·드래그하면 **타이핑 없이 바로** 필드로 확정된다. **오프셋이 자동으로
  매겨진다** — 도구로 들어올 때마다 0부터 세고, 찍을 때마다 하나씩 늘어난다.
  7칸을 순서대로 찍으면 저절로 0~6이 된다. 서식은 여러 개를 골라 한꺼번에
  바꿀 수 있다(4번 버그 수정 덕분에 오프셋은 각자 그대로 남는다).

  텍스트에 `<+0>` 같은 표기를 직접 타이핑해서 인식시키는 방법(정적 글자와
  필드를 한 상자 안에 섞어 쓰는 것)도 사용자가 제안했지만, `TextObject`가
  "글자 전체가 필드 하나"라는 지금 모델을 벗어나는 더 큰 변경이라 미뤘다 —
  필요해지면 별도로 설계하고 시작한다.

  **UX 다듬기는 나중으로 미룸.** 실제로 써보고 "이해되고 쓸 수는 있는데
  다듬을 여지가 있다"는 평가를 받았다. 구체적으로 뭘 다듬을지는 아직
  정하지 않았다 — 다음에 다시 써보면서 무엇이 걸리는지부터 짚는다.

  **회전 배치에서 손으로 그린 선은 안전영역을 자동으로 피하지 않는다
  (버그 아님, 원래 동작).** `avoidSafeZone`은 자동 도트·격자에만 적용되고
  표·그리기 도구로 직접 그린 선(`LineObject`)에는 적용된 적이 없다.
  뒷면 타공 위치가 정확해진 뒤(9b 수정 2) 실제로 인쇄해보니, 손으로 그은
  줄노트가 안전영역을 침범하는 게 보였다 — 사용자가 직접 확인해 원인을
  가렸다. 줄을 안전영역 밖으로 다시 그리면 해결된다.

월간 달력은 8단계가 끝난 뒤 별도 단계.

### 9단계 — 양면 인쇄

- **9a 완료** — `Template.back`(뒷면) 데이터 모델과 앞면·뒷면 편집 탭.
  뒷면은 앞면과 같은 속지 규격(`insert`)을 공유하되 도트 격자와 그린 것은
  따로 지닌다. `store.ts`의 `side: 'front' | 'back'`가 지금 편집 중인 쪽을
  가리키고, `patchActiveSide`가 그리기·되돌리기·격자 설정을 그 쪽으로만
  보낸다. 저장 파일도 뒷면을 함께 담되(`toProject`/`toTemplates`) 실행취소
  이력은 여전히 담지 않는다. 9a 이전 파일은 뒷면 없이(단면) 열린다.
  양식을 바꾸거나(`selectTemplate`) 새로 만들거나(`addTemplate`) 복제하거나
  (`copyTemplate`) 파일을 불러오면(`loadProject`) 항상 앞면으로 돌아간다.
  **PDF 출력과 인쇄 미리보기는 아직 앞면만 본다** — 9b의 몫이다.
- **9b 완료** — PDF와 인쇄 미리보기가 뒷면을 그린다.
  `core/layout.ts`의 `mirrorLayout(layout, paperWidth)`가 칸의 x좌표만
  뒤집는다(`용지폭 − 칸의 x − 칸폭`) — 이 공식이 사는 유일한 곳이다. 절취선은
  칸 좌표만 보고 계산하므로 이 배치를 그대로 넘기면 저절로 앞뒤가 맞는 자리에서
  잘린다. `core/template.ts`의 `capacityPerSheet`(양면이면 칸수 ×2)와
  `frontBackFilled`(이 장에서 앞·뒤 각각 몇 칸 채우는지 — 앞을 다 채운 뒤 뒤를
  채운다)가 쪽수·용지매수 계산을 맡는다. `pdf/export.ts`는 장마다 앞면 페이지
  뒤에 뒷면 페이지를 하나 더 추가하고, 낱장 조합에서는 칸마다 배정된 양식의
  뒷면을(`backSlotOverrides`), 반복 인쇄에서는 지금 양식의 뒷면 하나를
  (`defaultBack`) 쓴다 — 뒷면이 없는 칸은 완전히 빈 채로 찍힌다. store에
  `duplex` 설정이 생겼고(용지·간격과 같은 인쇄 작업 설정), 인쇄하기 탭에
  양면일 때만 앞면·뒷면 미리보기 토글이 뜬다.
  **뒤집는 방향(긴 가장자리 기준)은 아직 실제로 인쇄해보지 않았다** —
  "아직 인쇄해보지 않은 값"에 적어뒀다.
- **9b 수정** — 안전영역·타공 안내가 뒷면에서도 왼쪽에 그대로 있던 버그를
  고쳤다. `core/grid.ts`의 `gridArea`와 `core/punch.ts`의 `holeCenterX`에
  `mirror` 인자를 추가해, 뒷면이면 안전영역을 오른쪽에서 비우고 구멍 안내도
  오른쪽에 그린다 — 종이를 뒤집으면 앞면 왼쪽 구멍이 뒷면에서 오른쪽에 있기
  때문이다. 화면 안내뿐 아니라 `pdf/export.ts`의 실제 도트 배치도 함께
  고쳤다 — 뒷면에서 "안전영역 비우기"를 켰다면 그동안 구멍이 없는 왼쪽을
  비우고 실제 구멍이 있는 오른쪽엔 그대로 찍고 있었다.
- 7단계에서 미뤄뒀던 **낱장 조합 + 반복 인쇄 연결도 끝났다** — 7단계 항목에
  적어뒀다.
- **9b 수정 2 — 회전 배치(M5 등)에서 뒷면 안전영역·타공이 반대로 뒤집히던
  버그를 고쳤다.** M5(62×125mm)는 A4에 실제로 **회전 배치**(칸을 90도 눕혀서
  더 많이 넣는 배치)가 되는데, M6는 그렇지 않다 — 사용자가 M5로 실제 인쇄해
  보고서야 드러났다. 칸이 눕혀지면 속지의 가로축(구멍이 있는 축)이 용지의
  세로축과 겹쳐서, 용지를 좌우로 뒤집는 물리적 동작이 그 축에는 영향을 주지
  않는다 — 9b 수정에서 만든 "뒷면이면 무조건 뒤집기"가 이 경우엔 오히려
  구멍을 반대편으로 튀게 했다. `core/layout.ts`에 `backSafeZoneMirror(rotated)`
  를 만들어 "회전 배치면 뒤집지 않는다"는 판단을 한 곳에 모았고,
  `pdf/export.ts`·인쇄 미리보기(`App.tsx`)가 함께 쓴다. 이때는 **편집
  화면을 손대지 않았다** — 양식은 용지를 몰라서(설계문서 3장) 나중에 회전
  배치가 될지 알 수 없기 때문이다.
- **9b 수정 3 — 편집 화면도 지금 인쇄 설정을 참고하도록 고쳤다.** 수정 2를
  실제로 써보니 문제가 드러났다: 뒷면을 편집 화면에서 타공선에 안 걸치게
  디자인했는데도 인쇄해보면 걸쳤다. 원인은 편집 화면이 **항상** "회전 안
  됨"으로 가정하고 타공을 뒤집었던 것 — 이 속지는 실제 인쇄 설정(용지
  크기 등)에서 회전 배치가 되는 규격이라, 편집 화면과 실제 인쇄물의 타공
  방향이 어긋났다.

  `Template` 데이터 자체는 여전히 용지를 모른다 — 대신 **화면(EditorTab·
  SettingsPanel)이 지금 store의 인쇄 설정으로 `selectLayout`을 돌려 회전
  여부를 미리 짐작**하고, `backSafeZoneMirror`에 넘긴다. 이건 순수하게
  화면 표시를 위한 판단이라 Template에는 저장되지 않는다 — 나중에 다른
  용지로 인쇄 설정을 바꾸면 이 짐작도 달라진다(양식이 용지를 모른다는
  원칙은 지킨 채, 화면만 "지금 설정대로라면" 하고 참고하는 정도다).
- **9b 수정 4 — 회전 배치에서 PDF 글자가 안 돌아간 채로 찍히던 버그를
  고쳤다.** 사용자가 인쇄 미리보기와 실제 내보낸 PDF를 나란히 비교해서
  찾아냈다 — 화면은 글자가 옆으로(세로로) 누워 있는데, PDF는 그대로
  가로로 찍혔다. 원인: 화면(SVG)은 `<g transform="matrix(...)">` 하나가
  안의 글자까지 통째로 돌리지만, PDF는 **글자가 놓일 자리**(`place.map`)만
  옮겨줬을 뿐 pdf-lib에 "이 방향으로 돌려 그려라"는 말을 한 적이 없어서
  글자 자체는 항상 가로로 그려졌다. `page.drawText`에 `rotate: degrees(90)`
  (회전 배치일 때만)을 더했다 — 회전이 없으면 `degrees(0)`으로 지금까지와
  같다.

  각도(+90°, 반시계)는 `core/place.ts`의 회전 배치 변환식을 직접 따라가서
  구했다: 글자가 다음 글자로 넘어가는 방향(원래는 오른쪽)이 회전 배치에서는
  용지 위쪽으로 가야 하는데, "오른쪽 → 위쪽"이 정확히 +90도 회전이다.

  **이 성질은 자동 테스트로 확인하지 못했다.** 글자를 실제로 그리려면
  진짜 글꼴 파일이 있어야 하는데, 이 프로젝트의 PDF 테스트는 지금까지
  전부 글꼴 없이(또는 일부러 망가뜨려서 던지는지만) 검증해왔다 — 그 틀을
  벗어나는 글꼴 픽스처를 새로 들이는 대신, 계산을 세 번 다른 방식으로
  검산하고 사용자의 실제 확인에 맡겼다.
- **9b 수정 5 — 양면 뒤집는 축을 "회전 배치가 아니면 좌우, 회전 배치면
  상하"로 바꿨다. `backSafeZoneMirror`는 지웠다.** 사용자가 실제 프린터의
  "옆으로 넘김"(짧은 가장자리 기준 뒤집기)으로 M5를 인쇄해보고 뒷면
  디자인이 상하로 뒤집혀 나오는 걸 발견했다 — 지금까지는 회전 배치여도
  `mirrorLayout`이 항상 좌우로만 뒤집었다.

  **다시 유도해보니 이 쪽이 오히려 더 단순하고 일관됐다.** 프린터가 종이를
  물리적으로 뒤집는 방식은 긴 가장자리 기준(좌우 뒤집기)과 짧은 가장자리
  기준(상하 뒤집기) 두 가지가 있고, 회전 배치가 아닐 때는 좌우가, 회전
  배치일 때는 상하가 실제 인쇄 결과와 맞는다. 이 전제로 안전영역·타공이
  뒤집히는 축을 다시 계산해보니, **회전 여부와 무관하게 속지 자신의
  가로축(구멍이 있는 축)은 항상 뒤집힌다**는 하나의 규칙으로 정리됐다 —
  9b 수정 2·3에서 만든 "회전 배치면 안 뒤집는다"는 특례가 통째로 필요
  없어졌다. `backSafeZoneMirror` 함수를 지우고 모든 자리에서 그냥
  `mirror = true`(뒷면이면 항상)로 되돌렸다. 편집 화면도 다시 인쇄 설정을
  참고할 필요가 없어져 9b 수정 3에서 더한 `selectLayout` 참고 코드를
  걷어냈다.

  `mirrorLayout(layout, paperWidth, paperHeight)`로 시그니처가 바뀌었다
  (회전 배치면 상하로 뒤집을 때 용지 높이가 필요하다).

도형, 체크박스, 아이콘은 그 다음.

### 10단계 — 월간 달력

- **10a 완료 — 달력 계산 (core만, 화면 없음).** `core/dataset.ts`의
  `Dataset`을 `DateDataset | CalendarDataset` 유니언으로 바꿨다. 날짜형은
  그대로고, `CalendarDataset`(`{kind:'calendar', year, weekStart, showAdjacent}`)이
  새로 생겼다.

  **달력 그리드는 오프셋의 뜻이 다르다.** 날짜형은 오프셋이 "이 쪽의 몇
  번째 값"이지만, 달력은 매달 1일이 시작하는 요일이 달라서 순서대로 셀 수
  없다(설계문서 7장). 그래서 **6주×7일=42칸 그리드로 고정**하고
  (`CALENDAR_GRID_SIZE`), 오프셋 0~41을 "그리드에서 몇 번째 칸인가"로
  다시 정의했다. `calendarCellAt(dataset, page, offset)`이 그 칸의 실제
  날짜를 계산한다 — `page`는 몇 월인지(0~11), `weekStart`에 따라 그리드의
  첫 칸(칸 0)이 그 달 1일에서 며칠 전인지 구하고, 거기서 오프셋만큼
  더한다. 이번 달이 아닌 칸(지난달 끝자락·다음달 시작)은 `showAdjacent`가
  꺼져 있으면 null(빈 칸)이다.

  **월 제목("2027년 3월")은 오프셋으로 못 가리킨다.** 42칸 중 어느 칸도
  "몇 월인지"를 안정적으로 가리키지 않는다 — 1일이 어느 요일에 시작하느냐에
  따라 칸 0이 지난달일 수도 있어서다. 그래서 `TextObject.field`에
  `title?: boolean`을 더했다(사용자와 상의해 "오프셋 42를 특수값으로 쓰는
  방법" 대신 이 쪽을 골랐다 — 매직 넘버보다 명확하다). 켜면 오프셋을 무시하고
  `calendarTitleAt(dataset, page)`(그 달 1일, `showAdjacent`와 무관하게 항상
  값이 있음)를 가리킨다. `core/format.ts`의 `resolveObjectsForPage`가
  `dataset.kind`와 `field.title`을 보고 어느 계산을 쓸지 고른다
  (`dateForField` 헬퍼). `core/text.ts`의 `fieldPlaceholder`도 `title`이면
  `⟨월⟩`로 다르게 보인다. 서식은 `formatDate`에 `'YYYY년 M월'`을 더했다.

  **`datasetPages`가 calendar면 언제나 12다** — 1년 12달 고정이라 날짜형처럼
  "총 개수 ÷ 한 쪽당"을 계산할 필요가 없다.

  아직 편집 화면에서 달력형 데이터셋을 만들 방법이 없다 — `SettingsPanel`의
  `DatasetGroup`은 여전히 날짜형만 그린다(`repeat.dataset.kind === 'date'`일
  때만 렌더링하도록 좁혀뒀다). 10b에서 연도·시작 요일·이전달 표시 설정과
  "월 제목" 토글을 붙인다.
- **10b 완료 — 편집 화면.** 세트형을 켜면 이제 "데이터 종류"(날짜/월간
  달력)를 먼저 고른다. 달력을 고르면 `CalendarDatasetGroup`이 연도·시작
  요일(일요일/월요일)·이전·다음 달 표시 여부를 보여준다 — 총 쪽수는 언제나
  12쪽이라 입력받지 않는다. 순서(순차/겹치기)·무더기는 날짜형과 똑같이
  쓰므로 `CutStackRows`로 뽑아 두 곳이 같이 쓴다.

  속성 막대(`StyleBar`)의 "자동 필드" 체크박스 옆에, **달력형일 때만**
  "월 제목" 체크박스가 추가로 뜬다. 켜면 오프셋 칸은 그대로 두되(무시되므로
  값은 상관없다) `field.title`이 켜지고 서식이 `YYYY년 M월`로 자동
  바뀐다. 여러 글자를 선택했는데 일부만 켜져 있으면 자동 필드 체크박스와
  같은 방식으로 어중간한(indeterminate) 표시를 쓴다.

  **실제로 만드는 순서**는: 표 도구로 6행 7열(42칸) 만들기 → 자동 필드
  도구(F)로 42칸을 순서대로 찍기(오프셋이 저절로 0~41이 된다) → 제목 글자
  하나 더 그려서 자동 필드 켜고 "월 제목" 체크 → 인쇄하기 탭에서 세트형·
  월간 달력 선택, 연도·시작 요일 정하기.

  `pdf/export.ts`·인쇄 미리보기(App.tsx)는 손대지 않았다 — 둘 다
  `resolveObjectsForPage(objects, dataset, page)`를 그대로 부르고, 안에서
  `dataset.kind`를 보고 알아서 갈라지므로(10a) 이미 달력을 지원하고 있었다.
- **10b 이후 방향을 바꿨다 — 42칸을 손으로 찍는 방식은 너무 번거롭다는
  사용자 피드백.** "표 도구로 6행 7열 만들고 자동 필드로 42번 찍고 제목도
  따로 켜기"는 실제로 써 보니 복잡했다. 대신 **달력을 통째로 하나의
  오브젝트로 만들어서, 박스 하나를 그리듯 놓고 모서리를 끌어 크기를 바꾸면
  안의 글자(요일·날짜·제목)가 전부 같이 커지고 작아지는 방식**으로
  바꾼다. "그림처럼 크기조정"을 진짜 이미지(PNG)로 만드는 방법도 검토했지만
  — 작은 글자를 인쇄하면 화질이 흐려지고, 달마다 날짜가 달라 12개월 각각
  다른 그림을 새로 만들어야 해서 — 진짜 벡터 글자를 쓰는 새 오브젝트
  종류로 정했다(그림처럼 다루되 실제로는 글자).

  이 결정에 따라 10a/10b에서 만든 것 중 **필드 기반 접근에만 쓰던 부분은
  되돌렸다**: `TextObject.field.title`, `fieldPlaceholder`의 `⟨월⟩`
  자리표시, `core/format.ts`의 `resolveObjectsForPage`가 달력을 갈라
  처리하던 부분(`dateForField`), 속성 막대의 "월 제목" 체크박스. 남긴 것:
  `core/dataset.ts`의 `calendarCellAt`·`calendarTitleAt`(그리드 계산 자체는
  여전히 옳다 — 다만 어떤 오브젝트가 그 계산을 쓰느냐만 바뀐다), `formatDate`의
  `'YYYY년 M월'` 서식.

  **시작 요일·이전달 표시는 데이터셋에서 오브젝트로 옮겼다.** "몇 년치
  데이터인가"(연도)는 데이터셋에 남지만, "어떻게 보일까"(시작 요일·이전달
  표시·요일 언어)는 그리는 물건 자체의 속성이 더 알맞다 — `CalendarDataset`은
  이제 `{kind:'calendar', year}` 뿐이고, `calendarCellAt`도
  `(year, page, offset, weekStart, showAdjacent)`처럼 필요한 값을 직접
  받는다. `SettingsPanel`의 `CalendarDatasetGroup`도 연도 하나만 남았다.
- **10c 완료 — 새 오브젝트의 기하 계산(core만, 화면 없음).**
  `core/calendar.ts`를 새로 만들었다. `calendarLayout(box)`가 박스
  가로×세로 하나만 받아서, 위에서부터 제목 1줄·요일 머리글 1줄·날짜
  6줄(총 8줄)로 나누고 각 칸의 가운데 좌표·밑선과 글자 크기(줄 높이의
  절반)를 계산한다. 세로 위치는 `core/text.ts`의 `baselineY`(한 줄 가운데
  정렬)를 그대로 썼다 — 이 프로그램의 다른 모든 글자와 같은 공식이어야
  나란히 놓아도 어색하지 않다. 박스가 커지면 `fontSize`도 커진다
  (테스트로 "2배 박스 → 2배 글자" 확인).

  `weekdayLabels(lang, weekStart)`도 여기 있다 — 한글·영어·한자 세
  언어(`WeekdayLang`) 중 골라서 시작 요일에 맞게 정렬한 7개 이름을
  돌려준다. 한자 표기가 실제로 인쇄됐을 때 지금 쓰는 Pretendard 글꼴에
  있는지는 아직 확인 못 했다 — "아직 인쇄해보지 않은 값"에 적어둔다.

  다음 단계(그리기+크기조정)에서 `CalendarObject`라는 새 객체 종류를
  만들고, 편집 화면 도구줄에 달력 도구를 붙인다.
- **10d 완료 — 그리기 + 크기조정.** `core/objects.ts`에 `CalendarObject`
  (`x·y·width·height` 박스 + `weekStart?·showAdjacent?·weekdayLang?·color?`)를
  더했다. `isLine`/`isText`처럼 `isCalendar` 판별 함수를 만들고, `boxOf`·
  `moveObject`가 셋을 함께 다루도록 정리했다(`isLine`이 아니면 상자 모양).

  **모서리를 끌어 크기를 바꾸는 상호작용을 새로 만들었다.** 지금까지는
  선의 끝점만 끌 수 있었다 — 이미지 편집 프로그램처럼 맞은편 모서리는
  고정한 채 크기를 바꾸는 건 이 프로그램에 처음 생기는 몸짓이다.
  `core/objects.ts`의 `resizeBox(box, corner, to)`가 계산을 맡고(최소
  크기 5mm 아래로는 안 줄어든다), `ui/gestures.ts`에 `boxHandle`이라는
  새 드래그 종류와 `previewBox`(선의 `preview`와 짝)를 더했다.

  편집 화면 도구줄에 달력 도구(C)가 생겼다. 표 도구처럼 **실제로 끌어야
  만들어진다**(그냥 눌렀다 뗀 칸 하나는 무시). 놓자마자 자동으로
  고른 상태가 되어, 도구를 바꾸지 않고 바로 모서리를 끌어 다듬을 수
  있다. 딱 하나 고르면 네 모서리에 손잡이가 뜬다(선의 끝점 손잡이와
  같은 자리에, `HANDLE_GRAB` 반경 안에서 잡힌다).

  속성 막대에 `CalendarControls`가 새로 생겨 시작 요일·요일 언어(한글·
  영어·한자)·이전달 표시를 고른다 — 여러 개를 골랐는데 값이 다르면
  `LineControls`와 같은 방식으로 `—`(어중간)를 보여준다.

  `ui/InsertView.tsx`에 `CalendarLayer`를 붙여 편집 화면에서 바로
  보인다. **아직 실제 데이터셋과 연결되지 않는다** — 자동 필드가
  `⟨+0⟩` 자리표시를 보여주던 것과 같은 이유로, 지금은 오늘 날짜가 속한
  달을 미리보기로 보여준다(`core/dataset.ts`의 `calendarCellAt`·
  `calendarTitleAt`을 그대로 재사용). 실제로 몇 년치가 찍힐지는 인쇄하기
  탭의 데이터셋(연도)이 정하고, 그 연결은 10e의 몫이다. PDF도 아직
  손대지 않았다 — 편집 화면과 인쇄 미리보기·PDF가 같은 오브젝트를 각자
  그리게 되는 것도 10e에서다.
- **10e 완료 — 진짜 값 채우기. 10단계(월간 달력) 마무리.** 인쇄
  미리보기·PDF가 실제 연도로 진짜 날짜를 그린다.

  **달력은 자동 필드와 다르게 연결된다.** 자동 필드는 `resolveObjectsForPage`가
  미리 값을 채운 글자로 바꿔치기하지만, 달력 오브젝트는 그대로 둔 채
  **그릴 때** 연도·쪽 번호로 날짜를 계산한다 — 오브젝트 하나가 매달
  다른 42개 날짜를 내야 해서, 미리 하나의 값으로 바꿔치기할 수 없기
  때문이다.

  - `ui/InsertView.tsx`가 `calendarContext?: {year, page}`를 받는다.
    없으면(편집 화면) 여전히 오늘이 속한 달을 보여준다.
  - `ui/PaperPreview.tsx`의 `PreviewSlotContent`에 `calendarPage`를,
    `PaperPreview` 자체에 `calendarYear`를 더해 칸마다 `InsertView`로
    넘긴다.
  - `ui/App.tsx`의 세트형 미리보기 계산에서 `calendarPage: page`를
    함께 넣고, `<PaperPreview>`·`buildPdf` 양쪽에 `calendarYear`(달력형
    데이터셋의 연도)를 넘긴다.
  - `pdf/export.ts`에 `drawCalendars`를 새로 만들었다 — `drawTexts`와
    같은 자르기(clip)·회전(`rotate: degrees(...)`) 규칙을 따르되, 가운데
    정렬은 화면의 `text-anchor="middle"` 대신 글자 폭을 재서 왼쪽 끝을
    직접 계산한다(PDF는 언제나 왼쪽 기준으로 그리기 때문 — drawTexts와
    같은 이유). `pageFor(i)`가 이 칸의 쪽 번호를 알려주고, `calendarYear`와
    함께 `calendarCellAt`·`calendarTitleAt`을 직접 불러 42칸·제목을
    계산한다. **글자가 하나도 없고 달력만 있어도 글꼴을 심어야 한다** —
    글꼴이 필요한지 보던 조건이 `isText`만 보고 있어서, `isCalendar`도
    함께 보도록 고쳤다(안 고쳤으면 텍스트 없이 달력만 있는 양식은 글꼴
    없이 시도되어 빈 칸으로 찍혔을 것이다).
  - 테스트는 core/calendar·core/dataset이 이미 촘촘히 재는 날짜 계산은
    다시 재지 않고, 배선만 본다 — "글자 없이 달력만 있어도 글꼴을
    심으려 한다"(망가진 글꼴 바이트로 강제로 예외를 던져 확인, 이
    파일의 기존 방식과 같다)와 장수 안전성 정도다. 실제 글자 모양은
    진짜 글꼴 파일 없이는 확인할 방법이 없다(이 프로젝트의 PDF 테스트
    전체가 그렇다).

  **아직 인쇄해서 확인 못한 것**: 회전 배치(M5 등)에서 달력 오브젝트의
  글자가 제자리에 돌아가는지(`drawTexts`가 같은 방식으로 이미 검증됐으니
  구조적으로는 맞아야 하지만, 달력은 42개 글자를 한꺼번에 그려 아직
  실제로 뽑아보지 않았다), 한자 요일(`weekdayLabels('hanja', ...)`)이
  Pretendard에 있는지. "아직 인쇄해보지 않은 값"에 옮겨 적었다.

도형, 체크박스는 그다음.

### 11단계 — 이미지 삽입

- **완료.** "아이콘"을 어떻게 만들지 사용자와 상의했다 — 내장 벡터 아이콘
  모음(직접 그리거나 구해야 하는 종류가 제한적)과 사용자 이미지 업로드
  (PNG·JPG를 직접 올려서 놓는 방식) 중, **사용자 이미지 업로드**를 골랐다.
  아이콘뿐 아니라 사진·로고 등 뭐든 넣을 수 있게 됐다.

  **글꼴 등록과 똑같은 틀이다** — `images/registry.ts`가 파일을
  이번 세션에만 들고 있는다(`registerImage`·`imageBytes`·`hasImage`·
  `reserveImageIds`, fonts/registry와 1:1 대응). 저장 파일엔 이름만
  담기고(`core/project.ts`의 `SavedImage`, 옛 파일엔 없을 수 있어
  optional로 뒀다), 다시 열면 "다시 등록해야 합니다" 안내가 뜬다
  (`missingImages`, `ProjectFile.tsx`가 글꼴 안내와 합쳐서 보여준다).

  **10단계에서 만든 "박스 하나 그리고 모서리로 크기조정" 틀을 그대로
  재사용했다.** `core/objects.ts`의 `resizeBox`·`Corner`·`MIN_BOX_SIZE`가
  이미 달력 전용이 아니라 범용으로 설계돼 있어서(`isBoxResizable(o): o is
  CalendarObject | ImageObject`), 손잡이·미리보기(`ui/gestures.ts`의
  `boxHandle`·`previewBox`) 쪽은 **한 줄도 새로 안 만들고** `isCalendar`를
  `isBoxResizable`로 넓히기만 했다. `ImageObject`는 `{x, y, width, height,
  imageId}`뿐이다 — 좌우 비율은 고정하지 않는다(모서리로 자유롭게
  늘이고 줄인다).

  **이미지는 "먼저 고르고 나서 놓는다".** 도구줄에 이미지 도구(I)를
  누르면 속성 막대에 이미지 선택 드롭다운(`ImageControls`, `FontPicker`와
  같은 틀)이 뜬다 — 파일을 새로 등록하거나 이미 등록한 것 중 고르면
  store의 `draftImageId`에 담기고, 그 상태로 박스를 그리면
  `commitImage`가 그 사진으로 오브젝트를 만든다. 이미 놓인 이미지를
  고르면 같은 드롭다운이 그 오브젝트의 사진을 바꾼다(`styleImage`).
  아직 아무 이미지도 고르지 않았는데 박스를 그리면 조용히 아무 일도
  안 한다.

  화면(`ui/InsertView.tsx`의 `ImageLayer`)은 등록된 파일이 있으면
  data URL로 `<image>`를 그리고, 없으면(저장 파일을 열고 아직 다시
  등록 안 함) 점선 자리(`.image-missing`)만 남긴다 — 글꼴이 기본
  글꼴로 대신하는 것과 달리 이미지는 대신할 게 없어서다.

  `pdf/export.ts`의 `drawImages`는 `drawTexts`와 같은 자르기(clip)·
  회전(`rotate: degrees(...)`) 규칙을 쓴다 — 왼쪽 아래 모서리를
  기준점으로 잡아 `place.map`으로 옮기고 `rotate`로 그림 자체를
  돌린다(회전 배치에서 텍스트가 돌아가는 것과 같은 원리). PNG는
  `embedPng`, JPG는 `embedJpg`로 심고, 실제로 쓰인 이미지만 심는다
  (글꼴과 같은 규칙). 테스트는 진짜 1×1 PNG 바이트를 만들어 "심으면
  파일이 커진다"·"안 쓰인 건 안 심는다"·"깨진 파일이면 던진다"를
  확인했다 — 글꼴 테스트와 달리 이번엔 진짜 파일로 검증할 수 있었다
  (pdf-lib의 embedPng·embedJpg가 파일 구조를 실제로 검사하기 때문).

  회전 배치(M5)에서도 실제로 인쇄해 제자리로 돌아가는 것을 확인했다.

### 12단계 — 글자 회전 + 로고 칸

사용자가 두 가지를 요청했다: (1) 타공 위아래(또는 A5·TA6·DA6처럼 구멍이
3+3으로 나뉜 규격은 그 사이) 빈 자리에 로고·텍스트를 놓는 칸, (2) 그 칸의
글자가 타공 쪽이 위로 오게 종이를 돌렸을 때 바로 읽혀야 한다는 것 — 이건
일반적인 "글자를 90도 돌리는 기능"이 먼저 있어야 풀리는 문제라, 회전
기능부터 만들고 로고 칸은 그 위에 얹기로 했다.

- **글자 회전(0·90·270도) 완료.** `TextObject`에 `rotate?: 90 | 270`을
  더했다(180은 뒤집힌 글자라 실용성이 없어 뺐다). 상자의 **가운데를
  축**으로 돈다 — 정렬·줄바꿈 등 다른 계산은 전부 회전하지 않은 자기
  상자 기준 그대로 두고, 회전은 그린 결과를 통째로 돌리는 마지막 한
  단계로만 다룬다. 그래서 `core/text.ts`의 기존 `anchorX`·`lineBaselines`는
  전혀 손대지 않았다.

  `core/text.ts`의 `textRotationOf(box, rotate)`가 core/place의
  `Placement`(`map`·`svg`)와 똑같은 모양으로 여섯 숫자짜리 행렬을 낸다 —
  화면(SVG 행렬)과 PDF(좌표 변환)가 같은 숫자에서 나와야 손 방향이
  어긋나지 않는다. **이 행렬은 순수 기하 계산이라 실제 글꼴 없이도 정확한
  좌표를 테스트로 검증할 수 있었다**(달력·이미지 때와 달리, 화면 렌더링을
  기다릴 필요가 없었다) — 상자 가운데는 축이라 회전해도 그대로인지, 왼쪽
  위 모서리가 90도·270도에서 서로 반대쪽으로 움직이는지를 정확한 좌표로
  단언했다.

  **90도(시계 방향)는 회전 배치(core/layout의 `mirrorLayout`)의 거울상이다**
  — 손 방향(반시계)이 반대라 부호가 다르다. 270도 쪽이 오히려 회전
  배치와 같은 손 방향이라, PDF에서 짝지어 쓰는 각도(`pdfRotateDegreesOf`)는
  270도만 9b 수정 4에서 이미 인쇄로 검증된 조합(`rotate: degrees(90)`)을
  그대로 재사용하고, 90도(거울상)는 부호만 반대로 추론했다 — **아직
  인쇄로 확인하지 못했다.**

  화면(`ui/InsertView.tsx`의 `TextLayer`)은 회전이 있으면 `<text>`를
  `<g transform={svg 행렬}>`로 감싸기만 한다 — 위치 계산은 회전 없을
  때와 똑같이 그대로 두고 화면이 통째로 돌려 그린다. PDF(`pdf/export.ts`의
  `drawTexts`)는 각 줄의 자리를 `textRotationOf(...).map(...)`으로 먼저
  돌린 다음 `place.map`으로 칸 배치(회전 배치 포함)를 반영하고, `rotate:`
  각도도 회전 배치 각도와 글자 자신의 각도를 더한다(두 각도 모두 같은
  손 방향 표기라 그냥 더하면 합쳐진 회전이 나온다). 속성 막대에
  회전 없음/90도/270도 드롭다운을 더했다.

- **로고 칸 완료 — 실제로 써보고 두 번 고쳤다.**

  **1차: 자리를 맨 위·맨 아래 여백에 크게 잡았더니 실제로는 너무 컸다.**
  사용자 피드백으로 다시 설계했다 — `core/punch.ts`의
  `logoSlotAreas(insertWidth, insertHeight, punch, mirror)`가 이제
  **구멍과 비슷한 작은 크기(6×10mm, `LOGO_SLOT_WIDTH`·`LOGO_SLOT_HEIGHT`)의
  칸을 구멍과 같은 세로줄 위, 구멍과 구멍 사이 정중앙**에 낸다. 구멍이
  고르게 배치된 규격(`groupGap`이 null인 M5·M6·DA9 등)은 **인접한 구멍
  쌍마다 하나씩**(구멍 N개면 자리 N−1개) — M6는 5자리, M5는 4자리가
  나온다. 3+3 묶음 배치 규격(`groupGap`이 있는 A5·TA6·DA6 등)은 두 묶음
  사이 큰 빈 공간 한가운데에 하나만 낸다. 구멍 간격(19mm, 바인더 표준
  고정값)이 칸 높이(10mm)보다 훨씬 넓어서 자리가 없어서 빠지는 경우는
  없다 — 이전 판의 "여백이 좁으면 빠진다" 로직은 필요 없어져 지웠다.

  텍스트 도구줄의 "로고 칸" 버튼이 첫 번째 자리에 입력을 여는 것은
  그대로지만, 칸이 작아진 만큼 기본 글자 크기를 앱 기본(9pt)이 아니라
  **6pt**로, 정렬도 가운데·중단으로 미리 맞춰서 연다.

  **2차: 회전한 글자를 고칠 때 입력칸이 안 돌아가 있던 버그를 고쳤다.**
  `ui/EditorTab.tsx`의 `TextInput`(타이핑 중 SVG 위에 겹쳐 놓는 진짜
  `<textarea>`)이 `editing.style.rotate`를 전혀 보지 않고 있었다 — 확정된
  글자는 화면(`InsertView`)에서 돌아간 모습으로 보이는데, 정작 타이핑하는
  동안의 입력칸은 안 돌아간 자리에 그대로 있어서 "칸에서 나와서 고쳐지는"
  것처럼 보였다. CSS `transform: rotate(...)`를 더해서 고쳤다 — 부호는
  core/text의 `textRotationOf`가 화면(SVG matrix)에 쓰는 것과 같은 손
  방향이다(둘 다 브라우저 좌표계라 그대로 맞아떨어진다).

  `ui/PunchGuide.tsx`가 이 자리를 점선으로 항상 보여준다(타공 안내를
  켜둔 동안) — 이미지는 이 점선을 보고 직접 끌어다 놓는다.

  **회전 방향은 앞뒤가 반대다.** 앞면은 구멍이 왼쪽이라 270도(반시계),
  뒷면은 구멍이 오른쪽이라 90도(시계) — "타공 부분이 위로 오게 종이를
  돌렸을 때"의 물리적 회전 방향이 앞뒤에서 서로 반대이기 때문이다(나침반
  방향으로 따져보면 왼쪽→위는 시계 방향 회전이 필요하고, 그 종이 회전을
  상쇄하려면 글자를 미리 반시계로 돌려둬야 한다 — 오른쪽→위는 그 거울상).
  **이 추론은 아직 인쇄로 확인하지 못했다** — 위 "글자 회전" 항목의
  90도(거울상) 미검증과 같은 이유다.

  **이미지를 로고 칸에 놓는 건 여전히 손이 많이 갔다** — 점선을 보고
  6×10mm짜리 작은 박스를 손으로 정확히 끌어야 했다. 사용자가 "이미지
  넣기가 너무 힘들다"고 지적했고, "앞면·뒷면·로고칸을 나눈 별도 편집
  화면"을 먼저 제안받았지만, 실제로 겪은 문제(회전한 글자가 클릭이 안
  됨, 칸이 여럿이면 첫 칸만 접근됨, 이미지를 정밀하게 못 끎)를 짚어보니
  더 간단한 답이 나왔다 — **아예 정확한 위치를 자동으로 만들어주는 대신
  구멍과 같은 세로줄에 "달라붙기"만 시켜주는 것.**

- **12d 완료 — 로고 칸을 정렬선으로 단순화 + 실제 버그 두 개 수정 +
  잠금 모드.**

  **로고 칸의 작은 박스(6×10mm)를 완전히 없앴다.** `core/punch.ts`의
  `logoSlotAreas`를 지우고, `PunchGuide`에 구멍과 같은 세로줄
  (`holeCenterX`) 하나만 점선으로 남겼다. "로고 칸" 버튼도 없앴다 —
  이제 그냥 평소처럼 텍스트·이미지 도구로 그 줄 근처에 놓으면
  (`ui/EditorTab.tsx`의 `snapToLogoLine`, 3mm 안이면 달라붙는다) 가로
  자리가 자동으로 그 줄에 맞춰진다. 세로 위치·크기는 여전히 사용자
  마음대로다 — 정확한 자리를 강제로 계산해주는 대신 "가로만 맞춰주고
  나머지는 손에 맡긴다"로 요구 수준을 낮췄다.

  **버그 1 — 회전한 글자가 클릭으로 안 골라지던 문제.** `hitAt`이
  글자의 실제 크기를 잴 때 쓰는 `getBBox()`는 **회전하기 전(자기 상자
  기준) 자리**를 돌려준다 — 화면에 보이는, 실제로 돌아간 자리가 아니다.
  그래서 클릭 지점을 반대 방향으로 먼저 되돌려("회전 전이라면 어디를
  짚은 셈인지") `getBBox()`와 비교하도록 고쳤다. 90도와 270도는 같은
  축을 반대로 돌아 서로의 역회전이라, `textRotationOf`에 반대 각도를
  넣는 것만으로 되돌리기 계산을 새로 안 만들고 그대로 재사용했다.

  **버그 2 — "로고 칸" 버튼이 항상 첫 자리만 열던 문제는 그 버튼 자체를
  없애며 함께 해결됐다.**

  **잠금 모드 완료.** 설계문서 6장에 처음부터 계획돼 있었지만
  안 만들었던 `locked` 속성을 이제 네 오브젝트 종류(선·글자·달력·이미지)
  모두에 더했다(`core/objects.ts`의 `isLocked`). 잠근 것은 클릭으로도
  감싸기(마퀴)로도 골라지지 않는다 — `hitAt`·마퀴 필터 양쪽에서
  건너뛴다. 편집 화면에 "잠그기"(고른 것을 잠그고 선택을 비움) 버튼과,
  잠긴 게 있을 때만 뜨는 "잠긴 것 N개 · 전부 해제" 버튼을 더했다 —
  하나씩 푸는 목록 UI 대신 전부 한 번에 푸는 쪽을 골랐다(더 간단하고,
  실수로 잠근 것도 쉽게 되돌릴 수 있다).

  **속지 바깥에서 시작한 드래그도 감싸기(마퀴)가 되게 했다.** 지금까지는
  포인터 이벤트가 `<svg>` 하나에만 걸려 있어서, 속지 여백(패딩) 쪽에서
  드래그를 시작하면 아무 일도 안 일어났다. 감싸는 `.editor-canvas` div에
  `onOutsideDown`이라는 좁은 핸들러를 새로 붙였다 — 도구가 select일 때,
  그리고 클릭 지점이 svg 안이 아닐 때만 마퀴 드래그를 시작한다(svg
  안에서 시작한 클릭은 svg 자신의 핸들러가 이미 처리하므로 건너뛴다).
  드래그를 옮기고 끝내는 나머지는 기존 `onMove`·`onUp`을 그대로
  재사용했다 — 어느 태그에서 시작했는지와 무관하게 `dragRef`만 보고
  판단하는 함수라 그대로 붙여도 안전하다.
- **12d 수정 — 실제로 써보고 버그 두 개를 더 고쳤다.**

  **정렬선이 실제로는 거의 안 붙던 문제.** `snapToLogoLine`이 커서의
  실제 자리가 아니라 **격자에 이미 붙은 점**을 기준으로 "정렬선에서
  3mm 안인가"를 쟀다. 격자점은 간격·여백에 따라 정해진 자리에만
  찍히므로, 정렬선(구멍의 가로 자리) 3mm 안에 격자점이 하나도 없는
  경우가 흔했다 — 그러면 아무리 정렬선 가까이 커서를 대도 안 붙는다.
  커서의 실제 자리(`raw`)로 재도록 고쳤다 — 정렬선은 격자와 무관한
  안내선이라, 격자가 없어도(칸이 없는 속지라도) 붙어야 맞다.

  **달력·이미지 도구를 쓰는 중에 이미 놓은 것을 끌어도 안 옮겨지던
  문제.** `tool === 'calendar' || tool === 'image'`일 때 이미 있는
  것을 클릭하면 고르기(`select`)만 하고 끌기(`move`) 몸짓을 시작하지
  않았다 — 도구를 select(V)로 바꿔야만 옮길 수 있었다. 고르기 도구의
  같은 로직을 `startMove` 함수로 뽑아 달력·이미지 도구에서도 똑같이
  쓰도록 고쳤다.

  **"로고 칸" 버튼을 없앤 건 사용자와 상의 없이 임의로 한 판단이라
  틀렸다** — 사용자가 원래 그리던 사용법(불러오기 → 버튼 클릭 → 정렬선에
  놓기)에 버튼이 들어 있었는데, 안 물어보고 지워버렸다. 되물어서
  확인한 역할대로 다시 넣었다: **도구는 바꾸지 않고, 정렬선만 2.6초간
  도드라지게 깜빡인다**(`ui/PunchGuide.tsx`의 `emphasizeLine` prop,
  `.logo-line-emphasis`의 CSS 애니메이션). 늘 옅게 보이는 점선이라
  놓치기 쉬웠던 것만 보완한다 — 정렬 자체는 이미 텍스트·이미지 도구를
  그 줄 근처에 놓으면 저절로 된다.
- **12g 완료 — 정렬선이 상자 폭을 0으로 만들던 버그 + 이미지 자유
  회전 + 회전한 것들의 선택 테두리·손잡이.**

  **정렬선 버그의 진짜 원인.** `snapToLogoLine`이 끄는 동안의 두 점(from·to)을
  각각 줄에 붙이고 있었다 — 둘 다 같은 x로 고정되니 상자 폭이 늘 0이 되어
  아무것도 안 놓이는 것처럼 보였다. **끄는 중인 두 점이 아니라 완성된
  상자**를 보고, 그 상자의 **가운데** x가 줄 3mm 안이면 폭은 그대로 두고
  왼쪽 x만 밀어 가운데를 맞추도록(`centerOnLogoLine`) 다시 짰다 — 손을
  떼기 전 미리보기도 같은 계산을 써서 결과를 미리 볼 수 있다.

  **회전 계산을 글자 전용에서 범용으로 옮겼다.** `core/text.ts`의
  `textRotationOf`·`pdfRotateDegreesOf`를 `core/objects.ts`의
  `rotationOf`·`pdfRotateOf`로 옮기고, 각도를 `90 | 270` 대신 **임의의
  수(도)** 를 받게 일반화했다. 0·90·180·270도는 여전히 정확한 정수
  계수(0, 1, -1)로 따로 계산한다 — `Math.cos(Math.PI/2)`가 부동소수점
  오차로 정확히 0이 아니라서, 그대로 쓰면 "회전 없음"에 아주 작은 회전이
  섞여 들어간다. 일반화한 공식이 기존 90·270도 값과 정확히 같은 숫자를
  내는지 테스트로 재검증했다(`core/objects.test.ts`로 옮김).

  **이미지에 자유 회전을 더했다.** `ImageObject.rotate?: number`(글자와
  달리 임의의 각도), 속성 막대에 숫자 입력(±360도)을 달았다. 화면
  (`ImageLayer`)·PDF(`drawImages`) 둘 다 글자와 같은 `rotationOf`·
  `pdfRotateOf`를 그대로 쓴다.

  **선택했을 때 뜨는 초록 테두리·모서리 손잡이도 회전한 것과 같이
  돈다.** 지금까지는 글자가 돌아가 있어도 테두리는 안 돌아간 상자
  그대로였다(사용자가 지적한 문제). `ui/EditorTab.tsx`에 오브젝트
  자신의 회전 각도를 돌려주는 `rotationDegOf`와, 화면 좌표를 그
  오브젝트의 회전 전 좌표로 되돌리는 `toLocal`을 더해서, 클릭 판정·
  모서리 손잡이 판정·테두리 그리기가 전부 이 하나의 계산을 함께 쓴다.

  **이미지가 돌아가 있어도 클릭으로 골라지도록 미리 고쳤다** — 사용자가
  아직 지적하지 않았지만, 글자 때와 똑같은 종류의 버그(회전 전 자리로
  클릭 지점을 되돌리지 않으면 화면에 보이는 자리를 눌러도 안 잡힌다)라
  이미지에도 미리 적용했다. 모서리 손잡이로 크기를 바꾸는 것도 돌아간
  상자 기준으로 정확히 계산된다(`resizeBox`는 회전 전 좌표만 알면 되므로
  기존 계산을 그대로 재사용했다).
- **12h 완료 — 정렬을 "로고 칸" 버튼이 켠 5초짜리 창 안에서만 하도록
  바꿈 + 방향키 미세이동.**

  **여전히 가운데에 안 맞던 진짜 이유.** 정렬선 근처에서 늘 자동으로
  달라붙게 해뒀더니, 글자를 입력하는 동안 상자가 자라는(`growBox`,
  "왼쪽 위는 절대 움직이지 않고 오른쪽·아래쪽으로만 자란다") 과정에서
  처음에 맞춰둔 가운데가 다시 어긋났다 — 폭이 늘어나면 왼쪽은 고정된
  채 오른쪽 끝만 밀리므로, 타이핑을 마칠 때의 진짜 가운데는 처음 맞춘
  자리와 달라진다. 회전까지 걸려 있으면 그 어긋남이 화면에서 어느
  방향으로 나타날지 가늠하기도 어렵다.

  **그래서 자동으로 늘 붙는 대신, 버튼이 여는 짧은 창 안에서만 붙게
  했다.** "로고 칸" 버튼을 누르면 `logoLineEmphasis`가 5초간 켜지고
  (`LOGO_LINE_EMPHASIS_MS`), **그 사이에 텍스트·이미지를 새로 만들거나
  이미 있는 것을 옮길 때만** 가운데가 정렬선에 달라붙는다. 만들기는
  이미 다 만든 뒤(타이핑까지 끝낸 뒤) 옮기면서 맞추는 쪽이 자연스러워
  보여서, 딱 하나 고른 텍스트·이미지를 끄는(`move`) 몸짓에도 같은
  `centerOnLogoLine` 계산을 새로 붙였다 — 여럿을 한꺼번에 고르면
  (`lone`이 없다) 어느 것 기준으로 맞출지 애매해 손대지 않는다. 창이
  닫히면(5초 뒤) 자동으로 다시 안 붙어서, 그 칸 근처에 다른 걸 놓을
  때 뜻하지 않게 끌려가지 않는다.

  **방향키 미세이동도 더했다.** 골라둔 것이 있을 때 방향키를 누르면
  0.5mm씩(`NUDGE_STEP`), ⇧를 누른 채면 5mm씩(`NUDGE_STEP_SHIFT`)
  옮긴다. 정렬선 달라붙기와는 무관하게 늘 켜져 있다 — 방향키는 오히려
  "달라붙지 않고 내가 원하는 자리로 정밀하게" 옮기려는 의도라 스냅과
  섞지 않았다. ⌘·Ctrl과 함께 누르면(브라우저 단축키일 수 있어) 건드리지
  않는다.

---

### 13단계 — 도형·체크박스

설계문서의 9단계 "나머지" 중 마지막 둘. 사각형·타원을 따로 만들지 않고
**둥글기 하나로 잇기로** 했다 — 0(각짐)에서 4(원·타원)까지 올리면 저절로
타원이 된다.

- **도형(사각형·타원) 완료.** 새 `ShapeObject`(`core/objects.ts`) — 상자
  하나에 둥글기 단계(`roundness: 0|1|2|3|4`)와 테두리 굵기·색·모양을
  지닌다. 채우기는 아직 없다(테두리만) — 필요해지면 그때 더한다.

  **둥글기 계산은 `core/shape.ts`에 한 번만 있다.** `cornerRadiusOf`가
  단계를 실제 반지름(rx, ry)으로 바꾼다 — 0~3단계는 짧은 변의 절반을
  기준으로 25%씩 늘어나서, 세로로 길든 가로로 길든 3단계까지는 "그래도
  사각형"으로 보인다. 4단계만 특별하게 상자의 가로·세로 절반을 각각
  반지름으로 쓴다 — 짧은 변 기준을 그대로 쓰면 직사각형에서 알약 모양
  (스타디움)이 되어 "원"이라는 말과 안 맞기 때문이다. 정사각형이면
  완전한 원, 직사각형이면 완전한 타원이 된다.

  **화면과 PDF가 완전히 같은 경로 문자열을 그린다.** `roundedRectPath`가
  SVG 경로(`M`·`L`·`C`·`Z`)를 mm로 만들면, 화면은 그대로 `<path d>`에
  쓰고 PDF는 pdf-lib의 `drawSvgPath`에 그대로 넘긴다 — 모서리가 화면과
  인쇄에서 다시 어긋날 일이 구조적으로 없다. 모서리는 호(arc) 대신
  카파 상수(≈0.5523)로 근사한 3차 베지어를 쓴다 — SVG·PDF 라이브러리가
  arc 명령을 조금씩 다르게 해석하는 위험을 피하기 위해서다. 이 하나의
  경로 함수가 둥글기 0~3단계(사각형)와 4단계(타원)를 분기 없이 전부
  만든다 — 4단계에서는 네 변의 직선이 저절로 길이 0이 되어 베지어
  곡선만으로 완전한 타원이 그려진다.

  달력·이미지와 같은 규칙으로 모서리를 끌어 크기를 바꾸고
  (`isBoxResizable`에 도형도 더했다), 놓자마자 바로 고른 상태가 된다.
  회전은 이번에는 넣지 않았다 — 필요해지면 이미지와 같은 `rotationOf`를
  그대로 쓸 수 있다.

- **체크박스 완료.** 켜고 끄는 토글은 없다 — 인쇄물이라 상태가 없고,
  체크는 나중에 손으로 한다. 대신 **표와 같은 몸짓**(칸 범위를 드래그)으로
  걸친 칸마다 하나씩 아이콘을 찍는 도장 도구다. `core/grid.ts`에 새
  `cellsIn(lattice, a, b)`을 더했다 — `tableSize`·`tableLines`와 같은
  격자점 계산(`between`)을 보므로 몇 칸인지와 실제 찍히는 자리가 서로
  다른 계산에서 나와 어긋날 일이 없다.

  **칸 하나가 그대로 아이콘이 되지 않는다** — `core/checkbox.ts`의
  `checkboxIconBox`가 "칸보다 조금 작게"(짧은 변의 15%를 네 방향에서
  줄인다, 가운데는 그대로) 줄인 상자를 오브젝트에 새겨 넣는다. 도장을
  찍는 순간 계산해서 박아두므로, 나중에 격자 간격을 바꿔도 이미 찍은
  체크박스는 그대로다.

  **아이콘 여섯 가지**(네모·동그라미·세모·다이아·별·하트)는 전부
  `core/checkbox.ts`의 `checkboxPath` 하나가 만드는 SVG 경로 문자열이고,
  화면·PDF가 그대로 같이 쓴다(도형과 같은 원칙). 네모·동그라미는 도형의
  `roundedRectPath`를 그대로 빌린다(둥글기 0단계·4단계와 정확히 같다).
  별은 삼각함수로 꼭짓점 10개(바깥 5+안쪽 5, 안쪽 반지름은 정오각별
  비율 sin18°/sin54°≈0.382)를 잇는다. **하트는 검토 결과 그대로 넣었다**
  — 잘 알려진 3차 베지어 6개짜리 근사 공식(바닥 뾰족점 → 두 봉우리 →
  가운데 오목한 곳)을 상자 비율(0~1)로 정규화해서 썼다. 어색해 보이면
  나중에 계수만 손보면 된다.

  **찍기 전에 아이콘을 먼저 고를 수 있다.** `drawStyle`(선)·`textDraftStyle`
  (글자)와 같은 자리에 `checkboxDraftStyle`을 더해서, 도구만 켠 상태에서
  속성 막대로 "다음에 찍을 모양"을 미리 정해둔다 — 매번 네모로 찍고
  하나하나 고치지 않아도 된다.

  **체크박스는 모서리로 크기를 못 바꾼다** — 칸에 맞춰 찍는 도장이라
  이번에는 넣지 않았다(`isBoxResizable`에는 안 넣었다). 다만 **클릭으로
  고르고 옮기는 건 된다** — `isBoxResizable`과 "상자로 클릭 판정하는
  대상"을 갈라 `isBoxShaped`를 새로 만들었다. 체크박스 도구를 켠 채로
  이미 찍은 것을 클릭하면(달력·이미지·도형처럼) 그 자리에서 바로 옮길
  수 있고, 빈 곳을 끌면 새로 찍는다 — 이번 세션 앞부분에서 고친
  "달력·이미지 도구에서 못 옮기던 버그"와 같은 종류의 문제라 처음부터
  같은 방식으로 만들었다.
- **13c 완료 — 도형을 따로 도구로 두지 않고 그리기 도구에 합쳤다.**
  사용자 요청("도형은 그냥 그리기 모드랑 합치면 안될까?"). 도구 버튼을
  없애고, 그리기 도구의 속성 막대에 "모서리 둥글기" 드롭다운을 하나
  더했다 — `store.ts`의 `drawStyle`(원래 선의 굵기·색·모양)에
  `roundness`를 얹었다(`DrawStyle = LineStyle & { roundness? }`,
  `LineObject`에는 없는 값이라 core가 아니라 store 계층에서만 쓰는
  타입이다). `commitShape`가 `drawStyle`의 `width`(도형의 `strokeWidth`가
  된다)·`color`·`dash`·`roundness`를 그대로 물려받는다. 끄는 동안의
  미리보기는 그대로 각진 사각형이다 — 손을 떼야 실제로 둥근 모양이
  보인다(간단하게 두기로 했다, 미리보기까지 둥글게 하려면 계산이 하나
  더 필요하다).
  **처음엔 둥글기가 0(각짐)이면 선 4개, 1 이상이면 도형 하나로
  갈랐는데 — 13e에서 사용자가 써보고 "각진 사각형도 그냥 도형 취급하는
  게 낫다"고 해서 사각형을 끌면 둥글기와 무관하게 항상 도형이 되도록
  바꿨다. 아래 13e 참고.**
- **13d 완료 — 도형·체크박스 테두리가 인쇄에서 화면보다 훨씬 굵게
  나오던 버그.** 사용자가 실제로 인쇄해보고 발견했다("0.2mm인데 엄청
  굵게 나온다"). pdf-lib의 `drawSvgPath`는 `scale` 옵션으로 mm→pt를
  바꾸는데, **그 굵기(`borderWidth`)·점선(`borderDashArray`) 값도 이미
  scale이 걸린 좌표계 "안"에서 적용된다**(pdf-lib이 CTM에 scale을 먼저
  얹은 다음 그 위에서 선 굵기를 잰다). 그런데 `drawShapes`·
  `drawCheckboxes`가 굵기를 `mmToPt(strokeW)`로 미리 바꿔 넘기고
  있었다 — 그 값이 scale만큼 또 곱해져, 0.2mm를 요청하면 실제로는
  0.2 × scale(≈2.83) ≈ 0.57mm로 찍혔다(경로 좌표처럼 mm 값을 그대로
  넘겨야 scale이 딱 한 번만 먹는다). `borderWidth: strokeW`,
  `borderDashArray: dashPattern(...)`로 mm 그대로 넘기도록 고쳤다.

  **재발 방지로 회귀 테스트를 더했다.** `pdf/export.test.ts`에
  `contentStreamWidths(bytes)` 헬퍼를 만들어, 완성된 PDF의 압축된
  스트림을 직접 풀어(`zlib.inflateSync`) `w`(선 굵기) 연산자 값을
  꺼낸다 — glyph가 그 방향으로 눕는지는 인쇄해봐야 알지만, 굵기는
  그냥 숫자라 기계로 확인할 수 있다. 고친 코드를 잠깐 되돌려 이
  테스트가 실제로 실패하는지 확인한 뒤 다시 고쳤다.
- **13e 완료 — 그리기 도구로 끈 사각형은 둥글기와 무관하게 항상
  도형이다.** 13c에서는 둥글기가 0(각짐)이면 선 4개를 만들었는데,
  사용자가 써보고 "각진 사각형도 그냥 도형 취급해도 괜찮다"고 판단해서
  바꿨다 — 선 4개로 남으면 나중에 그 사각형을 골라 둥글기를 바로 바꿀
  방법이 없기(따로따로인 선 넷일 뿐이라) 때문이다. `EditorTab.tsx`의
  그리기 도구 onUp에서 `if (drawStyle.roundness) 도형 else 선 4개`이던
  분기를 없애고, 사각형으로 끌면(클릭해서 잇는 선과는 여전히 다르다)
  항상 `commitShape`를 부른다. 속성 막대의 둥글기 드롭다운은 이제
  "다음에 끌 도형의 기본 둥글기"를 미리 정해두는 자리로 뜻이 바뀌었다
  — 이미 만든 도형의 둥글기는 그걸 고르면 뜨는 `ShapeControls`에서
  바꾼다.

---

### 14단계 — 로고 정렬선 다듬기

설계문서 9단계(도형·체크박스·양면 인쇄·아이콘)까지 원래 계획했던 건
전부 끝났다. 이제부터는 실제로 써보면서 나오는 버그를 잡는 단계다.

- **14a 완료 — "로고 칸" 버튼(깜빡임)을 없앴다.** 사용자 요청. 버튼과
  `logoLineEmphasis` 상태, 5초 타이머, CSS 펄스 애니메이션
  (`.logo-line-emphasis`)을 전부 지웠다. `PunchGuide`의 `emphasizeLine`
  prop도 같이 없앴다 — 이제 정렬선은 늘 같은 옅은 점선이다.

- **14b 완료 — 로고 정렬선 근처에서 "다른 곳에 놓아도 도트 중앙에
  붙어버리는" 버그.** 원인은 정렬(`centerOnLogoLine`)이 텍스트·이미지를
  **만드는 순간**(드래그로 상자를 막 그렸을 때)에도 걸려 있었던 것이다.
  이 경로는 12h에서 이미 한 번 문제를 일으킨 자리다 — 만드는 순간
  맞춘 가운데가 타이핑하며 상자가 자라면(`growBox`) 다시 어긋난다.
  12h는 이걸 "5초 창" 뒤에 숨기는 방식으로 임시 봉합했는데, 만드는
  경로 자체에 여전히 정렬 로직이 남아 있다 보니(단지 조건부로 꺼져
  있었을 뿐), 어떤 상태에서 켜졌는지 알기 어려운 채로 남아 있었다 —
  사용자가 보고한 "엉뚱한 자리에 붙는" 증상이 정확히 이 경로에서
  나왔을 것으로 보인다.

  **고친 방법은 원인을 없애는 것이었다 — 만드는 순간의 정렬을 아예
  뺐다.** 이제 텍스트·이미지는 만들 때는 평범하게 도트 격자에만
  붙고, **딱 하나 고른 것을 옮길 때만** 정렬선 근처로 오면 가운데가
  달라붙는다(`onUp`의 `move` 처리, 이제 버튼·타이머 없이 항상 켜져
  있다). 이 경로는 이미 다 만들어진(타이핑까지 끝낸) 상자를 다루므로
  `growBox`와 부딪힐 일이 없다 — 12h가 막으려던 문제 자체가 애초에
  생기지 않는 자리다. 결과적으로 사용법이 더 단순해졌다: 아무 데나
  텍스트·이미지를 만들고, 정렬선 쪽으로 끌어다 놓으면 저절로 맞는다
  — 버튼을 누를 필요가 없다.

  **다시 확인해보니 "이전 오류"는 따로 없었다.** 14a/14b를 고친 뒤에도
  사용자가 같은 증상을 다시 보고했는데, 다시 살펴보니 **회전(90·270도)한
  텍스트가 도트 사이 칸이 아니라 도트 가운데를 걸치고 도는 것**을 보고
  버그로 느낀 것이었다 — 이건 회전이 상자 **가운데**를 축으로 돌기
  때문에 나오는 정상 동작이다(12a부터의 설계 그대로). `centerOnLogoLine`
  경로와는 무관했다.

- **14c 완료 — 표의 테두리도 도형으로 만들어 모서리를 둥글게 할 수
  있다.** 사용자 요청. `core/grid.ts`에 `tableSplit(lattice, a, b)`을
  더했다 — `tableLines`가 돌려주던 선 뭉치를 **테두리**(도형 하나가
  되는 상자)와 **안쪽 칸 선**(그대로 `LineObject`)으로 나눈다. 표를
  그으면 `commitShape(split.border)` + `drawLines(split.innerLines)`가
  된다 — 안쪽 칸 선은 둥글게 할 수 없다(선일 뿐이니까), 테두리만
  `ShapeControls`에서 둥글기를 고를 수 있다. 한 줄로만 끌었거나(테두리
  조차 없다) 한 점이면 `tableSplit`이 `null`을 돌려주고, 그때는
  `tableLines` 그대로 선 하나로 처리한다(도형이 될 상자 자체가 없다).

  속성 막대의 둥글기 드롭다운(13e에서 그리기 도구에 더했던 것)을 표
  도구에서도 보이게 넓혔다 — 표도 도형처럼 "다음에 그을 표 테두리의
  둥글기"를 미리 고를 수 있다.

---

### 15단계 — 글꼴·이미지를 브라우저에 남기기

사용자 요청("어떤 파일에 넣어놓고 걔를 항상 인식하게 할 수는 없나?").
지금까지는 fonts/registry·images/registry가 파일 바이트를 **이번
세션에서만** 메모리에 들고 있어서, 새로고침하거나 저장 파일을 다시
열면 같은 글꼴·이미지 파일을 매번 다시 골라야 했다. "확정되지 않은
채로 둔 것"에 미뤄뒀던 항목이다.

- **서버는 필요 없다.** 브라우저의 **IndexedDB**에 파일 바이트를
  캐싱해두는 것으로 충분하다 — 계정·업로드·인프라 없이 그 컴퓨터
  안에서만 기억한다.

- **`storage/idb.ts`를 새로 만들었다.** `idbPut`·`idbEntries` 둘뿐인
  아주 얇은 창구다. **실패를 전부 조용히 삼킨다** — 사생활 보호
  모드거나 용량이 찼거나 `indexedDB` 자체가 없어도(테스트 환경 등)
  앱이 멈추면 안 된다. 캐싱은 있으면 좋고 없어도 그만인 기능이라,
  이 파일 하나로 위험을 다 가둬뒀다.

- **등록할 때 같이 저장하고, 앱을 열 때 되살린다.** `registerFont`·
  `registerImage`는 성공하면(화면에 심는 것까지 마치면) 파일 이름을
  키로 IndexedDB에도 같이 쓴다. `restoreCachedFonts`·`restoreCachedImages`
  (새로 만든 함수)가 그 자리를 반대로 훑어서, 등록 없이 조용히
  되살린다 — `ui/App.tsx`가 마운트될 때 한 번 부르고, 결과를 store의
  `addUserFont`·`addUserImage`에 넣는다. **파일 선택 창이 아예 뜨지
  않는다** — 이름이 같은 파일이 캐시에 있으면 그걸로 끝이다.

  등록과 복원이 "이름·바이트로 실제 심기"까지는 완전히 같은 코드를
  쓴다(`loadFont`·`loadImage`로 뽑아냈다) — 등록은 `File`에서 바이트를
  읽어 이 함수를 부르고, 복원은 캐시에서 읽은 바이트를 그대로 넘긴다.

- **키는 파일 이름이다.** 저장 파일(.json)이 글꼴·이미지를 이름으로만
  기억하는 것과 같은 규칙이라 자연스럽게 이어진다. 다만 **이름이 같은
  서로 다른 파일이면 헷갈릴 수 있다** — 흔치 않을 것으로 보이지만
  확정하지 않은 채로 남겨둔다.

- **이번 세션에 두 번 되살리지 않는다.** `restoredNames` Set으로
  막았다 — React StrictMode(개발 모드)가 마운트 이펙트를 두 번 부르면
  같은 이름의 글꼴이 서로 다른 id로 두 번 등록될 뻔했다.

- 저장 파일(.json)에는 여전히 이름만 남는다 — 이 캐싱은 **그 브라우저
  안에서만** 유효하다. 다른 컴퓨터에서 저장 파일을 열면 지금처럼 다시
  등록해야 한다(사용자가 원한 것도 "이 컴퓨터에서 매번 다시 안 하게"였다).

---

### 16단계 — 도구 막대가 좁아지면 두 줄로

사용자 요청. 양식 만들기 화면을 좁히면 "지우기"·"잠그기" 같은 버튼이
줄어들다 못해 글자가 한 자씩 줄바꿈되어 세로로 보였다 — `.editor-bar`가
`flex-wrap` 없이 한 줄이라, 자리가 모자라면 버튼 하나하나가 내용보다
좁게 눌렸기 때문이다.

- **도구·지우기·잠그기·undo/redo·확대**를 `.editor-bar-tools`로, **속성
  막대(또는 안내 문구)**를 `.editor-bar-style`로 묶었다. 바깥
  `.editor-bar`만 `flex-wrap: wrap`이고, `.editor-bar-tools` 안의 것들은
  전부 `flex-shrink: 0`(+ `white-space: nowrap`)이라 줄어들 수 없다 —
  그래서 자리가 모자라면 개별 버튼이 눌리는 대신, 폭이 가변적인 속성
  막대 **전체가 통째로** 다음 줄로 내려간다.
- 넓을 때는 지금처럼 한 줄 그대로다 — `flex-wrap`은 실제로 자리가
  모자랄 때만 접는다. 아주 좁은 화면 대비로 `.editor-bar-tools` 자체도
  한 번 더 접히게(`flex-wrap`) 해뒀다.

---

### 17단계 — 도구 막대·달력·텍스트·이미지 한꺼번에 다듬기

사용자가 한 번에 여섯 가지를 요청했다. 하나씩 나눠 만들고 커밋한다.

- **17a 완료 — 도구 순서·이름표, 도구 막대를 항상 두 줄로.**
  - **순서**: 선택 → 그리기(선) → 표 → 글자 → 달력 → 이미지 →
    체크박스 → 자동 필드. 자동 필드가 맨 뒤로 밀렸다.
  - **이름표를 동사 없이 짧게 바꿨다** — "그릴 표"·"쓸 글자"·"찍을
    체크박스" 같은 동사를 다 떼고 "표"·"글자"·"체크박스"로. 선택
    도구는 원래 문장형 안내만 있었는데, 사용자 요청대로 앞에 짧은
    이름을 붙여 "선택 | 클릭해서 고르고, 빈 곳에서 끌어 여러 개를
    감쌉니다" 형식으로 만들었다. **달력도 같은 형식으로 만들었는데
    ("달력 | 끌어서 놓으면 이번 달이 자동으로 채워집니다"), 정확히
    어떤 문구를 원하는지는 확인 중이다** — 사용자가 보고 고쳐달라고
    할 수 있다.
  - **항상 두 줄로 바꿨다.** 16단계에서는 "좁을 때만 접힌다"로
    만들었는데, 이번엔 "넓어도 항상 두 줄"로 바꿨다 — `.editor-bar`를
    `flex-direction: column`으로, 도구줄(`.editor-bar-tools`)과 속성
    막대줄(`.editor-bar-style`)을 항상 위아래로 둔다.

- **17b 완료 — 이미지 상자를 초록·파랑과 다른 색(보라)으로.** 사용자
  요청. 그동안 이미지의 선택 테두리는 다른 모든 상자와 같은
  초록(`.picked rect`), 드래그 중 미리보기는 글자·달력·체크박스와 같은
  파랑(`.text-drag`)을 그대로 썼다 — 그래서 어느 도구를 쓰는 중인지
  색만 보고 구분이 안 됐다. `core/style.ts`에 `IMAGE_SNAP_COLOR`(보라,
  `#8654c9`)를 더하고, 이미지일 때만 `.image-picked`·`.image-drag`
  클래스를 붙였다(`isImage(o)` 조건) — 초록(그리기)·파랑(글자)과
  겹치지 않는 세 번째 색이다.

- **17c 완료 — 달력 옵션 네 가지.** 사용자 요청.
  - **이전·다음 달 표시 기본 OFF.** `CALENDAR_SHOW_ADJACENT`를
    `true`→`false`로. 기존 테스트 하나가 옛 기본값을 검증하고
    있었어서 같이 고쳤다.
  - **이전·다음 달 날짜를 옅게.** 절대 색을 계산하는 대신(임의의
    글자 색을 밝게 만드는 계산은 색마다 다르게 튄다) **투명도**로
    옅게 했다 — `CALENDAR_ADJACENT_OPACITY`(0.35), 어떤 글자 색을
    골라도 똑같이 옅어 보인다. "이번 달인지"는 `calendarCellAt`이
    이미 한 번 계산하지만 밖으로 안 돌려주므로, `core/dataset.ts`에
    같은 셈을 하는 `isInMonth`를 새로 만들어 화면·PDF가 같이 쓴다.
  - **글자 색·크기 옵션 추가.** 색은 원래 `CalendarObject.color`에
    이미 있던 값인데 속성 막대에 드롭다운이 없었을 뿐이라 그냥
    붙였다. 크기는 **절대값이 아니라 배율**(`sizeScale`, 80~130%)로
    뒀다 — 달력 글자 크기는 원래 상자 높이에서 자동으로 정해지므로
    (`calendarLayout`), 절대값을 따로 두면 상자 크기를 바꿀 때마다
    다시 어긋난다. 배율은 그 자동값에 곱해질 뿐이라 상자를 늘리고
    줄여도 여전히 맞는다.
  - **영어 요일에 "MTWTFSS"(한 글자씩) 옵션 추가.** `WeekdayLang`에
    `'en-short'`를 더했다. 기준 배열은 일요일부터
    `['S','M','T','W','T','F','S']`로 저장해두고, 월요일 시작으로
    바꾸면 사용자가 든 예시 그대로 "MTWTFSS"가 나온다 — 화·목이
    둘 다 T, 토·일이 둘 다 S로 겹치는 건 이 표기의 흔한 관례다.

- **17d 완료 — 글자 입력 중 Enter는 그냥 줄바꿈.** 사용자 요청. 전에는
  Enter가 확정, ⇧Enter가 줄바꿈이었는데 뒤집었다 — Enter는 textarea
  기본 동작(줄바꿈) 그대로 두고, 확정은 Esc나 바깥으로 포커스가 나갈
  때(onBlur)뿐이다. 관련 주석·속성 막대의 줄 간격 툴팁도 같이 고쳤다.

- **17e 완료 — 글자 상자도 손잡이로 크기를 조절할 수 있다. 상자에
  맞춰 글자 크기도 같이 바뀐다.** 사용자 요청 세 개(상자 크기조절,
  타이핑 중 자동으로 줄어들기, 조절하면 글자 크기도 비례)를 한 번에
  다룬다.

  **타이핑 중 자동 맞춤이 이제 줄기도 한다.** `core/objects.ts`의
  `growBox`(커지기만 했다)를 `fitBox`로 바꿨다 — 글자를 지우면 상자도
  같이 준다. 왼쪽 위가 고정이고 오른쪽·아래쪽으로만 늘거나 주는 규칙은
  그대로다.

  **글자도 `isBoxResizable`에 들어가 모서리 손잡이가 생겼다.** '선택'
  도구로 고르면(다른 상자형 오브젝트와 같은 방법) 달력·이미지·도형처럼
  손잡이로 크기를 바꿀 수 있다. '글자' 도구가 켜져 있을 때는 여전히
  클릭하면 바로 고쳐 쓰기로 들어간다(안 바꿨다) — 손잡이로 조절하려면
  '선택' 도구로 바꾼다, 다른 오브젝트들과 같은 규칙이다.

  **세로(높이) 비율로 글자 크기가 같이 바뀐다** — 달력이 상자 높이로
  글자 크기를 정하는 것과 같은 생각이라 일관된다. 가로만 늘리거나
  줄이면 글자 크기는 그대로고 여백만 바뀐다(자동 줄바꿈이 없어 줄
  수가 안 바뀐다). `ui/EditorTab.tsx`의 `resizeTextBox`가 계산한다.

  **최소 크기는 "지금 글자가 실제로 필요로 하는 크기"다.** 임의의
  숫자가 아니라, 옮기기 전 크기에서 `measureTextBox`로 잰 값을 새
  비율만큼 그대로 늘려 쓴다 — 다시 잴 필요가 없다(자동 줄바꿈이 없어
  줄 수가 안 바뀌므로 필요한 크기는 글자 크기에 정확히 비례한다).
  세로는 이 조건이 수학적으로 항상 저절로 만족된다(상자와 글자
  크기가 같은 비율로 줄기 때문에, 원래 상자가 글자에 맞았다면 줄인
  뒤에도 여전히 맞는다) — 그래도 안전망 삼아 코드에 남겨뒀다. 가로는
  실제로 막아야 한다 — 가로만 세게 줄이면 그 비율에서는 넘칠 수 있다.

  **되돌리기(undo) 한 번에 묶인다.** 상자와 글자 크기를 따로 커밋하면
  되돌리기를 두 번 눌러야 한 번의 조절이 없어진다. `resizeObject`
  액션에 `extra`(글자 전용, `size`·`lineHeight`)를 더해 한 번의
  커밋으로 같이 넣는다.

- **17f 완료 — 사용자가 써보고 찾은 버그 세 개.**

  **표를 다 그리고 고치려 하면 테두리만 진해 보이던 문제.** 원인은
  13c/17이 표를 테두리(도형, `commitShape`가 곧바로 고른 상태로
  둔다)와 안쪽 칸 선(그냥 `LineObject`, 고르지 않는다)으로 나눈 것과,
  달력·이미지처럼 "만들자마자 고른 상태로" 두는 규칙이 부딪힌
  결과였다 — 테두리만 고른 상태가 되니 초록 선택 테두리가 겉에만
  둘러져 안쪽 칸 선과 다르게(진하게) 보였다. `commitShape` + `drawLines`
  두 번 부르던 것을 새 액션 `commitTable`(테두리 + 안쪽 선을 한 번에
  만들고, **둘 다 함께 고른다**)로 합쳤다 — 되돌리기도 한 번으로
  묶인다.

  **도형 속성 막대의 항목 순서가 대기 화면과 고른 뒤가 서로 달랐다.**
  `ShapeControls`(고른 뒤)는 둥글기·굵기·색·모양 순서였는데, 그리기
  도구의 대기 화면은 굵기·색·모양·둥글기(둥글기가 나중에 덧붙여진
  탓이다) 순서였다 — 같은 도형인데 언제 보느냐에 따라 막대가 다르게
  보였다. `ShapeControls`도 굵기·색·모양·둥글기 순서로 맞췄다.

  **글자 상자를 손잡이로 줄이면 상자가 밀려나며 글자가 깨지던 버그.**
  `resizeTextBox`가 최소 크기(지금 글자가 필요로 하는 크기)에 걸려
  상자를 raw보다 키울 때, 폭·높이만 늘리고 `x`·`y`는 그대로 뒀다 —
  `resizeBox`는 반대쪽(고정된) 모서리를 기준으로 자라는데, 여기서
  그 규칙을 따라가지 않아서 고정돼야 할 모서리가 밀렸다. 끄는 손잡이가
  서쪽(`w`)을 포함하면(왼쪽 손잡이, 오른쪽이 고정) 늘어난 만큼 `x`를
  왼쪽으로, 북쪽(`n`)을 포함하면(위쪽 손잡이, 아래쪽이 고정) 늘어난
  만큼 `y`를 위로 보정하도록 고쳤다.

  **속성 막대 줄 높이도 다듬었다.** 드롭다운이 있을 때와 글자만 있을
  때(선택·달력의 "이름 | 설명" 문구 등) 줄 높이가 달라 도구 막대가
  들쭉날쭉해 보였다 — `.editor-hint`·`.picked-count`에 `select`와
  같은 padding·투명 테두리를 줘서 상자 모양(높이)을 맞췄다.

- **17g 완료 — 이미지도 상자부터 그리고, 그 상자를 클릭하면 사진을
  고른다.** 사용자 요청 — 사진을 먼저 골라야 상자를 그릴 수 있던
  순서를 뒤집어, 달력·도형처럼 자리부터 그리게 했다.

  **`ImageObject.imageId`를 선택값으로 바꿨다**(`imageId?: string`).
  상자를 그리면(`commitImage`) 사진 없이 빈 채로 만들어지고 곧바로
  고른 상태가 된다(달력과 같은 이유). 화면(`.image-missing` 점선
  자리표시)·PDF(빈 상자는 건너뛴다) 둘 다 "저장 파일에서 이름만 살아
  돌아온 이미지"와 같은 자리로 다룬다 — 새 상태 하나 늘리는 대신
  이미 있던 처리를 그대로 썼다.

  **`draftImageId`(도구가 다음에 놓을 이미지를 미리 잡아두던 상태)를
  전부 없앴다.** 이제 그릴 때 미리 고를 게 없다 — 상자부터 그린다.

  **빈 이미지 상자 하나만 고른 상태가 되면 파일 선택 창이 스스로
  열린다.** `ui/StyleBar.tsx`의 `ImageControls`에 `useEffect`를
  더해서, "빈 이미지 하나만 선택됨"을 감지하면 숨겨둔
  `<input type=file>`을 코드로 클릭한다 — 상자를 그리자마자
  곧바로 고른 상태가 되므로(`commitImage`), 그리는 즉시 창이 뜨는
  것처럼 보인다. 나중에 빈 상자를 다시 클릭해도 같은 일이 일어난다.

- **17h 완료 — 17f/17g를 써보고 나온 후속 요청 네 가지.**

  **속성 막대 너비.** 선택·달력처럼 글자 하나만 뜨는 경우, 드롭다운이
  늘어선 다른 도구 줄과 나란히 봐도 좁아 보이지 않게
  `.editor-hint`·`.picked-count.muted-label`에 `min-width: 420px`를
  줬다(17f는 높이만 맞췄었다).

  **표를 고치면 테두리·안쪽 칸 선이 같이 바뀐다.** 표를 그으면
  테두리(도형)+안쪽 칸 선(여러 `LineObject`)이 함께 골라지긴
  했지만(17f), 속성 막대의 `showShape` 조건이 `pickedLines.length===0`을
  요구해서 표를 고르면 `LineControls`로 빠져 **안쪽 칸 선만** 바뀌고
  테두리는 그대로였다. 새 `TableControls`를 만들어 굵기·색·모양은
  `styleShape`+`styleSelected`를 함께 불러 테두리·안쪽 선 둘 다에게,
  둥글기는 `styleShape`만(안쪽 칸 선은 둥글게 할 수 없다) 적용한다.
  `showTable`(도형과 선을 함께 골랐을 때)이 `showShape`보다 먼저
  가로챈다.

  **빈 이미지 상자 — 화면엔 회색, 인쇄엔 없음, 크로스헤어 커서.**
  `.image-missing`에 `fill: #9e9e9e`(투명도 0.15)를 줘서 화면에서
  회색으로 채워 보이게 했다 — 인쇄(`pdf/export.ts`의 `drawImages`)는
  원래부터 `imageId`가 없으면 아예 안 그리므로 손댈 게 없었다.
  이미지·체크박스 도구에 크로스헤어 커서(`.insert-sheet.image`,
  `.insert-sheet.checkbox`)를 더했다(그리기·표·자동 필드와 같다).

  **빈 이미지 상자를 나중에 다시 클릭해도 파일 창이 다시 뜨도록
  고쳤다.** 17g의 `useEffect`는 "선택된 이미지의 id"만 지켜봤는데,
  이미 선택된 상자를 다시 클릭해도 id 값이 그대로면 리액트가 이펙트를
  다시 부르지 않는다 — 눌러도 안 열리는 경우가 있었다. store에
  `pickImageFor`(신호)를 새로 두고, `commitImage`(막 그렸을 때)와
  `EditorTab.tsx`의 `onDown`(이미지 도구로 빈 상자를 클릭했을 때) 둘
  다 클릭마다 새 값을 써넣는다 — `ImageControls`가 그 신호를 보고 열고
  나면 곧바로 `null`로 되돌려서, 다음 클릭에서도 다시 켜진다.

  **자동 필드 자리표시에 서식 갈래 글자를 붙였다.** 오프셋만
  보이면(`⟨+0⟩`) 같은 쪽에 월·주·일·요일 필드를 함께 놓았을 때
  전부 같은 숫자라 구분이 안 됐다. `core/text.ts`에 `fieldCategory`를
  더해 서식(id)을 네 갈래로 나눈다 — 날짜 하나를 통째로 가리키는
  것(D·M/D·M월 D일·YYYY-MM-DD)은 전부 "일"(D)로 묶는다(연속한
  날마다 값이 바뀌는 정도가 같다), 요일 서식은 DW, 월 서식은 M, 주
  서식은 W. 자리표시가 `⟨+D0⟩`·`⟨+M0⟩`·`⟨+W0⟩`·`⟨+DW0⟩`처럼 나온다.

- **17i 완료 — 17h를 써보고 나온 후속 요청 두 가지.**

  **속성 막대 너비 — 17h가 새로 만든 문제.** `.picked-count.muted-label`에
  둔 `min-width:420px`가 글자·표처럼 조작칸(`TextControls` 등)이
  뒤따르는 경우에도 걸려서, 그 조작칸들이 밀려나 화면 밖으로
  나가버렸다(스크린샷으로 확인). `:last-child`로 좁혔다 — 조작칸이
  없어 이 글자 하나만 뜰 때만(달력이 아직 안 그려졌을 때) 최소 폭이
  걸린다.

  **이미지 상자 — 그리자마자 열리던 파일 창을 더블클릭으로 옮겼다.**
  여러 개를 잇달아 그릴 때 매번 창이 뜨는 게 방해가 된다는 사용자
  피드백. `commitImage`에서 `pickImageFor` 세팅을 뺐다 — 그리기만 하면
  빈 채로 남는다. 대신 캔버스의 기존 `onDoubleClick`(더블클릭하면
  글자를 쓰는 핸들러)에 분기를 하나 더했다 — 더블클릭한 게 빈
  이미지면 글자 도구로 안 바뀌고 `pickImageFor`를 세팅해 파일 창을
  연다. `.image-missing`(빈 이미지 자리)의 테두리를 점선·회색에서
  실선·보라(`IMAGE_SNAP_COLOR`)로 고정했다 — 사용자가 고르는 옵션이
  아니라 "빈 이미지 자리"라는 뜻만 늘 같은 모양으로 보여준다.

- **17j 완료 — 자동 필드를 글자 중간에 손으로 쳐서 만들기 + 찍기 전
  서식 미리 고르기.**

  **글자 안에 자리표시 패턴이 있으면 그 자리만 필드로 바꾼다.**
  `"⟨+D0⟩입니다"`처럼 쳐도 `⟨+D0⟩` 부분만 인식되고 "입니다"는 그대로
  남는다. `core/text.ts`에 `parseFieldText`(패턴을 찾아 `{offset,
  format}`으로 되돌린다, `<+D0>`·`⟨+D0⟩` 둘 다 받는다)를 더했다 —
  갈래 글자(D)만으로는 서식을 하나로 정할 수 없어서(D 갈래에 서식이
  넷이다) `CATEGORY_DEFAULT_FORMAT`에서 무난한 것 하나를 기본으로
  쓴다(정확한 서식은 만든 뒤 속성 막대에서 고른다).

  **`displayText`·`resolveObjectsForPage`를 "패턴이 있으면 그 자리만,
  없으면 통째로" 규칙으로 다시 짰다.** 자동 필드를 처음 만드는 방법이
  둘이다 — (1) 자동 필드 도구로 찍으면 `text`가 자리표시 그 자체라
  패턴이 있고, (2) 속성 막대의 "자동 필드" 체크박스로 이미 있는 보통
  글자를 필드로 켜면 `text`에 패턴이 없다(원래 글자에 그런 표시가
  있을 리 없다). 처음엔 항상 "패턴 찾아 바꿔치기"로만 짰다가 (2)의
  경우 아무것도 안 바뀌는 회귀가 생겨서(기존 테스트가 잡아냈다),
  패턴이 없으면 통째로 바꾸는 옛 규칙으로 되돌아가는 예외를 뒀다.

  **편집을 열었다가 그대로 닫으면 서식이 안 바뀐다.** `ui/gestures.ts`의
  `Editing`에 `field`·`originalText`(편집을 시작할 때의 글자, `displayText`로
  다시 그린 것)를 더했다. `finishEditing`은 `text`가 `originalText`와
  같으면(아무것도 안 고쳤으면) 있던 `field`를 그대로 두고, 달라졌으면만
  `parseFieldText`로 새로 인식한다 — 안 그러면 자동 필드를 열어보기만
  해도 사용자가 따로 골라둔 서식(예: `YYYY-MM-DD`)이 매번 갈래
  기본값(`M/D`)으로 되돌아간다.

  **자동 필드 도구에 "다음에 찍을 서식" 선택칸을 더했다.** 지금까지는
  찍고 나서(선택한 뒤) `FieldControls`에서만 서식을 바꿀 수 있었다 —
  찍기 전에는 고를 자리가 없었다. store에 `fieldDraftFormat`(drawStyle의
  자동 필드 판)을 더해서, 도구만 켜고 아직 안 찍었을 때도 서식을
  미리 고를 수 있다. 찍을 때 이 값을 쓴다.

- **17k 완료(1·2단계) — 뒷면=앞면 복사, 객체 복사·붙여넣기.** 앞뒤를 한
  화면에서 편집하는 3단계(17l 예정) 전에 먼저 끝낸 두 작은 기능.

  **뒷면을 앞면과 똑같이 만들기.** `core/template.ts`에 `backFromFront(t)`를
  더했다 — `duplicateTemplate`이 뒷면을 물려받을 때와 같은 방식(격자·그린
  것을 복사하고 실행취소 이력은 새로 시작)이라 그대로 재사용했다. store의
  `copyFrontToBack`은 뒷면이 없으면 만들고 있으면 덮어쓴다 — `removeBack`과
  같이 확인창 없이 바로 하고 ⌘Z로 되돌릴 수 있다. `SideBar`에 "앞면 그대로
  복사" 버튼을 뒷면 탭에서만 보이게 넣었다.

  **객체 복사·붙여넣기(⌘C/⌘V).** OS 클립보드가 아니라 앱 안에서만 도는
  store의 `clipboard: DiaryObject[]`다 — 저장 파일에는 안 들어간다.
  `core/objects.ts`에 `cloneObject(o, dx, dy)`를 더했다 — 새 id를
  종류별 접두어(`newId`가 쓰는 것과 같은 규칙, 예: 선은 `l`, 도형은 `sh`)로
  매기고 기존 `moveObject`로 옮긴다. 붙여넣기는 5mm 오프셋해서 원본과
  겹치지 않게 하고, 붙여넣은 것을 선택 상태로 만든다. 키보드 처리는
  `EditorTab`의 기존 `onKey`에서 ⌘Z 바로 다음, 도구 단축키를 막는
  `if (e.metaKey...) return`(그 자리 주석에 적힌 대로 지금까지 ⌘V를 조용히
  삼키고 있었다)보다 앞에 뒀다.

- **17l 완료 — 앞뒤를 한 화면에서 나란히 편집(뒷면 왼쪽·앞면 오른쪽), 선은
  경계를 넘어 이어 그을 수 있다.**

  코드 짜기 전에 계획 모드로 설계를 정리하고 사용자 승인을 받았다
  (`/Users/jiing/.claude/plans/silly-gliding-llama.md`). 처음 짠 설계는
  캔버스 하나를 `viewBox 0 0 (속지폭×2) 높이`로 합쳐 `<g transform>`으로
  양쪽을 나누는 방식이었는데, 구현하다 보니 **svg 두 개를 그냥 나란히
  두는 쪽이 훨씬 간단하다는 걸 알게 됐다** — `getScreenCTM()`이 화면
  좌표를 그 svg 자신의 로컬 좌표로 바꾸는 계산이라, 활성 쪽 svg의 좌표계는
  지금까지 코드가 이미 가정해온 "쪽 하나, 로컬 좌표 0~속지폭"과 똑같이
  둘 수 있었다. 그래서 최종 구조는:

  - 활성 쪽(지금 그리기·고르기가 실제로 향하는 쪽) — **지금까지의 `<svg>`를
    한 글자도 안 고치고 그대로 쓴다.** `onDown`·`onMove`·`onUp`·`hitAt`
    전부 무손. `rawMm`도 그대로다.
  - 비활성 쪽 — 별도의, 정적으로만 그리는(손댈 수 없는) 작은 `<svg>` 하나
    (`renderInactiveCanvas`). `useObjects`·`useDotGrid`에 선택적 `side`
    인자를 더해(`s.side`와 무관하게 특정 쪽을 읽는다) 이 쪽의 그린 것·격자를
    가져온다. 클릭하면(`onInactivePointerDown`) 초점이 옮겨진다(오늘까지의
    탭 전환과 같은 뜻) — `switchSide`가 `pending`·드래그·편집 중이던 글자를
    정리하고 `setSide`한다.
  - `.editor-canvas`가 이미 `display:flex`라 svg 두 개(또는 뒷면 자리표시 +
    앞면 svg)를 순서대로 넣으면 여백 없이 나란히 붙는다. 다만 겹치는 경우
    (아래) 활성 쪽이 위로 보여야 해서 `.active-canvas`/`.inactive-canvas`에
    z-index를 줬다 — 뒷면이 활성이면 DOM 순서상 왼쪽(먼저)이라 가만두면
    나중에 그려지는 오른쪽 정적 앞면에 덮인다.
  - 이 프로젝트는 편집 화면에서 속지 경계를 넘는 것도 일부러 그대로
    보여준다(`.insert-sheet { overflow: visible }`) — 그래서 여백 없이
    붙이기만 해도 활성 쪽에서 그리는 미리보기(예: 선을 잇기 전 대기 중인
    점에서 마우스까지의 점선)가 자연히 옆 캔버스 위로 흘러넘쳐 보인다,
    따로 처리할 필요가 없었다.

  **선이 경계를 넘으면 이어붙는다(요청한 유일한 "동시 조작").** 그리기
  도구로 점 하나를 찍어(`pending`) 대기 중일 때 비활성 쪽을 클릭하면,
  `completeCrossBoundaryLine`이 경계에서 만나는 반쪽 두 개로 쪼갠다.
  두 캔버스가 서로 다른 좌표계라 좌표를 직접 비교할 수 없어서, **각
  점에서 경계까지의 거리**로 비율을 구한다 — `core/objects.ts`의
  `splitAtBoundary`(순수 함수, 테스트 있음)가 이 계산을 맡는다. 반쪽마다
  `drawLines`를 따로 불러 각자의 History에 커밋한다 — "실행취소는
  앞뒤가 따로"라는 원칙 그대로, ⌘Z 한 번은 방금 그은 반쪽 하나만
  되돌린다. 다른 도구(도형·표·체크박스·글자·달력·이미지·자동필드)는
  경계를 넘는 특별 처리를 하지 않는다 — 시작한 쪽에 그대로 머문다.

  **버그 두 개를 구현 중에 미리 잡았다(사용자가 겪기 전에):**
  - 비활성 쪽 클릭이 `.editor-canvas`의 `onOutsideDown`(바깥 여백 마퀴
    시작)까지 거품처럼 올라가, select 도구일 때 활성 쪽 좌표계로 엉뚱한
    마퀴가 동시에 시작될 뻔했다 — `onInactivePointerDown`에
    `stopPropagation`을 추가했다.
  - 뒷면이 없을 때(`!hasBack`) "뒷면 만들기" 자리표시도 같은
    `.editor-canvas` 안에 들어오면서, select 도구로 그 버튼을 누르면 같은
    문제가 생겼다 — `onOutsideDown`에 `.back-empty`를 걸러내는 가드를
    추가했다.

  **SideBar의 탭(앞면/뒷면 전환 버튼)을 없앴다** — 캔버스를 직접 클릭하는
  게 곧 전환이라 더 이상 필요 없다. 대신 지금 활성 쪽을 알려주는 글자와,
  뒷면 쪽에서만 보이는 "앞면 그대로 복사"·"뒷면 지우기" 버튼만 남았다.

- **17m 완료 — 17l 사용자 피드백 세 개(경계 넘는 선 되돌리기, 마퀴 선
  선택, 경계에서 둘째 점이 격자에 안 붙던 것) 전부 고쳤다.**

  **경계를 넘어 이은 선이 되돌리기 하나로 묶인다.** 17l에서는 반쪽 둘을
  `drawLines`로 따로따로 커밋했는데, 그러면 History가 앞뒤로 완전히
  갈라져 있어서(설계문서 원칙) ⌘Z 한 번에 반쪽 하나만 없어졌다 — 사용자
  피드백: "아예 하나의 선으로 인식되게 해줘." store에 전용 액션
  `commitCrossBoundaryLine(backSeg, frontSeg, focusSide)`을 새로 만들어
  반쪽 둘을 **한 액션 안에서** 커밋하고, 만들어진 두 id를
  `crossBoundaryPairs`(세션 한정, 저장 파일에 안 들어감)에 짝지어 기억해둔다.
  `undo`·`redo`는 이제 되돌리기 전에 "지금 지우려는/살리려는 객체 하나가
  등록된 짝이고, 반대쪽도 지금 그 짝을 지우거나/살리는 차례인가"를 먼저
  본다 — 맞으면 앞뒤를 함께 되돌리고, 아니면(짝이 아니거나 중간에 다른
  일이 끼어 반대쪽이 이미 어긋났으면) 지금처럼 그 쪽만 되돌린다. 그래서
  "앞뒤가 따로"라는 원칙은 평소엔 그대로고, 짝지어진 선일 때만 예외가
  적용된다(store.test.ts에 짝 사이에 다른 그리기가 끼어도 제 차례엔 여전히
  같이 묶이는 것까지 테스트해뒀다).

  **마퀴로 감쌀 때 선은 걸치기만 해도 골라진다.** 지금까진 `segmentInRect`가
  "양 끝이 다 안에 있어야" 골랐다(의도한 설계였지만, 사용자 피드백으로
  뒤집었다: "드래그했을 때 조금이라도 겹쳐지는 선이 있으면 선택되게 해줘").
  `core/objects.ts`에 일반적인 선분-선분 교차 판정(`segmentsCross`)을
  더해서, 끝점 하나가 안에 있거나 사각형을 그냥 관통해도(양 끝 다 밖이어도)
  골라지게 했다. 도형·글자·달력·이미지·체크박스는 그대로 완전히 들어와야
  골라진다 — 사용자가 요청한 건 선뿐이었다.

  **셋째(경계 지점에서 앞뒤 그려지는 위치가 조금 다르게 보인다는 것)도
  마저 고쳤다.** 처음엔 안전영역·구멍 안내선 얘기인 줄 알았는데(그건
  의도한 대칭이다), 물어보니 사용자가 그린 선 자체가 오른쪽으로 갈수록
  격자에서 살짝 벗어나 보인다는 뜻이었다. 원인은 `onInactivePointerDown`
  — 활성 쪽에서 선을 그을 때(`onDown`)는 클릭 자리를 항상
  `snapToLattice`로 격자에 앉히는데, 비활성 쪽(경계를 넘는 선의 둘째
  점)을 짤 때 이 스냅을 빠뜨려서 클릭한 픽셀 자리 그대로 박혔다 —
  왼쪽(첫 점, 스냅됨)은 점에 딱 붙고 오른쪽(둘째 점, 스냅 안 됨)은
  안 붙어서 기울어 보였다. 비활성 쪽의 격자(`inactiveLattice`, 새로
  계산해 더함)로 스냅하도록 고쳤다 — 이제 두 점 다 격자에 앉는다.

- **17n 완료 — 17m 후속 피드백 세 개.**

  **경계를 넘어 잇는 선의 미리보기가 비활성 쪽에서 안 보이던 버그를
  고쳤다.** 첫 점을 찍고 마우스를 비활성 쪽으로 옮기면, 활성 쪽
  `<svg>`의 `onPointerLeave`가 그 순간 `hover`를 `null`로 지워버려서
  (따로 있는 두 svg 사이를 넘어가는 거라 `pointerleave`가 진짜로
  뜬다) 잇는 선 미리보기가 사라졌다. 비활성 쪽 svg에
  `onInactivePointerMove`를 새로 달아, 그리기 도구로 pending 중일
  때만 비활성 쪽 로컬 좌표를 "경계까지 거리"를 거쳐 활성 쪽 좌표계로
  연장한 값(`insert.width`를 넘거나 0보다 작아짐)으로 `hover`를
  다시 채운다 — completeCrossBoundaryLine과 같은 변환이다. 다른
  때는(도구가 다르거나 pending이 없으면) 손대지 않는다.

  **도트 격자가 앞뒤 공유값이 됐다.** 지금까진 `BackPage`가 자기
  `dotGrid`를 따로 가져서(뒷면을 줄노트로, 앞면을 도트로 두는 식이
  의도적으로 가능했다) 어느 한쪽에서 격자를 고쳐도 반대쪽은 안
  바뀌었는데, 사용자 피드백으로 뒤집었다: "도트 격자 같은 속지 내용을
  수정하면 양쪽이 다 변하게끔 해줘." 속지 크기·구멍처럼 같은 종이 한
  장의 물리적 특성이라는 게 이유다.
  - `BackPage`에서 `dotGrid` 필드를 뺐다 — 이제 `Template.dotGrid`
    하나뿐이다. `patchDotGrid`는 `patchActiveSide`(쪽마다 다른 것)
    대신 `patchActive`(양식 전체, 쪽 무관)를 타도록 바꿨다.
  - `useDotGrid()`가 `side` 인자를 받지 않게 됐다(양쪽이 같은 값이라
    구분할 이유가 없다) — `EditorTab`에서 `inactiveGrid`가 사라지고
    `grid` 하나를 활성·비활성 렌더링 둘 다에 쓴다(격자 배치 자체는
    `mirror` 플래그가 쪽마다 갈라서 `lattice`·`inactiveLattice`는
    여전히 따로 계산한다 — 안전영역 회피 위치만 다르다).
  - 저장 파일 하위호환: 옛 파일(뒷면 격자가 따로였던 판)에
    `back.dotGrid`가 남아 있어도 `toTemplates`가 그 필드를 읽지
    않는다 — 조용히 앞면 값(공유값)으로 통일된다. 테스트로 확인해뒀다.
  - `backFromFront`(뒷면=앞면 복사)도 이제 그린 것만 옮긴다 — 격자는
    이미 공유값이라 옮길 게 없다.

  **마퀴로 선 고르기(걸치면 골라지는 것)는 다시 확인했지만 코드
  단에서 추가로 찾은 문제는 없었다** — `segmentInRect`·`segmentsCross`
  둘 다 기존 테스트가 여전히 통과하고, 수직선·중간 토막만 걸치는 경우까지
  다시 손으로 검산해봐도 맞았다. 이번에 코드를 더 건드리진 않았다 —
  브라우저를 새로고침한 뒤에도 여전히 안 되면 어떤 선을 어떻게
  드래그했는지 알려달라고 부탁하기로 함.

- **17o 완료 — 마퀴 고르기를 도형에도 확장, 뒷면 만들기를 툴바에,
  속성 막대 줄 높이 고정.**

  **17n에서 "여전히 안 됨"으로 넘어간 마퀴 선택 — 원인은 선이 아니라
  도형이었다.** 스크린샷으로 보니 사각형(도형) 한쪽만 감싸는 드래그로,
  도형은 애초에 "완전히 들어와야 골라진다"는 **의도한 설계 그대로**
  작동한 것뿐이었다(선만 걸치면 되게 고쳤었지 도형까지는 아니었다).
  사용자 피드백으로 이 구분을 없앴다 — `objectInRect`가 도형·글자·달력·
  이미지·체크박스도 이제 선처럼 조금만 걸쳐도 고른다(사각형-사각형
  겹침 판정, `core/objects.test.ts`에 테스트 추가). "큰 상자 한쪽
  귀퉁이만 스쳐도 딸려오면 놀란다"던 예전 설계 의도보다, 실제로 쓸 때
  선택이 잘 되는 쪽을 사용자가 골랐다.

  **"뒷면 만들기" 버튼을 위쪽 도구막대에도 넣었다.** 지금까진 뒷면이
  없을 때 캔버스 자리표시 안의 큰 버튼으로만 만들 수 있었다 — "지우기"
  버튼 왼쪽에 작은 버튼을 하나 더 뒀다(뒷면이 없을 때만 보인다). 자리표시
  버튼은 그대로 남아 있다, 없앤 게 아니라 늘린 것이다.

  **속성 막대(`.editor-bar-style`) 줄에 최소 높이(28px)를 박았다.**
  안내 문구만 뜰 때(12px 글자·투명 테두리)와 드롭다운(select, 13px
  글자·실선 테두리)이 늘어설 때가 안의 글자 크기부터 달라서 줄 자체의
  자연스러운 높이가 몇 px씩 어긋났다 — 고르거나 놓을 때마다 그 아래
  캔버스가 흔들리듯 움직이던 이유였다. 줄에 최소 높이를 박아 무엇이
  들어오든 항상 같은 높이가 되게 했다 — 이후로 어떤 조합이 새로 생겨도
  같은 방식으로 안전하다.

- **17p 완료 — 17o 후속 피드백 두 개.**

  **선을 여럿(또는 도형과 함께) 마퀴로 골랐을 때 안 골라진 것처럼
  보이던 문제.** 스크린샷으로 보니 실제로는 선도 골라지고 있었다 —
  다만 `.picked line`(고른 선 표시)이 실선에 0.7mm 굵기 초록색뿐이라
  원래 선과 겹쳐 잘 안 보였고, 딱 하나만 골랐을 때만 끝점 손잡이(작은
  네모)가 따로 떠서 눈에 띄었다. 도형의 선택 테두리(`.picked rect`)는
  점선이라 훨씬 뚜렷했던 것과 대비됐다. `.picked line`도 점선으로
  바꿔서(도형과 같은 방식) 몇 개를 골랐든 눈에 띄게 했다 — 선택 로직
  자체는 원래도 맞았다(코드에 셀 수와 관련된 분기가 없다).

  **뒷면이 없을 때 편집 화면의 안내 상자("아직 뒷면이 없습니다")를
  없앴다.** 이제 뒷면이 없으면 앞면 캔버스 하나만 보인다 — 17o에서
  도구막대에 넣은 "뒷면 만들기" 버튼이 유일한(그리고 더 자연스러운)
  진입점이 됐다. 안 쓰게 된 `.side-slot.back-empty`·`.back-empty-msg`
  CSS와, 그 안내 상자를 겨냥했던 `onOutsideDown`의 가드도 함께 지웠다.

- **17q 완료 — 17p 후속 피드백 두 개.**

  **"여전히 안 됨" — 선택 표시를 훨씬 두껍게, 클릭 허용 거리도 늘렸다.**
  17p의 점선 0.9mm로는 아직 눈에 덜 띄었던 모양이다. `.picked line`을
  1.4mm·더 큰 점선(2.2·1.4)으로 키웠다 — 원래 선 굵기(대개 0.2mm)는
  그대로고 이 표시만 두꺼워진다. 추가로 "골라져도 옮기기 어렵다"는
  피드백에 맞춰 `ui/gestures.ts`의 `GRAB`(선을 집었다고 볼 거리)을
  1.6mm → 2.0mm로 늘렸다 — 클릭 판정 자체가 넓어져서 재선택·드래그
  시작이 더 쉬워진다.

  **SideBar 줄에도 최소 높이(28px)를 박았다.** `.editor-bar-style`과
  같은 문제였다 — 뒷면 탭에서만 보이는 "앞면 그대로 복사"·"뒷면
  지우기" 버튼이 있을 때와 없을 때(앞면 탭, 라벨 글자 하나만) 줄
  높이가 달라서, 뒷면을 만들거나 지울 때마다 그 아래 캔버스가
  들썩였다.

- **17r 완료 — "선택이 이상하다"의 진짜 원인을 찾았다: 저장 파일을
  불러온 뒤 새로 그은 것이 파일 속 것과 id가 겹치는 버그. 상단바
  높이·선택 표시 두께도 마저 다듬었다.**

  사용자가 화면 녹화를 보내줬다: "가장 처음 만들어진 선만 선택이
  되고, 그 뒤에 만든 선은 선택도 안 되고 선택돼도 얇게 표시된다."
  이 실마리로 진짜 원인을 찾았다 — **`newId`의 카운터가 새로고침하면
  0부터 다시 시작하는데, 저장 파일을 불러오면 그 안에 이미 `l1`·`t3`
  같은 id가 들어 있다.** 그대로 두면 불러온 뒤 처음 그리는 선이
  파일 속 어떤 선과 **같은 id**를 받는다 — 그 순간부터 리액트가 둘을
  같은 `key`로 보고, 마퀴로 그 자리를 감싸도 새로 그린 쪽이 아니라
  파일 속 원본이 골라지거나 렌더링이 뒤엉킨다. "가장 처음 그린 선"이
  멀쩡했던 건 그게 새로고침 직후 카운터가 1일 때 그은 첫 선이라
  우연히 파일 속 낮은 번호 id와 안 겹쳤을 뿐이었다.

  글꼴·이미지는 이미 이 문제를 알고 있었다(`fonts/registry`의
  `reserveIds`, `images/registry`의 `reserveImageIds`가 불러온 뒤
  카운터를 올려둔다) — 객체·양식 id에는 같은 처리가 빠져 있었다.
  `core/objects.ts`에 `ensureIdCounterAbove`, `core/template.ts`에
  `ensureTemplateIdCounterAbove`를 추가해 같은 방식으로 맞췄다.
  `store.ts`의 `loadProject`가 파일을 연 직후 불러온 모든 양식·객체
  (앞뒤 다) id로 두 카운터를 올려둔다. 재현하는 테스트를 store.test.ts에
  추가했다(카운터를 일부러 아주 큰 id로 앞질러 두고, 그 뒤에 그은
  것과 안 겹치는지 확인).

  진짜 원인이 이거였다는 게 밝혀지면서, "선택 표시가 흐려서"라고
  짐작하고 17q에서 1.4mm까지 두껍게 만들었던 건 되돌렸다 — 사용자
  요청대로 0.5mm로, 점선 비율도 그에 맞게 줄였다. `GRAB`(클릭 판정
  거리, 1.6→2.0mm)은 그 자체로도 쓸모 있는 여유라 그대로 뒀다.

  **도구막대(`.editor-bar-tools`)가 줄바꿈 대신 가로 스크롤을 쓰게
  바꿨다.** "뒷면 만들기" 버튼이 뒷면 없을 때만 끼어들면서, 화면
  너비에 따라 이 줄이 두 줄로 접혔다 펴졌다 해 그 아래 캔버스가
  흔들렸다 — 줄바꿈은 너비가 바뀌면 줄 수도 바뀌어 높이를 지킬 수
  없다. 넘치면 감추는(스크롤) 쪽이 사용자가 요청한 "높이 고정"에
  더 맞는다.

- **17s 완료 — SideBar 높이 불일치의 진짜 원인 찾음(라벨과 버튼의
  글자 크기 차이), 17r의 가로 스크롤은 되돌림.**

  사용자가 다시 짚어줬다: 높이가 어긋나는 진짜 자리는 도구막대
  (`.editor-bar-tools`, "뒷면 만들기" 있고 없고)가 아니라 **SideBar**
  ("지금 · 앞면" 라벨만 있을 때와 "지금 · 뒷면 / 앞면 그대로 복사 /
  뒷면 지우기"로 버튼이 곁들 때)였다 — "앞면"과 "뒷면지우기" 글자
  크기가 안 맞는다고 정확히 짚어줬다. 17q의 `.side-bar { min-height:
  28px }`는 **바닥**만 깔아줄 뿐, 실제 내용(`.active-side-label`은
  13px·패딩 없음, `button.ghost`는 14px·상하 4px 패딩)의 자연스러운
  높이 차이 자체는 안 없앴다 — 어느 한쪽이 28px보다 크면 그대로
  삐져나온다.

  이번엔 브라우저를 직접 열어 `getComputedStyle`로 재서 확인했다(이
  프로젝트는 평소 화면 확인을 사용자에게 맡기지만, 같은 수정을 두 번
  실패한 뒤라 직접 재보는 쪽을 택했다) — `.active-side-label`에
  `button.ghost`와 **똑같은 패딩(4px 9px)·글자 크기(14px)**를
  주니 앞면 상태 40.5px, 뒷면 상태 40.5px로 정확히 같아졌다.
  min-height 하나로 어림잡는 대신, 상자 모양 자체를 맞추는 쪽이
  확실하다.

  17r에서 "뒷면 만들기 버튼 유무로 도구막대가 두 줄이 된다"고 보고
  줄바꿈을 가로 스크롤로 바꿨던 건 **엉뚱한 곳을 고친 것**이었다 —
  게다가 스크롤 때문에 그 버튼이 화면 밖으로 숨을 수 있어 더
  나빠졌다("맨 위에 있는 게 좋다"는 사용자 피드백). 원래 방식(줄바꿈)
  으로 되돌렸다.

  **마퀴로 3개 이상 골라도 안 된다는 것도 같은 브라우저에서 직접
  재현해봤다** — 새 세션(불러온 파일 없음)에서 선 3개를 그리고
  마퀴로 감쌌더니 3개 다 정확히 골라지고 점선 표시도 뚜렷했다.
  17r의 id 겹침 버그(불러온 파일 속 id와 새로 그은 것의 id가
  겹치는 것)가 원인이었을 가능성이 크다 — 그 수정이 반영되기 전
  코드로 테스트했거나, 겹친 id가 이미 낀 상태에서 계속 쓰고
  있었을 것으로 보인다. localhost 접속이 끊겼던 것도 새로 켜뒀다
  (사용자 요청 1번).

- **17t 완료 — 선·표·글자를 섞어 골라도 색을 한 번에 바꾸는 기능.**

  사용자가 화면 녹화를 보내와서 프레임 단위로 분석했다(qlmanage로는
  한 장만 뽑혀서, ffmpeg가 없어 swift+AVFoundation으로 직접 12장을
  고르게 뽑는 스크립트를 짜서 썼다). 알고 보니 "마퀴가 안 된다"던 건
  예전 코드를 물고 있는 탭이었을 가능성이 크다는 게 밝혀졌지만(같은
  브라우저에서 새 탭으로 재현하니 멀쩡했다), 그 과정에서 진짜 문제를
  하나 찾았다 — **선 3개 + 도형 1개**를 같이 고르면 `showTable`
  조건(`pickedShapes>0 && pickedLines>0`)에 걸려 표로 취급됐는데, 표
  자체는 색 조작이 이미 되고 있었다(`TableControls`). 진짜 빠진 건
  **글자가 선·도형과 함께 섞였을 때**였다 — `showText`는 선이 하나라도
  있으면 꺼지고, `showShape`·`showTable`은 글자를 아예 안 보므로,
  "선 두 개 + 글자 하나" 같은 조합은 지금까지 아무 조작칸도 못 띄웠다.

  `MixedColorControls`를 새로 만들어 `showMixedColor`(글자가 선·도형
  중 하나와 섞였을 때만, calendar·image보다 뒤·table보다 앞 우선순위)
  로 가로챈다. 굵기·모양(점선 등)은 종류마다 뜻이 달라 하나로 못
  묶지만 색은 선·도형·글자 셋 다 있다. 새 저장소 액션은 안 만들었다
  — `styleSelected`(선)·`styleShape`(도형)·`styleText`(글자)가 이미
  각자 자기 종류가 아닌 id는 걸러내므로, 고른 것 전체의 id를 셋 다에
  그냥 넘겨도 안전하다. 기본색이 선·도형(연하게, `#9e9e9e`)과
  글자(진하게, `#3a3a3a`)가 서로 달라서 "빈 문자열=기본값" 관례는 못
  쓰고, 섞인 선택에서는 실제 색 id를 그대로 보여준다.

  브라우저에서 직접 선 3개+글자 하나를 골라 색을 "검정"으로 바꿔보고,
  `getComputedStyle`로 선·글자 둘 다 실제로 `#000000`이 됐는지 확인했다.

- **17u 완료 — 새로고침해도 마퀴 버그가 남아있던 진짜 이유: 저장 파일
  안에 이미 박혀 있던 id 겹침.**

  사용자가 새로고침한 뒤에도 여전히 안 됐다고 확인해줬다 — "선택했을
  때 선택선이 안 보여서 삭제는 되는데 이동은 안 된다"는 결정적인
  단서였다. 서버 프로세스를 확인해보니(`lsof`) 사용자 크롬과 이
  세션의 브라우저 도구가 **같은 dev 서버 하나**에 붙어 있었고, 그
  서버가 내려주는 CSS(`curl`로 직접 확인)도 최신이었다 — 즉 코드는
  최신인데 딱 그 파일에서만 재현되는 상황이었다.

  17r에서 고친 id 겹침 버그(새로고침하면 `newId` 카운터가 0부터
  다시 시작하는데 불러온 파일엔 이미 `l1` 같은 id가 있어서, 불러온
  뒤 처음 그리는 것이 파일 속 객체와 id가 겹치던 것)를 다시 떠올렸다
  — **그 수정은 "앞으로" 생길 겹침만 막을 뿐, 이미 예전에 저장된
  파일 안에 박혀 있는 겹침은 못 고친다.** 사용자의 실제 작업 파일은
  그 버그가 있던 시절에 만들어졌을 테니, 새로고침해서 다시 불러와도
  겹친 id가 그대로 돌아왔던 것이다 — "삭제는 되는데 이동은 안 된다"는
  것도 정확히 들어맞는다: 삭제는 배열을 통째로 걸러내니 겹쳐도
  상관없지만, 옮기기는 `objects.find(o => o.id === id)`로 하나만
  짚어서 겹친 것 중 어느 쪽이 실제로 움직일지 꼬인다.

  `core/project.ts`에 `dedupeObjectIds`를 추가해 `toTemplates`(파일을
  불러올 때마다 거치는 통로)에서 **양식 하나의 앞뒤는 물론, 프로젝트
  전체를 통틀어** 겹치는 id를 찾아 나중에 나온 쪽에 새 id를 매기게
  했다(먼저 나온 것은 그대로 둔다 — `cloneObject(o, 0, 0)`으로 자리는
  안 옮기고 id만 새로 받는다). 이러면 이미 망가진 저장 파일도 여는
  순간 스스로 고쳐진다. `core/project.test.ts`에 겹친 id가 있는 파일을
  흉내낸 테스트, 앞면·뒷면에 걸쳐 겹치는 경우까지 추가했다.

  "선이 얇으면 선택이 잘 안 된다"는 17q에서 이미 `GRAB`을 1.6→2.0mm로
  늘려둔 것과 같은 증상으로 보인다 — 겹친 id 문제가 겹쳐서 더
  나빠 보였을 가능성이 크다.

- **17v 완료 — 자주 쓰는 이미지를 그리드에서 바로 고르는 기능.**

  알고 보니 **persistence는 이미 다 있었다.** `images/registry.ts`가
  진작부터 등록한 이미지를 IndexedDB(`storage/idb.ts`)에 파일 이름으로
  캐싱해뒀고, 앱을 시작할 때마다(`App.tsx`) `restoreCachedImages`가
  그 전부를 `userImages`에 자동으로 되살려놓고 있었다 — 즉 "한 번 쓴
  이미지"는 이미 세션을 넘어 전부 남아 있었는데, 그걸 쓸 수 있는
  화면이 없었을 뿐이다. 그래서 이번엔 새 저장소를 만들지 않고, 그
  목록을 보여주는 창만 만들었다.

  **빈 이미지 상자를 더블클릭하면 이제 `ImagePickerDialog`가 먼저
  뜬다.** 예전엔 곧바로 OS 파일 창이 열렸다. 새 창은 `userImages` 중
  실제 파일이 있는 것(`url`이 있는 것)만 썸네일 그리드로 보여주고,
  아래에 "파일 불러오기…" 버튼을 그대로 둔다 — 고르면 바로 씌우고
  닫히고, 파일 불러오기를 누르면 지금까지 쓰던 숨은 `<input
  type=file>`을 그대로 연다. `GalleryTab.tsx`의 `Modal`(바깥을 누르거나
  Esc로 닫히는 그 틀)을 그대로 가져다 썼다 — export만 새로 열었다.

  **삭제 버튼도 달았다**(썸네일 오른쪽 위 작은 ×). 그냥 지우면 지금
  그 이미지를 쓰는 상자가 깨지므로, `removeUserImage`(store)가 먼저
  프로젝트 전체(모든 양식의 앞뒤)를 뒤져 **지금 쓰는 중이면 조용히
  아무 일도 하지 않는다.** `images/registry.ts`에 `removeImage`(이름으로
  IndexedDB에서 지운다), `storage/idb.ts`에 `idbDelete`를 새로 추가했다.

  브라우저에서 끝까지 직접 눌러가며 확인했다 — 빈 목록 상태, 파일
  올린 뒤 그리드에 뜨는 것, 그리드에서 골라 바로 씌워지는 것, 쓰는
  중인 이미지는 삭제가 막히는 것, 안 쓰면 삭제되는 것까지 전부 손으로
  재현해봤다.

- **17w 완료 — 색을 회색 5단계에서 전체 색으로, 이미지 관리 창을
  양식 관리에 따로.**

  **색이 회색 5단계(아주 연하게~검정)뿐이었다.** 선·도형·글자·표·
  체크박스·달력, 그리고 17t의 MixedColorControls까지 전부 이
  드롭다운 하나(`COLORS` 상수)를 같이 쓰고 있었다 — "빨주노초파남보
  다 되게" 해달라는 요청으로, 드롭다운을 브라우저 기본 색
  선택기(`input type="color"`)로 통째로 바꿨다. 16진 코드를 직접
  입력할 수도, 색상환에서 고를 수도 있다.

  일곱 군데(LineControls·CalendarControls·ShapeControls·
  MixedColorControls·TableControls·CheckboxControls·TextControls)를
  전부 `ColorField`(새 공용 칸) 하나로 바꿨다. "섞임" 상태는 실제
  색 하나로 나타낼 수 없어서, 색 자체는 첫 번째 값을 보여주고
  테두리만 점선으로 바꿔 섞였다는 것을 알린다. `기본값이면 undefined로
  지운다`는 기존 규칙은 그대로 지켰다 — 고른 색이 그 종류의 기본색과
  같으면 키 자체를 지운다. 브라우저에서 실제로 회색 아닌 색(청록)을
  입력해 선의 `stroke` 속성이 진짜 그 색으로 바뀌는 것까지 확인했다.

  **이미지 관리 창을 "양식 관리" 화면에 따로 만들었다.** 지금까지는
  빈 이미지 상자를 더블클릭해야만(ImagePickerDialog) 저장된 이미지를
  볼 수 있었는데, 상자 없이도 훑어보고 정리할 자리가 필요했다.
  `ImagePickerDialog.tsx`에 그리드+삭제 부분을 `ImageThumbGrid`로
  뽑아내 공용으로 만들고, 그걸 쓰는 `ImageLibraryDialog`를 새로 추가했다
  — 고르기 창과 달리 썸네일이 눌러지지 않는다(어디에도 씌울 데가
  없다), 대신 자체 "파일 추가…" 버튼으로 상자 없이도 미리 이미지를
  등록해둘 수 있다. "양식 관리" 탭 위쪽에 "이미지 관리" 버튼을
  더했다. 빈 상태·업로드·삭제까지 브라우저에서 확인했다.

  **나중에 할 것(메모만, 아직 안 함)**: 이미지가 많아지면 이름으로
  찾거나 핀터레스트처럼 모든 사진이 다 보이는 큰 그리드로 정리할 수
  있게 해달라는 요청. 지금 그리드(4열 고정, 스크롤)로는 이미지가
  많아지면 찾기 불편해질 것 — 나중에 다시 요청하면 손본다.

- **17x 완료 — 도형은 테두리를 눌러야 골라지게, 선을 그을 때도 mm와
  함께 몇 칸인지 보여주기.**

  **도형(표 테두리 포함) 안에 선이 있으면 안쪽을 눌러도 도형이
  골라졌다.** `hitAt`의 상자류(달력·이미지·도형·체크박스) 판정이
  전부 "안이 꽉 찬 사각형"으로 맞혀서, 도형 안쪽 어디를 눌러도 그
  도형이 먼저 집혔다 — 표의 안쪽 칸 선이나 도형 안에 직접 그려 넣은
  장식선을 다시 고를 수 없었다. 도형(`isShape`)만 따로 떼어
  `distanceToBoxEdge`(네 변까지의 최소 거리, `rectLines`+
  `distanceToSegment` 재사용)로 **테두리 근처(GRAB)만** 집히게
  바꿨다 — 달력·이미지·체크박스는 그대로 안이 꽉 찬 판정이다(그
  안에 다른 걸 겹쳐 그릴 일이 없다). 브라우저에서 도형 안에 선을
  그어놓고, 안쪽 클릭(선택 안 됨)·선 클릭(선 선택)·테두리 클릭(도형
  선택) 세 가지를 다 확인했다.

  **선을 그을 때(클릭-클릭) mm 옆에 몇 칸인지도 보여준다.** 표
  (`tableSize`)가 쓰는 계산을 그대로 재사용했다 — 가로나 세로
  한쪽으로만 그은 흔한 경우엔 그 방향 칸 수 하나만("11칸"), 대각선처럼
  둘 다 걸치면 표처럼 "가로 × 세로"("9 × 3칸")로 보여준다. 선 끝점을
  끌어 고칠 때도 같은 표시가 뜬다. 브라우저에서 대각선을 그으며
  "45 × 15mm · 9 × 3칸"으로 정확히 뜨는 것까지 확인했다.

- **17y 완료 — 이미지 상자 크기 조정이 도트에 안 붙던 것 고침 +
  가로·세로를 숫자로 직접 입력.**

  **모서리 손잡이를 처음 잡는 순간 격자에 안 붙어 있었다.** 손잡이를
  드래그로 끌기 시작한 뒤(`onMove`)부터는 매번 `snapped(e)`로 격자에
  앉혔지만, **잡는 바로 그 순간**(`onDown`)의 시작 값은 `raw`(안 붙은
  마우스 자리)를 그대로 썼다 — 마우스가 그 뒤에 조금이라도 움직이면
  다음 `onMove`에서 바로 잡히지만, 짧게 끌고 놓는 손짓에서는 이
  안 붙은 시작 값이 그대로 최종 값이 되어 "도트에 잘 안 붙는다"고
  느껴졌다. 두 자리(이미지·달력 도구일 때, select 도구일 때) 다
  `raw` 대신 `snap ?? raw`로 고쳤다.

  **이미지 가로·세로를 숫자로 직접 입력할 수 있게 했다.** 드래그가
  안 미더우면 정확한 mm를 타이핑하는 쪽이 확실하다. 속성 막대에
  "회전" 칸 옆에 "가로"·"세로" `NumField`를 새로 뒀다 — 왼쪽 위(x,y)는
  그대로 두고 오른쪽·아래로만 자라거나 줄어든다(모서리로 끄는 것과
  같은 기준점). 새 저장소 액션은 안 만들고 기존 `resizeObject`를
  그대로 썼다. 여럿을 골랐으면 각자 x·y는 유지한 채 크기만 같이
  바뀐다. 브라우저에서 가로를 30→60으로 입력해 왼쪽은 그대로,
  오른쪽으로만 자라는 것까지 확인했다.

- **17z 완료 — "저장"이 프로젝트 전체가 아니라 지금 양식 하나만 담게 바꿈,
  저장 위치도 직접 고를 수 있게.**

  **"저장"을 누르면 갖고 있는 양식이 전부 한 파일에 묶였다.** 위클리를
  고치고 저장했는데 다른 날 만들던 먼슬리까지 같은 파일에 딸려 나오니,
  어느 게 최신인지 헷갈렸다. `toProject`를 부를 때 `templates: [activeTemplate]`
  하나만 넘기도록 바꿨다 — 글꼴·이미지 목록도 `usesFont`/`usesImage`가
  그 한 양식만 보고 걸러주므로 자동으로 그 양식이 실제로 쓰는 것만 남는다.
  파일 이름도 날짜 대신 **양식 이름 + 날짜**(`위클리1_2026-08-15.json`)로
  바꿔서, 여러 개 저장해도 파일 목록만 보고 뭐가 뭔지 알 수 있다.

  **양식 하나짜리 파일을 여러 개 한 번에 불러올 수 있게 했다.** "낱장 조합"에서
  여러 양식을 한 인쇄물에 섞어 쓰려면, 오늘 저장한 위클리와 어제 저장한 먼슬리를
  같이 열 수 있어야 한다. 파일 선택 창에 `multiple`을 달아 여러 파일을 한 번에
  고를 수 있게 했고, "불러오기"의 의미를 **"갈아끼우기"에서 "더하기"로**
  바꿨다 — 지금 프로젝트를 지우지 않고 고른 파일들의 양식을 끝에 이어붙인다.
  기존 "프로젝트 전체 갈아끼우기"(`loadProject`)는 그대로 남아 있다 — 옛날에
  저장해둔, 양식이 여러 개 든 파일을 열어도 여전히 읽히고(그 안의 양식들이
  전부 더해진다), 용지·배치 설정만은 더할 때 건드리지 않는다(양식이 아니라
  지금 프로젝트에 속한 값이라서).

  **더할 때 id가 겹치는 문제를 처음부터 막았다.** `newId`의 카운터는 프로그램을
  새로 켤 때마다 0부터 시작해서, 다른 날 따로 저장한 두 파일은 거의 항상 같은
  id(`l1`, `t3`...)를 쓴다. 그대로 더하면 지금 프로젝트의 기존 객체와 겹쳐
  마퀴 선택이 깨졌던 것과 똑같은 문제가 생긴다. `core/template.ts`에
  `refreshTemplateIds`를 새로 만들어 — 불러온 양식과 그 안의 모든 객체(앞뒤
  모두)에 **항상** 새 id를 준다(겹치는지 검사하지 않고 무조건 새로 매긴다 —
  `duplicateTemplate`이 이미 하던 "복제하면 새 id" 방식과 같다). 이름도
  `uniqueName`으로 겹치지 않게 정리한다.

  **저장 위치는 크롬 계열에서 직접 고를 수 있게 했다.** 웹 앱은 브라우저
  보안 때문에 사용자 몰래 특정 폴더에 파일을 쓸 수 없다 — 대신
  `showSaveFilePicker`(File System Access API)가 있으면 저장할 때마다 위치를
  고르는 창을 띄운다. 처음에 프로젝트 폴더 안에 "양식저장" 폴더를 만들어
  거기를 고르면, 브라우저가 다음 저장부터 그 폴더를 기억해준다(브라우저가
  알아서 하는 것이라 앱이 강제할 수는 없다). 이 기능이 없는 브라우저(사파리
  등)에서는 예전처럼 다운로드 폴더로 받아진다 — 두 경로 다 되게 `catch`로
  대신하게 했다.

### 18단계 — 달력 글자 꾸미기

- **18a 완료 — 달력에 글씨체·자간·줄간격·직접 쓰는 제목 추가.**

  **글씨체.** `CalendarObject`에 `font`를 추가했다 — `TextObject.font`와
  똑같은 규칙(등록한 글꼴 id, 이번 세션에만 유효)이라 속성 막대의
  `FontPicker`를 그대로 재사용했다. PDF에서도 달력이 실제로 쓰는 등록
  글꼴만 심는다(글자와 같은 검사를 하나 더했다).

  **자간.** mm 단위로 `letterSpacing`을 추가했다. 화면은 SVG `<tspan dx>`로
  글자 사이를 벌린다(CSS letter-spacing 대신 — `dx`가 이 SVG 좌표계와 같은
  mm 단위라 PDF와 같은 값을 그대로 쓸 수 있다). **pdf-lib에는 자간 기능이
  없어서** 글자를 하나씩 폭을 재 그리는 걸 새로 만들었다 — 간격이 0(대부분)이면
  지금까지처럼 통째로 그려서 성능·용량 손해가 없다.

  **줄(주) 간격.** `rowScale` 배율을 추가했다 — 기존 `sizeScale`(글자 크기)과
  독립적으로, 제목·요일·날짜 8행 사이 간격만 넓히거나 좁힌다.
  `core/calendar.ts`의 `calendarLayout`이 새 간격으로 전체 격자 높이를
  계산한 뒤, 상자보다 커지거나 작아진 만큼을 위아래로 똑같이 나눠 가운데로
  맞춘다.

  **제목 직접 쓰기.** `title`을 정하지 않으면 지금처럼 "2026년 8월"이
  자동으로 뜬다. 정하면(빈 문자열 포함) 그 글자를 그대로 쓰고 달이 바뀌어도
  안 바뀐다 — "AUGUST"처럼 자유롭게 쓸 수 있고, 완전히 지우면(빈 문자열)
  제목 줄이 안 보인다. `core/calendar.ts`의 `titleOf`가 화면·PDF가 함께
  부르는 이 판단을 한 곳에 모았다(이전엔 두 파일에 `formatDate(...,
  'YYYY년 M월')`가 각각 적혀 있었다). 속성 막대에 글자 입력칸과, 자동으로
  되돌리는 작은 버튼을 뒀다.

  **공휴일 등 날짜별 색은 만들지 않기로 했다.** 요일(일·토) 기준이면
  쉽지만, 특정 날짜(공휴일, 특히 음력)는 음력 변환·연도별 데이터베이스가
  필요하고, 달력 하나가 만년형 세트 인쇄에서 여러 달에 재사용되는 구조라
  "이 칸이 공휴일"을 오브젝트에 고정해 저장할 수도 없다 — 사용자에게
  설명하고 보류.

### 19단계 — 절취선 겹침·뒷면 복사 좌우 뒤집기·화면 도트 연하게

- **19a 완료 — 칸 사이 간격이 좁으면 절취선 십자 표시가 겹쳐 하나로
  이어져 보이던 것 고침, 앞면 그대로 복사 시 뒷면 자리를 좌우로 뒤집기,
  편집 화면 도트를 연하게.**

  **절취선(십자 마크)의 팔 길이(`CROP_ARM` 3mm)가 칸 사이 간격보다
  길면 옆 표시와 겹쳤다.** 간격이 6mm보다 좁은 배치(사용자의 예처럼
  기본 3mm 간격 등)에서, 양옆에서 뻗은 팔이 서로 넘어가 잘라낼 부분에
  끊기지 않는 선 하나가 남았다 — 십자 마크를 쓰는 이유(속지 위에 자국을
  남기지 않는 것) 자체가 무색해졌다. `core/crop.ts`에 `armLengths`를
  추가해, 각 좌표의 팔 길이를 **옆 좌표까지 거리의 절반**으로도 제한한다
  (기존처럼 넓은 간격에서는 그대로 3mm). 인쇄 화면에서 SVG `<line>`
  좌표를 직접 읽어 확인했다 — 2mm 간격에서 양쪽 팔이 정확히 간격 가운데
  (반지름 1mm씩)에서 만나고, 넘어가지 않는 것까지 봤다.

  **"앞면 그대로 복사"가 그림 자리를 그대로 옮겨서, 타공과의 거리가
  앞뒤에서 달라졌다.** 타공은 뒷면에서 반대쪽에 있다(core/punch의
  `holeCenterX`가 이미 하는 계산) — 앞면에서 타공 옆 1cm를 비워 그렸다면,
  뒷면에서도 (반대쪽) 타공 옆 1cm가 비어야 종이를 뒤집었을 때 자연스럽다.
  `core/objects.ts`에 `mirrorObjectX`(속지 폭 기준으로 자리만 좌우로
  뒤집는다 — 글자 정렬·회전 방향은 안 건드린다, 뒤집으면 오히려 못
  읽는다)를 추가해 `backFromFront`(=앞면 그대로 복사)가 객체마다 이걸
  거치게 했다. 브라우저에서 앞면 타공 쪽에 선을 그어 복사해보니, 뒷면에서
  반대쪽 타공 옆(같은 거리)에 나타나는 것까지 확인했다. 함께 신고된
  "옮길 때 도트에 안 맞는 버그"는 직접 옮기기·크기 조절을 여러 번
  시도해봤지만 재현되지 않았다 — 아마 이 뒤집힘 버그 때문에 손으로
  자리를 맞추려다 겪은 것으로 보인다. 계속 보이면 다시 알려달라고 안내.

  **편집 화면 도트가 너무 진해서 그 위의 디자인이 잘 안 보였다.**
  `SCREEN_GRID_COLOR`를 `#8c8c8c`에서 `#c9c9c9`로 낮췄다. 인쇄에는
  영향이 없다(화면 전용 값 — `CONTENT_COLOR.dot`은 그대로).

- **19b 완료 — 마퀴로 여러 개를 고르면 글자 위에 브라우저 자체 텍스트
  선택(파란 하이라이트)이 같이 뜨던 것 고침.**

  마퀴(감싸기)로 여러 개를 고르는 드래그가 글자 오브젝트 위를 지나가면,
  이 앱의 "고름" 표시(초록 점선)와 별개로 **브라우저가 자기 나름의 글자
  선택을 같이 시작**했다 — `user-select`를 막아두지 않아서였다. 그
  파란 하이라이트가 눈에 확 띄어서, 옆에 같이 고른 선들의 (원래도 옅은)
  초록 점선 표시가 상대적으로 안 보이는 것처럼 느껴졌다 — 실제로는
  선도 제대로 고른 채였다(300% 확대, 브라우저 DOM 좌표까지 확인함).
  `.insert-sheet`에 `user-select: none`을 줘서, 이 캔버스 위에서는
  브라우저의 글자 선택이 아예 시작되지 않게 했다.

- **19c 완료 — 절취선 표시를 속지 모서리에 바로 붙이지 않고, 여백 쪽으로만
  떨어뜨려 긋기.**

  **지금까지는 표시가 모서리에 정확히 걸쳐 있었다.** 십자의 절반은 항상
  속지 내용 쪽으로 들어갔다 — 자르고 나면 그 자국이 완성된 속지 위에
  남는다. 사용자가 손으로 그린 그림(모서리에서 1mm 띄워 4mm짜리 표시,
  L자 모양)을 보고 설계를 다시 짰다.

  **핵심은 "이 모서리의 네 방향(상하좌우) 중 어느 쪽에 실제로 속지
  내용이 있는가"를 직접 확인하는 것이다.** `core/crop.ts`에
  `openDistance`를 새로 만들어, 그 방향으로 곧장 나아갈 때 어느
  속지(`layout.slots`)의 사각형과도 안 겹치고 갈 수 있는 최대 거리를
  잰다. 내용이 있는 방향(예: 도형 자신의 안쪽)은 즉시 막혀(거리 0)
  아무것도 안 그리고, 열린 방향(여백·용지 밖)에만 모서리에서
  `CROP_MARK_GAP`(1mm) 띄운 자리부터 `CROP_MARK_LENGTH`(4mm)만큼 짧게
  긋는다 — 네 방향을 각각 검사하므로 모서리 하나에서 0~4개의 표시가
  나온다(테두리 바깥 모서리는 보통 2개, L자 모양).

  **마주보는 두 표시가 서로 넘어가지 않게, 열린 거리의 절반까지만
  쓴다** — 지난번(19a) "옆 표시와 안 겹치기"와 같은 생각을 방향별로
  다시 적용했다. 간격이 넉넉하면(2mm 이상 남으면) 그대로 4mm를 다
  긋고, 좁으면 그만큼 줄이고, GAP(1mm)조차 못 낼 만큼 좁으면(간격
  2mm 이하) 아예 안 그린다.

  `CROP_ARM`(팔 하나 길이, 3mm)은 없앴고 `CROP_MARK_GAP`(1mm)·
  `CROP_MARK_LENGTH`(4mm)로 바꿨다. 테스트를 전부 새 모양에 맞게
  다시 짰다 — 특히 "표시가 어느 칸의 내용도 침범하지 않는다"는
  모든 배치 조합에서 실제 칸 사각형과 좌표를 직접 비교해 확인한다.
  인쇄 화면에서 SVG 좌표를 직접 읽어, 모서리(25,23.5)에서 왼쪽·위로
  각각 1mm 띄운 자리부터 4mm씩(L자) 나가는 것까지 확인했다.

- **19d 완료 — "십자·안쪽까지"(markAll)는 바깥 테두리는 19c 그대로,
  안쪽(사방이 다 내용인 갇힌 교차점)만 원래 모양(모서리에 걸친 십자)
  으로.**

  처음엔 markAll 전체를 옛 방식으로 되돌렸는데, 사용자가 "겉부분은
  수정한 대로 두고 가운데만 옛 십자로"라고 정정해줬다 — markAll도
  바깥 테두리는 mark와 똑같이 내용을 침범하지 않는 새 방식을 쓰고,
  mark가 아예 건너뛰는 **안쪽(사방이 다른 칸으로 둘러싸여 간격이
  0인) 교차점만** `CROP_ALL_ARM`(3mm)짜리 옛 십자로 채운다 — 그런
  자리는 물러날 여백이 아예 없어 내용을 침범하는 것 말고는 뭔가
  보여줄 방법이 없기 때문이다.

- **19e 되돌림 — 화면 절취선을 진하게 하는 수정을 되돌렸다.**

  "절취선이 안 보인다"는 게 굵기 문제가 아니라 특정 자리에 표시
  자체가 없다는 뜻이었다 — 두께는 원래(인쇄와 같은 0.1mm)대로
  둬야 한다는 정정을 받아 `SCREEN_CROP_WIDTH`·`SCREEN_CROP_COLOR`를
  없던 일로 했다.

  브라우저 DOM에서 좌측 상단 정렬(간격 3mm, M6 2×2)의 표시 22개를
  전부 좌표로 확인한 결과: **속지가 용지 가장자리에 완전히 붙는
  방향(좌측 상단 정렬의 맨 왼쪽·맨 위)으로 뻗는 표시는 좌표가
  음수(용지 밖)라 화면·인쇄 어느 쪽에도 안 보인다** — 그 방향엔
  종이 자체가 없어서 계산은 맞지만 그릴 자리가 없다. 반대로
  자투리가 몰리는 오른쪽·아래는 계산·길이가 전부 정확했다(간격이
  좁은 칸 사이는 19a·19c 규칙대로 짧아진 채로). 이게 정확히 사용자가
  짚어준 "안 보이는" 자리였다 — 아래 19f에서 예외로 고쳤다.

- **19f 완료 — 속지가 용지 가장자리에 딱 붙으면(정렬 좌측 상단),
  "내용을 침범하지 않는다"는 원칙의 예외로 안쪽에 짧게 표시한다.**

  19e에서 확인한 "종이 자체가 없어 표시가 아예 안 나오는" 자리에
  대한 사용자 요청 — "표시가 없으면 어디서 잘라야 할지 모른다.
  이 경우엔 속지를 침범해도 되니 예외로 봐달라"는 정정을 받았다.
  `core/crop.ts`에 `paperDistance`(그 방향으로 남은 진짜 용지
  거리)를 추가해, 그 거리가 GAP(1mm)에도 못 미치면(=종이 자체가
  없으면) 원칙을 깨고 **모서리에서 GAP 없이 바로, 반대쪽(속지 내용
  쪽)으로 CROP_MARK_LENGTH(4mm)만큼** 표시한다. 진짜 여백이 있으면
  (`paperDistance`가 충분하면) 지금까지처럼 내용을 안 건드리는
  방식을 그대로 쓴다. 브라우저에서 좌측 상단 정렬로 확인 — 왼쪽·
  위쪽 가장자리에 이제 뚜렷하게 표시가 나온다.

- **19g 완료 — 양면 인쇄에서 뒷면도 앞면과 같은 자리(좌측 상단 등)로
  모으기 — 칸 자리 자체는 더 이상 좌우(또는 상하)로 뒤집지 않는다.**

  지금까지는 뒷면의 칸 **자리 자체**를 `mirrorLayout`으로 뒤집어서,
  정렬을 좌측 상단으로 하면 앞면은 왼쪽에, 뒷면은 오른쪽에 몰렸다
  — 물리적으로 한 장을 뒤집어 인쇄하는 상황을 가정한 계산이었지만,
  사용자가 실제로 인쇄해보니 이게 오차로 이어졌다. 이제 뒷면도
  앞면과 **완전히 같은 칸 자리**를 쓴다(`ui/App.tsx`·`pdf/export.ts`
  둘 다 `mirrorLayout` 호출을 없앴다) — 정렬이 좌측 상단이면 앞뒤
  모두 좌측 상단으로 모인다. 칸 **안의** 안전영역·타공 안내는
  지금처럼 그대로 mirror로 뒤집는다(구멍은 종이를 뒤집으면 반대쪽에
  있어야 하므로) — 바뀐 건 칸을 그리드 안 "어디에" 배치하느냐뿐이다.

  **`mirrorLayout` 함수 자체는 지우지 않고 남겨뒀다** — 사용자가
  "내가 프린트해서 확인해볼게"라고 해서, 실제 인쇄 결과가 이 방향과
  다르면(예: 낱장 조합처럼 칸마다 다른 양식을 넣어 진짜 한 장을
  물리적으로 뒤집어 인쇄하는 경우, 칸 자리까지 뒤집어야 앞뒤가
  정확히 맞물릴 수 있다 — 그럴 땐 이번 변경이 오히려 안 맞을 수
  있다고 미리 알려뒀다) 되돌리기 쉽게 하기 위해서다. 브라우저에서
  양면 인쇄를 켜고 뒷면 탭을 봤을 때, 칸이 앞면과 똑같이 좌측
  상단에 모이고 안전영역·타공만 반대쪽으로 뒤집힌 것까지 확인했다.

### 20단계 — 글자 서식 사라짐·선 손잡이 오작동·최소 크기·200% 스크롤·도형 채우기

- **20a 완료 — 있는 글자를 고치고 나면 글꼴·굵게·회전이 기본값으로
  되돌아가던 것 고침.**

  `ui/gestures.ts`의 `editingFor`가 편집을 시작할 때 그 글자의 스타일을
  물려받는데, `font`·`bold`·`rotate`를 빠뜨리고 있었다. 이 값을
  놓치면 그냥 안 보이는 게 아니라 — `finishEditing`(EditorTab)이 이
  스타일로 객체를 통째로 새로 만들기 때문에 — 고치고 나면 실제로
  지워진다. `TextStyle`의 여섯 키(`size`·`align`·`valign`·`color`·
  `lineHeight`·`bold`·`font`·`rotate`)를 전부 챙기게 고쳤다.

- **20b 완료 — 그리기 모드에서 선의 끝점을 잡아끌면 상자(도형)로
  바뀌던 것 고침.**

  그리기 도구는 클릭-클릭이면 선, 클릭-드래그면 상자로 갈리는데
  (`onDown`의 기존 분기), 선의 끝점 손잡이를 잡는 검사(`handleAt`)가
  **지금 고른 것**(`lone`)에만 좁혀져 있었다 — 그리기 도구를 쓰는
  중엔 대개 아무것도 고른 상태가 아니라, 손잡이를 잡았어도 그냥
  "빈 데서 드래그 시작"으로 보여 새 상자를 만들어버렸다. `lone`과
  무관하게 모든 선을 뒤지는 `anyLineHandleAt`을 새로 만들고, 그리기
  도구의 `onDown` 맨 앞에서 먼저 확인하게 했다.

- **20c 완료 — 텍스트 상자·도형이 도트 한 칸(1mm)까지 작아지게, 글자
  내용을 고치는 동안은 상자 크기·글자 크기를 손대지 않게.**

  모서리 손잡이로 크기를 줄이는 `resizeBox`(core/objects.ts)의
  최소값이 `MIN_BOX_SIZE`(5mm) 하나로 달력·이미지·도형·글자상자
  전부에 공용이었다. `MIN_FREE_BOX_SIZE`(1mm)를 새로 만들고
  `resizeBox`에 `minSize` 인자를 받게 해, 텍스트 상자(`resizeTextBox`)와
  도형(`d.kind==='boxHandle'`에서 `isShape`인 경우)만 1mm까지, 달력·
  이미지는 그대로 5mm를 지키게 갈랐다.

  텍스트 입력칸(`TextInput`)의 `onChange`가 매 글자마다 `fitBox`로
  상자를 다시 재고 있었다 — **새로 만드는 글자**(`editing.id`가
  없음)에는 필요하지만, **있는 글자를 고칠 때**(`editing.id` 있음)는
  상자가 계속 흔들렸다. `editing.id`가 있으면 이 자동 맞춤을 완전히
  건너뛰게 했다 — "글자는 상자를 넘칠 수 있다, 상자는 정렬 기준일
  뿐 글자를 가두는 틀이 아니다"라는 원래 원칙 그대로다. 글자 크기
  자동조정도 이 호출 안에서만 일어나던 거라 같이 멈춘다.

- **20d 완료 — 200%로 확대했을 때 뒷면(왼쪽 캔버스) 맨 왼쪽까지
  스크롤이 닿지 않던 것 고침.**

  `.editor-canvas`가 `justify-content: center`로 속지를 가운데
  뒀는데, 확대해서 속지(뒤+앞 두 장, 양면이면)가 화면보다 커지면
  **넘친 양을 한쪽에만 있는 것처럼 계산해** 반대쪽(왼쪽) 끝까지
  스크롤이 안 닿는, flex 가운데 정렬의 잘 알려진 동작이다.
  `justify-content: center`를 없애고, 두 캔버스를 감싸는
  `.insert-sheets`에 `margin: 0 auto`를 줘서 대신 가운데 두게
  했다 — auto 여백은 넘친 양을 양쪽에 정확히 나눠 계산해 양쪽 다
  스크롤로 닿는다.

- **20e 완료 — 그리기 모드에서 만든 상자(도형) 안을 색+투명도로
  채울 수 있게.**

  `ShapeObject`에 `fillColor`·`fillOpacity`(0~1)를 추가했다 —
  안 정했으면 지금처럼 안 채운다(테두리만). `core/shape.ts`에
  `fillColorOf`(없으면 null)·`fillOpacityOf`(없으면 1)를 추가해
  화면(`ShapeLayer`)·PDF(`drawShapes`, pdf-lib `drawSvgPath`의
  `color`·`opacity` 옵션)가 같은 값을 쓰게 했다. 속성 막대
  (`ShapeControls`)에 "채우기" 체크박스를 추가 — 켜면 옆에 색
  고르기와 투명도 슬라이더(0~100%)가 나온다. 처음 켤 때 기본
  채우기색은 흰색(`SHAPE_FILL_COLOR`).

- **20f 미해결 — 이미지 가로·세로 숫자 입력칸에 쓴 값이 도트 간격
  값으로 되돌아간다는 신고.**

  속성 막대의 이미지 가로·세로 `NumField`(오늘 낮 17y에서 추가,
  당시 30→60 입력으로 확인된 기능)와 그 값을 저장하는
  `resizeObject`(store.ts)를 다시 훑었지만, 이 경로에는 도트
  격자에 맞추는 코드가 전혀 없다 — 입력한 값이 그대로 저장된다.
  전역 키보드 단축키(`onKey`)도 `input`/`select`/`textarea`에
  포커스가 있으면 건너뛰므로 간섭하지 않는다. 코드로는 원인을
  못 찾았고, [[user-verifies-in-browser]] 원칙에 따라 직접
  재현해보지 않았다 — 사용자에게 구체적 증상(입력칸 자체가
  되돌아간다)까지는 확인받았지만 정확한 재현 경로는 아직이다.
  다음에 다시 보고되면, 정확한 재현 순서(그린 직후인지 저장했다
  불러온 뒤인지, 도트 간격 값, 몇 번째 입력에서 그러는지)부터
  받는 게 좋겠다.

### 21단계 — 19g 되돌림: 양면 인쇄 뒷면 칸 자리, mirrorLayout으로 다시 뒤집기

- **21a 완료 — 낱장 조합(칸마다 다른 양식)을 M6로 실제 양면 인쇄해보니
  칸별 앞뒤가 뒤섞여 나왔다. 19g에서 없앤 `mirrorLayout`(뒷면 칸 자리
  뒤집기)을 되돌렸다.**

  사용자가 A·B·C·D 네 양식을 2×2로 조합해 자동 양면(긴 가장자리
  기준)으로 인쇄한 사진을 보내왔다 — 앞면은 A B / C D인데, 잘라서
  맞춰보니 뒷면은 B A / D C가 맞아야 하는데 실제로는 안 맞았다.

  코드를 다시 보니, 낱장 조합·세트형 둘 다 **뒷면 칸 `i`에는 앞면
  칸 `i`와 같은 양식의 뒷면**을 넣는데(`ui/App.tsx`), 19g가 칸
  **자리** 자체의 뒤집기(`mirrorLayout`)를 없애 앞뒤 칸 자리가
  완전히 같아졌다. 그러니 실제 인쇄에서 종이가 물리적으로 뒤집히는
  동안 **아무 보정도 없는** 상태였다 — 모든 칸 내용이 같은
  반복 인쇄에서는 이 문제가 전혀 안 보였을 뿐이다. `ui/PaperPreview.tsx`의
  `mirror` prop 주석("호출하는 쪽이 mirrorLayout으로 미리 뒤집어
  넘긴다")도 19g 때 안 고쳐진 채 그대로 남아 있던 것이 방증이다.

  **사용자에게 인쇄 방식을 확인**받았다 — 프린터 자동 양면, "긴 가장자리
  기준"(M6는 회전 배치가 아니므로 이 앱이 가정하는 그대로). 이 조합은
  9b 수정 5에서 이미 실제 인쇄로 확인됐던 바로 그 가정이다 — 19g는
  (브라우저 미리보기만으로) 다른 문제라고 판단해 되돌렸지만, 이번
  실제 인쇄로 그 판단이 틀렸다는 게 드러났다. `ui/App.tsx`·
  `pdf/export.ts`에서 뒷면 배치를 다시 `mirrorLayout(layout, width,
  height)`로 계산하게 되돌렸다(19g 커밋 diff를 그대로 역으로 적용) —
  칸 **내용** 배정 코드(인덱스 `i` 그대로 짝짓기)는 손대지 않았다,
  자리만 뒤집으면 인덱스는 원래도 맞는 계산이었다.

  `mirrorLayout` 자체의 좌우/상하 뒤집기 수학은 `core/geometry.test.ts`가
  이미 비회전(M6형)·회전(M5형) 배치 양쪽으로 자세히 검증해 두고 있어
  (19g 동안에도 안 건드려서 계속 통과하고 있었다), 어떤 칸 수·격자
  조합이든 이 공식 자체는 안전하다.

  **아직 실제 인쇄로 못 본 것 — "다른 사이즈"에서 진짜 확인이 필요한
  부분은 계산이 아니라 프린터 쪽 가정이다.** 이번엔 비회전 배치(M6)
  + 긴 가장자리 기준만 확인했다. 회전 배치(M5 등, 이 앱은 "짧은
  가장자리 기준"을 가정한다)로 낱장 조합을 양면 인쇄해서 칸별 앞뒤가
  맞는지는 아직 확인 전이다 — 아래 "아직 인쇄해보지 않은 값"에 추가.

- **21b 완료 — 글자 입력칸이 속지 왼쪽 바깥으로 벗어나 보이던 것 고침.**

  20d(200% 줌 스크롤 고치기)에서 앞뒤 SVG를 `.insert-sheets`라는
  새 div로 감쌌는데, 글자를 치는 동안 SVG 위에 겹쳐 놓는 진짜
  `<textarea>`(`TextInput`)는 여전히 `.editor-canvas`의 자식으로
  남아 있었다. `TextInput`은 자기 자리를 `svg.parentElement`(이제
  `.insert-sheets`)의 화면 좌표를 기준으로 계산하는데, `<textarea>`
  자신의 CSS `position: absolute` 기준(가장 가까운 `position:
  relative` 조상)은 여전히 `.editor-canvas`였다 — 계산 기준과 실제
  배치 기준이 서로 다른 조상을 가리켜, `.insert-sheets`를 가운데
  두려고 준 `margin: auto`만큼 입력칸이 왼쪽으로 밀려 보였다.
  `TextInput`을 `.insert-sheets` 안으로 옮기고, `.insert-sheets`에
  `position: relative`를 줘서 계산 기준과 배치 기준을 다시 같은
  요소로 맞췄다.

### 22단계 — "이미지 관리"를 "디자인 관리"로: 매소너리·이름 바꾸기·저장/최근 10개

- **22a 완료 — 이미지 관리 화면을 핀터레스트 스타일 매소너리로, 이름
  표시·변경을 더하고, "디자인 관리"로 개명해 글꼴 관리를 합쳤다.**

  `ui/ImagePickerDialog.tsx`의 `ImageLibraryDialog`(고정 4열 정사각
  크롭, 이름 텍스트 없음, 이름 변경 없음)를 걷어내고 새 파일
  `ui/DesignLibraryDialog.tsx`로 옮겼다 — 원본 비율 그대로 놓는
  매소너리는 칸 높이를 재는 계산 없이 CSS `columns`(다단)만으로
  만들었다(`break-inside: avoid`로 항목이 단 경계에서 안 잘리게).
  이름은 항목 아래 텍스트로 보이고, 더블클릭하면 고치는 칸이
  뜬다(`GalleryTab.tsx`의 양식 카드 이름 바꾸기와 같은 상호작용).
  `Modal`(GalleryTab.tsx)에 `size="large"` 옵션을 추가해 이 창만
  화면을 거의 다 쓰게 하고(`.modal-large`), 기존 "이미지 고르기"
  팝업(빈 이미지 상자 더블클릭 시)은 원래 크기·정사각 그리드
  그대로 남겨뒀다 — 빨리 고르는 자리와 훑어보는 자리는 다르다.

  같은 창 안에 "이미지"/"글꼴" 탭을 추가했다. 글꼴 탭도 이름을
  더블클릭해서 바꿀 수 있다 — 자기 글꼴로 미리보기 글자("가나다
  ABC")를 보여준다. 두 종류 다 `images/registry.ts`·`fonts/registry.ts`의
  `UserImage`·`UserFont`에 `label?`(안 정했으면 원래 파일 이름
  `name`을 그대로 쓴다 — `imageLabelOf`·`fontLabelOf`)을 추가하고,
  바꾼 이름은 IndexedDB에도 다시 남긴다(`persistImageMeta`·
  `persistFontLabel` — 파일 바이트는 그대로 두고 부가정보만
  갈아 끼운다). 속성 막대(StyleBar.tsx)의 이미지·글꼴 드롭다운도
  이제 `label`을 보여줘서, 이름을 바꾸면 그 목록에도 바로 반영된다.

  **되살리기(orphan) 매칭 기준인 `name`(원래 파일 이름)은 안
  건드렸다** — 표시용 `label`만 새로 얹었다. 저장 파일을 다시 열고
  같은 파일을 재등록하는 경로가 파일 이름으로 짝을 맞추므로, 여기를
  건드리면 이름을 바꾼 이미지·글꼴이 다시 안 살아날 수 있었다.

- **22b 완료 — 디자인 관리에서 명시적으로 저장하지 않은 이미지는
  "최근 사용" 10개까지만 남고 나머지는 자동으로 지운다.**

  `UserImage`에 `saved?`·`usedAt?`을 추가했다. 새 파일을 등록하면
  기본은 저장 안 된 "최근 사용" 상태이고(`registerImage`가 등록
  순간을 `usedAt`으로 남긴다), 디자인 관리의 "☆ 저장" 버튼을 눌러야
  `saved: true`가 되어 영구히 남는다(`saveUserImage`/`unsaveUserImage`).
  이미지를 다시 씌우는 것(`styleImage`)도 "쓴다"로 쳐서 `usedAt`을
  다시 지금 시각으로 올린다 — 오래전에 등록해놓고 방금 다시 꺼내
  쓴 이미지가 곧바로 지워지면 안 되기 때문이다.

  `store.ts`에 `evictExcessRecent`를 추가했다 — 저장 안 하고 **지금
  어느 오브젝트에서도 안 쓰는 중인** 이미지가 10개(`RECENT_IMAGE_CAP`)를
  넘으면 `usedAt`이 오래된 것부터 지운다. "지금 쓰는 중"인지는
  기존 `removeUserImage`가 이미 하던 검사(`imageInUse`로 이름 붙여
  공용화)를 그대로 재사용한다 — 화면에 그려진 이미지가 개수 제한
  때문에 갑자기 사라지는 일은 없다. `addUserImage`(새로 등록할 때)와
  `unsaveUserImage`(저장을 끌 때) 양쪽에서 이 정리를 거친다.

  이름 바꾸기·저장 켜기와 마찬가지로 `store.test.ts`에 케이스를
  붙였다 — 10개 넘으면 가장 오래된 것부터 지워지는지, 저장한
  것과 쓰는 중인 것은 아무리 오래돼도 안 지워지는지, 이름 바꾸기·
  저장 토글이 제대로 도는지. 사용자 데이터를 스스로 지우는 로직이라
  브라우저 확인보다 이 테스트를 우선으로 삼았다.

  **글꼴에는 이 저장/최근 10개 규칙을 적용하지 않았다** — 이번엔
  이미지에만 적용하기로 정했다(사용자 확인). 글꼴은 이름 바꾸기만
  된다.

- **22c 완료 — 써보고 나온 것들: 스크롤이 아래까지 안 닿던 것, 이름
  글자가 너무 진했던 것, 저장·삭제 버튼 글자 크기.**

  `.design-grid`(이미지 매소너리)와 `.font-manage-list`가 각자
  `max-height` + `overflow-y: auto`로 **안쪽에 따로** 스크롤 영역을
  갖고 있었다 — 창 전체(`.modal`)도 스크롤이 있는데 스크롤 영역이
  이중으로 겹쳐, 안쪽 그리드를 다 내려도 그 아래 "파일 추가" 버튼·
  오류 문구까지는 안 닿았다. 안쪽 `max-height`·`overflow-y`를 없애고
  창 전체 스크롤 하나로 합쳤다.

  이름(`.design-name`)을 `var(--ink)`(진한 색)에서 `var(--muted)`
  (연한 회색)로 낮췄다. "☆ 저장"·"삭제" 버튼(`.design-actions`)은
  양식 카드의 "이름·복제·삭제" 버튼(`.card-actions .ghost`)과 같은
  크기(11px)로 맞췄다 — 이 창만 유난히 큰 글자로 떠 있었다.

- **22d 완료 — 진짜 문제는 글자 색이 아니라 `.design-name` 버튼의
  진한 초록 배경이었다. 이미지를 더 작게, 글꼴 목록을 한 줄에 3개로.**

  22c에서 이름 글자 색만 옅게 바꿨는데도 여전히 "진하다"는 말을
  들었다 — `.design-name`이 `<button>`인데 `background`·`border`를
  따로 정해두지 않아서, 이 앱의 버튼 기본값(`button { background:
  var(--accent) }`, 진한 초록)을 그대로 물려받고 있었다. 글자만
  연한 회색으로 바뀌었을 뿐, 뒤에는 여전히 진한 초록 막대가 버튼
  모양 그대로 깔려 있었던 것 — 이미지 탭·글꼴 탭 둘 다 이름을
  `.design-name`으로 그려서 똑같이 나타났다. 양식 카드의 이름
  버튼(`.card-name`)이 이미 이 문제를 `background: none; border:
  0`으로 풀어뒀던 것과 같은 방식으로 맞췄다.

  `.design-grid`의 다단 수를 4에서 6으로 늘려 이미지를 더 작게
  줄였다. `.font-manage-list`는 세로로 쌓던 것을 `grid-template-
  columns: repeat(3, 1fr)`로 바꿔 한 줄에 셋씩 늘어놓는다.

- **22e 완료 — 매소너리를 그만두고 사진 크기와 무관한 고정 칸으로,
  창 크기 자체도 고정.**

  사진 크기(원본 비율)에 따라 매소너리 칸 높이가 들쭉날쭉해지면서
  창 전체 높이도 같이 흔들렸다 — "화면 크기가 안 바뀌게 해달라"는
  요청. 두 가지로 풀었다.

  1. `.modal-large`의 `max-height`를 `height`로도 같이 준다 —
     안에 사진이 몇 장이든, 크기가 어떻든 창 자체는 늘 화면 세로의
     같은 비율을 차지한다. 안 채워진 만큼은 빈 채로, 넘치면
     (`.modal`의 기존 `overflow: auto`로) 스크롤만 생긴다.
  2. `.design-grid`를 CSS 다단(masonry)에서 보통의 정사각 그리드로
     바꿨다 — 칸 크기가 사진마다 다르지 않고 늘 같다. `object-fit:
     cover`(잘라서 채움) 대신 `contain`을 써서 사진을 잘라내지
     않고 칸 안에 통째로 보여준다(남는 자리는 배경색).

  이제 안 쓰는 `.design-item`(매소너리 전용 `break-inside` 규칙)은
  CSS·JSX 양쪽에서 걷어냈다.

### 23단계 — 인쇄하기 탭: 확대·여러 장 스크롤·양면 나란히 (1단계)

인쇄하기 탭을 직접 편집 가능하게 만드는 큰 작업을 2단계로 나눴다 —
지금은 1단계(확대·여러 장 스크롤·양면 나란히, 아직 읽기 전용)만
끝났다. 2단계(칸을 클릭해 그 자리에서 그리기)는 "클릭한 칸 하나만
활성화"하는 방식(양식 만들기의 activeId·side를 그대로 재사용, 다른
칸은 정적으로만 보임)으로 하기로 사용자와 확인했고, 아직 시작 전이다.

- **23a 완료 — 확대(줌) + 여러 장을 한꺼번에 쌓아 스크롤 + 양면을
  나란히(좌우 순서 토글).**

  **구조가 다른 두 가지 크기 계산 방식을 하나로 합쳤다.** 양식
  만들기(EditorTab)는 "내용 크기를 배율만큼 키우고 컨테이너가
  스크롤"하는 방식(zoom state → `scale = zoom/100 × PX_PER_MM_AT_100`
  → svg width/height px)이고, 인쇄하기 탭은 지금까지 "뷰포트 높이에
  못박힌 상자에 aspect-ratio로 맞춰 축소 표시"하는 정반대 방식이었다
  (`.preview-wrap`). 여러 장을 쌓고 확대하려면 첫 번째 방식이어야
  해서, 인쇄하기 탭도 같은 계산(`ZOOMS`·`PX_PER_MM_AT_100`, 이제
  `ui/pixels.ts`에 모아 두 탭이 같이 쓴다)으로 바꿨다.

  **"지금 보는 장 하나"(previewPage)를 넘겨보던 것을 없애고, 모든
  장을 한꺼번에 계산해 쌓는다.** `App.tsx`의 칸 내용 계산을
  `overridesForSheet(sheet)` 함수로 뽑아내 장 번호를 인자로 받게
  했다 — 낱장 조합은 장마다 내용이 같아(comboSheets는 같은 배치의
  반복) 한 번 계산한 값을 그대로 돌려주고, 반복 인쇄·세트형은 장마다
  달라서 그때그때 계산한다. `RepeatPrint`(장 넘기기 버튼)는 이제
  필요 없어져 요약 문구만 남기고 넘기기 UI는 뺐다.

  **양면을 나란히 두고, 어느 쪽이 왼쪽인지 토글로 고른다.** "양면
  인쇄" 토글 옆에 "뒷면을 왼쪽에" 체크박스를 뒀다(로컬 state,
  store에는 안 넣는다 — 그냥 화면에서 어떻게 보고 있는지일 뿐).
  기존 앞면/뒷면 탭 전환은 없앴다 — 나란히 보이니 전환할 이유가
  없어졌다.

  **아직 실제로 안 그려본 것 — 세트형 쪽수가 아주 많을 때(수백 쪽)의
  속도.** 지금은 화면에 보이든 안 보이든 스크롤 안의 모든 장을 다
  계산해서 그린다(가상 스크롤 없음) — 세트형에서 자동 필드를 실제
  값으로 바꾸는 계산(`resolveObjectsForPage`)이 장마다 도는데,
  쪽수가 수백 개면 느려질 수 있다. 필요해지면 화면에 보이는 장만
  그리는 가상화를 붙인다. 몇 장 안 되는 보통 경우엔 문제없을 것으로
  보이지만 실제로 큰 세트형으로 확인은 안 했다.

- **23c 완료 — 2단계: 양식 만들기에 "속지/용지" 보기를 나눠, 용지
  전체를 보면서 직접 그릴 수 있게 했다.**

  처음엔 "인쇄하기 탭에서 직접 그리기"로 설계했지만, 조사해보니
  양식 만들기(EditorTab)의 그리기 코드 전체가 "지금 활성 양식
  하나·앞뒤 한쪽"(`activeId`+`side`)에만 의존하게 짜여 있어서, 인쇄
  탭에 옮기려면 그 코드를 통째로 재사용 가능한 형태로 떼어내야 했다
  — 그러면 기존 양식 만들기 화면과 둘로 나뉘어 언젠가 어긋날 위험이
  있었다. 대신 사용자 제안대로 **양식 만들기 화면 자체를 "속지"/
  "용지" 두 보기로 나눴다** — 그리기 코드는 그대로 두고, 보여주는
  좌표계만 "칸 하나"에서 "용지 전체"로 바꾸는 방향이라 훨씬 안전했다.

  **핵심은 "용지 mm ↔ 칸 속지 mm" 왕복 변환 하나다.** 칸 하나의
  배치는 이미 `core/place.ts`의 `placeSlot`(속지 mm → 용지 mm)이
  갖고 있었다 — 화면(PaperPreview)·PDF가 이미 이 함수로 칸을
  그린다. 그 반대 방향(용지 mm → 속지 mm)이 없어서 `unplaceSlot`을
  새로 추가했다(테스트로 `placeSlot`과 왕복이 정확히 맞는지 확인).
  이 하나만 있으면, **그 이후의 그리기 로직(스냅·손잡이·마퀴 등)은
  전혀 손대지 않고 그대로 재사용된다** — `rawMm`(마우스 자리 →
  좌표)만 "용지 모드면 용지 mm를 구해서, 지금 활성 칸이면
  `unplaceSlot`으로 속지 mm로 한 번 더 바꿔 돌려준다"로 감쌌다.
  "속지" 모드에서는 이 감싸기가 그대로 통과라(`viewMode !== 'paper'`
  이면 바로 반환), 기존 동작은 코드 경로 자체가 완전히 같다 — 지금
  가장 많이 쓰는 화면을 건드리지 않는다는 원칙을 지켰다.

  **어느 칸이 "활성"(손댈 수 있는)인지는 인쇄하기 탭과 같은 계산을
  쓴다.** 낱장 조합은 `resolveSlotTemplates`가 정한 칸마다 다른
  양식 중 지금 활성 양식과 같은 id인 칸만 활성, 반복 인쇄·세트형은
  모든 칸이 같은 양식을 보여주므로 전부 활성이다. 활성이 아닌 칸을
  클릭하면(`onDown`이 제일 먼저 확인한다) 그리지 않고 그 칸의
  양식으로 옮겨간다(`selectTemplate`) — 이게 앞서 확인받은 "클릭한
  칸 하나만 활성화" 방식이다. `App.tsx`와 계산 방식이 갈리지
  않도록, 인쇄 방식 판정(`printModeOf`)을 `core/template.ts`로
  뽑아 두 화면이 같이 쓰게 했다.

  **드래그 중에는 시작한 칸에 좌표 기준을 고정한다**(`gestureSlotRef`)
  — 안 그러면 손이 살짝 옆 칸으로 넘어가는 순간 좌표 기준이 통째로
  바뀌어 그리던 것이 엉뚱한 자리로 튄다. 드래그가 끝나면
  (`setDragBoth(null)`) 자동으로 풀려서, 다음 호버부터는 다시
  마우스를 따라 칸을 찾는다.

  **회전 배치(M5 등)도 처음부터 지원한다.** `unplaceSlot`이 `placeSlot`과
  정확히 반대 계산이라 좌표 자체는 문제없고, 글자를 입력하는 동안
  뜨는 진짜 `<textarea>`(SVG가 아니라 HTML이라 SVG의 `<g transform>`을
  못 받는다)의 자리도 맞춰야 했다 — `placeSlot`이 낸 SVG
  `matrix(a b c d e f)`를 CSS `matrix()`로 그대로 옮기고(이동량만
  mm→px로 바꿔서) 입력칸을 감싸는 바깥 `<div>`에 얹었다. SVG
  `matrix()`와 CSS `matrix()`가 같은 여섯 숫자 규칙을 쓰기 때문에
  가능했다.

  **아직 실제로 안 써본 것들 — 다음에 확인이 필요하다.**
  - 회전 배치 용지 모드에서 실제로 그려보고, 글자 입력칸 자리·기울기가
    맞는지. 좌표 계산은 테스트(`unplaceSlot` 왕복)로 확인했지만, 화면
    렌더링까지 실제로 눌러본 것은 아니다.
  - 반복 인쇄·세트형처럼 칸이 아주 많을 때(수십~수백 칸), 모든 칸이
    "활성"이라 전부 인터랙티브 레이어(고른 표시·손잡이 등)를 그리는데
    — 칸 수가 많으면 느려질 수 있다. 낱장 조합처럼 일부만 활성인
    경우는 이 부담이 없다.
  - 용지 여백(어느 칸에도 안 속한 자리)을 클릭했을 때 지금은 아무
    일도 안 한다 — 고른 것을 해제하는 동작 정도는 나중에 추가할 수
    있다.

### 24단계 — 23c 방향 뒤집기: "용지" 편집은 인쇄하기 탭으로, 손댄 자리는 원본 양식을 건드리지 않는다 (1단계: 데이터 모델)

23c에서 만든 "양식 만들기 > 용지" 보기를 사용자가 실제로 써보니 두 가지
문제가 나왔다.

- **화면에 맞춤 줌이 하나도 안 맞았다.** 원인은 `stageWidth`를 재는
  `ResizeObserver`를 붙이는 `useEffect`의 의존성 배열이 `[]`였던
  것 — 앱의 첫 화면은 `tab === 'gallery'`라 그 순간엔 `.stage-area`
  (`tab === 'print'`일 때만 그려짐)가 아직 없어서 `stageRef.current`가
  `null`이었고, 한 번 실패한 뒤로는 다시 시도하지 않았다. `stageWidth`가
  영원히 0으로 남아 `fitScale`이 폴백값(`PX_PER_MM_AT_100`, 즉 실물
  100% 크기)을 계속 썼다 — A4 한 장(양면이면 두 장)이 화면에 그대로
  넘쳤던 이유다. 의존성 배열을 `[tab]`로 바꿔, 인쇄하기 탭으로 옮길
  때마다 다시 시도하게 고쳤다.
- **더 근본적으로, "용지" 보기가 실제 쓰임새와 안 맞았다.** 한 용지에
  여러 양식을 낱장 조합으로 배치했을 때, 양식별로 다른 부분(자동
  필드·칸마다 다른 이미지 등)을 고치고 싶은데 "양식 만들기"는 한 번에
  양식 하나에만 초점이 맞아 있어 다른 칸들을 한눈에 볼 수가 없었다.
  이미 여러 장을 한꺼번에 쌓아 보여주는 "인쇄하기" 탭(23a)이 이
  "전체를 보면서 문제 있는 자리만 고친다" 용도에 훨씬 맞는 자리라는
  데 합의했다 — **"용지" 보기는 인쇄하기 탭으로 옮긴다.**

  옮기면서 범위가 한 번 더 커졌다. 세트형(같은 양식을 반복해서 수백
  쪽을 찍는 방식)에서도 "이 페이지 하나만" 이미지나 글자를 다르게
  하고 싶을 수 있는데, 지금 구조는 모든 페이지가 하나의 공유 양식에서
  100% 계산되어 나와 페이지별로 남는 상태가 전혀 없다 — 이것도
  하기로 확인했다. 그리고 인쇄하기에서 고친 내용은 **"최종 수정"**으로
  두기로 했다 — 이전에 만든 양식(원본)은 절대 바뀌지 않는다. 이건
  낱장 조합 칸 편집에도 똑같이 적용된다 — 원래는 칸을 고치면
  `activeId`를 바꿔 원본 양식을 직접 수정하게 설계했었는데, 이번
  합의로 그것도 뒤집혔다.

  **모델은 파워포인트의 "슬라이드 마스터 vs. 개별 슬라이드 수정"과
  같다.** 안 고친 페이지·칸은 원본 양식을 그대로 따라가며 계속
  자동 계산되어 보인다. 손대는 순간 그 페이지·칸만 원본에서
  "떨어져 나가" 독립된 내용을 갖는다 — 그 뒤로 원본 양식이 바뀌어도
  따라가지 않는다. (되돌리기로 다시 원본을 따르게 하는 기능은 아직
  안 만들었다 — 다음 단계.) 오브젝트 하나하나의 속성까지 따로
  추적하는 훨씬 정교한 방식도 가능은 하지만 훨씬 복잡해지므로,
  "페이지·칸 전체를 통째로 떼어낸다"는 이 단순한 모델로 정했다.

  **이번 조각(1단계)은 데이터 모델과, 화면·PDF가 그 데이터를 읽는
  부분까지만이다** — 인쇄하기 탭에서 실제로 클릭해서 고치는 UI는
  아직 없다.

  - `Template`에 `pageOverrides`·`pageBackOverrides`
    (`Record<쪽번호, DiaryObject[]>`)를 추가했다 — 세트형은 페이지
    묶음(`repeat.dataset`)이 이미 양식에 속한 값이라 여기 같이 뒀다.
  - `Settings`에 `comboSlotOverrides`·`comboSlotBackOverrides`
    (`Record<칸번호, DiaryObject[]>`)를 추가했다 — 낱장 조합의 칸
    배정(`slotAssignment`)이 이미 이 자리(양식이 아니라 인쇄 작업
    전체)에 속한 값이라 같이 뒀다.
  - `core/format.ts`에 `resolvePageObjects(objects, dataset, page,
    overrides?)`를 새로 뒀다 — `overrides?.[page] ?? resolveObjectsForPage(...)`.
    손본 페이지가 있으면 그 내용을 그대로, 없으면 지금까지처럼
    자동 계산한다. **화면(`App.tsx`)·PDF(`exportPdf`) 둘 다 이 함수
    하나만 거친다** — "화면과 PDF는 같은 값을 두 번 적지 않는다"는
    원칙 그대로다.
  - 뒷면 손본 내용은 **원본 양식에 뒷면이 아예 없어도** 그대로 쓴다 —
    한 번 "최종 수정"이 생기면 원본의 모양과 무관하게 그게 맞는
    내용이 된다는 원칙을 그대로 따른 것. 낱장 조합·세트형 양쪽 다
    `objects: 원본.back ? resolvePageObjects(...) : backOverride!`
    형태로 처리했다.
  - store에 `setComboSlotOverride`/`clearComboSlotOverride`/
    `setPageOverride`/`clearPageOverride` 네 액션을 추가했다. 앞면·
    뒷면을 하나의 함수로 묶으려고 `side === 'back' ? 'xBackOverrides'
    : 'xOverrides'`로 계산한 키 이름을 그대로 갱신 객체의 키로 쓰는데,
    삼항연산자의 결과 타입이 문자열 리터럴 유니온이라 타입스크립트가
    문제없이 받아들인다.

- **24b 완료 — 그림자 양식, store 쪽 기반.** 처음엔 `activeId`를
  그림자 양식으로 돌리는 방식을 생각했는데, 코드를 보니 `activeId`는
  인쇄하기 화면 전체(어느 인쇄 작업을 보여줄지, 장수·배치·설정
  요약까지 전부)가 함께 보는 값이라 그렇게 하면 칸 하나 손보는
  동안 인쇄하기 화면 전체가 그 그림자 양식의(엉뚱한) 인쇄 작업으로
  바뀌어 버린다 — "나머지는 그대로 보여야 한다"는 요구와 정면으로
  부딪힌다. 그래서 `activeId`는 전혀 건드리지 않고, 대신 store에
  별도로 `shadowTemplate`(그림자 양식 자신, 없으면 `null`)과
  `shadowSide`(그중 지금 손보는 쪽)를 뒀다.

  **그리기 액션들은 하나도 안 고쳤다.** `drawLines`·`commitText`·
  `moveSelected`처럼 실제로 그리는 액션 ~15개가 전부 거쳐 가는
  좁은 문(`patchActiveSide`·`activeObjects` 두 함수, `commitObjects`는
  그중 하나를 감쌀 뿐이다) 안에서만 "`shadowTemplate`이 있으면
  `activeId` 대신 그쪽을 고친다"는 분기 하나를 넣었다 — 그래서
  각 액션의 코드는 여전히 이 두 함수를 그대로 부를 뿐, 어디를
  고치는지는 전혀 모른다. `undo`·`redo`는 이 두 함수를 거치지
  않고 `activeTemplate(s)`를 직접 읽는 자리가 따로 있어서(앞뒤
  경계를 넘어 이은 선의 "짝 되돌리기" 때문에 더 복잡한 코드다)
  맨 앞에 같은 뜻의 분기를 하나 더 추가했다 — 그림자 세션은 한
  번에 한 쪽만 손보므로 경계를 넘는 선이 생길 수 없어, 그 분기는
  짝 찾기 없이 바로 되돌리고 끝난다.

  `beginShadowEdit(insert, dotGrid, objects, side)`로 시작하고
  `endShadowEdit()`으로 버린다(결과를 `comboSlotOverrides`/
  `pageOverrides`로 옮기는 것은 부르는 쪽의 몫 — 아직 그 부르는
  쪽을 안 만들었다). **`shadowTemplate`은 `templates` 배열에
  안 들어간다** — 그래서 "양식 관리" 목록에서 숨기는 작업도,
  저장 파일에서 빼는 작업도 따로 필요 없다(애초에 그 배열에
  없으니까).

  테스트 5개로 확인했다: 시작하면 준 내용으로 채워지고 진짜
  `templates`엔 안 들어간다는 것, 그림자를 손보는 동안 진짜
  활성 양식은 안 바뀐다는 것, 뒷면 세션은 그림자의 `back`에
  담긴다는 것, 실행취소·다시실행이 그림자 세션 안에서만 도는
  것, 끝내면 사라지고 그 뒤로는 다시 진짜 양식이 바뀐다는 것.
  기존 store 테스트 전부(86개)도 그대로 통과한다 — `shadowTemplate`
  기본값이 `null`이라 지금까지의 동작은 손 하나 안 댄 것과 같다.

  **아직 안 한 것 — 다음 단계가 진짜 큰 작업이다.** 인쇄하기
  탭에서 칸·페이지를 실제로 클릭해 그리는 UI 자체가 없다. 계획은
  EditorTab의 "속지"(칸 하나만 보여주는, 양면 나란히는 뺀) 모드의
  그리기 코드를 본떠 새 컴포넌트를 만들고, 그 컴포넌트의 SVG를
  인쇄하기 탭의 그 칸 자리에 `placeSlot`의 변환을 CSS `matrix()`로
  옮겨(`TextInput`이 이미 쓰는 방식과 같다) 겹쳐 놓는 것이다 — 이
  렇게 하면 SVG의 `getScreenCTM()`이 회전·이동을 전부 알아서
  처리해줘서 `unplaceSlot` 없이도 좌표가 바로 그 칸의 속지 mm로
  나온다. 나머지 칸·장은 지금처럼 `PaperPreview`가 그대로 그려서
  "다른 양식을 한 번에 본다"는 요구를 만족한다. 클릭해서 편집을
  시작·끝내는 것, 손본 칸·페이지에 표시(배지)를 다는 것, 되돌리기
  버튼도 이 단계에 들어간다.

- **24c 완료 — 인쇄하기 탭에서 실제로 칸·페이지를 클릭해 그리는 화면.**
  계획대로 만들었다. 새 컴포넌트 `PrintSlotEditor`는 `EditorTab`의
  "속지" 모드(양면 나란히·용지 모드는 뺀, 칸 하나만 그리는 가장 단순한
  형태) 그리기 코드를 그대로 옮겨왔다 — `store.ts`가 `shadowTemplate`이
  있으면 그리기 액션의 대상을 알아서 바꿔주므로, 이 컴포넌트는 자기가
  낱장 조합 칸을 손보는지 세트형 페이지를 손보는지 전혀 모른다.

  **자리 잡기는 `placeSlot`의 SVG `matrix()`를 CSS `matrix()`로 그대로
  옮기는 방식**(TextInput이 이미 쓰던 방식)이라 `unplaceSlot`을 부를
  필요가 없었다 — `getScreenCTM()`이 회전·이동을 전부 알아서 반영해줘서
  마우스 좌표가 곧바로 그 칸의 속지 mm로 나온다. 도구막대는 회전된
  wrapper 밖에서 칸의 axis-aligned 바깥 상자 기준으로 따로 자리를
  잡는다(회전과 무관하게 항상 똑바로 서 있어야 하므로).

  **`PaperPreview`에 `onSlotClick`을 추가했다.** 칸을 클릭하면 그 칸
  번호와 함께, `PaperPreview`가 렌더링에 실제로 쓴 `content`(override로
  이미 골라진 값)를 그대로 넘겨준다 — 클릭한 쪽(App.tsx)이 다시 계산하지
  않는다, "화면에 보이는 것과 손보기 시작하는 것이 항상 같아야 한다"는
  원칙 그대로다. 반복 인쇄(만년형)는 모든 칸이 완전히 같은 내용이라
  "이 칸만" 손본다는 개념이 없어 클릭할 수 없게 뺐다.

  **버그를 하나 잡았다 — `useInsert`·`useDotGrid`를 그림자 인식으로
  바꿨다가 되돌렸다.** 처음엔 `useObjects`처럼 이 둘도 그림자를 먼저
  보게 했는데, `SettingsPanel`이 인쇄하기 탭에서도 함께 떠 있어서
  (`App.tsx`의 `tab !== 'gallery' && <SettingsPanel />`) 칸을 손보는
  동안 그 패널이 그림자의 속지 설정을 보여주면서 실제로 고치면(`patchInsert`
  등은 그림자를 모른다) 진짜 활성 양식이 바뀌는 어긋난 상태가 됐다.
  `useObjects`만 그림자를 보게 남기고(StyleBar가 이걸로 재사용된다),
  `PrintSlotEditor`의 insert·격자는 `shadowTemplate`을 직접 읽게 바꿔서
  해결했다.

  **또 하나 — "완료"를 눌렀는데 방금 입력하던 글자가 override에 안
  담기는 문제.** "완료"는 `finishEditing()`(글자를 store에 커밋)을
  부른 바로 뒤에 `onFinish`(override로 옮겨 담기)를 부르는데, 옮겨
  담는 쪽이 `useStore()`로 렌더 시점 스냅샷을 읽고 있어서 방금 커밋한
  글자를 못 봤다. `useStore.getState()`로 그 자리에서 바로 읽게 고쳤다
  (App.tsx가 새로고침 뒤 글꼴·이미지를 복원할 때 이미 쓰던 것과 같은
  패턴이다).

  **열어만 보고 안 고쳤으면 override를 만들지 않는다.** 그림자의
  내용이 씨앗(seed)과 참조가 같으면(한 번도 `commit`이 안 불렸으면
  `initHistory`가 원본 배열을 그대로 물고 있다) 그냥 손 안 대고
  끝낸 것으로 보고 override를 안 남긴다 — 안 그러면 클릭해서 열어만
  봐도 그 칸·페이지가 원본 양식을 더는 안 따라가게 되어 놀란다.

  **아직 안 한 것.** 손본 칸·페이지의 표시(배지), 되돌리기 버튼(store
  액션 `clearComboSlotOverride`·`clearPageOverride`는 이미 있지만
  부르는 UI가 없다), 그리고 23c의 "속지/용지" 보기 걷어내기 — 이
  화면이 실제로 써 보고 괜찮다고 확인된 다음으로 미룬다.

- **24d 완료 — 24c를 실제로 써보고 나온 문제 둘을 고쳤다.**

  **도구막대가 칸을 따라다녀서 스크롤할 때마다 화면 밖으로 나갔다**
  (사용자 피드백, 스크린샷으로 확인). `.print-sheet-page`(칸이 있는
  그 장) 기준으로 자리를 잡던 것을, `createPortal`로 완전히 다른 자리
  — `.print-bar` 바로 아래, `.stage-area`(스크롤되는 영역) 밖 — 에
  그리도록 바꿨다. App.tsx가 그 자리로 쓸 빈 `<div>`(`editBarSlot`)를
  두고, `PrintSlotEditor`는 도구막대 JSX를 그 자리로 포털링만 한다 —
  EditorTab의 `.editor-bar`와 같은 원리(스크롤 영역 밖에 있어 늘 같은
  자리)를 다른 컴포넌트 트리 위치에서 재현한 것이다. 첫 렌더에는 그
  `<div>`가 아직 커밋 전이라 포털 대상이 없을 수 있어, `ref` 콜백 대신
  `useState`로 받아 DOM이 실제로 생기면 그때 다시 그려지게 했다.

  **빈 칸(도트 사이 공간)을 클릭하면 반응이 없었다** — 브라우저에서
  직접 눌러보다가 발견했다(처음엔 프로그램으로 클릭 이벤트를 흉내내서
  "됐다"고 착각할 뻔했는데, `dispatchEvent`는 실제 좌표의 무엇이
  그려져 있는지와 무관하게 대상에 바로 꽂히는 반면 진짜 클릭은
  `elementFromPoint`처럼 그 자리에 실제로 칠해진 것만 받는다 —
  `document.elementFromPoint`로 확인해보니 빈 칸을 눌렀을 때 맨 아래
  `<rect class="paper">`가 대신 받고 있었다). SVG는 `fill`이 없는
  자리는 클릭을 받지 않는다 — 칸 전체에 안 보이는 `fill="transparent"`
  판을 깔아서(`fill="none"`과 다르다, transparent는 안 보이면서도
  클릭은 받는다) 칸 어디를 눌러도 걸리게 했다.

  둘 다 브라우저에서 직접 눌러보지 않았으면 몰랐을 문제라, 이번엔
  예외적으로 직접 클릭해가며 확인했다(보통은 사용자가 확인한다).

- **24e 완료 — 손본 칸·페이지 표시(배지) + 되돌리기.**

  **배지**는 `PaperPreview`가 그린다. `PreviewSlotContent`에
  `overridden?: boolean`을 더해서, App.tsx가 override 맵을 채울 때
  (`comboSlotOverrides`/`pageOverrides`가 실제로 있는 칸·페이지만)
  true로 표시한다 — 낱장 조합에서 **다른 양식이 배정됐을 뿐**인 칸은
  `previewOverrides`엔 들어가도(그려야 하니까) `overridden`은 아니다,
  배지는 "직접 손봤다"는 뜻이지 "기본값과 다르다"는 뜻이 아니어서다.
  회전 배치에서도 늘 같은 모서리(용지 기준 오른쪽 위)에 뜨도록, 칸이
  도는 안쪽 `<g>` 밖(용지 좌표 그대로)에 작은 점을 찍는다. 인쇄되지
  않는 화면 전용 표시라 `mode`(실제 인쇄 모습만 보기)와 무관하게 늘
  보인다 — 어느 칸을 손봤는지는 인쇄 미리보기 중에도 계속 알아야 하는
  정보라고 판단했다.

  **되돌리기**는 `PrintSlotEditor` 도구막대에 버튼으로 뒀다 —
  `clearComboSlotOverride`/`clearPageOverride`(이미 있던 액션)를
  부르고 그림자를 그냥 버린다("완료"의 `commitEdit`과 정반대: 그림자의
  지금 내용을 override로 옮기는 대신, 있던 override 자체를 지운다).
  손보기를 시작할 때 이미 override가 있었는지(`EditingSession.
  hadOverride`)로 버튼을 보일지 정한다 — 지금 막 새로 손보기 시작한
  칸(아직 override가 없는)에는 되돌릴 게 없으니 안 보인다. 확인창은
  안 뒀다 — "지우기"·"뒷면 지우기"처럼 이 프로젝트가 이미 확인창 없이
  두는 것들과 같은 결로 맞췄다.

  브라우저에서 직접 확인했다: 배지가 뜬 칸에 들어가면 도구막대에
  "되돌리기"가 보이고, 누르면 배지가 사라지며 원본 양식 내용으로
  돌아간다. 옆 칸의 override는 그대로다.

- **24f 완료 — 23c의 "속지/용지" 보기를 걷어냈다.** 인쇄하기 탭에서
  칸·페이지를 직접 손보는 기능(24b~24e)이 자리를 대신하면서 필요
  없어졌다(사용자 확인).

  `EditorTab`에서 `viewMode` 상태·"속지"/"용지" 탭·"용지" 전용 계산
  (`activeTemplateFull`·`printMode`·`paperLayout`·`slotTemplates`·
  `isActiveSlot`·`slotAt`·`switchToSlot`·`staticSlotContent`)·칸 추적용
  ref(`gestureSlotRef`·`editingSlotRef`)를 전부 지웠다. `rawMm`은 다시
  `rawSvgMm`을 그대로 돌려주는 단순한 함수로 돌아갔다("속지" 모드에서는
  원래도 그 뜻이었다).

  **`TextInput`의 `paperSlot`도 같이 지웠다.** 이 컴포넌트를 부르는
  두 곳(`EditorTab`의 "속지" 모드, `PrintSlotEditor`) 다 이제 늘
  `null`만 넘기던 참이라 — 그림자 편집 캔버스도 자기 바깥 `<div>`가
  이미 칸의 자리·회전을 다 잡아둔 채라, `TextInput`은 `svg.
  parentElement` 기준으로 `box.x·y`만 더하면 되고 따로 칸 배치를 알
  필요가 없었다. 프로퍼티 자체를 지우니 `outerTransform` 분기·바깥
  `<div>` 감싸기까지 한 번에 단순해졌다.

  **`core/place.ts`의 `unplaceSlot`도 지웠다** — "용지" 모드가 유일한
  호출자였는데, 이제 아무 데서도 안 부른다. `placeSlot`(반대 방향, 인쇄
  미리보기·그림자 편집 캔버스가 계속 쓴다)은 그대로 뒀다. 짝이었던
  왕복 테스트(`core/place.test.ts`)도 같이 지웠다 — 651/651 테스트
  통과(652에서 1개 줄었다, 지운 테스트만큼).

  죽은 CSS(`.paper-canvas .insert` 등, "용지" 모드 전용 스타일)도
  같이 걷어냈다. 브라우저에서 직접 확인: 도구막대에 탭이 없어졌고,
  기존 "속지" 모드(앞뒤 나란히·클릭으로 전환·앞면 그대로 복사)는
  그대로 잘 된다.

### 25단계 — 인쇄하기 손보기 화면을 실제로 써보고 나온 것들

24단계를 써본 뒤 한꺼번에 나온 피드백 8가지 중 결정이 필요없는 것들을 먼저
처리했다(2번은 물리적으로 검증된 기존 결정과 충돌해 사용자에게 먼저 물었다 —
아래 "확정되지 않은 채로 둔 것" 참고).

- **글자 상자가 타이핑하는 동안 저절로 커지거나 줄어들던 것을 없앴다.**
  새로 만드는 글자 상자는 "칸 하나만 눌러도 편하게" 자동으로 맞춰주던
  기능(17단계 즈음)이었는데, 상자를 일부러 크게 그려 만들어도 타이핑을
  시작하면 그 크기가 무시되고 다시 줄어드는 문제로 이어졌다(사용자
  피드백, 영상으로 확인). 이제 새로 만들든 이미 있던 걸 고치든 상자는
  손댄 그대로 두고, 글자가 상자를 넘칠 수 있다는 기존 원칙(이미 있던
  글자를 고칠 때는 이렇게 동작했다)을 새 글자에도 똑같이 적용했다.

- **위클리 등에서 한 쪽이 두 달에 걸치면 "몇 월"만 보여주는 자동 필드가
  그 쪽의 대표 달(더 많은 날이 속한 달)을 보여준다.** 예: 8/31~9/6인
  쪽은 8월이 1일, 9월이 6일이라 이제 "9월"로 표시된다(전에는 오프셋
  0(8/31)만 보고 "8월"이었다). `core/dataset.ts`의 새 함수
  `majorityMonthDate`가 그 쪽의 모든 날을 세어 다수결로 정한다.
  날짜 하나를 정확히 짚어야 하는 다른 서식(일·요일·주차 등)은 그대로
  오프셋의 날짜를 쓴다 — "몇 월"만 보여주는 서식(M·M월·MMM·MMMM)에만
  적용된다.

- **색 고르는 칸에 16진 코드 입력칸을 기본으로 뒀다.** 브라우저 기본
  색 선택기(`input type="color"`)를 열면 RGB부터 보여줘서 코드를 직접
  아는 색을 넣기 불편했다(사용자 피드백). 이제 코드 입력칸이 늘 먼저
  보이고, 색 네모는 색상환으로 눈으로 고르고 싶을 때만 쓴다. 코드
  입력은 6자리 16진수만 받고, 다 안 쓴 채로 포커스를 옮기면 원래
  값으로 되돌아간다.

- **인쇄하기에서 칸을 손보는 중, 그 칸 바깥 빈 곳을 더블클릭하면
  완료된다.** Esc는 그대로 유지했다. `.print-slot-editor-canvas`(지금
  손보는 칸 자신)나 `.paper-slot-clickable`(다른 칸 — 그쪽은 이미
  자기 클릭으로 옮겨가는 중이다) 위의 더블클릭은 지나치고, 진짜 빈
  여백을 더블클릭했을 때만 완료 처리한다.

- **되돌리기를 다시 되돌릴 수 있게 했다.** "되돌리기"를 누르면 지우기
  직전의 override 내용을 `lastReverted`에 잠깐 담아두고, `.print-bar`에
  "되돌리기 취소" 버튼이 뜬다 — 누르면 그대로 되살아난다. 다른 양식으로
  넘어가면(세트형 pageOverrides는 양식에 속한 값이라) 비운다 — 엉뚱한
  양식에 되살리는 사고를 막는다. 한 번만 기억한다(그다음 되돌리기를
  하면 앞의 것은 잊는다).

- **저장에 세트형 손본 페이지(pageOverrides)가 안 담기던 진짜 버그를
  찾아 고쳤다.** `core/project.ts`의 `SavedTemplate`에
  `pageOverrides`·`pageBackOverrides`를 추가했다 — 저장·불러오기·
  (다른 파일에서) 더 불러오기·복제 전부에서 앞면·뒷면 객체와 똑같이
  다룬다(id 겹침 검사도 함께 받는다). **낱장 조합의 칸 배정
  (`slotAssignment`)과 그 칸의 손본 내용(`comboSlotOverrides`)은
  저장 쪽(`ProjectFile.tsx`)에서 아예 빠져 있던 것도 발견해 같이
  고쳤다** — `loadProject`(store.ts)는 이미 `slotAssignment`를 받을
  준비가 돼 있었는데(`pruneSlotAssignment`) 저장하는 쪽에서 담는 걸
  빠뜨리고 있었다. 다만 "저장"은 지금 활성 양식 **하나만** 담으므로,
  낱장 조합처럼 여러 양식이 얽힌 조합 전체(다른 칸에 배정된 다른
  양식들)까지는 이 파일 하나로 못 담는다 — 배정 자체와 그 칸의 override는
  다시 열 때도 보이지만, 그 배정이 가리키는 다른 양식은 따로 저장·
  불러오기 해야 한다.

- **양식 만들기에서 고치면, 인쇄하기에서 손 안 댄 쪽만 따라간다는
  것을 브라우저로 직접 확인했다.** 세트형 페이지 하나의 앞면만
  손보고(override 생김) 뒷면은 안 건드린 채로, 양식 만들기에서 원본
  양식의 앞면에 선을 하나 더 그었다 — 손본 페이지의 앞면은 그대로,
  손 안 댄 다른 페이지들의 앞면에는 새 선이 그대로 나타났다. 설계한
  그대로 동작한다.

### 26단계 — 양식마다 갖는 색상판(메인·서브색)

25단계에서 상단바에 늘 떠 있던 16진 입력칸이, 그래도 매번 코드를 치는
건 번거롭다는 피드백으로 이어졌다. 색 고르는 칸을 통째로 버튼+말풍선
방식으로 바꿨다.

- **`Template`에 `palette`(메인색 1개 + 서브색 배열)가 새로 생겼다.**
  `dotGrid`와 같은 자리 — 양식마다 따로 갖고, 복제하면 물려받되 배열을
  공유하지 않는다(`core/palette.ts`, `core/template.ts`). 저장 파일에도
  `dotGrid`처럼 담기고, 옛 파일(26단계 이전)은 빈 색상판(`{ main: null,
  subs: [] }`)으로 읽는다.
- **색 고르는 버튼을 누르면 말풍선이 뜬다.** 첫 줄은 모든 양식이
  공유하는 붙박이 회색 5단계(아주 연하게·연하게·중간·진하게·검정 —
  17w 이전에 쓰다가 뺐던 값을 그대로 되살렸다). 둘째 줄은 이 양식의
  메인색 1칸, 그 뒤로 서브색이 늘어선다(개수 제한 없음).
- **서브색은 맨 끝 `+`로 하나씩 늘리고, 각자 옆의 `×`로 지운다.** 메인이
  아직 없으면 그 자리가 `+`로 보이고, 있으면 옆에 작은 `✎`로 다시 정할
  수 있다. `+`나 `✎`를 누르면 그 자리에 네모(색상환)+16진 입력칸이
  나타나는데, 이게 바로 25단계에서 상단바에 있던 그 조작 방식이다 — 값을
  정하는 즉시 적용도 함께 된다.
- **말풍선은 SettingsPanel의 말풍선과 같은 규칙으로 여닫는다.** 바깥을
  누르거나 Esc를 누르면 닫힌다. 스와치를 눌러 색을 적용해도 말풍선은
  안 닫힌다 — 여러 색을 잇달아 눌러보거나 서브색을 여러 개 이어 등록할
  때 매번 다시 여는 게 귀찮아서다.
- 선·도형(테두리·채우기)·글자·달력·체크박스·표·MixedColorControls —
  `ColorField`를 쓰는 곳 전부가 API를 그대로 유지해서(`value`·`mixed`·
  `onChange`·`title`) 자동으로 새 말풍선을 받는다. 따로 고친 곳은 없다.
- **써보고 나온 버그 — 메인색이 이미 있으면 `✎`를 눌러도 반응이 없었다.**
  `main ? (…) : definingMain ? (…) : (…)` 순서였는데, 메인이 있으면
  `main`이 늘 참이라 그 뒤의 `definingMain` 상태를 절대 보여줄 수 없는
  구조였다. `definingMain`을 가장 먼저 검사하도록 순서만 바꿔 고쳤다.

### 27단계 — 노트 제작(1단계: 틀·크기·타공 없음·재단여백 안내)

지금까지는 링 바인더 속지만 만들었는데, 스테이플러·바느질로 매는
"노트"도 만들 수 있게 넓히는 첫 단계다. 실제로 접었을 때 순서가 맞도록
인쇄 배치를 바꾸는 일(책처럼 인쇄하기)은 계산이 완전히 새 영역이라 다음
단계로 미뤘다 — 이번엔 만들고 실제로 인쇄→재단→접어서 손으로 순서를
맞춰보는 데까지다.

- **`Template`에 `kind: 'insert' | 'notebook'`이 생겼다**(`core/template.ts`,
  기본값 `'insert'`, 옛 파일도 이렇게 읽는다·`core/project.ts`). 복제하면
  물려받는다.
- **탭이 갈렸다.** "양식 만들기"가 "속지 제작"·"노트 제작" 두 버튼이
  됐고, 지금 고른 양식의 `kind`에 맞는 쪽만 눌린다(`ui/App.tsx`). 편집
  화면(`EditorTab`) 자체는 완전히 같은 컴포넌트를 그대로 쓴다.
- **"새 양식"에 종류 고르기가 생겼다**(`ui/GalleryTab.tsx`). 노트를
  고르면 규격 프리셋 목록 대신 "완성 페이지 가로·세로" 입력만 뜨고,
  실제로 그려지는 양식 크기(`core/notebook.ts`의 `notebookInsertSize`)는
  **가로만 두 배**(세로는 그대로 — 가로 중심에서 한 번 접으니까)로
  자동 계산돼 미리 보여준다. 재단여백은 이 계산에 넣지 않는다 — 처음엔
  5mm를 더한 크기(예: 7cm 페이지 → 14.5cm)를 생각했다가, 사용자가
  "굳이 안 더해도 될 것 같다"고 다시 정리해 뺐다.
- **타공은 기본적으로 안 보인다.** 확인해보니 타공 안내(`punch.show`)는
  애초에 화면 전용 안내선이라 인쇄에는 전혀 안 들어간다 — 노트로 만들면
  이 값만 꺼진 채로 시작한다.
- **화면에 좌·우 재단여백 후보를 회색으로 보여준다**(`ui/
  NotebookMarginGuide.tsx`, `PunchGuide`와 같은 자리 — 화면 전용, 인쇄
  안 됨). 양 끝 각 5mm(`NOTEBOOK_TRIM_GUIDE`)를 안내만 할 뿐 치수·재단선
  계산에는 전혀 관여하지 않는다. 위·아래는 표시하지 않는다(접는 축과
  무관해서) — 이것도 사용자가 재확인해 정리한 결과다.
- "칸 수로" 격자를 만들 때 쓰던 폭이 완성 페이지 폭이 아니라 실제로
  그려지는(두 배) 양식 폭을 쓰도록 고쳤다 — 안 그러면 노트에서 "가로
  10칸"을 시켜도 실제로는 20칸이 생기는 어긋남이 있었다(`GalleryTab.tsx`).

**다음 단계(따로 설계할 것) — 책처럼 인쇄되는 배치.** 접힌 낱장 한 장이
페이지 4쪽 분량이 되므로(양면 인쇄 기준), 여러 장을 겹쳐 접어 스테이플할
때 펼친 상태에서 어느 쪽이 어디에 인쇄돼야 접었을 때 순서대로 읽히는지
(전통적인 "새들스티치 임포지션") 계산하는 새 로직이 필요하다. 지금 있는
"겹치기 배치"(`cutStackPage`, 접지 않고 같은 자리끼리 쌓는 방식)와는
수학이 다르다.

### 28단계 — 노트 제작 2단계: 표지·쪽 데이터 모델 + 인쇄 배치 계산

27단계를 써본 뒤 나온 요청 넷(가운데 접힘선 화면 표시, 표지 디자인,
편집·인쇄 위치 분리, 쪽수 입력+스크롤 편집) 중 데이터 쪽을 먼저 만들었다.
화면(스크롤하며 편집하는 노트 제작 탭)은 다음 단계다 — 지금 EditorTab은
"활성 캔버스 하나" 구조라 사실상 새 컴포넌트가 필요해서, 검증 가능한
단위로 먼저 쪼갰다.

**결정된 것들(대화로 확인)**
- **가운데 접힘선을 화면 전용 안내로 추가했다**(`NotebookMarginGuide`에
  `.fold-line` 추가). 재단여백 안내와 마찬가지로 인쇄 안 됨.
- **표지는 별도 디자인 자리 하나(오른쪽=앞표지·왼쪽=뒤표지)면 충분하다.**
  내지 1쪽·마지막 쪽이 "혼자 시작·끝나는" 특별한 자리라는 관찰은 맞지만,
  모든 쪽이 처음부터 독립적으로 편집되므로(반복되는 기본 디자인이 없음)
  따로 특별한 필드가 필요 없다 — 그냥 1쪽·마지막 쪽도 원하는 대로 그리면 된다.
- **한 판(왼쪽·오른쪽) 안에서 두 쪽은 속지의 앞뒤면처럼 독립적이다.** 각자
  자기 좌표(0~완성 페이지 폭)·자기 도트 격자·자기 되돌리기 이력을 갖는다.
  화면에서는 나란히 붙여 보여줄 뿐, 데이터로는 완전히 남남이다. 그래서
  경계를 넘나드는 그림(두 쪽에 걸친 그림 하나)은 이번엔 지원하지 않는다.
- **총 쪽수가 4의 배수가 아니면 자동으로 다음 4의 배수까지 빈 쪽으로
  채운다.** 종이 한 장을 접으면 늘 쪽 4개(앞뒤 × 좌우)가 나오므로 "장"은
  4쪽 단위로만 늘어날 수 있다 — 예: 22를 입력하면 24가 되고 마지막 2쪽은
  비어 있다.

**만든 것**
- `core/notebook.ts`: `NotebookHalf`(반쪽 하나 — 그린 것·격자·되돌리기
  이력), `paddedPageCount`, `notebookImposition`(새들스티치 배치표 — 쪽수를
  주면 각 장 앞/뒤·좌/우에 몇 쪽이 오는지). 전부 화면 없이 테스트로
  검증했다(장 1개·2개 예시, "접어서 편 순서로 늘어놓으면 0쪽부터 순서대로
  한 번씩 나온다" 왕복 검증, id 안 겹침 검증).
- `Template`에 `cover`(표지 앞뒤)·`pageCount`·`pages`(쪽 배열, 인덱스
  0 = 1쪽) 추가 — 전부 `kind === 'notebook'`일 때만 뜻이 있다. `newTemplate`
  이 노트로 만들 때 기본 8쪽으로 채워서 시작한다. `duplicateTemplate`은
  반쪽마다 새 되돌리기 이력으로 물려받는다(objects·back과 같은 원칙).
- `core/project.ts`: 저장/불러오기에 dotGrid·objects와 같은 방식으로
  담았다. id 겹침 검사(`dedupeObjectIds`)도 표지·쪽까지 포함하도록
  넓혔다 — 앞면·뒷면·손본 페이지에 이미 있던 것과 같은 파이프라인이다.

**써보다 찾은 것 — 테스트가 우연히 통과하고 있었다.** id 겹침을 확인하는
기존 테스트 몇 개가 `line`(`id: 'l1'`)을 앞면·뒷면에 동시에 재사용하고
있었는데, 겹쳤을 때 새로 받는 id가 항상 `"l" + 카운터` 모양이라 **카운터가
정확히 0에서 시작하면 새로 받은 id가 우연히 다시 "l1"이 되어**(카운터
0→1) "겹쳤는데도 안 바뀐 것처럼" 보였다. 이번에 앞쪽에 테스트를 더 끼워
넣어 카운터 시작값이 달라지자 바로 드러났다. `뒷면이 그대로 돌아온다`
테스트는 애초에 겹칠 의도가 없었던 것이라 겹치지 않게 고쳤고, 새로 쓴
표지·쪽 테스트는 숫자로 안 끝나는 id(`'dup-id'`)를 써서 카운터 값과
절대 우연히 겹치지 않게 했다.

---

## 하지 않기로 한 것 — 기울임

Pretendard에 **이탤릭 파일이 아예 없다.** 있는 것은 굵기 9종뿐이다. 브라우저는
가짜 기울임을 만들어내지만 pdf-lib에는 그 기능이 없어서, 넣는다면 화면만 기울고
인쇄물은 똑바로 나온다 — `OBJECT_LINE_CAP`으로 이미 겪은 그 사고다. 굵게는 Bold
파일을 담아 해결할 수 있지만 기울임은 담을 파일조차 없다.

한글 양식에서 기울임은 거의 쓰지 않는다. 강조는 굵게와 색으로 충분하다.
나중에 필요해지면 굵기 단계(Light·Medium·SemiBold)를 여는 쪽이 낫다.

## 아직 인쇄해보지 않은 값

- **글자 90도 회전은 로고 칸(뒷면)으로 실제 인쇄해서 확인했다(2026-08-09,
  괜찮다고 함).** 270도(앞면)는 이미 9b 수정 4로 검증된 조합이었고, 이번에
  90도(그 거울상)도 확인됐다.
- **이미지의 자유 회전(12g)도 아직 인쇄로 확인 못 했다.** 0·90·180·270도는
  글자와 같은 계산·같은 검증 상태를 물려받지만, 그 사이의 임의 각도(예:
  37도)는 처음 실제로 쓰이는 경우라 자리·기울기가 인쇄물에서 정확히
  맞는지 따로 확인이 필요하다.
- **도형·체크박스(13단계)는 실제 인쇄로 확인했다 — 모두 괜찮다고 함.**
  테두리 굵기(13d에서 고친 것), 모서리 둥글기, 회전 배치(M5)에서의
  자리, 체크박스 여섯 아이콘(하트·별 포함)까지 전부 포함한다.
- **월간 달력 한자 요일도 확인했다 — Pretendard에 그 글자(日月火水木金土)가
  있다.**
- **양면 인쇄의 뒤집는 방향은 실제 인쇄로 확인했다(9b 수정 5).** 회전 배치가
  아니면 프린터의 "긴 가장자리 기준"(좌우 뒤집기) 설정을, 회전 배치(M5 등)면
  "짧은 가장자리 기준"(상하 뒤집기) 설정을 써야 한다 — 앱이 그 가정으로
  `mirrorLayout`을 계산한다. 프린터마다 이 설정 이름이 다를 수 있으니("옆으로
  넘김"/"위로 넘김" 등) 처음 양면으로 뽑을 때 속지가 회전 배치인지부터 보고
  (인쇄하기 탭 하단 "한 장에 N개" 옆에 "· 90도 회전"으로 표시된다) 맞는 설정을
  고른다.
- **낱장 조합(칸마다 다른 양식)의 양면 인쇄에서, 칸별 앞뒤가 실제로
  짝이 맞는지는 비회전 배치(M6, 긴 가장자리 기준)만 실제 인쇄로
  확인됐다(21a, 2026-08-16 — 처음엔 어긋났다가 `mirrorLayout`을
  되돌린 뒤 다시 확인 예정).** 회전 배치(M5 등, 짧은 가장자리 기준)로
  낱장 조합·세트형을 양면 인쇄했을 때도 칸별 앞뒤가 맞는지는 아직
  확인 전이다 — 계산(`mirrorLayout`의 상하 뒤집기)은 같은 함수라
  `core/geometry.test.ts`로 검증돼 있지만, 실제 프린터가 "짧은
  가장자리 기준"에서도 이 앱의 가정과 같은 방식으로 물리적으로
  뒤집는지는 별개의 확인이 필요하다.
- **회전 배치(M5 등)에서 달력 오브젝트의 42개 글자가 전부 제자리에서
  돌아가는지 아직 실제로 뽑아보지 않았다.** 같은 회전 방식(`drawTexts`의
  `rotate: degrees(...)`)을 그대로 재사용했고 그건 9b 수정 4에서 이미
  검증됐지만, 한 오브젝트가 42개 글자를 한꺼번에 그리는 건 처음이라
  따로 확인이 필요하다.
회전 배치(M5)에서 이미지 오브젝트가 제자리로 돌아가는지도 인쇄해서
확인했다(2026-08-09) — 괜찮다고 함.

마지막으로 남아 있던 `GRID_LINE_WIDTH 0.12mm`(그리드·줄노트 선 굵기)를
2026-08-06에 인쇄해서 확인했다 — 괜찮다고 함.
- 글자 색 `TEXT_COLOR #3a3a3a` · 기본 크기 9pt — 인쇄해서 확인함(마음에 든다고 함)

그은 선의 기본값(`#9e9e9e` · 0.2mm)과 도트(`#a6a6a6` · 0.2mm)는 인쇄해서 확인했다.

---

## 확정되지 않은 채로 둔 것

- **"앞면 그대로 복사"(뒷면 만들기)가 자리를 좌우로 뒤집는 것 — 사용자가
  다시 문제 삼았다(2026-08-18, 영상으로 확인).** `core/template.ts`의
  `backFromFront`가 `mirrorObjectX`로 각 객체의 자리를 뒤집는다 — 19a
  (2026-08-XX 이전)에서 **일부러** 넣은 동작이다: 뒷면 좌표계에서는
  타공이 반대쪽에 있으므로(`holeCenterX`), 안 뒤집으면 앞면에서
  타공과 멀던 자리가 뒷면에서는 타공에 가까워져 실제 인쇄물에서
  앞뒤의 "타공까지 거리"가 달라진다는 게 그때 이유였다. 이번에
  사용자는 반대로 "데칼코마니처럼 뒤집지 말고 자리가 완전히 똑같이
  (예: 오른쪽에 있던 상자가 뒷면에서도 오른쪽에) 복사되면 좋겠다"고
  했다 — 두 요구가 정면으로 부딪힌다. 물리적으로 어느 쪽이 실제
  인쇄물에서 맞는지는 추측하지 않고 사용자에게 직접 물어봤다(사용자
  응답 대기 중) — 코드는 아직 19a 그대로(뒤집는 채로) 뒀다.

- **A5를 148mm로 뒀다.** A4의 정확한 절반은 148.5mm라 148로 두면 1mm가 남는다.
  그래도 **속지 크기가 우선**이라는 원칙에 따라 규격값을 택했다. 자투리 1mm는
  좌측 상단 정렬 덕분에 전부 아래로 몰리므로 한 번만 자르면 된다.
- **좌측 상단 정렬에서 프린터가 가장자리를 못 찍을 수 있다.** 왼쪽·위쪽 십자가
  사라질 수 있는데, 재단선마다 반대쪽 끝에도 표시가 있어 자르는 데는 지장이 없다.
  실제로 어떤지는 인쇄해봐야 안다.

- **M6 3단접이 세로를 125mm로 가정했다.** 사용자가 220×120이라고 했다가 실수라고 정정했는데,
  정확한 값은 확인하지 않았다. M6 본체와 같아야 구멍이 맞으므로 125로 뒀다.
- **DA9(60×80, 3공)는 추정값이다.** 사용자에게 실물이 없어 19mm 간격 3구멍을 중앙 정렬했다.
- **M6·M5의 실측 여백**(19mm, 10mm)은 채택하지 않았다. 기하학적으로 불가능하다 —
  M6는 세로 125mm에 구멍 6개라 19×5=95mm가 필요하고, 남는 30mm의 절반인 15mm가 여백의 최댓값이다.
  어차피 구멍은 인쇄되지 않는 화면 안내선이라 문제되지 않는다.
- **글꼴·이미지를 브라우저(IndexedDB)에 캐싱하기로 한 것은 15단계에서
  실제로 만들었다.** 아래 "15단계" 참고.

---

## 작업 방식

- **한국어로 대화한다.**
- **코드 작성 전에 구조를 먼저 설명하고 동의를 받는다.** 큰 덩어리를 한 번에 받으면 검토가 어렵다.
- **각 단계는 끝날 때 실제로 실행되는 결과물이 나오도록 쪼갠다.** 사용자가 직접 써보고 다음을 정한다.
- 기능을 늘리는 것보다 빠른 완성이 우선이다. 애매하면 뺀다.
