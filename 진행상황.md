# 진행 상황

마지막 갱신 2026-08-06

이 파일은 **"지금 어디까지 했고 다음에 뭘 할지"**만 적는다.
설계 내용은 [설계문서.md](설계문서.md), 실행 방법은 [README.md](README.md)에 있다.

---

## ✅ 7단계 — 여러 쪽 생성

- 테스트 313개 통과 · 타입 검사 · 빌드 통과

`Template.repeat`이 생겼다 — `single`(기본, 다른 양식과 섞어 낱장 조합)과
`repeat`(이 양식 하나로 여러 장). 설계문서 8장 "두 가지 작업"의 원칙대로,
반복 모드로 바꾸면 인쇄하기 탭의 낱장 조합(칸 배정) UI가 사라지고 매수 안내로
바뀐다 — 두 가지 인쇄 방식은 한 인쇄 작업에 섞이지 않는다.

### 왜 "인쇄 매수"로는 안 되나

내용이 다 똑같으니 프린터의 매수 설정으로 되지 않나 싶었는데, 안 된다. 한
용지에 칸이 여러 개 배치되기 때문이다. M6 4칸짜리 A4에 53쪽이 필요할 때
53장을 인쇄하면 53×4=212칸이 나와 159칸이 버려진다. 실제로는 `ceil(53÷4)=14`장
뿐이고, **마지막 장의 남는 칸 2개만 비워야** 한다. 이 계산과 "마지막 장 일부만
비우기"가 7단계의 핵심이다.

### PDF가 여러 페이지를 만든다

`buildPdf`에 `totalSlots?: number`를 더했다. **주지 않으면 지금까지처럼 한 장,
모든 칸을 채운다** — 기존 호출부(5c·6단계)는 전부 그대로 통과했다. 주면
`sheetsNeeded(totalSlots, 칸 수)`장을 만들고, 마지막 장에서 모자라는 칸은
`BLANK_SLOT`(인쇄 안 함·빈 objects)으로 완전히 비운다. 내용을 억지로 채우거나
앞 칸을 복사해 늘리지 않는다.

**칸을 채우는 로직(`drawDotGrid`·`drawObjects`·`drawTexts`)은 슬롯 인덱스로
동작하던 것을 그대로 페이지 루프 안으로 옮겼을 뿐이다.** 재단선·눈금자도 장마다
다시 그린다 — 한 장 한 장이 완결된 인쇄 단위여야 한다.

### 화면 미리보기와 PDF가 같은 함수로 빈 칸을 정한다

`core/template.ts`에 `filledSlots(sheet, totalSlots, slotsPerSheet)`를 뽑아냈다.
pdf/export.ts와 App.tsx(화면 미리보기)가 **이 함수 하나를 같이 쓴다** — 각자
계산하면 미리보기에서 본 빈 칸이 실제 인쇄물과 어긋날 수 있다. 인쇄하기 탭에
`이전/다음` 버튼으로 장을 넘겨보는 미리보기를 넣었다(`RepeatPrint.tsx`).

### 반복 양식은 낱장 조합에 못 낀다

`SlotAssign`(5c)의 같은 규격 목록에서 `repeat` 양식을 뺐다. 배정해둔 양식이
나중에 반복 모드로 바뀌는 경우도 있어서, `resolveSlotTemplates`가 규격뿐 아니라
`repeat.mode === 'single'`인지도 함께 확인하도록 방어선을 하나 더 뒀다(테스트로
고정) — 자기 하나로 여러 장을 채우는 양식이 다른 양식과 한 칸씩 섞이면 안 된다.

### 저장 파일

`SavedTemplate.repeat`을 선택 필드로 뒀다. **7단계 이전 파일에는 없으므로
`single`로 읽는다** — 판 번호 원칙 그대로다.

### 덤 — 표 도구의 커서 표시를 칸 수로

말씀하신 대로, 표 도구로 드래그할 때 우측하단 치수가 `15 × 10mm` 대신
`3 × 2칸`으로 뜬다. `core/grid.ts`에 `tableSize(lattice, a, b)`를 추가했는데,
`tableLines`와 같은 `between` 계산을 쓴다 — 미리보기 숫자와 실제로 그어질 선이
다른 계산에서 나오면 언젠가 어긋난다. 그리기(선) 도구는 그대로 mm다.

---

## ✅ 6단계 — 표. 도구가 하나 더 생겼다

- 테스트 290개 통과 · 타입 검사 · 빌드 통과

`선택(V) · 그리기(D) · 표(G) · 글자(T)`. 표를 **새 객체 타입이 아니라 새 도구**로
만들었다 — 설계문서의 `TableObject`(colWidths·rowHeights·cells·hiddenBorders)를
그대로 만들지 않았다는 뜻이다.

### `TableObject`가 필요 없는 이유

표에 긋는 선은 그냥 `LineObject`다. 이미 있는 `toggleLines`(같은 자리를 다시 그으면
지워지고, 없는 것만 채운다)가 칸 하나하나의 구분선에도 그대로 적용된다. 그래서
"구분선 끄기"·"행·열 늘리기·줄이기"에 **새 코드가 필요 없었다** — 안쪽 선 하나를
다시 그으면 꺼지고(칸 병합), 표를 더 크게 다시 드래그하면 칸이 늘고, 선을 선택해
지우면 줄어든다. 설계문서도 이 점을 명시한다: "colSpan/rowSpan 같은 복잡한 구조가
필요 없다."

### 새로 만든 것은 `tableLines` 하나다

`core/grid.ts`의 `tableLines(lattice, a, b)` — 두 점 사이의 **모든** 격자점을 잇는
가로·세로 선을 낸다. 기존 "면 그리기"(`rectLines`)는 테두리 4개만 내는데, 표는
안쪽 칸 선까지 전부 낸다.

**격자점은 `lattice.xs`·`ys`에서 그대로 가져온다.** 간격으로 다시 계산하지 않는다 —
안전영역을 피해 격자가 밀려 있거나 끝까지 채우기로 가장자리가 덧붙어도, 실제로
존재하는 점 사이에만 선이 생겨야 한다. 한 줄로만 끌면(가로나 세로가 한 칸도
없으면) 선 하나만 나온다 — `rectLines`와 같은 규칙이다.

### 그리기 도구는 하나도 안 바뀌었다

지난 회차에 "드래그가 항상 표가 되면 기존 사각형 그리기가 사라진다"고 걱정했는데,
**도구를 아예 나누는 것으로 해결했다.** 그리기(D)는 지금처럼 테두리만, 표(G)는
안쪽 선까지. 몸짓(점에서 끄는 드래그)은 똑같고, `onUp`에서 지금 도구를 보고
`rectLines`를 쓸지 `tableLines`를 쓸지만 가른다. 기존 동작은 한 줄도 안 바뀌었다.

표 도구에서 **드래그 없이 클릭만 하면 아무 일도 하지 않는다.** 표는 최소 2칸은
돼야 의미가 있어서, 그리기 도구의 "점 찍고 다음 점 기다리기" 같은 대기 상태를
두지 않았다.

속성 막대·붙을 자리 표시·드래그 중 미리보기(안쪽 칸 선까지 보여준다)는 전부
그리기 도구와 같은 코드를 그대로 탄다 — 표의 선도 결국 보통 선이라 굵기·색·모양
설정을 공유하는 게 자연스럽다.

---

## ✅ 5c — 낱장형 칸 배정. 5단계가 끝났다

- 테스트 281개 통과 · 타입 검사 · 빌드 통과

인쇄하기 탭에서 용지 한 장의 칸마다 **다른 양식**을 넣는다. M6 4칸에 위클리·데일리·
메모·습관을 하나씩 배정하는 것이 이 기능의 목적이다.

### 같은 규격끼리만 섞는다

배치 계산(칸 크기·개수)이 한 규격을 기준으로 **한 번만** 일어난다(설계문서 8장,
"두 가지 작업" 중 낱장 조합). 그래서 지금 양식과 같은 규격의 양식만 칸에 배정할
수 있다. 규격이 다른 것을 억지로 넣으면 배치 자체가 성립하지 않는다.

`store.slotAssignment: Record<number, string>` — 키는 슬롯 번호, 값은 그 칸에
넣을 양식 id. **정하지 않은 칸은 지금 고른 양식(activeId)을 쓴다** — 지금까지의
"모든 칸이 같다"는 동작이 곧 이 값이 비어 있는 경우다.

`resolveSlotTemplates(s, count)`가 칸마다 실제로 쓸 양식을 정한다. 배정이 없거나,
배정된 양식이 지워졌거나, **규격이 다르면** 조용히 기본값(활성 양식)으로 돌아간다.
양식을 지우면 그 양식을 가리키던 배정도 함께 걷어낸다(`pruneSlotAssignment`) —
안 그러면 드롭다운이 있지도 않은 항목을 가리키게 된다.

### 화면과 PDF가 같은 배열을 본다

`App.tsx`가 `resolveSlotTemplates`를 **한 번만** 불러 화면 미리보기와 PDF 양쪽에
넘긴다. 둘이 따로 계산하면 언젠가 어긋난다는 게 이 프로젝트의 오래된 교훈이다.

지금 양식과 같은 칸은 넘기지 않는다. 대부분의 칸이 지금 양식을 그대로 쓰므로,
**기본값 + 다른 칸만** 넘기는 쪽이 다루기 쉽다. 이 "기본값 + 예외"라는 모양을
화면(`PaperPreview.slotOverrides`)과 PDF(`pdf/export.SlotContent`) 양쪽에
같은 식으로 만들었다 — 기존 호출부(단일 양식)는 override가 없는 특수한 경우일
뿐이라 코드도 테스트도 그대로 통과했다.

### PDF는 인쇄 여부도 칸마다 다르게 본다

전에는 "격자를 인쇄할지"를 문서 전체에서 한 번만 봤다. 낱장 조합에서는 어떤
양식은 격자를 켜고 어떤 양식은 끄고 쓸 수 있으므로, 그 판단을 칸을 그리는 순간
마다 하도록 옮겼다(`drawDotGrid`). 배정이 없을 때는 모든 칸이 같은 값을 보므로
예전과 결과가 같다.

글꼴은 문서 전체에 **한 번만** 심는다. 기본 칸에는 글자가 없고 배정된 칸에만
있어도 그 글자를 알아채야 하므로, 폰트를 심을지 판단할 때는 기본값과 모든
override의 글자를 합쳐서 본다.

### 칸 배정 UI

인쇄하기 탭에 `칸 배정` 줄이 생긴다. 칸 배치(가로·세로)와 같은 모양으로 드롭다운을
늘어놓아 어느 칸을 고치는지 바로 알아본다. **같은 규격의 양식이 하나뿐이면(자기
자신뿐이면) 아무것도 보여주지 않는다** — 고를 게 없는데 빈 자리만 차지하면 안 된다.

---

## ✅ 5a·5b 다듬기 — 빈 화면에서 시작한다

- 테스트 269개 통과 · 타입 검사 · 빌드 통과

### 양식 없이 시작한다

새로고침하면 **양식 관리 탭**이 먼저 열리고 갤러리가 비어 있다. 예전에는 M6 양식이
하나 미리 들어가 있었는데, 원하지 않는 규격으로 시작하게 되고 지울 수도 없었다.

`templates`가 빌 수 있게 되면서 `activeTemplate`이 `null`을 돌려준다. 양식이 없으면
`양식 만들기`·`인쇄하기` 탭이 잠기고 갤러리만 열린다. 화면이 읽는 값은 참조가
고정된 기본값으로 받쳐뒀다 — **부를 때마다 새 객체를 만들면 zustand가 매번 바뀐
것으로 보고 무한히 다시 그린다.**

### 이름이 규격에서 나온다

`M6-1`, `M6-2`, `M5-1`. 규격마다 따로 센다. `양식 1`·`양식 2`로는 목록에서 M6와 A5를
구분할 수 없다. 중간을 지워도 번호가 겹치지 않게 가장 큰 번호 다음을 쓴다.

### 새 양식 창에서 용지·속지·격자를 다 고른다

만들고 나서 설정을 하나씩 찾아 고치는 대신, 시작할 때 한 번에 정한다.
**지금 값으로 만들면 몇 칸이 되는지 창 안에서 바로 보여준다.**

**칸 수로도 만들 수 있다.** `가로 10칸`처럼 세면 간격이 딱 나누어떨어지게 계산된다
(`spacingForCells`). 다만 **간격은 가로·세로가 하나뿐이라** 한 축을 정하면 다른 축은
따라 나온다 — 둘 다 마음대로 정하면 칸이 정사각형이 아니게 되고, 그러면 도트
격자라고 부를 수 없다. 그래서 어느 축으로 셀지 고르게 했다.

### 여백 없이 끝까지 — 그린 선도 끝까지 간다

`둥근 끝(round cap)`은 선을 0.1mm **늘린다.** 짧아지는 원인이 아니었다.

진짜 원인은 붙을 자리였다. 격자선은 영역 끝까지 그어지는데(`gridShapes`)
격자점은 안쪽에서 끝나서, 테두리를 그으려 해도 양끝이 여백만큼 못 미쳤다.

```
80mm · 6mm 간격
  격자선  0 ────────────────── 80
  격자점     1  7 … 73  79        ← 여기까지만 붙을 수 있었다
```

끝까지 채울 때는 **영역 가장자리도 붙을 자리로 넣는다.** 끝까지 채우겠다는 뜻은
거기에 그릴 수 있다는 뜻이다.

대신 **그 가장자리 띠는 칸으로 세지 않는다.** 간격보다 좁아 무언가를 앉힐 수 없다.
`xs.length - 1`로 세면 틀리므로 `Lattice.cols`·`rows`를 core가 직접 세어 내놓는다.

### 알림을 오른쪽 아래로

위쪽은 탭·저장·PDF 버튼이 몰려 있어 알림이 그것들을 가렸다.

### 가장자리 도트는 화면에만 (뒤이어 고침)

끝까지 채울 때 넣은 가장자리 자리에 **도트까지 찍혀서** 격자가 한 줄 더 있는
것처럼 보였다. 그 자리는 격자의 일부가 아니라 **끝까지 그을 수 있게 하려고 넣은
안내**다. 그래서 `Lattice.padded`로 표시해두고 **인쇄할 때만 도트를 뺀다.**

- 화면 — 보여야 어디에 붙는지 알고 끝까지 그을 수 있다
- 인쇄 — 찍지 않는다. 재단선 위에 반쯤 잘린 도트가 남으면 격자가 어긋나 보인다

**선은 인쇄해도 그대로 긋는다.** 잘린 칸을 종이 끝에서 닫아주는 것이 그 선이라
빼면 칸이 열려버린다. 도트와 선을 다르게 다루는 것이 요점이라 테스트로 고정했다.

### 편집 화면에서도 칸 수로 격자를 정한다

새 양식 창에만 있던 것을 설정 패널에도 넣었다. **지금 몇 칸인지 보여주고, 고치면
간격이 거꾸로 계산된다.** 가로·세로를 나란히 두되 간격은 하나뿐이라 한쪽을
고치면 다른 쪽이 따라 움직인다.

---

## ✅ 5b — 파일로 저장하고 불러온다

- 테스트 251개 통과 · 타입 검사 · 빌드 통과

맨 위에 `저장` · `불러오기`가 생겼다. 탭이 아니라 헤더에 둔 이유는 저장이
**지금 보고 있는 양식 하나가 아니라 작업 전체**에 대한 일이기 때문이다.

이 프로그램은 서버가 없어서 새로고침 한 번에 작업이 사라진다. 손에 쥘 수 있는
파일이 있어야 안심하고 쓴다.

### 담지 않는 것 둘

**실행취소 이력.** 되돌리기는 이번 작업 중의 이야기다. 파일을 열었더니 남의
과거로 되돌아갈 수 있으면 오히려 놀란다. 지금 모습만 담는다.

**글꼴 파일.** 이름만 담는다. 2.7MB짜리를 base64로 넣으면 파일이 3.6MB씩 불어나고,
남의 글꼴을 실어 옮기는 것은 라이선스상으로도 애매하다. 그리고 **실제로 쓰인 것만**
담는다 — 등록만 해놓고 안 쓴 글꼴까지 적으면 불러올 때 필요도 없는 파일을 찾게 된다.

### 같은 이름으로 다시 등록하면 되살아난다

파일에 이름만 있으니, 열고 나면 그 글꼴을 쓰던 글자는 기본 글꼴로 보인다.
드롭다운에는 `내글꼴 (파일 없음)`으로 남아 무엇이 빠졌는지 알려주고, 불러온 직후에도
어떤 글꼴이 필요한지 알림으로 띄운다.

**같은 이름의 파일을 다시 등록하면 그때의 id를 물려받는다.** 새 id를 주면 글자들이
영영 없는 글꼴을 가리키게 된다. `registerFont(file, reuseId)` 한 줄과 `reserveIds`
(불러온 id와 새 id가 부딪히지 않게 번호를 밀어두는 것)로 해결했다.

### 판 번호를 맨 앞에 박았다

`version: 1`. 앞으로 뒷면이나 표가 생겨도 옛 파일을 읽을 수 있어야 한다.
**없는 값은 기본값으로 채운다** — `dash`·`toEdge`·`minMargin`이 없던 시절의 파일도
새 설정의 기본값으로 열린다(테스트로 고정).

반대로 **더 새로운 판은 거른다.** 판 번호를 알려주며 열지 않는다. 모르는 구조를
억지로 읽어서 조용히 망가뜨리는 것보다 낫다.

### 검사는 화면이 죽지 않을 만큼만

좌표가 숫자인지까지 뜯어보면 코드가 배로 늘고, 그렇게까지 망가진 파일은 사용자가
손으로 고친 경우뿐이다. **양식 목록이 있는지, 각 양식에 크기와 객체 배열이 있는지**만
본다. 걸리면 무슨 일인지 알려주고 **지금 작업은 그대로 둔다.**

---

## ✅ 5a — 양식이 여러 개가 됐다

- 테스트 231개 통과 · 타입 검사 · 빌드 통과
- 기능은 그대로다. **담는 그릇이 바뀌었다**

### 나중에 고친 것 둘

**갤러리 썸네일이 검은 바탕으로 나왔다.** `.insert-sheet .sheet-bg { fill: #fff }`가
편집 화면 안에서만 걸리는 규칙이라, 갤러리의 `<svg>`에서는 **SVG 기본 채움색인
검정**이 그대로 나왔다. 속지 바탕은 어디서 그리든 흰색이므로 부모를 따지지 않게 했다.

**맨 위 `+ 새 양식`이 규격을 묻지 않고 바로 만들었다.** 지금 규격을 물려받는
동작인데, 그건 크기 그룹의 `+`에 어울리는 뜻이다("이 크기로 하나 더"). 맨 위
버튼은 가운데 창을 띄워 규격을 고르게 했다 — **지금 규격이 처음부터 골라져 있어서**
그대로 누르면 예전과 같은 결과가 된다.

### 속지 한 장에 대한 것이 전부 양식 안으로 들어갔다

```
전                                후
  store.insert                      store.templates[]
  store.dotGrid           →           ├ insert
  store.objects                       ├ dotGrid
  store.paper · gap · align…          └ objects (실행취소 포함)
                                    store.activeId
                                    store.paper · gap · align…  (그대로)
```

**양식은 용지를 모른다**(설계문서 3장). 그래서 용지·배치 설정만 store 루트에 남았다.

읽는 자리가 38곳이라 `activeTemplate(s)` 하나로 모았다. 화면은 `useInsert()`
`useDotGrid()` `useObjects()` 지름길을 쓴다.

**실행취소가 양식마다 따로다.** 양식을 바꿨다가 ⌘Z를 눌렀는데 다른 양식의 작업이
사라지면 무섭다. 양식을 바꾸면 고른 것도 함께 푼다 — 다른 양식의 id가 선택 목록에
남아 있으면 지우기·스타일이 엉뚱하게 동작한다.

이 두 가지가 이 변경의 핵심이라 store 테스트로 못 박았다.

### 양식 관리 탭

`양식 관리 → 양식 만들기 → 인쇄하기`. **탭 순서가 곧 작업 순서다** — 고르고,
그리고, 인쇄한다. 지금까지는 첫 칸이 비어 있었다.

크기별로 묶어서 보여준다. **양식이 크기를 소유하는 것이 아니라 크기를 속성으로
가진다.** 같은 M6 양식이 열 개여도 되고, 갤러리는 그 속성으로 줄을 세울 뿐이다.

썸네일은 **편집 화면과 같은 `InsertView`를 작게 그린 것이다.** 따로 이미지를 만들지
않는다 — 화면이 이미 SVG로 그리고 있어서 viewBox만 줄이면 그대로 미리보기가 된다.
양식이 수십 개가 되면 SVG를 수십 장 그리게 되는데, 느려지면 그때 손본다.

`+ 새 양식`은 맨 위에, 각 크기 그룹에도 `+`를 따로 뒀다. 그룹의 `+`는 그 크기로
바로 만든다 — 대부분 한 규격으로 계속 작업하므로 매번 고르게 하면 손이 더 간다.

### 규격을 바꿔 복제한다 — 비례 조정은 하지 않는다

80×125로 만든 것을 75×125로도 뽑고 싶을 때가 있다. **원본을 고치는 대신 사본을
만든다.** 실패해도 잃는 것이 없다.

**비례로 줄이지 않는다.** 80 → 75는 93.75%인데 세로는 그대로라 글자가 납작해지고,
세로까지 줄이면 5mm 격자가 4.69mm가 된다. 5mm 격자는 시판 다이어리와 맞추려고 정한
값이고 모든 작업이 그 위에서 일어난다 — 4.69mm 격자는 아무 의미가 없다.

조용히 자동으로 바뀌는 값이 사고를 낸다는 것을 이 프로젝트는 이미 두 번 겪었다
(줄 간격이 도트를 따라가던 것, `OBJECT_LINE_CAP`이 화면과 PDF에서 달랐던 것).

실제로 일어나는 일은 훨씬 단순하다. 격자 간격도 글자도 선도 그대로고, **칸 하나가
줄어들 뿐이다.** 오른쪽 끝에 걸친 것만 밖으로 나간다. 그래서 **몇 개가 나가는지만
미리 세어서 알려준다**(`outsideCount`). 지우지도 옮기지도 않는다 — 화면과 PDF 모두
속지 밖을 이미 잘라내므로 동작은 안전하고, 몰라서 놀라는 것만 막으면 된다.

### 타입 검사를 잘못 돌리고 있었다

`npx tsc --noEmit`은 루트 tsconfig가 `files: []`에 참조만 걸어둔 구성이라 **아무것도
검사하지 않는다.** 진짜 검사는 `npm run build`의 `tsc -b`였다. 다행히 매 커밋마다
빌드를 돌렸으므로 지나간 것은 없다. 앞으로는 `tsc -b`를 쓴다.

---

## ✅ 4c-2c — 글꼴 파일 등록. 4단계가 끝났다

- 테스트 202개 통과 · 타입 검사 · 빌드 통과

`.ttf`/`.otf`/`.woff`를 등록해서 **글자마다** 골라 쓴다. 속성 막대 맨 앞에 글꼴
드롭다운이 생겼고, 목록 맨 아래 `글꼴 파일 추가…`가 파일 창을 연다.

고르기와 등록을 드롭다운 하나에 함께 둔 것은, **글꼴을 바꾸려다 없다는 걸 아는
순간이 곧 등록하고 싶은 순간**이기 때문이다. 등록하면 방금 그 글자에 바로 씌운다 —
등록만 하고 또 고르게 하면 손이 한 번 더 간다.

### 화면과 PDF가 같은 파일을 본다

화면은 `FontFace`로 브라우저에 심고, PDF는 같은 바이트를 pdf-lib에 넘긴다.
등록하는 순간 `face.load()`까지 마치고, 실패하면 목록에 넣지 않는다 — 넣어놓고
나중에 안 그려지는 것보다 낫다.

**바이트는 store에 넣지 않았다.** 수 MB짜리 버퍼가 상태에 있으면 구독하는
컴포넌트마다 그것을 들여다보게 된다. `fonts/registry`가 들고 있고 store에는
목록(id·이름·CSS 이름)만 둔다.

CSS 이름은 파일 이름이 아니라 id에서 만든다(`user-font-f1`). 같은 이름을 두 번
등록하거나 파일 이름에 따옴표가 들어가도 깨지지 않는다.

### 등록 글꼴에는 굵게를 막았다

그 글꼴의 Bold 파일이 없기 때문이다. 브라우저는 가짜 굵게를 만들어주지만
pdf-lib에는 그 기능이 없어서, 허용하면 **화면만 굵고 인쇄물은 그대로** 나온다.
`core/text`의 `canBold`가 이 규칙을 쥐고 있고, 속성 막대의 굵게 버튼이 흐려진다.
굵은 글꼴이 필요하면 Bold 파일을 따로 등록해서 고른다.

### 새로고침하면 사라진다

양식 저장(5단계)이 생기기 전까지는 어쩔 수 없다. 2.7MB짜리 파일을 양식 JSON에
넣을 수는 없다. 그 글꼴을 쓰던 글자는 **기본 글꼴로 되돌아간다** — 화면도 PDF도
같이 그렇게 동작한다. 글자가 안 보이는 것보다 낫다.

### 폭을 재는 글꼴도 같이 바꿨다

`measureTextBox`가 글꼴 이름을 받는다. 상자가 자동으로 늘어날 때 쓰는 값인데,
그리는 것과 다른 글꼴로 재면 상자가 안 맞는다. 굵기를 넣을 때와 같은 이유다.

---

## ✅ 격자 여백과 선 모양을 열었다

- 테스트 199개 통과 · 타입 검사 · 빌드 통과

여백을 줄이거나 없애고 싶다는 요청. 원래는 **일부러 입력받지 않던 값**이라
그 원칙을 지키는 선에서 열었다.

### 여백이 아니라 `최소 여백`을 받는다

여백 자체를 값으로 받으면 간격과 서로 맞지 않는 상황이 생긴다 — 처음에 피한
이유가 그것이다. 그래서 공식은 그대로 두고 **하한만** 설정으로 열었다.

```
칸 개수 = floor((영역 − 최소여백 × 2) ÷ 간격)     ← 최소여백만 설정값이 됐다
여백    = (영역 − 칸 개수 × 간격) ÷ 2             ← 여전히 종속 변수
```

`MIN_MARGIN` 상수가 `DotGrid.minMargin`이 되고, 상수는 `DEFAULT_MIN_MARGIN`(3mm)로
이름이 바뀌어 기본값만 맡는다. 0~20mm.

**0으로 둬도 정확히 0은 아니다.** 나누어떨어지지 않고 남는 만큼은 좌우에 반씩
남는다 — 80mm에 5mm 간격이면 딱 0이지만 6mm 간격이면 1mm가 남는다. 테스트로
못 박아뒀다. 0에 가까우면 바깥쪽이 재단선에 붙어 자를 때 날아갈 수 있는데,
막지 않고 설정 패널에서 경고만 띄운다.

### 여백 없이 끝까지 — 잘린 칸을 허용한다

처음에는 "선만 영역 끝까지 늘이기"로 만들었는데, 써보니 그게 아니었다. 격자점은
여전히 여백 안쪽에서 끝나서 **양끝에 세로선 없는 띠가 남았다.** 원한 것은
"끝 칸이 잘려도 좋으니 격자 자체가 종이 끝까지 가는 것"이었다.

그래서 `linesToEdge`를 `toEdge`로 바꾸고 의미를 넓혔다.

한 번 더 고쳤다. 처음엔 왼쪽 위 모서리에서 시작하게 했는데, 그러면 **오른쪽과
아래만 잘려서 격자가 한쪽으로 쏠려 보인다.** 가운데 정렬을 그대로 두어야 남는
만큼이 양끝에 반씩 붙고 네 모서리가 고르게 잘린다.

```
80mm · 5mm 간격 → 0, 5, …, 80        딱 떨어져 잘린 칸이 없다
80mm · 6mm 간격 → 1, 7, …, 79        양끝 1mm씩이 잘린 칸
```

그래서 이 옵션은 결국 **여백의 하한을 0으로 두는 것**과 같은 계산이다. 특수한
분기가 없다 — `gridLattice`가 `toEdge`면 `minMargin` 대신 0을 넘길 뿐이다.
선을 영역 끝까지 긋는 일만 `gridShapes`가 따로 맡는다.

**잘린 칸이 버그가 아니라 이 옵션의 요점이다.** 그래서 최소 여백은 이때 쓰이지
않고, 설정 패널에서도 숨는다 — 여백을 없애는 것이 목적인데 하한을 받을 이유가 없다.

이번에는 **격자점 좌표가 실제로 달라진다.** 붙는 자리도 함께 옮겨간다. 간격을
바꾸는 것과 같은 종류의 설정이라 문제될 것이 없다 — 이미 그려둔 것의 좌표는
건드리지 않는다. 보이는 선만 늘이고 붙는 자리는 그대로 두는 것이 오히려 나빴다:
선은 있는데 거기 붙지는 못하는 자리가 생긴다.

끝까지 채울 때는 선도 영역 끝까지 긋는다. 잘린 마지막 칸이 종이 끝에서 닫혀야
칸으로 읽힌다. 기준은 속지가 아니라 **격자 영역**이라, 안전영역을 비워뒀다면
구멍 쪽으로 넘어가지 않는다.

### 격자선도 파선·점선

`DotGrid.dash`. 그은 선과 같은 세 가지다.

점선 비율은 `core/line`의 `dashPattern` 하나에서 나온다. 격자선은 객체가 아니고
화면과 인쇄의 굵기도 다르지만(`SCREEN_GRID_LINE_WIDTH` 대 `GRID_LINE_WIDTH`)
비율은 한 군데여야 한다. 그래서 `dashPattern(dash, width)`로 굵기를 받도록 바꾸고,
선 하나에서 꺼내 쓰는 `dashPatternOf(o)`를 그 위에 얹었다.

점선일 때는 끝을 둥글게 처리한다. 길이가 굵기와 같아서 정확히 원이 된다 —
그래야 `점선`이 점으로 읽힌다.

---

## ✅ 4c-2b — 굵게, 그리고 단축키·격자 복귀

- 테스트 181개 통과 · 타입 검사 · 빌드 통과

### 굵게는 진짜 Bold 파일로 그린다

`Pretendard-Bold.ttf`를 함께 담았다. **가짜 굵게(synthetic bold)를 쓰지 않는다** —
브라우저는 Regular의 획을 부풀려 굵은 척을 해주지만 pdf-lib에는 그 기능이 없어서,
그대로 두면 화면만 굵고 인쇄물은 보통 굵기로 나온다. `OBJECT_LINE_CAP`으로 이미
겪은 그 사고다.

받아온 파일이 저장소의 Regular와 **같은 판인지 해시로 대조했다**(v1.3.9의
`static/alternative`, 두 파일 모두 `6d0af525…`). 판이 다르면 글자 폭이 미묘하게
달라져 화면과 PDF가 어긋난다.

- `core/text.ts`에 `boldOf` — 화면·PDF·폭 재기가 모두 이걸 부른다
- `core/style.ts`에 `FONT_WEIGHT = { regular: 400, bold: 700 }` — `@font-face`
  선언과 같은 값이어야 진짜 파일이 쓰인다
- **폭을 재는 글꼴과 그리는 글꼴이 반드시 같아야 한다.** Bold는 획이 두꺼운 만큼
  폭도 넓어서, Regular로 재고 Bold로 그리면 가운데 정렬이 어긋난다
- 파일 하나가 2.7MB라 **굵은 글자가 실제로 있을 때만** 받고 심는다. 없으면
  망가진 바이트를 넘겨도 터지지 않는다(테스트로 고정)

### 단축키가 크롬과 부딪히던 것

`⌘V`를 누르면 붙여넣기가 되면서 **동시에** 도구가 `고르기`로 바뀌었다. `⌘T`는
새 탭 + `글자`, `⌘L`은 주소창 + `그리기`. 크롬이 우리 것을 뺏은 게 아니라 우리가
크롬 것에 끼어든 쪽이었다 — 단축키 처리가 `e.key`만 보고 수정키를 보지 않았다.

`if (e.metaKey || e.ctrlKey || e.altKey) return;` 한 줄로 빠진다. 키를 `⌘⇧T` 같은
조합으로 옮기는 것은 답이 아니다. 그것도 크롬이 쓰고(닫은 탭 열기), 게다가
`⌘T`·`⌘N`·`⌘W`는 웹페이지가 `preventDefault`로 막을 수도 없다. 맨 글자는 브라우저가
쓰지 않으므로 가장 안전한 자리다.

**그리기를 `L`에서 `D`로 옮겼다.** `L`은 오른손 끝이라 멀다. `V`·`D`·`T`는 왼손만으로
다 닿는다.

### 한 번 격자를 벗어나면 영원히 벗어나 있던 것

4c-2a의 ⌘ 이동으로 밀어낸 글자가, ⌘ 없이 다시 옮겨도 어긋난 만큼을 그대로 달고
다녔다. **이동량**을 칸 단위로 반올림했기 때문이다.

**이제 이동량이 아니라 도착지를 맞춘다.** 고른 것 전체를 감싸는 네모의 왼쪽 위
(`anchor`)가 앉을 자리를 격자에 맞추고, 그 차이만큼 전부 함께 옮긴다. anchor가 이미
격자 위면 돌아갈 어긋남이 0이라 예전과 결과가 정확히 같다(테스트로 고정).

격자점 **목록**이 아니라 **위상**(첫 점 + n칸)에 맞춘다. 목록은 여백 앞에서 끝나서,
그것에 맞추면 지금 가능한 "여백 쪽으로 밀어내기"가 막힌다.

### 그래서 생긴 문제 — 클릭만 해도 튀었다

도착지를 맞추자, 격자를 벗어나 있던 것은 **손이 1픽셀만 떨려도** 최대 반 칸이
밀렸다. 고르려고 눌렀을 뿐인데 옮겨진다.

`DRAG_START = 1mm`를 넘기기 전까지는 **누른 것**으로 본다. 클릭·드래그 판정을
이동량이 0인지가 아니라 손이 움직였는지로 바꿨다.

붙는 자리를 정하는 스냅에는 여전히 문턱값이 없다. 이건 **몸짓을 가르는** 문턱이지
좌표를 정하는 문턱이 아니다.

### 개발 서버가 `127.0.0.1`로 안 열리던 것

`vite.config.ts`에 `server` 설정이 없어 기본값을 쓰는데, Node 17부터 DNS 순서가
바뀌어서 IPv6(`[::1]`)에만 붙었다. `localhost`는 열리는데 `127.0.0.1`은 연결이
거부됐다. `host: true`로 둘 다 연다 — 같은 네트워크의 다른 기기에도 보이므로
집에서만 쓴다는 전제다.

---

## ✅ 4c-2a — 격자를 풀고 옮긴다

- 테스트 173개 통과 · 타입 검사 · 빌드 통과

**⌘(윈도우는 Ctrl)을 누른 채 끌면 격자를 벗어난 자리에 놓인다.** 칸 한가운데에
제목을 얹거나, 선과 선 사이에 글자를 맞춰 넣을 때 쓴다. 누르지 않으면 지금까지처럼
칸 단위로 움직인다.

키를 ⌥가 아니라 ⌘로 정했다. Figma·스케치가 ⌘를 "스냅 무시"에 쓰고, ⌥는 대부분의
도구에서 **복제**라 나중에 복제를 넣을 때 부딪힌다.

### 계산은 core/snap의 moveDelta가 한다

원래 EditorTab 안에서 `Math.round(...)`를 직접 하고 있었다. 격자를 푸는 조건이
붙으면서 규칙이 생겼으므로 core로 옮겼다. 테스트 7개가 붙었다.

**여전히 객체를 다시 스냅하지 않고 이동량만 맞춘다.** 그래서 선의 길이와 각도가
변하지 않고, 이미 격자를 벗어나 있던 것도 그 상대적인 자리를 지키며 함께 움직인다.
격자를 한 번 푼 글자를 나중에 격자 모드로 옮겨도 어긋난 만큼은 그대로 유지된다 —
칸에 다시 붙이고 싶으면 지우고 새로 놓는 것이 아니라 그냥 ⌘ 없이 끌면 된다.

**격자를 풀 때는 0.01mm로 정리한다.** 부동소수점 꼬리(`37.294857...`)가 그대로
저장 파일에 들어가면 사람이 읽을 수 없고, 0.01mm는 어떤 프린터도 구별하지 못한다.

### 상태줄에 띄운다

숨은 단축키는 있는지조차 알 수 없다. 옮기는 동안 맨 아래에 `옮김 3.2 , 1.7 mm ·
⌘로 격자 해제`가 뜨고, 실제로 풀린 동안에는 초록색으로 `· 격자 해제`가 된다.

---

## ✅ 코드 분리 점검 — 고칠 때 헤집는 범위를 줄였다

- 테스트 166개 통과 · 타입 검사 · 빌드 통과
- 기능은 하나도 바뀌지 않았다. 같은 동작을 다른 자리에 놓은 것뿐이다

4c-2에 들어가기 전에 **"고치고 더하기 쉬운 구조인가"**를 점검했다. 두 가지가 걸렸다.

### 선(線)은 core로 절반만 뽑혀 있었다

글자는 화면과 PDF가 `sizeOf`·`alignOf`·`valignOf`·`lineHeightOf`라는 **같은 함수**를
부른다. 그런데 선은 두 뷰가 각자 계산하고 있었다.

| | 화면 | PDF |
|---|---|---|
| 기본 굵기 | `o.width ?? OBJECT_LINE_WIDTH` | 같은 식을 다시 |
| 점선 비율 | `width*6, width*4` | `width*6, width*4` |

양쪽 주석에 *"화면과 같은 비율을 쓴다"*고 적혀 있었다. **알고 있었지만 주석은
강제가 아니다.** 이 프로젝트는 이미 정확히 이 사고를 겪었다 — `OBJECT_LINE_CAP`을
화면만 `round`, PDF는 `butt`로 둬서 **인쇄물에서만** 굵은 선의 꼭짓점이 끊겼던 건.
그때는 값을 core/style로 모아 고쳤는데, `?? 기본값`과 점선 비율 `6·4·1·3`은
그대로 두 군데에 남아 있었다.

[core/line.ts](app/src/core/line.ts)를 만들어 `widthOf`·`colorOf`·`dashOf`·`dashPattern`
넷을 두었다. core/text와 대칭이다. `dashPattern`은 **mm로** 돌려주고, 화면은 그대로
이어 붙이고 PDF는 pt로 바꿔 쓴다 — 비율이 한 군데에만 있다. 테스트 7개로 고정했다.

부수 효과로 `OBJECT_LINE_WIDTH`·`OBJECT_LINE_COLOR`를 import하던 뷰가 사라졌다.
이제 기본값을 아는 건 core뿐이다.

### EditorTab.tsx 1258줄을 쪼갰다

파일이 큰 것 자체보다, **한 번 고칠 때 훑어야 하는 범위**가 문제였다. 지난 회차에
버그 하나 고치면서 `finishEditing` → `setEditingStyle` → `TextInput` → `StyleBar` →
`TextControls` 다섯 군데를 오갔는데 전부 한 파일이라 1200줄을 위아래로 스크롤했다.

자를 자리는 이미 주석으로 그어져 있었고(`/* ── 속성 막대 ── */`, `/* ── 몸짓 ── */`),
실제로 붙어 있지도 않았다 — 속성 막대 구간이 EditorTab 본문과 공유하는 건
`Editing` 타입 **하나뿐**이었다.

```
ui/StyleBar.tsx   388줄  속성 막대. store와 props로만 이야기하고 캔버스를 모른다
ui/gestures.ts    117줄  확정되기 전의 것들(Drag·Editing)과 그 계산. React가 없다
ui/EditorTab.tsx  803줄  (1258에서)
```

**덤으로 Fast Refresh가 살아났다.** EditorTab이 컴포넌트 말고 `sizeLabelOf`도
내보내고 있어서 vite가 매번 `Could not Fast Refresh`를 띄우고 전체를 다시 붙였다.
이제 EditorTab이 내보내는 건 컴포넌트 하나뿐이라 고치는 즉시 화면만 갈린다.

**아직 남은 것:** `EditorTab` **함수 하나가 여전히 648줄**이다. 파일에서 뺀 것은
함수 바깥에 있던 것들이라 컴포넌트 본문은 그대로다. 다음에 자른다면 `<svg>` 안의
"조작 중인 것 미리보기"(527~648줄, 선택 표시·손잡이·유령선·마키·치수)를 통째로
떼는 자리다. 다만 4c-2에서 손댈 곳은 속성 막대와 몸짓 쪽이라 급하지 않다.

---

## ✅ 4c-1 다듬기 4차 — 속성 막대를 손봤다

- 테스트 159개 통과 · 타입 검사 통과

### 크기를 고르려는 순간 편집이 끝나버렸다

글자를 더블클릭해 고치는 중에 속성 막대의 드롭다운을 누르면, 방금 쓴 글이 아니라
**다음에 쓸 글**에 크기가 걸렸다.

입력칸에 `onBlur={onDone}`이 걸려 있던 탓이다. 드롭다운을 누르는 것도 입력칸에게는
포커스를 잃는 일이라 그 순간 편집이 확정·종료되고, 막대는 이미 `쓰는 중`에서
`쓸 글자`로 바뀌어 있었다.

**포커스가 막대 바깥으로 나갈 때만 확정한다.** `relatedTarget`이 `.editor-bar` 안이면
여전히 이 글자를 고치는 중으로 본다. 그리고 드롭다운을 고르고 나면 **입력칸으로
포커스를 돌려준다** — 안 그러면 무엇을 고쳤는지는 보이는데 이어서 칠 수도, Enter로
확정할 수도 없다.

직접 치는 칸(크기·줄 간격)에서는 돌려주지 않는다. 한 글자 칠 때마다 포커스를
빼앗기면 값을 끝까지 칠 수 없다. 대신 그 칸에서 Enter를 치면 돌아간다.

### `기본`이라는 선택지를 없앴다

`기본 0.2mm`와 `0.2mm`가 목록에 나란히 있으면 무엇이 다른지 되묻게 된다.
이제 **목록에서 기본값에 해당하는 항목이 그냥 처음부터 골라져 있다** — 그 항목의
value가 빈 문자열이다. 굵기·선 색·글자 색 셋 다 같은 방식이고, 정렬처럼 이미
그렇게 돼 있던 것들과도 규칙이 맞았다.

안에서는 **여전히 값을 비워둔다.** 나중에 기본값을 바꾸면 손대지 않은 것들이 같이
따라온다는 성질은 그대로다. 예전에 명시적으로 0.2mm를 골라둔 선도 같은 항목에
표시되도록 읽을 때 한 번 정규화한다.

### 글자 크기와 줄 간격은 직접 친다

고를 수 있는 몇 개(6·7·8·9…24pt)로 묶어두면 6.5pt를 쓸 수 없다. 손바닥만 한
속지에서는 0.5pt 차이가 한 줄을 더 넣느냐 마느냐를 가른다. 설정 패널에서 도트
간격을 직접 치는 것과 같은 방식으로 바꿨다.

치는 도중에는 `6.`이나 빈 칸처럼 아직 숫자가 아닌 상태를 반드시 거친다. 그 값을
그대로 올리면 크기가 0이 되거나 NaN이 샌다. 그래서 화면에 보이는 글자는 `NumField`가
따로 들고 있고 **읽을 수 있는 값이 됐을 때만** 위로 올린다.

**줄 간격은 이번에 새로 열었다.** 만들 때 도트 간격이 새겨지는 것은 그대로고,
그 값을 시작점으로 고칠 수 있게 됐다. 한 번 고쳐두면 크기·색과 마찬가지로 다음에
쓰는 글자로 이어진다 — `newTextStyle`이 직접 정해둔 값을 도트 간격으로 덮어쓰지
않는다(`core/text.ts`, 테스트 5개).

### 도트 크기에 글자 크기를 묶지 않기로 했다

2mm 칸에 글자 상자를 만들면 상자가 칸보다 훨씬 크게 나온다는 지적이 있었고,
도트가 작아지면 글자도 따라 작아지게 하자는 제안이 있었다. **하지 않기로 했다.**

바로 위 회차에서 고친 버그와 같은 종류다. 줄 간격이 현재 도트 간격을 읽는 바람에
20mm로 쓴 글이 5mm로 바꾸는 순간 무너졌다. 글자 크기를 도트에 묶으면 도트를 바꾸는
순간 이미 만든 양식의 글자가 전부 변한다. 게다가 글자 크기는 인쇄 가독성 문제이지
격자 문제가 아니다 — 2mm에 비례시키면 5~6pt가 되는데 뽑아보면 읽기 어렵다.
촘촘한 격자는 작은 글자가 아니라 **정밀한 배치**를 위해 쓰는 경우가 많다.

원하던 것은 이미 된다. 마지막에 쓴 크기가 다음 글자로 이어지므로, 촘촘한 격자에서
한 번 6pt로 바꿔두면 그 뒤로 계속 6pt다. 크기가 자유 입력이 되면서 훨씬 편해졌다.

---

## ✅ 4c-1 다듬기 3차 — 글자 편집 버그 두 가지

- 테스트 154개 통과 · 빌드 성공
- **GitHub 저장소 개설** — https://github.com/jiing2222/Diary-Designer

### 인쇄 안내문을 맨 아래 상태줄로

용지 오른쪽 아래 구석에 작게 얹었는데도 미리보기를 가렸다. `용지 210 × 297mm`가
적힌 맨 아래 줄로 옮겼다. 인쇄하기 탭에서만 보인다 — 양식 만드는 중에는 상관없는
이야기다.

### 더블클릭 수정이 깨진 원인

지난 회차에 "고치는 중인 글자를 화면에서 감추기"를 만들면서 그 글자를 목록에서
**빼버렸다**. 그런데 클릭 판정(`hitAt`)은 DOM의 `<text>` 요소를 훑어서 찾는다 —
**편집을 여는 순간 그 글자는 클릭으로 찾을 수 없게 됐다.**

글자 도구에서 더블클릭하면: 첫 클릭이 편집을 열고 → 글자가 DOM에서 사라지고 →
둘째 클릭이 그걸 못 찾아 "빈 곳"으로 판단해 편집을 강제 종료하고 새 빈 상자를
만들었다.

**제거 대신 `visibility: hidden`으로 감춘다.** SVG에서 visibility는 화면에서만
없애고 좌표(`getBBox`)는 그대로 남기므로, 감추는 목적을 지키면서 판정이 산다.
`InsertView`에 `hiddenId` prop을 추가했다.

### 줄 간격을 만들 때 정해서 새겨둔다

전에는 그리는 순간마다 **현재** 도트 간격을 읽었다. 그래서 20mm 도트에서 쓴 글이
5mm로 바꾸는 순간 줄이 소급해서 좁아졌다 — 만들어둔 양식이 설정 하나에 무너지는
셈이었다.

`TextObject.lineHeight`를 추가해 **만들 때의 도트 간격을 객체에 새겨둔다.**
크기·색·정렬과 똑같은 규칙이다(값이 있으면 그것, 없으면 기본값). `core/text.ts`의
`lineHeightOf(t)`가 그 값을 읽고, 화면·PDF·자동 늘어남·타이핑 중 입력칸이 전부
같은 값을 쓴다.

브라우저에서 실측 확인: 20mm 도트에서 3줄을 쓰면 줄 간격 20mm, 도트를 5mm로
바꿔도 `[43.3255, 63.3255, 83.3255]` 그대로다.

---

## ✅ 4c-1 다듬기 2차 — 써보고 나온 것들

- 테스트 151개 통과 · 빌드 성공

### 고치는 중인 글자는 화면에서 감춘다

입력칸이 같은 자리에 겹쳐 있는데 밑에 원래 글자가 그대로 남아 있어서, 지금 고치는
중인 게 무엇인지 헷갈렸다. `EditorTab`이 `InsertView`에 넘기는 `objects`에서
`editing.id`를 걸러낸다 — 입력칸이 뜨는 동안은 그 객체 하나만 화면에서 빠진다.
목록(store)에서 지우는 게 아니라 **그 순간의 렌더링에서만** 뺀다.

### 줄 간격이 도트 간격을 따른다

⇧Enter로 여러 줄을 쓰면 이제 **줄마다 정확히 다음 도트 줄에 앉는다.** 전에는
글자 크기의 1.4배(대략 4.4mm)를 줄 간격으로 썼는데, 5mm 도트보다 조금 좁아서
줄이 늘수록 칸 한가운데에서 위쪽으로 밀렸다.

`core/text.ts`에 `effectiveLineHeight(size, spacing)`을 추가했다 — **도트 간격을
그대로 줄 간격으로 쓰되**, 글자가 도트 간격보다 커서 겹칠 것 같을 때만(예: 2mm
도트에 24pt 글자) 글꼴이 실제로 차지하는 높이로 물러난다. `blockHeight`·
`lineBaselines`가 이 값을 받도록 시그니처가 바뀌어서 PDF(`pdf/export.ts`)·화면
(`ui/InsertView.tsx`)·자동 늘어남 계산(`ui/measureText.ts`)·타이핑 중인 입력칸의
CSS `line-height`(`ui/EditorTab.tsx`)까지 전부 같은 값을 쓰게 고쳤다.

### 편집 화면에서는 넘친 것도 그냥 보인다

두 가지가 겹친 문제였다.

1. **글자가 상자를 넘어 속지 밖으로 나가면 클리핑 때문에 안 보였다.** 하지만
   그건 인쇄할 때나 문제고, 작업 중에는 얼마나 넘쳤는지 보여야 다음에 뭘 할지
   판단할 수 있다. `InsertView`의 클리핑을 `mode === 'print'`일 때만 걸도록 바꿨다
   — 편집 화면(mode='edit')은 이제 자르지 않는다.
2. **선 길이 표시(`.size-label`)가 속지 오른쪽 끝 가까이에서 커서를 따라가다
   보면 사라졌다.** 이건 클리핑이 아니라 **SVG 자체의 기본 동작**이었다 —
   `viewBox` 밖으로 나간 내용은 `overflow: hidden`이 기본값이라 뷰포트에서
   잘린다. `.insert-sheet`에 `overflow: visible`을 줘서 편집 화면 전체가
   뷰박스 밖으로 나간 것도 보여주게 했다.

**둘 다 편집 화면(EditorTab)에만 적용된다.** 인쇄하기 탭의 미리보기(PaperPreview,
mode='print' 기본)와 실제 PDF는 여전히 자른다 — 옆 칸을 침범하는 건 진짜 문제이기
때문이다. PDF를 직접 렌더링해서 옆 칸을 안 넘는 것과, 편집 화면에서 넘친 글자가
캔버스 밖 회색 배경까지 보이는 것 둘 다 확인했다.

### 왼쪽 아래에 칸 개수를 항상 띄운다

도트든 그리드든 가로줄이든 세로줄이든, 지금 격자가 몇 칸인지 `editor-foot`에
계속 보인다(`칸 14 × 23개`). 모양과 무관하게 같은 격자(`gridLattice`)에서 나오는
값이라 스타일을 바꿔도 같은 수가 나온다.

### 인쇄하기 화면을 다시 배치했다

- **미리보기 토글이 양식 만들기의 도구줄과 같은 자리(상단)로 올라갔다.**
  `.print-bar`를 새로 만들어 `.editor-bar`와 같은 자리·같은 생김새로 맞췄다.
  **기본값도 켜짐으로 바꿨다** — 어차피 실제 인쇄 모습을 보려고 켜는 화면이니
  매번 켜는 수고를 없앴다.
- **"100% 인쇄" 안내문을 용지 오른쪽 아래 구석에 작게 옮겼다.** 전에는 위쪽에
  큼직하게 있어서 실제 인쇄 모습을 가렸다.
- **칸 경계선(회색 사각형)을 인쇄 미리보기에서 뺐다.** 작업할 땐 어디가 한 장인지
  보여주는 안내선이지만 실제로 인쇄되는 선이 아닌데, 미리보기에도 계속 남아 있어서
  진짜 인쇄물처럼 안 보였다.
- **"프린터가 못 찍는 가장자리" 표시도 인쇄 미리보기에서 뺐다.** 타공 안내와
  같은 이유 — 화면 전용 경고라 실제 인쇄 모습에는 없다.
- **50mm 검증 눈금자는 기본을 꺼둔 채로 바꿨다.** 1단계에서 이미 50mm 정확도를
  실측으로 검증했으니 매번 다시 잴 필요가 없다. 다만 이건 실제로 PDF에도
  찍히는 내용이라(자로 재는 용도), 켜뒀다면 인쇄 미리보기에서도 그대로 보인다
  — 화면 전용 안내들과 달리 이건 빼지 않는다.

이 작업 중 CSS 버그를 하나 잡았다. 안내문을 용지 위에 얹으려고 `<PaperPreview>`를
`position: relative` 래퍼로 감쌌더니 용지가 갑자기 0×0으로 쪼그라들었다 — SVG는
내재 크기가 없는 요소라, 부모가 플렉스 자식이면서 크기가 애매한 일반 블록이면
브라우저가 크기를 못 정하고 0으로 접어버린다. 래퍼에 `aspect-ratio`(용지 가로:세로,
App.tsx가 인라인으로 전달)와 `height`를 명시적으로 못 박고 SVG는 그 안을 100%로
채우게 해서 고쳤다. **크기를 애매하게 두지 않는 것**이 핵심이었다.

---

## ✅ 4c-1 다듬기 — 여러 줄, 자동 늘어남, 인쇄 미리보기

- 테스트 149개 통과 · 빌드 성공

### 굵은 선의 꼭짓점이 끊기던 것을 고쳤다

**화면과 PDF가 다른 선 끝 처리를 쓰고 있었다.** 화면은 `strokeLinecap="round"`인데
PDF는 지정을 안 해 기본값 `butt`였다 — 그래서 인쇄물에서만 굵은 선의 꼭짓점에
네모난 구멍이 남았다. `OBJECT_LINE_CAP`을 core/style.ts로 올려 화면·PDF가 같은
값을 쓰게 했다. **좌표는 건드리지 않는다** — round는 그리는 순간에만 굵기의
절반을 연장하므로 50mm 선은 여전히 정확히 50mm다.

### 텍스트 상자가 스스로 커진다

칸 하나만 누르고 길게 써도 매번 손으로 늘릴 필요가 없다. 입력하는 동안 실제로
필요한 크기를 캔버스로 재서(`ui/measureText.ts`), 모자라면 격자 칸 단위로
키운다(`core/objects.ts`의 `growBox`). **왼쪽 위는 움직이지 않고, 줄지도 않는다** —
글자를 지운다고 상자가 따라 줄면 입력 중 화면이 계속 흔들린다. 속지 밖으로도
자라지 않는다.

### ⇧Enter로 여러 줄을 쓴다

Enter는 확정, ⇧Enter는 줄바꿈이다. 메모·할일목록처럼 여러 줄이 흔해서 이렇게
배정했다(문서 프로그램과는 반대). `core/text.ts`의 `lineBaselines`가 화면·PDF
모두에 줄 위치를 낸다 — 한 줄일 때는 예전 `baselineY`와 정확히 같은 값이 나오도록
`lineBaselines(...,1)`에 위임했다(회귀 없음).

### T모드 하나로 쓰기·고치기·꾸미기를 다 한다

- **칸 클릭** — 그 칸에 새로 쓴다. **글자를 클릭하면** — 그 자리에서 바로 고친다
  (더블클릭 없이 한 번으로). 다른 도구에서도 **더블클릭하면 T모드로 바뀌며 열린다**
- **속성 막대가 셋으로 나뉜다** — 입력 중이면 그 상자 자체(즉시 반영),
  아니면 고른 것, 그것도 없으면 **앞으로 쓸 글자의 기본값**(`textDraftStyle`).
  이 순서는 store.drawStyle이 선에 이미 하던 방식과 같다
- **스타일이 새는 버그를 고쳤다** — 예전엔 상자를 고칠 때 굵기·정렬·색이 전부
  기본값으로 리셋됐다(`Editing`에 `size`만 있고 `align/valign/color`가 없었다).
  이제 `Editing.style: TextStyle`이 전부 들고 다닌다

### 도구마다 커서 색이 다르다

그리기는 초록(`SNAP_COLOR`), 글자는 파랑(`TEXT_SNAP_COLOR`). T모드도 그리기처럼
마우스가 올라간 칸을 미리 보여준다(`.text-hover`) — 이전엔 그리기만 이 피드백이 있었다.

### 화면 도트가 간격에 따라 작아지고 옅어진다

촘촘한 격자가 위에 그린 것을 가리던 문제. 간격 5mm를 기준으로, 좁을수록 도트가
작고 연해진다(`InsertView.tsx`의 `screenDotSize`/`screenDotOpacity`, 화면 전용 계산이라
"core에는 계산이 없다"는 style.ts 원칙을 지키려 core가 아닌 ui에 뒀다). **인쇄되는
값(DOT_SIZE)은 그대로다** — 화면 표시만 바뀐다.

### 글자만 속지 영역으로 자른다

디자인 화면을 넘어간 글자가 인쇄물에서도 옆 칸까지 찍히던 문제. 화면은 SVG
`clipPath`로, PDF는 pdf-lib의 저수준 연산자(`pushGraphicsState`·`clip`·`endPath`)로
칸마다 속지 경계에서 자른다. **도트·선은 자르지 않는다** — 애초에 격자 안에서만
그려지므로 넘칠 일이 없다. 실제 PDF를 렌더링해서 옆 칸을 침범하지 않는 것을 확인했다.

### 인쇄 미리보기 — 설정을 안 건드리고 실제 모습만 본다

인쇄하기 탭에 토글을 하나 뒀다. 켜면 도트·타공 화면 설정과 무관하게 **실제로
인쇄될 모습만** 보여준다 — 화면용 큰 도트 대신 진짜 0.2mm 도트를, 타공 안내는
아예 없이. `InsertView`에 `mode: 'edit' | 'print'`를 추가해 처리했다. store에는
넣지 않았다 — 탭을 나가면 잊혀도 되는, 보는 방식만 바꾸는 값이다.

### 타입 검사 관련

`npm run build`(`tsc -b`)로 검사한다. `npx tsc --noEmit`은 tsconfig가 solution
스타일이라 아무것도 검사하지 않는다(지난 회차에 확인). 이번 회차에서 fs/zlib을
테스트에 쓸 수 있는지도 확인했는데 — **node 내장 모듈은 import 자체가 안 된다**
(`tsconfig.app.json`의 `types`가 `["vite/client"]`뿐이라 @types/node가 전혀 안 실린다).
PDF 내용을 직접 파싱하는 테스트는 `DecompressionStream`(Web 표준, Node 18+ 전역)으로
우회할 수 있다는 것도 확인해뒀다 — 이번엔 실제 폰트 바이트를 구할 방법이 없어
자동화 테스트 대신 `/tmp` + vite-node + 스크린샷으로 검증했다.

---

## ✅ 4c-1 완료 — 글자를 쓰고 인쇄한다

- 테스트 137개 통과 · 빌드 성공

### 글자 상자는 도트가 만드는 칸이다

```
칸 하나 클릭      그 칸 크기의 상자
여러 칸 드래그    걸친 칸 전체가 상자
빈 채로 나가면    아무것도 안 남는다
```

**빈 상자가 남지 않기 때문에 "글자를 클릭해서 고른다"는 규칙이 성립한다.**
보이지 않는 빈 상자를 잘못 집을 일이 없다. 두 번 누르면 다시 고칠 수 있다.

글자는 상자를 넘칠 수 있다(설계문서 6장). 줄바꿈하지 않으므로 선을 끈 자리에
긴 제목을 쓸 수 있고, 만들기도 훨씬 간단하다.

### 클릭 판정만 화면이 잰다

글자가 실제로 차지하는 크기는 **글꼴이 정하므로 core가 알 수 없다.** 화면에는 이미
그려져 있으니 브라우저에 물어본다 — viewBox가 mm라 `getBBox()`가 곧 mm를 돌려준다.
감싸기 선택은 상자로 판정하고, 클릭만 이 값을 쓴다.

### 글꼴 — 서브셋을 쓰지 않는다

**설계문서 10장의 계획이 한글에서 안 통했다.**

| | 크기 | 결과 |
|---|---|---|
| `subset: true` | 5KB | **글자 누락** — `2027년 3월 셋째 주` → `3월` |
| `subset: false` | 1.2MB | 정상 |

pdf-lib의 서브셋이 한글 글꼴에서 글리프를 빠뜨린다. 전체 임베딩으로 간다 —
글꼴은 문서에 한 번만 들어가므로 쪽수가 늘어도 1.2MB 그대로다.
글자가 하나도 없으면 아예 심지 않는다.

Pretendard(SIL OFL)를 담아뒀다. **OTF는 서브셋 시도 자체가 실패**해서(`Not a CFF Font`)
TTF 판을 쓴다. 화면도 같은 파일을 @font-face로 쓴다 — 다른 글꼴로 보여주면 글자 폭이
달라져서 화면에서 칸에 맞춘 것이 인쇄물에서 넘친다.

### 놓는 자리는 core/text가 한 번만 정한다

화면은 SVG의 `text-anchor`에 맡기고 PDF는 글자 폭을 재서 좌표를 직접 옮긴다.
**방법이 전혀 다르므로 기준점을 한 군데서 낸다.**

세로는 글꼴의 윗선·아랫선 비율(`TEXT_ASCENT` / `TEXT_DESCENT`)로 잡는다. 실제 글꼴
지표를 쓰지 않는 이유는 화면 쪽에서 그 값을 얻기 어렵기 때문이다. 같은 비율을 두 뷰가
함께 쓰면 적어도 서로 어긋나지 않는다.

### 화면의 커닝을 껐다

두 뷰의 가로 정렬을 실제로 재보니 라틴 문자만 어긋났다.

```
              PDF        화면(커닝 켬)   화면(커닝 끔)
Weekly 11주차  30.2518    30.3354        30.2548
2027년 3월…    27.2798    27.2844        27.2844
```

브라우저는 `We` 같은 짝을 자동으로 좁히지만 pdf-lib은 글자 폭을 그냥 더한다.
`font-kerning: none`으로 맞추니 **셋 다 0.005mm 이내**가 됐다.

### 글자 크기만 pt로 받는다

문서 프로그램에서 쓰던 단위라 10pt가 어느 정도인지 감이 있기 때문이다.
**받는 즉시 mm로 바꿔 저장하므로 모델에는 여전히 mm만 있다.** 기본값은 9pt —
손바닥만 한 속지라 문서 프로그램의 기본(11pt)보다 작다.

### 만들면서 알게 된 것

**`npx tsc --noEmit`이 아무것도 검사하고 있지 않았다.** tsconfig가 solution 스타일
(`files: []` + `references`)이라 아무 파일도 포함하지 않는다. 그동안의 타입 검사가
전부 헛돌았고, `DiaryObject`를 선·글자 두 종류로 나누자 20여 개의 오류가 한꺼번에
드러났다. **진짜 타입 검사는 `npm run build`(`tsc -b`)다.**

---

## ✅ 4b 마무리 — 치수가 보인다

- 테스트 125개 통과 (`npm test`) · 빌드 성공 · 타입체크 통과

### 치수 표시

```
끄는 중      커서 오른쪽에      35 × 45mm
고른 뒤      상단 막대에        4개  50 × 75mm
```

**선 하나면 길이, 여러 개면 감싸는 네모의 가로 × 세로다.**
면은 선 네 개라 "면의 크기"라는 값이 따로 없고, 감싸는 네모가 곧 그 크기다.
가로선·세로선은 길이만으로 충분하지만 대각선은 몇 칸짜리인지도 알아야 해서
가로 × 세로로 보여준다.

커서 옆 글자는 확대를 해도 화면에서 같은 크기다 (`11 / scale`). 격자 위에서도
읽히도록 흰 테두리를 두른다.

> SVG 안에서 CSS의 `mm` 단위는 viewBox 좌표가 아니라 물리 단위로 해석된다.
> 테두리 굵기를 CSS에 두면 확대할 때 어긋나므로 글자 크기에 맞춰 컴포넌트에서 정한다.

### 그리기 모드에도 모양 막대

아무것도 안 골랐으면 **앞으로 그을 선**의 모양을 보여준다. 그리기 전에 무엇이
그어질지 보이는 편이 낫다. 정하지 않은 값은 `기본 0.2mm`처럼 실제 값을 함께 적는다.

`store.drawStyle`이 그 값이고, 정하지 않은 키는 그은 선에도 들어가지 않는다 —
나중에 기본값을 바꾸면 그때 그은 선들도 같이 따라온다.

### 도구를 바꾸면 긋다 만 선을 버린다

첫 점만 찍고 고르기로 넘어가면 "다음 점을 누르세요"가 남아 무엇을 기다리는지
알 수 없었다.

### 인쇄 정렬 기본값을 가운데로

좌측 상단이 자르는 횟수는 적지만, 실제로 인쇄해보니 가운데가 낫다.
프린터가 가장자리를 못 찍어서 한쪽으로 몰면 재단 표시가 잘려나간다.

---

## ✅ 4b-2 완료 — 선마다 굵기·색·모양을 정한다

**4단계가 글자만 남았다.**

- 테스트 119개 통과 (`npm test`) · 빌드 성공 · 타입체크 통과

### 끝점 손잡이

**딱 하나만 골랐을 때만 나온다.** 여러 개 골라놓고 손잡이가 잔뜩 뜨면 어느 것을
잡는지 알 수 없다. 손잡이는 선 몸통보다 먼저 집힌다(2.2mm 대 1.6mm) —
겹쳐 있을 때 손잡이를 못 잡으면 늘이려다 매번 통째로 옮겨진다.

끝점도 격자에 붙는다. 두 끝이 한 점으로 모이면 선이 아니므로 그런 상태는 만들지 않는다.

끌다가 이미 있는 선과 같은 자리에 겹치면 **먼저 있던 것만 남긴다**(`dedupe`).
그대로 두면 "겹쳐 그어도 쌓이지 않는다"는 규칙이 깨지고, 나중에 그 자리를 다시
그었을 때 둘이 한꺼번에 사라져 무슨 일인지 알 수 없게 된다.

### 속성 막대

고른 것이 있으면 안내문 자리에 들어간다. 굵기 · 색 · 모양(실선/파선/점선).

**`기본`을 고르면 값을 지운다.** undefined로 두는 것이 아니라 키 자체를 없애서,
저장 파일에도 남지 않고 나중에 core/style의 기본값을 바꾸면 그 선도 같이 따라온다.

여러 개를 골랐는데 값이 서로 다르면 `—`로 두고, 고르면 전부에 적용한다.

### 만들면서 고친 것

**여러 개 골라둔 상태에서 그중 하나를 클릭해도 선택이 줄지 않았다.**
누를 때 선택을 줄이면 여러 개를 함께 끌 수가 없어서 그대로 두는데,
**끌지 않고 그냥 눌렀다 뗐을 때** 그 하나만 남기는 처리가 빠져 있었다.
판단이 손 뗄 때로 와야 한다.

### 화면 기본 확대를 100%로

200%는 너무 가깝다. 100%가 실제 크기다.

---

## ✅ 4b-1 완료 — 표를 그리고 고칠 수 있다

- 테스트 115개 통과 (`npm test`) · 빌드 성공 · 타입체크 통과

### 조작이 곧 종류다

도구를 오갈 필요가 없다. **무엇을 그릴지는 마우스 조작이 정한다.**

```
그리기
  점 클릭 → 점 클릭      선            (대각선도 된다)
  점에서 드래그          면 = 선 네 개

고르기
  클릭 · Shift+클릭 · 감싸기 → 선택
  고른 것 끌기 → 이동 · Delete → 삭제
```

도구는 `고르기`(V) · `그리기`(L) 둘뿐이다. 선/면 버튼은 필요 없다.

### 다시 그으면 지워진다

**하나라도 없으면 없는 것만 채우고, 전부 이미 있으면 전부 지운다.**
면을 반쯤 겹쳐 그렸을 때 일부는 생기고 일부는 사라지는 혼란을 막는다.
겹쳐 그어도 같은 자리에 선이 중복으로 쌓이지 않는다.

방향은 상관없다 — 왼쪽에서 오른쪽으로 긋든 반대로 긋든 같은 선이다.
지우개 도구 없이 마우스만으로 그리고 지운다.

### 면은 사각형이 아니라 선 네 개다

이래야 아랫변만 골라 지우면 도트가 다시 드러난다 — 설계문서 6장이 표에서 쓰는 방식 그대로다.
새 객체 종류도 PDF 코드도 늘지 않는다. 한 줄로 끌면 변이 겹치므로 선 하나만 만든다.

### 옮길 때는 이동량이 스냅된다

선 자체를 다시 스냅하는 것이 아니라 **"몇 칸 움직였나"**를 스냅한다.
7.3mm를 끌면 5mm 격자에서 정확히 5mm 움직이고, **길이와 각도는 한 치도 변하지 않는다.**

### 선을 집는 굵기는 보이는 굵기와 다르다

0.2mm 선은 손으로 정확히 찍을 수 없다. 1.6mm 안에 들어오면 집힌 것으로 치고,
여럿이 걸리면 가장 가까운 하나만 고른다. 스냅에서 "보이는 모양과 붙는 자리를
분리"한 것과 같은 발상이다.

**감싸기는 완전히 들어온 선만** 고른다. 걸치기만 해도 잡히게 하면 격자 위에서
의도치 않게 딸려온다.

### 선택은 실행취소에 들어가지 않는다

문서가 바뀐 것이 아니다. ⌘Z는 그린 것만 되돌리고 선택 상태는 건드리지 않는다.
다만 되돌려서 사라진 객체의 id는 선택 목록에서 걷어낸다.

### 굵기·색·모양은 선택적 필드다

`LineObject`의 `width` · `color` · `dash`는 **값이 없으면 core/style의 기본값을 따른다.**
손대지 않은 선은 값을 갖지 않으므로 나중에 기본값을 바꾸면 전부 같이 따라온다.
따로 정한 선만 자기 값을 지닌다. 화면과 PDF가 같은 규칙을 쓴다.

`export.test.ts`가 이 짝을 고정한다 — 값이 다르면 PDF가 달라지고, 기본값과 같게 주면
아무것도 안 준 것과 똑같은 PDF가 나온다.

---

## ✅ 4a 완료 — 도트에 붙는 선을 그을 수 있다

편집 화면이 생겼고, 도트에서 도트로 끌면 선이 그어진다. 실행취소가 되고 PDF에 그대로 나온다.

- 테스트 93개 통과 (`npm test`) · 빌드 성공 · 타입체크 통과

### 화면이 둘로 나뉘었다

설계문서 11장대로 탭 두 개가 생겼다. `양식 만들기`가 새 편집 화면이고,
1~2단계에서 만든 것이 `인쇄하기`가 됐다.

**속지 한 장의 내용을 그리는 코드를 [ui/InsertView.tsx](app/src/ui/InsertView.tsx)로 빼냈다.**
편집 화면과 인쇄 미리보기가 둘 다 이것을 쓴다. 각자 그리면 언젠가 어긋나는데,
그 어긋남은 인쇄해봐야 알게 된다.

```
              InsertView          ← 도트 → 선 → 글자
               ↙          ↘
      편집 화면              인쇄 미리보기
      크게 한 장             칸마다 place로 옮겨서
```

### 화면 도트는 일부러 크고 진하다

인쇄되는 도트는 0.2mm다. 그대로 화면에 그리면 1픽셀도 되지 않아 어디에 붙는지 보이지 않는다.
화면에서는 안내선 역할이 우선이므로 **0.6mm에 진한 회색**으로 그린다.

**위치는 조금도 다르지 않다.** 격자점 좌표는 화면과 PDF가 같은 core/grid에서 받는다.
다른 것은 굵기와 색뿐이다. "화면과 PDF는 같은 데이터를 각자 그리는 독립적인 뷰"라는 원칙은
데이터가 같다는 뜻이지 똑같이 보여야 한다는 뜻이 아니다.
인쇄물이 어떻게 보일지는 화면이 아니라 실제로 뽑아서 판단한다.

### 스냅에 문턱값이 없다

언제나 가장 가까운 격자점으로 간다. 도트를 벗어난 자리에는 아예 못 긋는다.
규칙이 하나뿐이라 헷갈릴 일이 없고, 설계문서 원칙("모든 작업은 도트 위에서 일어난다")과도 맞는다.

`21.3, 19.4`를 찍으면 `20, 20`으로 붙는 것을 브라우저에서 확인했다.
마우스가 올라간 자리를 초록 점으로 표시해서, 도트를 꺼둬도 어디에 붙을지 보인다.

### 실행취소는 직접 만들었다

설계문서는 `zundo`를 적어뒀지만 되돌릴 대상이 객체 목록 하나뿐이라
[core/history.ts](app/src/core/history.ts) 30줄로 끝났다. 직접 두니
**무엇이 취소되는지 정확히 정할 수 있다** — 용지 크기를 바꾼 것까지 같이 취소되면 곤란하다.
설정은 되돌리는 게 아니라 다시 고르면 되는 것이다.

### Konva를 쓰지 않기로 했다

설계문서 10장은 react-konva를 골랐지만 **코드가 한 줄도 없을 때 세운 계획**이었다.
2단계까지 순수 SVG로 만들어보니 viewBox를 mm로 잡는 방식이 잘 돌아갔고,
`getScreenCTM()` 덕분에 **브라우저가 마우스 위치를 mm로 직접 돌려준다.** 픽셀 통역이 없다.

스냅이 있어서 Konva가 필요한 이유(픽셀 단위 정밀도)가 애초에 없다.
모서리를 잡아당겨 크기를 바꾸는 기능이 필요해지면(6단계 표, 9단계 도형) 그때 그 부분만 만든다.

### 인쇄로 검증한 것

15,20 → 65,20 같은 상자를 그려 PDF 안의 좌표를 직접 쟀다.

```
첫 칸    15,20 → 65,20      (그린 좌표 그대로)
마지막 칸 95,145 → 145,145   (정확히 80,125만큼 밀림)
각 변의 길이  50mm · 40mm    (오차 없음)
```

### 설정이 아이콘 막대로 바뀌었다

항목이 계속 늘어나므로 한 줄로 다 펼쳐두지 않는다. 왼쪽에 아이콘 다섯 개만 세우고,
누른 것의 설정만 옆에 말풍선으로 띄운다. 아이콘 오른쪽 위의 점은 **그 안내가 지금 화면에
보이는 중**이라는 표시다.

```
용지 · 속지 · 도트 격자 · 타공 안내 · 배치·절취선
```

`인쇄 불가 영역`은 용지 안으로 넣었다. 성격이 다르다고 따로 뒀었는데, 용지를 볼 때
같이 보는 것이 자연스럽다.

닫히는 방법은 셋이다 — 같은 아이콘 다시 누르기, 바깥 누르기, Esc.
설정 폭 268px이 52px로 줄어 미리보기가 그만큼 넓어졌다.

### 편집 화면에도 타공이 보인다

[ui/PunchGuide.tsx](app/src/ui/PunchGuide.tsx)로 빼내 편집 화면과 인쇄 미리보기가 같이 쓴다.
글자가 구멍에 잘리는 걸 막는 안내라 그리는 동안 보여야 한다.

### 만들면서 고친 것

**누르고 떼는 것이 한 번에 일어나면 선이 안 그어졌다.** React의 상태 변경은 다음 렌더에나
반영되는데, 뗄 때 시작점이 아직 null로 보였다. 판단은 ref로 하고 화면 미리보기에만 state를 쓴다.

**`pointerup`을 놓치면 드래그가 멈춘 채로 남았다.** 붙잡기(pointer capture)가 안 풀려서
다른 곳을 눌러도 반응하지 않았다. `pointercancel`을 처리하고 뗄 때 명시적으로 풀어준다.

**편집 화면에서 안전영역이 새까맣게 칠해졌다.** 타공 안내의 CSS가 `.preview` 아래로만
걸려 있어서 편집 화면에서는 스타일이 안 붙고 기본 검정이 됐다. 두 화면이 같이 쓰는
조각이므로 부모에 매이지 않게 고쳤다.

---

## ✅ 2단계 완료 — 도트속지를 뽑을 수 있다

간격과 모양을 고르면 도트속지·그리드속지·줄노트가 나오고, **PDF로 인쇄된다.**

- 테스트 79개 통과 (`npm test`) · 빌드 성공 · 타입체크 통과

### 격자 계산 — [core/grid.ts](app/src/core/grid.ts)

함수를 둘로 나눈 것이 요점이다.

```
gridLattice(area, spacing)  →  xs[], ys[], marginX, marginY   ← 스냅이 쓸 좌표
gridShapes(lattice, style)  →  dots[], lines[]                ← 그릴 것
```

**스냅은 gridLattice 쪽을 쓴다.** 화면에 도트로 보이든 줄노트로 보이든 붙는 자리가
같아야 하므로, 이 규칙을 주석이 아니라 함수 경계로 박아뒀다. 6단계에서 표를 그릴 때
`gridLattice`를 그냥 부르면 된다.

네 가지 모양(도트·그리드·가로줄·세로줄)이 전부 같은 격자에서 나온다. 간격을 그대로 두고
모양만 바꾸면 속지 종류가 바뀐다.

### 여백은 입력받지 않는다

타공 여백과 같은 이유다. 간격을 정하면 여백은 저절로 결정되는 종속 변수다.

```
칸 개수 = floor((영역 − 3mm × 2) ÷ 간격)
여백    = (영역 − 칸 개수 × 간격) ÷ 2
```

속지 끝까지 격자를 채우면 바깥쪽 도트가 재단선 위에 앉아 반쯤 잘려나가므로 최소 3mm를 남긴다.

**여백을 항상 3~5mm에 넣는 것은 기하학적으로 불가능하다.** 중앙 정렬이면 여백은
`나머지 ÷ 2`로 못 박히는데, 125mm에 6mm 간격이면 칸 19개(여백 5.5mm)나 20개(여백 2.5mm)뿐이고
그 사이가 없다. 실제 보장은 `3mm ≤ 여백 < 3mm + 간격/2`이고, **간격 4mm 이하면 항상 5mm 밑이다.**
계산된 여백은 패널에 표시된다.

| 간격 | 격자 (M6) | 여백 |
|---|---|---|
| 2.5mm | 30 × 48 | 3.75mm |
| 3mm | 25 × 40 | 4mm |
| 5mm | 15 × 24 | 5mm |
| 7mm | 11 × 18 | 좌우 5 · 상하 3mm |

### 좌표 변환 — [core/place.ts](app/src/core/place.ts)

1단계까지는 필요 없었다. 재단선도 눈금자도 전부 용지 좌표였고, 타공은 화면 전용이라
PDF에 없었다. **도트가 처음으로 "칸 안에 그려지면서 인쇄까지 되는" 내용이다.**

화면은 SVG transform으로, PDF는 좌표를 직접 계산해서 그린다. 둘이 어긋나면 인쇄물만
틀어지고 화면으로는 알 수가 없다. 그래서 변환을 **행렬 여섯 숫자로 한 번만 정하고**
`map()`과 `svg`가 같은 숫자에서 나오게 했다. place.test.ts가 이 일치를 고정한다.

속지 크기는 따로 넘기지 않고 칸에서 되돌린다(`insertSizeOf`). 둘이 안 맞는 상황을
아예 만들지 않기 위해서다.

### 인쇄로 검증한 것

PDF 안의 도트 좌표를 직접 파싱해서 쟀다.

| | 도트 수 | 간격 | 범위 |
|---|---|---|---|
| M6 4장 | 1440 = 15 × 24 × 4 | x `5 · 10` / y `5 · 10` | x 5~155 · y 5~245 |
| 3단접이 (눕힘) | 1032 = 43 × 24 | 전부 `5` | x 5~120 · y 5~215 |

`10mm`는 칸 경계다 — 오른쪽 여백 5 + 다음 칸 왼쪽 여백 5. 눕힌 속지는 220mm 축이
용지 세로로 가서 5~215mm에 놓였고 모든 간격이 오차 없이 5mm다.
**화면(1440개)과 PDF(1440개)가 독립적으로 계산해 정확히 일치한다.**

### showOnScreen과 print를 분리했다

도트를 보면서 작업하고 인쇄할 땐 빼거나, 반대로 화면을 비워두고 설정한 도트만 인쇄할 수 있다.
둘이 어긋나 있으면 패널이 그 사실을 알려준다 — 안 보이는데 인쇄되는 상황을 눈치채지
못하면 곤란하다.

### 만들면서 고친 것

**간격이 속지보다 넓으면 점 한 줄만 찍히고 있었다.** M6에 90mm를 넣으면 여백이 40mm로
벌어지는데 경고도 안 떴다. 칸이 최소 1개는 나와야 격자로 치도록 바꿨다.

### 타공 안전영역 비우기

구멍에 걸리는 도트를 지우는 것이 아니라 **격자가 놓일 영역 자체를 좁힌다.**
좁아진 영역 안에서 중앙 정렬과 여백 3mm 보장이 그대로 다시 일어난다.

```
끔  도트 15 × 24개 · 여백 5mm                    x 5 ~ 75
켬  도트 13 × 24개 · 여백 좌 15 · 우 5 · 상하 5mm   x 15 ~ 75
```

**기본값은 끔이다.** 시판 도트 속지는 대개 구멍 쪽까지 도트가 깔려 있다.

`타공 위치 보기` 체크와는 무관하게 동작한다. 안내선을 껐다고 구멍이 없어지는 것은 아니다.
화면과 PDF 양쪽에서 도트 수(1248개)와 x 범위가 정확히 일치하는 것을 확인했다.

### 격자 오프셋은 넣지 않았다

설계문서 6장의 `offsetX`/`offsetY`를 뺐다. **맞출 대상이 없다** — 표도 선도 글자도
4단계·6단계에나 생긴다. 지금 넣으면 격자를 옆으로 미는 숫자일 뿐인데, 밀면 한쪽 여백이
3mm 밑으로 내려가 바깥 도트가 재단선에 걸린다. 그럼 "오프셋은 여백보다 크면 안 됨"
같은 규칙을 또 붙여야 하고, 이는 아직 존재하지 않는 문제를 위해 규칙을 만드는 셈이다.

4단계에서 정렬할 것이 생기면 그때 필요한 모양이 드러난다.

### 설계문서와 다르게 간 곳

`DotGrid`에서 세 가지를 뺐다.

- **`color`** — "색과 굵기는 core/style.ts 한 군데에만"이라는 규칙과 부딪힌다.
  인쇄해보고 값을 고칠 때 한 파일만 만지도록 규칙 쪽을 택했다
- **`style: 'none'`** — `showOnScreen`·`print` 두 불리언과 상태가 겹친다.
  안 보이게 하는 방법이 두 가지면 나중에 헷갈린다
- **`offsetX`/`offsetY`** — 위 참고

---

## ✅ 인쇄 테스트 통과 — 2단계로 갈 수 있다

M6 4장을 실제로 인쇄해 자로 쟀고 **50mm가 정확히 나왔다.**

이게 이 프로그램의 전제였다. 치수가 맞으니 나머지를 만들 의미가 생겼다.

### 인쇄해보고 정한 값

| | 값 | 비고 |
|---|---|---|
| 절취선 | 연파랑 `#8fb8de` · 0.1mm | 굵으면 어디를 잘라야 할지 애매해진다 |
| 십자 마크 | 한 변 6mm | 팔 하나 3mm |
| 눈금자 | 검정 0.1mm | 굵으면 자로 읽기 어렵다 |
| 도트 지름 | 0.2mm | **확정.** 0.15~0.40 비교 시트를 인쇄해서 고름 |
| 도트·격자 색 | 연회색 `#a6a6a6` | **확정.** 0.4→0.2mm로 줄이니 연해 보여서 색으로 되돌림 |
| 그은 선 | `#9e9e9e` · 0.2mm | **확정.** 선마다 바꿀 수 있고 이 값이 '기본'이다 |
| 배치 정렬 | 가운데 | **확정.** 한쪽으로 몰면 재단 표시가 잘려나간다 |
| 격자·줄 굵기 | 0.12mm | **잠정.** 아직 인쇄해보지 않았다 |
| 화면 도트 (인쇄 안 됨) | `#8c8c8c` · 0.6mm | 눈에 보이는 게 우선. 위치는 인쇄와 똑같다 |

전부 [core/style.ts](app/src/core/style.ts) 한 파일에 있다. 화면과 PDF가 같은 값을 쓴다.

---

## ✅ 1단계 완료 — 인쇄 정확도 검증

용지·속지를 고르면 자동 배치되고, 절취선과 50mm 검증 눈금자가 들어간 PDF가 나온다.
아직 속지 안에 그림은 못 그린다.

- 테스트 47개 통과 (`npm test`)
- 빌드 성공 (`npm run build`)
- 브라우저 동작 확인

### 검증된 것

A5 계산 결과가 설계문서 5장의 표와 정확히 일치한다.

```
상하 여백 29mm · 자동 계산됨
구멍 중심  32 · 51 · 70 · 140 · 159 · 178
```

속지 세로를 바꾸면 여백이 따라 움직인다 (브랜드마다 M6가 125~128mm로 다르므로).

```
125mm → 여백 13mm      127mm → 여백 14mm
126mm → 여백 13.5mm    128mm → 여백 14.5mm
```

PDF 페이지가 `210 × 297mm`로 소수점 9자리까지 정확하고, **이미지가 아니라 벡터로만** 그려진다.
이 두 가지는 `src/pdf/export.test.ts`가 고정하고 있다. 누가 캔버스를 이미지로 떠서 넣으면 테스트가 깨진다.

### 배치와 재단

용지를 남김없이 쓰는 것이 기본이다. 여백 0, 속지 사이 간격 0.

배치 덩어리는 **좌측 상단에 붙인다.** 가운데 두면 사방을 네 번 잘라야 하지만
좌측 상단이면 오른쪽과 아래 두 번으로 끝난다. 가운데 정렬도 선택지로 남겨뒀다.

십자 마크는 재단선이 만나는 자리에 놓되 두 경우를 뺀다.

- **배치 안쪽에 갇힌 교차점** — 재단선 하나는 양 끝 표시 두 개면 자를 수 있다.
  가운데 표시는 속지 위에 자국만 남긴다
- **용지 끝에 딱 붙은 자리** — 거기는 이미 종이 끝이라 자를 것이 없다

A4에 M6 4장이면 십자가 9개에서 7개로 준다. 좌표 계산은 [core/crop.ts](app/src/core/crop.ts)가
한 번만 하고 화면과 PDF가 같이 쓴다.

**속지가 작으면 바깥 표시만으로 부족하다.** 바깥 십자는 대부분 용지 가장자리에
놓이는데 프린터가 거기를 못 찍는다. A4에 M5 8장을 배치하면 십자 10개 중
**실제로 인쇄되는 것은 1개뿐이다.** 그래서 `십자 · 안쪽까지` 옵션을 뒀다.
같은 배치에서 13개 중 4개가 인쇄되고, 재단선마다 최소 하나씩은 보인다.

| 절취선 옵션 | 쓰임 |
|---|---|
| 십자 · 바깥만 | 기본. 속지가 커서 바깥 표시가 인쇄될 때 |
| 십자 · 안쪽까지 | 속지가 작아 바깥 표시가 잘려나갈 때 |
| 재단선 전체 | 하나도 빼지 않고 다 긋는다 |
| 없음 | |

### 화면 안내 — 인쇄되지 않는 것들

타공 위치와 같은 성격으로 **인쇄 불가 영역**(기본 3mm)을 추가했다. 프린터가 물리적으로
못 찍는 가장자리다. 화면에만 보이고 **배치 계산에 관여하지 않는다.** 배치를 실제로
밀어내는 것은 `용지 > 여백` 쪽이다. 둘을 따로 둔 이유는 성격이 다르기 때문이다 —
하나는 경고이고 하나는 배치 규칙이다.

### 만들면서 고친 것

`묶음 간격(groupGap)`에 조작 UI가 없었다. A5에서 크기만 M6로 바꾸면 70mm 간격이 남아
구멍이 속지를 벗어나는데 고칠 방법이 없었다. 컨트롤을 추가하고, 묶음으로 전환할 때
그 속지에 실제로 들어가는 간격을 제안하도록 했다 (`suggestGroupGap`).

`눈금자` 체크를 꺼도 화면에서 눈금자가 사라지지 않았다. `showRuler`가 미리보기까지
전달되지 않아 PDF에서만 빠지고 있었다.

여백이나 방향만 바꿔도 용지가 `사용자 지정`으로 풀렸다. 크기를 건드리지 않았는데
무엇을 고르고 있었는지 잊는 셈이다. 이제 **가로·세로가 실제로 달라질 때만** 풀린다.
두 버그 다 [store.test.ts](app/src/store.test.ts)가 고정한다.

---

## 남은 단계

전체 순서는 설계문서 12장에 있다. 아래는 지금까지 만든 것을 반영해 다시 센 것이다.

### 3단계 — 타공 위치 표시 · **이미 다 됐다**

규격 프리셋, 구멍 위치 자동 계산, 안전영역 표시, 커스텀 조정 — 네 항목 전부
[core/punch.ts](app/src/core/punch.ts)와 설정 패널에 들어가 있다. 1단계에서 배치를
만들려면 구멍 위치가 필요했고, 2단계에서 안전영역을 피하려면 안전영역이 필요했다.
**따로 할 일이 없다.**

### 4단계 — 그리기 도구 · **완료**

- **4a 완료** — 편집 화면, 도트에 붙는 선 긋기, 실행취소, 확대
- **4b-1 완료** — 도구 팔레트, 면 그리기, 다시 그어 지우기, 선택·이동·삭제
- **4b-2 완료** — 끝점 잡아당기기, 속성 막대(선마다 굵기·색·모양)
- **4b 마무리 완료** — 치수 표시, 그릴 선 모양 미리 보기
- **4c-1 완료** — 글자 상자, 내장 글꼴로 PDF까지
- **4c-1 다듬기 완료** — 여러 줄, 자동 늘어남, T모드 통합 편집, 인쇄 미리보기, 꼭짓점 수정
- **4c-2 완료** — 격자 해제 이동 · 굵게 · 글꼴 파일 등록.
  기울임은 **하지 않기로 했다**

### 5단계 — 양식 저장과 배치 · **완료**

- **5a 완료** — 양식 개념, 양식 관리 탭(갤러리)
- **5b 완료** — JSON 저장·불러오기
- **5c 완료** — 낱장형 칸 배정. 같은 규격의 양식끼리 칸마다 다르게 넣는다

### 6단계 — 표 · **완료**

도트 드래그로 표 생성, 행·열 편집, 구분선 끄기.

**2단계에서 깔아둔 `gridLattice`를 그대로 쓴다.** 스냅 좌표가 이미 모양과 분리돼 있어서
줄노트 위에서 표를 그려도 도트 자리에 붙는다.

### 7단계 — 여러 쪽 생성 · **완료**

만년형 반복, 총 쪽수·인쇄 매수 계산, 여러 쪽 PDF.

**cutStack(겹치기 배치)은 뺐다.** 만년형은 모든 칸 내용이 완전히 똑같아서, 자른
뒤 순서를 어떻게 섞어도 결과가 같다 — 섞을 이유 자체가 없다. 쪽마다 내용이
실제로 달라지는 연도형(8단계)에서 다시 볼 문제다.

### 8단계 — 자동 입력 필드

날짜·숫자·목록 데이터, 세트형 양식, 필드 지정, 서식·파생값, 미리보기. 월간 달력은 그다음.

**가장 큰 단계다.** 설계문서 7장 전체가 여기다.

### 9단계 — 양면 인쇄

- **9a 완료** — `Template.back`(뒷면) 데이터 모델과 앞면·뒷면 편집 탭.
  뒷면은 앞면과 같은 속지 규격(`insert`)을 공유하되 도트 격자와 그린 것은
  따로 지닌다. `store.ts`의 `side: 'front' | 'back'`가 지금 편집 중인 쪽을
  가리키고, `patchActiveSide`가 그리기·되돌리기·격자 설정을 그 쪽으로만
  보낸다. 저장 파일도 뒷면을 함께 담되(`toProject`/`toTemplates`) 실행취소
  이력은 여전히 담지 않는다. 9a 이전 파일은 뒷면 없이(단면) 열린다.
  양식을 바꾸거나(`selectTemplate`) 새로 만들거나(`addTemplate`) 복제하거나
  (`copyTemplate`) 파일을 불러오면(`loadProject`) 항상 앞면으로 돌아간다.
  **PDF 출력과 인쇄 미리보기는 아직 앞면만 본다** — 9b의 몫이다.
- **9b 완료** — PDF와 인쇄 미리보기가 뒷면을 그린다.
  `core/layout.ts`의 `mirrorLayout(layout, paperWidth)`가 칸의 x좌표만
  뒤집는다(`용지폭 − 칸의 x − 칸폭`) — 이 공식이 사는 유일한 곳이다. 절취선은
  칸 좌표만 보고 계산하므로 이 배치를 그대로 넘기면 저절로 앞뒤가 맞는 자리에서
  잘린다. `core/template.ts`의 `capacityPerSheet`(양면이면 칸수 ×2)와
  `frontBackFilled`(이 장에서 앞·뒤 각각 몇 칸 채우는지 — 앞을 다 채운 뒤 뒤를
  채운다)가 쪽수·용지매수 계산을 맡는다. `pdf/export.ts`는 장마다 앞면 페이지
  뒤에 뒷면 페이지를 하나 더 추가하고, 낱장 조합에서는 칸마다 배정된 양식의
  뒷면을(`backSlotOverrides`), 반복 인쇄에서는 지금 양식의 뒷면 하나를
  (`defaultBack`) 쓴다 — 뒷면이 없는 칸은 완전히 빈 채로 찍힌다. store에
  `duplex` 설정이 생겼고(용지·간격과 같은 인쇄 작업 설정), 인쇄하기 탭에
  양면일 때만 앞면·뒷면 미리보기 토글이 뜬다.
  **뒤집는 방향(긴 가장자리 기준)은 아직 실제로 인쇄해보지 않았다** —
  "아직 인쇄해보지 않은 값"에 적어뒀다.
- 9b가 끝났으니 이제 7단계에서 미뤄둔 **낱장 조합 + 반복 인쇄** 연결을 마무리한다.
  "다른 양식을 한 페이지에 넣으면 반복모드가 안 된다"는 문제였다 — 조합을
  반복할 때는 "매수"가 조합 한 세트(예: 위클리1+데일리1+메모1+습관1)를
  통째로 몇 번 반복하는지를 뜻하기로 사용자와 합의했다. 개별 칸마다 다른
  매수를 주는 것은 하지 않는다.

도형, 체크박스, 아이콘은 그 다음.

---

## 하지 않기로 한 것 — 기울임

Pretendard에 **이탤릭 파일이 아예 없다.** 있는 것은 굵기 9종뿐이다. 브라우저는
가짜 기울임을 만들어내지만 pdf-lib에는 그 기능이 없어서, 넣는다면 화면만 기울고
인쇄물은 똑바로 나온다 — `OBJECT_LINE_CAP`으로 이미 겪은 그 사고다. 굵게는 Bold
파일을 담아 해결할 수 있지만 기울임은 담을 파일조차 없다.

한글 양식에서 기울임은 거의 쓰지 않는다. 강조는 굵게와 색으로 충분하다.
나중에 필요해지면 굵기 단계(Light·Medium·SemiBold)를 여는 쪽이 낫다.

## 아직 인쇄해보지 않은 값

- **양면 인쇄의 뒤집는 방향(9b).** 설계문서 8장의 공식(`뒷면 칸의 x = 용지폭 − 앞면 칸의
  x − 속지폭`)은 "좌우로 뒤집는다"(긴 가장자리 기준 뒤집기)는 가정이다. 실제 프린터의
  양면 인쇄 설정(짧은 가장자리 기준으로 뒤집는 프린터도 있다)과 맞지 않으면 뒷면 칸이
  구멍과 어긋난 자리에 찍힌다. 처음 양면으로 뽑아볼 때 프린터의 "긴 가장자리로 넘기기"
  설정을 확인해야 한다.

마지막으로 남아 있던 `GRID_LINE_WIDTH 0.12mm`(그리드·줄노트 선 굵기)를
2026-08-06에 인쇄해서 확인했다 — 괜찮다고 함.
- 글자 색 `TEXT_COLOR #3a3a3a` · 기본 크기 9pt — 인쇄해서 확인함(마음에 든다고 함)

그은 선의 기본값(`#9e9e9e` · 0.2mm)과 도트(`#a6a6a6` · 0.2mm)는 인쇄해서 확인했다.

---

## 확정되지 않은 채로 둔 것

- **A5를 148mm로 뒀다.** A4의 정확한 절반은 148.5mm라 148로 두면 1mm가 남는다.
  그래도 **속지 크기가 우선**이라는 원칙에 따라 규격값을 택했다. 자투리 1mm는
  좌측 상단 정렬 덕분에 전부 아래로 몰리므로 한 번만 자르면 된다.
- **좌측 상단 정렬에서 프린터가 가장자리를 못 찍을 수 있다.** 왼쪽·위쪽 십자가
  사라질 수 있는데, 재단선마다 반대쪽 끝에도 표시가 있어 자르는 데는 지장이 없다.
  실제로 어떤지는 인쇄해봐야 안다.

- **M6 3단접이 세로를 125mm로 가정했다.** 사용자가 220×120이라고 했다가 실수라고 정정했는데,
  정확한 값은 확인하지 않았다. M6 본체와 같아야 구멍이 맞으므로 125로 뒀다.
- **DA9(60×80, 3공)는 추정값이다.** 사용자에게 실물이 없어 19mm 간격 3구멍을 중앙 정렬했다.
- **M6·M5의 실측 여백**(19mm, 10mm)은 채택하지 않았다. 기하학적으로 불가능하다 —
  M6는 세로 125mm에 구멍 6개라 19×5=95mm가 필요하고, 남는 30mm의 절반인 15mm가 여백의 최댓값이다.
  어차피 구멍은 인쇄되지 않는 화면 안내선이라 문제되지 않는다.

---

## 작업 방식

- **한국어로 대화한다.**
- **코드 작성 전에 구조를 먼저 설명하고 동의를 받는다.** 큰 덩어리를 한 번에 받으면 검토가 어렵다.
- **각 단계는 끝날 때 실제로 실행되는 결과물이 나오도록 쪼갠다.** 사용자가 직접 써보고 다음을 정한다.
- 기능을 늘리는 것보다 빠른 완성이 우선이다. 애매하면 뺀다.
