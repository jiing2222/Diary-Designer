/**
 * mm ↔ 화면 픽셀.
 *
 * 96 CSS px = 1인치는 브라우저 표준이지 우리가 정하는 값이 아니다. 그래서
 * core/units(mm ↔ pt)와 달리 여기 ui/에 둔다 — core는 화면을 몰라야 한다.
 *
 * 100% 확대에서 이 값을 곱하면 실제 크기가 된다.
 */
export const PX_PER_MM_AT_100 = 96 / 25.4;

/** 확대 배율 목록. 양식 만들기·인쇄하기 탭의 줌 드롭다운이 함께 쓴다. */
export const ZOOMS = [50, 75, 100, 150, 200, 300, 400];
