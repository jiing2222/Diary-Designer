# 클라우드 저장을 쓸 때만 정한다. 복사해서 .env.local 로 두면 된다.
#
# 비워두거나 이 파일을 아예 안 만들면 클라우드 기능이 화면에 나오지 않고,
# 지금까지처럼 완전히 로컬(브라우저 자동 저장 + 파일 저장)로만 돌아간다.
# 서버 세우는 법은 ../server/README.md 참고.
VITE_PB_URL=https://pb.example.com
